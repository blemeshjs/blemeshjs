---
title: Crypto Package
description: Mesh cryptographic helpers layered on top of the shared utility and protocol packages.
icon: Shield
---

{/* Generated by apps/docs/scripts/generate-api-docs.mjs. Do not edit by hand. */}
{/* Change the TSDoc comment in `packages/crypto/src/index.ts` and run `yarn workspace docs api:generate`. */}

Generated from `packages/crypto/src/index.ts`.

## Classes

### Crypto

A helper class for handling cryptography.

Method implementation is based on Security toolbox and other parts from the
Bluetooth Mesh Protocol 1.1.

It is backwards compatible with older versions of the specification.

#### Constructors

##### Constructor

```ts
new Crypto(): Crypto;
```

###### Returns

[`Crypto`](#crypto)

#### Methods

##### authenticate()

```ts
static authenticate(pdu: Uint8Array, key: Uint8Array): boolean;
```

Authenticates the received Secure Network beacon using the given Beacon Key.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `Uint8Array` |
| `key` | `Uint8Array` |

###### Returns

`boolean`

`true` if the beacon is authenticated, `false` otherwise.

##### calculateAid()

```ts
static calculateAid(key: Uint8Array): number;
```

Generates the Application Key Identifier (AID) based on the key.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Uint8Array` |

###### Returns

`number`

##### calculateConfirmation()

```ts
static calculateConfirmation(
   confirmationInputs: Uint8Array, 
   sharedSecret: Uint8Array, 
   random: Uint8Array, 
   authValue: Uint8Array, 
   algorithm: Algorithm
): Uint8Array;
```

Calculates the Provisioning Confirmation value.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `confirmationInputs` | `Uint8Array` |
| `sharedSecret` | `Uint8Array` |
| `random` | `Uint8Array` |
| `authValue` | `Uint8Array` |
| `algorithm` | `Algorithm` |

###### Returns

`Uint8Array`

##### calculateHash()

```ts
static calculateHash(data: Uint8Array, key: Uint8Array): Uint8Array;
```

Generates Node Identity hash using the given Identity Key.
Returns the last 8 bytes of AES-ECB(data, key).

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Uint8Array` |
| `key` | `Uint8Array` |

###### Returns

`Uint8Array`

##### calculateKeyDerivatives()

```ts
static calculateKeyDerivatives(key: Uint8Array): {
  beaconKey: Uint8Array;
  encryptionKey: Uint8Array;
  identityKey: Uint8Array;
  nid: number;
  privacyKey: Uint8Array;
  privateBeaconKey: Uint8Array;
};
```

Calculates key derivatives from the given Network Key.

Derives: NID (7 bits), Encryption Key, Privacy Key, Identity Key,
Beacon Key and Private Beacon Key (all 128 bits).

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Uint8Array` |

###### Returns

```ts
{
  beaconKey: Uint8Array;
  encryptionKey: Uint8Array;
  identityKey: Uint8Array;
  nid: number;
  privacyKey: Uint8Array;
  privateBeaconKey: Uint8Array;
}
```

###### beaconKey

```ts
beaconKey: Uint8Array;
```

###### encryptionKey

```ts
encryptionKey: Uint8Array;
```

###### identityKey

```ts
identityKey: Uint8Array;
```

###### nid

```ts
nid: number;
```

###### privacyKey

```ts
privacyKey: Uint8Array;
```

###### privateBeaconKey

```ts
privateBeaconKey: Uint8Array;
```

##### calculateKeys()

```ts
static calculateKeys(
   confirmationInputs: Uint8Array, 
   sharedSecret: Uint8Array, 
   provisionerRandom: Uint8Array, 
   deviceRandom: Uint8Array, 
   algorithm: Algorithm
): {
  deviceKey: Uint8Array;
  sessionKey: Uint8Array;
  sessionNonce: Uint8Array;
};
```

Calculates the Session Key, Session Nonce and Device Key from provisioning
inputs.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `confirmationInputs` | `Uint8Array` |
| `sharedSecret` | `Uint8Array` |
| `provisionerRandom` | `Uint8Array` |
| `deviceRandom` | `Uint8Array` |
| `algorithm` | `Algorithm` |

###### Returns

```ts
{
  deviceKey: Uint8Array;
  sessionKey: Uint8Array;
  sessionNonce: Uint8Array;
}
```

###### deviceKey

```ts
deviceKey: Uint8Array;
```

###### sessionKey

```ts
sessionKey: Uint8Array;
```

###### sessionNonce

```ts
sessionNonce: Uint8Array;
```

##### calculateNetworkId()

```ts
static calculateNetworkId(key: Uint8Array): Uint8Array;
```

Generates the Network ID based on the given 128-bit key.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Uint8Array` |

###### Returns

`Uint8Array`

##### calculateSharedSecret()

```ts
static calculateSharedSecret(privateKey: Uint8Array, publicKey: Uint8Array): string;
```

Calculates the ECDH Shared Secret from the local private key and
the remote device's public key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `privateKey` | `Uint8Array` | Local private key. |
| `publicKey` | `Uint8Array` | Remote device's public key (64 bytes, X || Y). |

###### Returns

`string`

The shared secret as a hex string.

##### calculateVirtualAddress()

```ts
static calculateVirtualAddress(virtualLabel: UUID): Address;
```

Calculate the 16-bit Virtual Address based on the 128-bit Label UUID.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `virtualLabel` | `UUID` | The Virtual Label of a Virtual Group. |

###### Returns

`Address`

16-bit hash, known as Virtual Address.

##### decodeAndAuthenticate()

```ts
static decodeAndAuthenticate(pdu: Uint8Array, key: Uint8Array): 
  | {
  ivIndex: IvIndex;
  keyRefreshFlag: boolean;
}
  | undefined;
```

Decodes and authenticates the received Private beacon using the given
Private Beacon Key.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `Uint8Array` |
| `key` | `Uint8Array` |

###### Returns

  \| \{
  `ivIndex`: `IvIndex`;
  `keyRefreshFlag`: `boolean`;
\}
  \| `undefined`

Network information obtained from the beacon, or `undefined` if
         authentication failed.

##### decrypt()

```ts
static decrypt(
   data: Uint8Array, 
   key: Uint8Array, 
   nonce: Uint8Array, 
   mic: Uint8Array, 
   aad?: Uint8Array<ArrayBufferLike>
): Uint8Array<ArrayBufferLike> | undefined;
```

Decrypts AES-CCM cipher text and validates the MIC.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Uint8Array` |
| `key` | `Uint8Array` |
| `nonce` | `Uint8Array` |
| `mic` | `Uint8Array` |
| `aad?` | `Uint8Array`\<`ArrayBufferLike`\> |

###### Returns

`Uint8Array`\<`ArrayBufferLike`\> \| `undefined`

Decrypted plaintext, or `undefined` if MIC validation fails.

##### encrypt()

```ts
static encrypt(
   data: Uint8Array, 
   key: Uint8Array, 
   nonce: Uint8Array, 
   micSize: number, 
   aad?: Uint8Array<ArrayBufferLike>
): Uint8Array;
```

Encrypts data using AES-CCM, appending MIC of `micSize` bytes.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Uint8Array` |
| `key` | `Uint8Array` |
| `nonce` | `Uint8Array` |
| `micSize` | `number` |
| `aad?` | `Uint8Array`\<`ArrayBufferLike`\> |

###### Returns

`Uint8Array`

##### generateKey()

```ts
static generateKey(): Uint8Array<ArrayBufferLike>;
```

###### Returns

`Uint8Array`\<`ArrayBufferLike`\>

##### generateKeyPair()

```ts
static generateKeyPair(_algorithm: Algorithm): {
  privateKey: string;
  publicKey: string;
};
```

Generates a P-256 Elliptic Curve key pair for use in provisioning.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_algorithm` | `Algorithm` |

###### Returns

```ts
{
  privateKey: string;
  publicKey: string;
}
```

The Private and Public Key as hex strings.

###### privateKey

```ts
privateKey: string;
```

###### publicKey

```ts
publicKey: string;
```

##### generateRandom()

```ts
static generateRandom(sizeInBits: number): Uint8Array;
```

Generates random data of given length, given in bits.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `sizeInBits` | `number` | Required size of the random data, in bits. |

###### Returns

`Uint8Array`

A Uint8Array of cryptographically random bytes.

##### obfuscate()

```ts
static obfuscate(
   data: Uint8Array, 
   random: Uint8Array, 
   ivIndex: number, 
   privacyKey: Uint8Array
): Uint8Array;
```

Obfuscates or deobfuscates given data by XORing it with PECB, which is
calculated by encrypting Privacy Plaintext (encrypted data used as Privacy
Random, and IV Index) using the given key.

Privacy Plaintext = 0x0000000000 || IV Index || Privacy Random
PECB = e(PrivacyKey, Privacy Plaintext)
ObfuscatedData = (CTL || TTL || SEQ || SRC) ⊕ PECB[0–5]

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Uint8Array` |
| `random` | `Uint8Array` |
| `ivIndex` | `number` |
| `privacyKey` | `Uint8Array` |

###### Returns

`Uint8Array`
