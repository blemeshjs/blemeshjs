---
title: Core Package
description: Reusable mesh engine types, messages, bearers, and provisioning primitives shared across runtimes.
icon: Layers
---

{/* Generated by apps/docs/scripts/generate-api-docs.mjs. Do not edit by hand. */}
{/* Change the TSDoc comment in `packages/core/src/index.ts` and run `yarn workspace docs api:generate`. */}

Generated from `packages/core/src/index.ts`.

## Functions

### applicationKeysAvailableForModel()

```ts
function applicationKeysAvailableForModel(applicationKeys: ApplicationKey[], model: Model): ApplicationKey[];
```

Returns a list of Application Keys known to the Node, that are not
bound to the given Model, and therefore can be bound to it.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](../../README.md#applicationkey)[] | List of Application Keys to check. |
| `model` | [`Model`](../../README.md#model) | The Model which keys will be excluded. |

#### Returns

[`ApplicationKey`](../../README.md#applicationkey)[]

List of Application Keys that may be bound to the given Model.

***

### boundToNetworkKey()

```ts
function boundToNetworkKey(applicationKeys: ApplicationKey[], networkKey: NetworkKey): ApplicationKey[];
```

Filters the list to contain only those Application Keys, that are
bound to the given Network Key.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](../../README.md#applicationkey)[] | List of Application Keys to filter. |
| `networkKey` | [`NetworkKey`](../../README.md#networkkey) | The Network Key of interest. |

#### Returns

[`ApplicationKey`](../../README.md#applicationkey)[]

Filtered list of Application Keys.

***

### containsKeyBoundTo()

```ts
function containsKeyBoundTo(applicationKeys: ApplicationKey[], networkKey: NetworkKey): boolean;
```

Returns whether any of the Application Keys in the array is bound to
the given Network Key. The Key comparison bases on Key Index property.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](../../README.md#applicationkey)[] | List of Application Keys to check. |
| `networkKey` | [`NetworkKey`](../../README.md#networkkey) | The Network Key to check. |

#### Returns

`boolean`

`True`, if the array contains an Application Key bound to
           the given Network Key, `false` otherwise.

***

### knownTo()

```ts
function knownTo(applicationKeys: ApplicationKey[], node: Node): ApplicationKey[];
```

Returns a new list of Application Keys containing all the Application Keys
of this list known to the given Node.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](../../README.md#applicationkey)[] | List of Application Keys to filter. |
| `node` | [`Node`](../../README.md#node) | The Node used to filter Application Keys. |

#### Returns

[`ApplicationKey`](../../README.md#applicationkey)[]

A new list containing all the Application Keys of this list known to the given node.

***

### notKnownTo()

```ts
function notKnownTo(applicationKeys: ApplicationKey[], node: Node): ApplicationKey[];
```

Returns a new list of Application Keys containing all the Application Keys
of this list NOT known to the given Node.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](../../README.md#applicationkey)[] | List of Application Keys to filter. |
| `node` | [`Node`](../../README.md#node) | The Node used to filter Application Keys. |

#### Returns

[`ApplicationKey`](../../README.md#applicationkey)[]

A new list containing all the Application Keys of this list
           NOT known to the given node.

---

## Variables

### noOob

```ts
const noOob: AuthenticationMethod;
```

## Functions

### fromProvisioningPdu()

```ts
function fromProvisioningPdu(pdu: Data): 
  | AuthenticationMethod
  | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `Data` |

#### Returns

  \| [`AuthenticationMethod`](../../README.md#authenticationmethod)
  \| `undefined`

***

### value()

```ts
function value(method: AuthenticationMethod): Data;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `method` | [`AuthenticationMethod`](../../README.md#authenticationmethod) |

#### Returns

`Data`

---

## Functions

### containsModelWithSigModelId()

```ts
function containsModelWithSigModelId(elements: Element[], sigModelId: number): boolean;
```

Returns whether any of Elements in the array contains a Model with given
Model identifier.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `elements` | [`Element`](../../README.md#element)[] | - |
| `sigModelId` | `number` | Bluetooth SIG model identifier. |

#### Returns

`boolean`

`True` if the array contains an Element with a Model with given Model identifier, `false` otherwise.

***

### toData()

```ts
function toData(elements: ElementData[]): Data;
```

Returns Elements and their Models as Data, to be sent in
Page 0 of the Composition Data.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `elements` | [`ElementData`](../../README.md#elementdata)[] | The array of Elements to be converted to Data. |

#### Returns

`Data`

---

## Functions

### fromRawValue()

```ts
function fromRawValue(value: number): InputAction | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

[`InputAction`](../../README.md#inputaction) \| `undefined`

---

## Functions

### knownToNode()

```ts
function knownToNode(networkKeys: NetworkKey[], node: Node): NetworkKey[];
```

Returns a new list of Network Keys containing all the Network Keys
of this list known to the given Node.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeys` | [`NetworkKey`](../../README.md#networkkey)[] | An array of Network Keys to filter. |
| `node` | [`Node`](../../README.md#node) | The Node used to filter Network Keys. |

#### Returns

[`NetworkKey`](../../README.md#networkkey)[]

A new list containing all the Network Keys of this list known to the given node.

***

### notKnownTo()

```ts
function notKnownTo(networkKeys: NetworkKey[], node: Node): NetworkKey[];
```

Returns a new list of Network Keys containing all the Network Keys
of this list NOT known to the given Node.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeys` | [`NetworkKey`](../../README.md#networkkey)[] | An array of Network Keys to filter. |
| `node` | [`Node`](../../README.md#node) | The Node used to filter Network Keys. |

#### Returns

[`NetworkKey`](../../README.md#networkkey)[]

A new list containing all the Network Keys of this list NOT known to the given node.

***

### primaryKey()

```ts
function primaryKey(networkKeys: NetworkKey[]): NetworkKey | undefined;
```

The primary Network Key, that is the one with key index 0.
If the primary Network Key is not known, it returns to `undefined`.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeys` | [`NetworkKey`](../../README.md#networkkey)[] |

#### Returns

[`NetworkKey`](../../README.md#networkkey) \| `undefined`

---

## Functions

### knowsApplicationKey()

```ts
function knowsApplicationKey(nodes: Node[], applicationKey: ApplicationKey): boolean;
```

Returns whether any of elements of this array is using the given
Application Key.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodes` | [`Node`](../../README.md#node)[] | The array of Nodes to check. |
| `applicationKey` | [`ApplicationKey`](../../README.md#applicationkey) | The Application Key to look for. |

#### Returns

`boolean`

`True` if any of the Nodes have knowledge about the Application Key with the same Key Index as given key, `false` otherwise.

***

### knowsApplicationKeyIndex()

```ts
function knowsApplicationKeyIndex(nodes: Node[], applicationKeyIndex: KeyIndex): boolean;
```

Returns whether any of elements of this array is using an
Application Key with given Key Index.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodes` | [`Node`](../../README.md#node)[] | The array of Nodes to check. |
| `applicationKeyIndex` | `KeyIndex` | The Application Key Index to look for. |

#### Returns

`boolean`

`True` if any of the Nodes have knowledge about the Application Key Index, `false` otherwise.

***

### knowsNetworkKey()

```ts
function knowsNetworkKey(nodes: Node[], networkKey: NetworkKey): boolean;
```

Returns whether any of elements of this array is using the given
Network Key.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodes` | [`Node`](../../README.md#node)[] | The array of Nodes to check. |
| `networkKey` | [`NetworkKey`](../../README.md#networkkey) | The Network Key to look for. |

#### Returns

`boolean`

`True` if any of the Nodes have knowledge about the
           Application Key with the same Key Index as given key,
           `false` otherwise.

***

### knowsNetworkKeyIndex()

```ts
function knowsNetworkKeyIndex(nodes: Node[], networkKeyIndex: KeyIndex): boolean;
```

Returns whether any of elements of this array is using an
Network Key with given Key Index.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodes` | [`Node`](../../README.md#node)[] | The array of Nodes to check. |
| `networkKeyIndex` | `KeyIndex` | The Network Key Index to look for. |

#### Returns

`boolean`

`True` if any of the Nodes have knowledge about the
           Network Key Index, `false` otherwise.

---

## Functions

### fromRawValue()

```ts
function fromRawValue(value: number): OutputAction | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

[`OutputAction`](../../README.md#outputaction) \| `undefined`

---

## Functions

### fromData()

```ts
function fromData(data: Data): PduType | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |

#### Returns

[`PduType`](../../README.md#pdutype) \| `undefined`

***

### nonceId()

```ts
function nonceId(pduType: PduType): number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](../../README.md#pdutype) |

#### Returns

`number`

---

## Functions

### fromProvisioningPdu()

```ts
function fromProvisioningPdu(pdu: Data): ProvisioningCapabilities;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `Data` |

#### Returns

[`ProvisioningCapabilities`](../../README.md#provisioningcapabilities-2)

***

### toString()

```ts
function toString(capabilities: ProvisioningCapabilities): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `capabilities` | [`ProvisioningCapabilities`](../../README.md#provisioningcapabilities-2) |

#### Returns

`string`

---

## Variables

### complete

```ts
const complete: Complete;
```

***

### provisioning

```ts
const provisioning: Provisioning;
```

***

### ready

```ts
const ready: Ready;
```

***

### requestingCapabilities

```ts
const requestingCapabilities: RequestingCapabilities;
```

## Functions

### capabilitiesReceived()

```ts
function capabilitiesReceived(capabilities: ProvisioningCapabilities): CapabilitiesReceived;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `capabilities` | [`ProvisioningCapabilities`](../../README.md#provisioningcapabilities-2) |

#### Returns

[`CapabilitiesReceived`](../../README.md#capabilitiesreceived)

***

### failed()

```ts
function failed(error: Error): Failed;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | `Error` |

#### Returns

[`Failed`](../../README.md#failed)

***

### toString()

```ts
function toString(state: ProvisioningState): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `state` | [`ProvisioningState`](../../README.md#provisioningstate-1) |

#### Returns

`string`

---

The type of Device Public key to be used.

This enumeration is used to specify the Public Key type during provisioning
in `ProvisioningManager.provision()`.

## Variables

### noOobPublicKey

```ts
const noOobPublicKey: PublicKey;
```

No OOB Public Key is used.

## Functions

### oobPublicKey()

```ts
function oobPublicKey(key: Uint8Array): PublicKey;
```

OOB Public Key is used. The key must contain the full value of the Public Key,
depending on the chosen algorithm.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | `Uint8Array` | The Public Key consists of 256-bit X and 256-bit Y of a point Q on P256 curve. |

#### Returns

[`PublicKey`](../../README.md#publickey)

***

### toString()

```ts
function toString(key: PublicKey): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | [`PublicKey`](../../README.md#publickey) |

#### Returns

`string`

---

## Functions

### from()

```ts
function from(pdu: Data): PublicKeyMethod | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `Data` |

#### Returns

[`PublicKeyMethod`](../../README.md#publickeymethod) \| `undefined`

---

## Functions

### fromRawValue()

```ts
function fromRawValue(value: number): 
  | RemoteProvisioningError
  | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

  \| [`RemoteProvisioningError`](../../README.md#remoteprovisioningerror)
  \| `undefined`

---

## Namespaces

| Namespace | Description |
| ------ | ------ |
| [ApplicationKeys](@blemeshjs/namespaces/ApplicationKeys.md) | - |
| [AuthenticationMethod](@blemeshjs/namespaces/AuthenticationMethod.md) | - |
| [Elements](@blemeshjs/namespaces/Elements.md) | - |
| [InputAction](@blemeshjs/namespaces/InputAction.md) | - |
| [NetworkKeys](@blemeshjs/namespaces/NetworkKeys.md) | - |
| [Nodes](@blemeshjs/namespaces/Nodes.md) | - |
| [OutputAction](@blemeshjs/namespaces/OutputAction.md) | - |
| [PduType](@blemeshjs/namespaces/PduType.md) | - |
| [ProvisioningCapabilities](@blemeshjs/namespaces/ProvisioningCapabilities.md) | - |
| [ProvisioningState](@blemeshjs/namespaces/ProvisioningState.md) | - |
| [PublicKey](@blemeshjs/namespaces/PublicKey.md) | The type of Device Public key to be used. |
| [PublicKeyMethod](@blemeshjs/namespaces/PublicKeyMethod.md) | - |
| [RemoteProvisioningError](@blemeshjs/namespaces/RemoteProvisioningError.md) | - |

## Enumerations

### AuthenticationMethodType

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-inputoob"></a> `inputOob` | `3` | Input OOB authentication. User need to input a value displayed on the Provisioner's screen on the Unprovisioned Device. The value to display to the user will be given using `ProvisioningDelegate.authenticationActionRequired()`. When user completes entering the value `ProvisioningDelegate.inputComplete()` will be called. **Param** **action** The chosen input action. **Param** **size** Number of digits or letters that can be entered. Size must be in range 1...32. |
| <a id="enumeration-member-nooob"></a> `noOob` | `0` | No OOB authentication. WARN: This method is considered not secure. |
| <a id="enumeration-member-outputoob"></a> `outputOob` | `2` | OOB authentication. The Provisionee will signal a random value using specified method. The value should be provided during provisioning using `ProvisioningDelegate.authenticationActionRequired()`. **Param** **action** The chosen action. **Param** **size** Number of digits or letters that can be output (e.g., displayed or spoken). Size must be in range 1...32. |
| <a id="enumeration-member-staticoob"></a> `staticOob` | `1` | Static OOB authentication. User will be asked to provide 16 or 32 byte hexadecimal value. The value can be read from the device, QR code, website, etc. See `UnprovisionedDevice.oobInformation` for location. |

***

### InputAction

Available input actions to be performed during provisioning.

For example, if the unprovisioned device is a light switch, then it would allow
the user to input the random number by pressing a button an appropriate number
of times. When the action is complete, `ProvisioningDelegate.inputComplete()`
will be called.

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-inputalphanumeric"></a> `inputAlphanumeric` | `3` |
| <a id="enumeration-member-inputnumeric"></a> `inputNumeric` | `2` |
| <a id="enumeration-member-push"></a> `push` | `0` |
| <a id="enumeration-member-twist"></a> `twist` | `1` |

***

### OutputAction

Available output actions to be performed during provisioning.

For example,if the Unprovisioned Device is a light, then it would blink random
number of times. That number should be provided to
`ProvisioningDelegate.authenticationActionRequired()`.

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-beep"></a> `beep` | `1` |
| <a id="enumeration-member-blink"></a> `blink` | `0` |
| <a id="enumeration-member-outputalphanumeric"></a> `outputAlphanumeric` | `4` |
| <a id="enumeration-member-outputnumeric"></a> `outputNumeric` | `3` |
| <a id="enumeration-member-vibrate"></a> `vibrate` | `2` |

***

### PduType

The PDU Type identifies the type of the message.

Bearers may use this type to set the proper value in the
payload. For ADV bearer it will be a proper AD Type (see Assigned
Numbers / Generic Access Profile), for GATT bearer the correct
Message type in the Proxy PDU.

Some message types are handled only by some bearers,
for example the provisioning PDU type must be sent using a
Provisioning Bearer (PB type of bearer).

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-meshbeacon"></a> `meshBeacon` | `1` | The message is a mesh beacon. See: Section 3.9 of Bluetooth Mesh Specification 1.0.1. |
| <a id="enumeration-member-networkpdu"></a> `networkPdu` | `0` | The message is a Network PDU. See: Section 3.4.4 of Bluetooth Mesh Specification 1.0.1. |
| <a id="enumeration-member-provisioningpdu"></a> `provisioningPdu` | `3` | The message is a Provisioning PDU. This message type may be used only in Provisioning Bearers (PB). See: Section 5.4.1 of Bluetooth Mesh Specification 1.0.1. |
| <a id="enumeration-member-proxyconfiguration"></a> `proxyConfiguration` | `2` | The message is a proxy configuration message. This message type may be used only for GATT Bearer. See: Section 6.5 of Bluetooth Mesh Specification 1.0.1. |

***

### ProvisioningStateType

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-capabilitiesreceived"></a> `capabilitiesReceived` | `2` |
| <a id="enumeration-member-complete"></a> `complete` | `4` |
| <a id="enumeration-member-failed"></a> `failed` | `5` |
| <a id="enumeration-member-provisioning"></a> `provisioning` | `3` |
| <a id="enumeration-member-ready"></a> `ready` | `0` |
| <a id="enumeration-member-requestingcapabilities"></a> `requestingCapabilities` | `1` |

***

### PublicKeyMethod

The type of Device Public key to be used.

This enumeration is used in `ProvisioningRequest.start()`
to encode the selected Public Key type.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-nooobpublickey"></a> `noOobPublicKey` | `0` | No OOB Public Key is used. |
| <a id="enumeration-member-oobpublickey"></a> `oobPublicKey` | `1` | OOB Public Key is used. The key must contain the full value of the Public Key, depending on the chosen algorithm. |

***

### RemoteProvisioningError

Set of errors which may be reported by an unprovisioned device
during provisioning process.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-cannotassignaddresses"></a> `cannotAssignAddresses` | `8` | The device cannot assign consecutive unicast addresses to all elements. |
| <a id="enumeration-member-confirmationfailed"></a> `confirmationFailed` | `4` | The computed confirmation value was not successfully verified. |
| <a id="enumeration-member-invaliddata"></a> `invalidData` | `9` | The Data block contains values that cannot be accepted because of general constraints. |
| <a id="enumeration-member-invalidformat"></a> `invalidFormat` | `2` | The arguments of the protocol PDUs are outside expected values or the length of the PDU is different than expected. |
| <a id="enumeration-member-invalidpdu"></a> `invalidPdu` | `1` | The provisioning protocol PDU is not recognized by the device. |
| <a id="enumeration-member-outofresources"></a> `outOfResources` | `5` | The provisioning protocol cannot be continued due to insufficient resources in the device. |
| <a id="enumeration-member-unexpectederror"></a> `unexpectedError` | `7` | The Data block was not successfully decrypted. decryptionFailed = 6, /** An unexpected error occurred that may not be recoverable. |
| <a id="enumeration-member-unexpectedpdu"></a> `unexpectedPdu` | `3` | The PDU received was not expected at this moment of the procedure. |

## Classes

### AddressRange

The range of addresses of Unicast or Group type.

#### Extends

- `RangeObject`\<`Address`\>

#### Constructors

##### Constructor

```ts
new AddressRange(range: ClosedRange<Address>): AddressRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `range` | `ClosedRange`\<`Address`\> |

###### Returns

[`AddressRange`](#addressrange)

###### Inherited from

```ts
RangeObject<Address>.constructor
```

#### Properties

##### allGroupAddresses

```ts
static allGroupAddresses: AddressRange;
```

A range containing all Group Addresses.

This range does not exclude Fixed Group Addresses or Virtual Addresses.

##### allUnicastAddresses

```ts
static allUnicastAddresses: AddressRange;
```

A range containing all valid Unicast Addresses.

#### Accessors

##### highAddress

###### Get Signature

```ts
get highAddress(): Address;
```

The upper bound of the range.

###### Returns

`Address`

##### isGroupRange

###### Get Signature

```ts
get isGroupRange(): boolean;
```

Returns `true` if the address range is in Group address range.

###### Returns

`boolean`

`True` if the address range is in Group address range, `false` otherwise.

##### isUnicastRange

###### Get Signature

```ts
get isUnicastRange(): boolean;
```

Returns `true` if the address range is in Unicast address range

###### Returns

`boolean`

`True` if the address range is in Unicast address range, `false` otherwise.

##### isValid

###### Get Signature

```ts
get isValid(): boolean;
```

Returns `true` if the address range is valid. Valid address ranges
are in Unicast or Group ranges.

###### Returns

`boolean`

`True` if the address range is in Unicast or Group range, `false` otherwise.

##### lowAddress

###### Get Signature

```ts
get lowAddress(): Address;
```

The lower bound of the range.

###### Returns

`Address`

#### Methods

##### decode()

```ts
static decode(jv: Record<string, unknown>): AddressRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`AddressRange`](#addressrange)

##### fromAddress()

```ts
static fromAddress(address: Address, elementsCount: number): AddressRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `address` | `Address` |
| `elementsCount` | `number` |

###### Returns

[`AddressRange`](#addressrange)

##### fromNode()

```ts
static fromNode(node: Node): AddressRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | [`Node`](#node) |

###### Returns

[`AddressRange`](#addressrange)

***

### ApplicationKey

Application Keys are used to encrypt mesh messages on Access Layer.

The Application Key is 128-bit long and is bound to a single Network Key.
To use the Application Key, the bound Network Key must be used to encrypt
the message on Network Layer.

Each key is identified by a `KeyIndex`. There can be up to 4095
Application Keys in a mesh network.

AID (Application Key identifier) is derived from the Application Key.

The key can be change using Key Refresh Procedure (KRP).

#### Extends

- `Key`

#### Constructors

##### Constructor

```ts
new ApplicationKey(
   $name: string, 
   $index: KeyIndex, 
   key: Data, 
   $boundNetworkKeyIndex: KeyIndex
): ApplicationKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `$name` | `string` |
| `$index` | `KeyIndex` |
| `key` | `Data` |
| `$boundNetworkKeyIndex` | `KeyIndex` |

###### Returns

[`ApplicationKey`](#applicationkey)

###### Overrides

```ts
Key.constructor
```

#### Properties

##### aid

```ts
aid: number;
```

##### meshNetwork?

```ts
optional meshNetwork?: MeshNetwork;
```

##### oldAid?

```ts
optional oldAid?: number;
```

#### Accessors

##### boundNetworkKey

###### Get Signature

```ts
get boundNetworkKey(): NetworkKey;
```

The Network Key bound to this Application Key.

###### Returns

[`NetworkKey`](#networkkey)

##### boundNetworkKeyIndex

###### Get Signature

```ts
get boundNetworkKeyIndex(): KeyIndex;
```

###### Returns

`KeyIndex`

###### Set Signature

```ts
set boundNetworkKeyIndex(boundNetworkKeyIndex: KeyIndex): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `boundNetworkKeyIndex` | `KeyIndex` |

###### Returns

`void`

##### key

###### Get Signature

```ts
get key(): Data;
```

###### Returns

`Data`

###### Set Signature

```ts
set key(key: Data): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Data` |

###### Returns

`void`

###### Overrides

```ts
Key.key
```

##### oldKey

###### Get Signature

```ts
get oldKey(): Data | undefined;
```

###### Returns

`Data` \| `undefined`

###### Set Signature

```ts
set oldKey(oldKey: Data | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `oldKey` | `Data` \| `undefined` |

###### Returns

`void`

#### Methods

##### bindToNetworkKey()

```ts
bindToNetworkKey(networkKey: NetworkKey): MeshNetworkError | undefined;
```

Bounds the Application Key to the given Network Key.
The Application Key must not be in use. If any of the network Nodes
already knows this key, this method throws an error.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to bound the Application Key to. |

###### Returns

`MeshNetworkError` \| `undefined`

`MeshNetworkError.keyInUse` if the key is already in use, `undefined` otherwise.

##### equals()

```ts
equals(other: ApplicationKey): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`ApplicationKey`](#applicationkey) |

###### Returns

`boolean`

##### isBoundToModel()

```ts
isBoundToModel(model: Model): boolean;
```

Returns whether the Application Key is bound to the given Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model to check. |

###### Returns

`boolean`

`True`, if the Application Key is bound to the Model.

##### isBoundToNetworkKey()

```ts
isBoundToNetworkKey(networkKey: NetworkKey): boolean;
```

Returns whether the Application Key is bound to the given
Network Key. The Key comparison bases on Key Index property.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to check. |

###### Returns

`boolean`

`True`, if the Application Key is bound to the given Network Key.

##### isUsedIn()

```ts
isUsedIn(meshNetwork: MeshNetwork): boolean;
```

Return whether the Application Key is used in the given mesh network.

A Application Key must be added to Application Keys array of the network
and be known to at least one node to be used by it.

An used Application Key may not be removed from the network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `meshNetwork` | [`MeshNetwork`](#meshnetwork-3) | The mesh network to look the key in. |

###### Returns

`boolean`

`True` if the key is used in the given network, `false` otherwise.

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### decode()

```ts
static decode(jv: Record<string, unknown>): ApplicationKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`ApplicationKey`](#applicationkey)

##### withProperties()

```ts
static withProperties(
   name: string, 
   index: KeyIndex, 
   key: Data, 
   networkKey: NetworkKey
): ApplicationKey | MeshNetworkError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `index` | `KeyIndex` |
| `key` | `Data` |
| `networkKey` | [`NetworkKey`](#networkkey) |

###### Returns

[`ApplicationKey`](#applicationkey) \| `MeshNetworkError`

***

### BaseGattProxyBearer

Base implementation for GATT Proxy bearer.

#### Extends

- [`Bearer`](#abstract-bearer)\<`this`\> & `BindableTinyEmitter`\<[`GattBearerHandler`](#abstract-gattbearerhandler), `this`\>

#### Type Parameters

| Type Parameter |
| ------ |
| `Service` *extends* *typeof* `MeshProvisioningService` \| *typeof* `MeshProxyService` |

#### Implements

- `CBPeripheralHandler`
- `CBCentralManagerHandler`

#### Constructors

##### Constructor

```ts
new BaseGattProxyBearer<Service extends typeof MeshProvisioningService | typeof MeshProxyService>(
   name: string | undefined, 
   uuid: UUID, 
   centralManager: CBCentralManager, 
   service: Service
): BaseGattProxyBearer<Service>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `uuid` | `UUID` |
| `centralManager` | `CBCentralManager` |
| `service` | `Service` |

###### Returns

[`BaseGattProxyBearer`](#basegattproxybearer)\<`Service`\>

###### Overrides

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).constructor
```

#### Properties

##### identifier

```ts
identifier: UUID;
```

The UUID associated with the peer.

##### logger?

```ts
optional logger?: LoggerHandler;
```

The logger receives logs sent from the bearer. The logs will contain
raw data of sent and received packets, as well as connection events.

#### Accessors

##### isOpen

###### Get Signature

```ts
get isOpen(): boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

###### Returns

`boolean`

###### Overrides

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).isOpen
```

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

The name of the peripheral.

###### Returns

`string` \| `undefined`

#### Methods

##### centralManagerDidDisconnectPeripheral()

```ts
centralManagerDidDisconnectPeripheral(
   _: CBCentralManager, 
   peripheral: CBPeripheral, 
   error?: Error
): void;
```

Called when a peripheral is disconnected.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The disconnected peripheral. |
| `error?` | `Error` | Optional error if the disconnection was unexpected. |

###### Returns

`void`

###### Implementation of

```ts
CBCentralManagerHandler.centralManagerDidDisconnectPeripheral
```

##### centralManagerDidDiscoverPeripheral()

```ts
centralManagerDidDiscoverPeripheral(
   _central: CBCentralManager, 
   peripheral: CBPeripheral, 
   rssi?: number, 
   _advertisementData?: unknown
): void;
```

Called when a peripheral is discovered.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The discovered peripheral. |
| `rssi?` | `number` | Optional received signal strength indicator (RSSI) of the peripheral. |
| `_advertisementData?` | `unknown` | - |

###### Returns

`void`

###### Implementation of

```ts
CBCentralManagerHandler.centralManagerDidDiscoverPeripheral
```

##### centralManagerDidUpdateState()

```ts
centralManagerDidUpdateState(_central: CBCentralManager, state: CBCentralManagerState): void;
```

Called when the central manager's state is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `state` | `CBCentralManagerState` | A string representing the new state (e.g., 'poweredOn', 'poweredOff', etc.). |

###### Returns

`void`

###### Implementation of

```ts
CBCentralManagerHandler.centralManagerDidUpdateState
```

##### close()

```ts
close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

###### Overrides

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).close
```

##### didDisconnect()

```ts
didDisconnect(_peripheral: CBPeripheral): void;
```

Called when the peripheral disconnects.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Implementation of

```ts
CBPeripheralHandler.didDisconnect
```

##### didUpdateState()

```ts
didUpdateState(_peripheral: CBPeripheral): void;
```

Called when the peripheral connection state changes.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Implementation of

```ts
CBPeripheralHandler.didUpdateState
```

##### didUpdateValueForCharacteristic()

```ts
didUpdateValueForCharacteristic(_peripheral: CBPeripheral, characteristic: CBCharacteristic): void;
```

Called when a characteristic's value is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_peripheral` | `CBPeripheral` | - |
| `characteristic` | `CBCharacteristic` | The characteristic whose value was updated. |

###### Returns

`void`

###### Implementation of

```ts
CBPeripheralHandler.didUpdateValueForCharacteristic
```

##### open()

```ts
open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

###### Overrides

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).open
```

##### readRSSI()

```ts
readRSSI(): Promise<number> | undefined;
```

Retrieves the current RSSI value for the peripheral while it is connected
to the central manager.

The result will be returned using `GattBearerDelegate.bearerDidReadRSSI()` callback.

###### Returns

`Promise`\<`number`\> \| `undefined`

##### send()

```ts
send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Overrides

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).send
```

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(Bearer, BindableTinyEmitter<GattBearerHandler>).supports
```

***

### `abstract` Bearer

The Bearer object is responsible for sending and receiving the data
to the mesh network.

#### Extends

- `BindableTinyEmitter`\<[`BearerHandler`](#abstract-bearerhandler)\<[`Bearer`](#abstract-bearer)\> & [`BearerDataHandler`](#abstract-bearerdatahandler)\<[`Bearer`](#abstract-bearer)\>, `this`\> & [`Transmitter`](#abstract-transmitter)\<`this`\>

#### Extended by

- [`MeshBearer`](#abstract-meshbearer)
- [`ProvisioningBearer`](#abstract-provisioningbearer)

#### Constructors

##### Constructor

```ts
new Bearer(): Bearer;
```

###### Returns

[`Bearer`](#abstract-bearer)

###### Inherited from

```ts
Mixin(
  BindableTinyEmitter<BearerHandler & BearerDataHandler>,
  Transmitter,
).constructor
```

#### Properties

##### isOpen

```ts
abstract isOpen: boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

#### Methods

##### close()

```ts
abstract close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

##### open()

```ts
abstract open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

##### send()

```ts
abstract send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

```ts
Mixin(
  BindableTinyEmitter<BearerHandler & BearerDataHandler>,
  Transmitter,
).send
```

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

***

### `abstract` BearerDataHandler

A bearer data handler processes mesh messages received by the bearer.

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* [`Bearer`](#abstract-bearer) | [`Bearer`](#abstract-bearer) |

#### Constructors

##### Constructor

```ts
new BearerDataHandler<T extends Bearer = Bearer>(): BearerDataHandler<T>;
```

###### Returns

[`BearerDataHandler`](#abstract-bearerdatahandler)\<`T`\>

#### Methods

##### bearerDidDeliverData()

```ts
abstract bearerDidDeliverData(
   bearer: T, 
   data: Data, 
   type: PduType
): void;
```

Callback called when a packet has been received using the Bearer.
Data longer than MTU will automatically be reassembled
using the bearer protocol if bearer implements segmentation.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | `T` | The Bearer on which the data were received. |
| `data` | `Data` | The data received. |
| `type` | [`PduType`](#pdutype) | The type of the received data. |

###### Returns

`void`

***

### `abstract` BearerHandler

The bearer handler will receive events when the bearer has been opened
or closed.

#### Extended by

- [`GattBearerHandler`](#abstract-gattbearerhandler)

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* [`Bearer`](#abstract-bearer) | [`Bearer`](#abstract-bearer) |

#### Constructors

##### Constructor

```ts
new BearerHandler<T extends Bearer = Bearer>(): BearerHandler<T>;
```

###### Returns

[`BearerHandler`](#abstract-bearerhandler)\<`T`\>

#### Methods

##### bearerDidClose()

```ts
abstract bearerDidClose(bearer: T, error?: Error): void;
```

Callback called when the Bearer is no longer open.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | `T` | The Bearer. |
| `error?` | `Error` | The reason of closing the Bearer, or `undefined` if closing was intended. |

###### Returns

`void`

##### bearerDidOpen()

```ts
abstract bearerDidOpen(bearer: T): void;
```

Callback called when the Bearer is ready for use.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | `T` | The Bearer. |

###### Returns

`void`

***

### `abstract` CompositionDataPage

A base protocol of a single Page of Composition Data.

The Composition Data state contains information about a Node,
the Elements it includes, and the supported models.

The Composition Data is composed of a number of pages of information.

#### Extended by

- [`Page0`](#page0)

#### Constructors

##### Constructor

```ts
new CompositionDataPage(): CompositionDataPage;
```

###### Returns

[`CompositionDataPage`](#abstract-compositiondatapage)

#### Accessors

##### page

###### Get Signature

```ts
get abstract page(): number;
```

Page number of the Composition Data to get.

###### Returns

`number`

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Composition Data parameters as Data.

###### Returns

`Data` \| `undefined`

***

### ConfigAppKeyAdd

A `ConfigAppKeyAdd` is an acknowledged message used to add an `ApplicationKey`
to the AppKey List on a Node and bind it to the `NetworkKey` identified by
`NetworkKey.index`.

The added Application Key can be used by the Node only as a pair with the specified
Network Key.

The Application Key is used to authenticate and decrypt messages it receives,
as well as authenticate and encrypt messages it sends.

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigNetAndAppKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigAppKeyAdd(
   networkKeyIndex: KeyIndex, 
   applicationKeyIndex: KeyIndex, 
   key: Data
): ConfigAppKeyAdd;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | - |
| `applicationKeyIndex` | `KeyIndex` | - |
| `key` | `Data` | The 128-bit Application Key data. |

###### Returns

[`ConfigAppKeyAdd`](#configappkeyadd)

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).applicationKeyIndex
```

##### key

```ts
key: Data;
```

The 128-bit Application Key data.

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x00;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).opCode
```

##### responseType

```ts
responseType: typeof ConfigAppKeyStatus = ConfigAppKeyStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x00;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data;
```

Access Layer payload, including the Op Code.

###### Returns

`Data`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).parameters
```

#### Methods

##### fromApplicationKey()

```ts
static fromApplicationKey(applicationKey: ApplicationKey): ConfigAppKeyAdd;
```

Creates a `ConfigAppKeyAdd` message.

Use `MeshNetwork.addApplicationKeyWithIndex()` to create a new
`ApplicationKey` and bind it to selected `NetworkKey` using
`ApplicationKey.bind()`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to be added. |

###### Returns

[`ConfigAppKeyAdd`](#configappkeyadd)

##### fromData()

```ts
static fromData(parameters: Data): ConfigAppKeyAdd | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigAppKeyAdd`](#configappkeyadd) \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetAndAppKeyMessage).fromData
```

***

### ConfigAppKeyDelete

A `ConfigAppKeyDelete` is an acknowledged message used to delete an ``ApplicationKey``
from the AppKey List on a Node.

To remove the key from the local Node you may use ``MeshNetwork.removeApplicationKey()``.

WARN: It is not guaranteed, that the target Node will remove the key from its
           AppKey List. To make sure the Node gets excluded, use ``ConfigAppKeyUpdate``
           to update the value of the key and skip the Node when distributing the new
           value. After the Key Refresh Procedure is complete, the target Node will
           effectively be excluded from the mesh network.

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigNetAndAppKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigAppKeyDelete(networkKeyIndex: KeyIndex, applicationKeyIndex: KeyIndex): ConfigAppKeyDelete;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeyIndex` | `KeyIndex` |
| `applicationKeyIndex` | `KeyIndex` |

###### Returns

[`ConfigAppKeyDelete`](#configappkeydelete)

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).applicationKeyIndex
```

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8000;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).opCode
```

##### responseType

```ts
responseType: typeof ConfigAppKeyStatus = ConfigAppKeyStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8000;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).parameters
```

#### Methods

##### fromApplicationKey()

```ts
static fromApplicationKey(applicationKey: ApplicationKey): ConfigAppKeyDelete;
```

Creates a ``ConfigAppKeyDelete`` message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to be removed. |

###### Returns

[`ConfigAppKeyDelete`](#configappkeydelete)

##### fromData()

```ts
static fromData(parameters: Data): ConfigAppKeyDelete | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigAppKeyDelete`](#configappkeydelete) \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigNetAndAppKeyMessage,
).fromData
```

***

### ConfigAppKeyGet

A `ConfigAppKeyGet` is an acknowledged message used to report all ``ApplicationKey``s
known to the Node that are bound to the given ``NetworkKey``.

The response to this message is a ``ConfigNetKeyList`` message.

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigNetKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigAppKeyGet(networkKeyIndex: KeyIndex): ConfigAppKeyGet;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeyIndex` | `KeyIndex` |

###### Returns

[`ConfigAppKeyGet`](#configappkeyget)

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).constructor
```

#### Properties

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8001;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

##### responseType

```ts
responseType: typeof ConfigAppKeyList = ConfigAppKeyList;
```

The Type of the response message.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8001;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigAppKeyGet | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigAppKeyGet`](#configappkeyget) \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).fromData
```

##### fromNetworkKey()

```ts
static fromNetworkKey(networkKey: NetworkKey): ConfigAppKeyGet;
```

Creates a ``ConfigAppKeyGet`` message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key for which Application Keys are requested. |

###### Returns

[`ConfigAppKeyGet`](#configappkeyget)

***

### ConfigAppKeyList

A `ConfigAppKeyList` is an unacknowledged message reporting all ``ApplicationKey``s
bound to requested ``NetworkKey`` that are known to the Node.

#### Extends

- `ConfigResponse`\<`this`\> & `ConfigStatusMessage`\<`this`\> & `ConfigNetKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigAppKeyList(
   status: ConfigMessageStatus, 
   networkKeyIndex: KeyIndex, 
   applicationKeyIndexes: KeyIndex[]
): ConfigAppKeyList;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `status` | `ConfigMessageStatus` | - |
| `networkKeyIndex` | `KeyIndex` | - |
| `applicationKeyIndexes` | `KeyIndex`[] | Application Key Indexes bound to the Network Key known to the Node. |

###### Returns

[`ConfigAppKeyList`](#configappkeylist)

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).constructor
```

#### Properties

##### applicationKeyIndexes

```ts
applicationKeyIndexes: KeyIndex[];
```

Application Key Indexes bound to the Network Key known to the Node.

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8002;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).opCode
```

##### status

```ts
status: ConfigMessageStatus;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).status
```

##### opCode

```ts
readonly static opCode: number = 0x8002;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigAppKeyList | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigAppKeyList`](#configappkeylist) \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).fromData
```

##### responseTo()

```ts
static responseTo(request: ConfigAppKeyGet, applicationKeys: ApplicationKey[]): ConfigAppKeyList;
```

Creates a ``ConfigAppKeyList`` message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | [`ConfigAppKeyGet`](#configappkeyget) | The request, for which this message is to be sent. |
| `applicationKeys` | [`ApplicationKey`](#applicationkey)[] | The list of Application Keys. |

###### Returns

[`ConfigAppKeyList`](#configappkeylist)

##### responseToWithStatus()

```ts
static responseToWithStatus(request: ConfigAppKeyGet, status: ConfigMessageStatus): ConfigAppKeyList;
```

Creates a ``ConfigAppKeyList`` message in case the request has failed.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | [`ConfigAppKeyGet`](#configappkeyget) | The request, for which this message is to be sent. |
| `status` | `ConfigMessageStatus` | The response status. |

###### Returns

[`ConfigAppKeyList`](#configappkeylist)

***

### ConfigAppKeyStatus

A `ConfigAppKeyStatus` is an unacknowledged message used to report a status for
the requesting message, based on the `NetworkKey.index` identifying the
`NetworkKey` on the NetKey List and on the `ApplicationKey.index` identifying
the `ApplicationKey` on the AppKey List.

#### Extends

- `ConfigResponse`\<`this`\> & `ConfigStatusMessage`\<`this`\> & `ConfigNetAndAppKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigAppKeyStatus(
   networkKeyIndex: KeyIndex, 
   applicationKeyIndex: KeyIndex, 
   status: ConfigMessageStatus
): ConfigAppKeyStatus;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeyIndex` | `KeyIndex` |
| `applicationKeyIndex` | `KeyIndex` |
| `status` | `ConfigMessageStatus` |

###### Returns

[`ConfigAppKeyStatus`](#configappkeystatus)

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).applicationKeyIndex
```

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8003;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).opCode
```

##### status

```ts
status: ConfigMessageStatus;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).status
```

##### opCode

```ts
readonly static opCode: number = 0x8003;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).parameters
```

#### Methods

##### fromApplicationKey()

```ts
static fromApplicationKey(applicationKey: ApplicationKey): ConfigAppKeyStatus;
```

Creates a `ConfigAppKeyStatus` message confirming the request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to confirm. |

###### Returns

[`ConfigAppKeyStatus`](#configappkeystatus)

##### fromData()

```ts
static fromData(parameters: Data): ConfigAppKeyStatus | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigAppKeyStatus`](#configappkeystatus) \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetAndAppKeyMessage,
).fromData
```

##### responseTo()

```ts
static responseTo(request: ConfigNetAndAppKeyMessage, status: ConfigMessageStatus): ConfigAppKeyStatus;
```

Creates a `ConfigAppKeyStatus` message in case of a failure.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | `ConfigNetAndAppKeyMessage` | The request, for which this message is to be sent. |
| `status` | `ConfigMessageStatus` | The response status. |

###### Returns

[`ConfigAppKeyStatus`](#configappkeystatus)

***

### ConfigCompositionDataGet

#### Extends

- `AcknowledgedConfigMessage`

#### Constructors

##### Constructor

```ts
new ConfigCompositionDataGet(page?: number): ConfigCompositionDataGet;
```

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `page` | `number` | `0` | Page number of the Composition Data to get. |

###### Returns

[`ConfigCompositionDataGet`](#configcompositiondataget)

###### Overrides

```ts
AcknowledgedConfigMessage.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x8008;
```

The message Op Code.

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

##### page

```ts
page: number = 0;
```

Page number of the Composition Data to get.

##### responseType

```ts
responseType: typeof ConfigCompositionDataStatus = ConfigCompositionDataStatus;
```

The Type of the response message.

###### Overrides

```ts
AcknowledgedConfigMessage.responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8008;
```

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.parameters
```

#### Methods

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromData()

```ts
static fromData(parameters: Data): ConfigCompositionDataGet | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigCompositionDataGet`](#configcompositiondataget) \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.fromData
```

***

### ConfigCompositionDataStatus

#### Extends

- `ConfigResponse`

#### Constructors

##### Constructor

```ts
new ConfigCompositionDataStatus($page: CompositionDataPage): ConfigCompositionDataStatus;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `$page` | [`CompositionDataPage`](#abstract-compositiondatapage) | The Composition Data page. |

###### Returns

[`ConfigCompositionDataStatus`](#configcompositiondatastatus)

###### Overrides

```ts
ConfigResponse.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x02;
```

The message Op Code.

###### Overrides

```ts
ConfigResponse.opCode
```

##### opCode

```ts
readonly static opCode: number = 0x02;
```

###### Overrides

```ts
ConfigResponse.opCode
```

#### Accessors

##### page

###### Get Signature

```ts
get page(): CompositionDataPage;
```

###### Returns

[`CompositionDataPage`](#abstract-compositiondatapage)

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
ConfigResponse.parameters
```

#### Methods

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromData()

```ts
static fromData(parameters: Data): 
  | ConfigCompositionDataStatus
  | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

  \| [`ConfigCompositionDataStatus`](#configcompositiondatastatus)
  \| `undefined`

###### Overrides

```ts
ConfigResponse.fromData
```

***

### ConfigDefaultTtlGet

#### Extends

- `AcknowledgedConfigMessage`

#### Constructors

##### Constructor

```ts
new ConfigDefaultTtlGet(): ConfigDefaultTtlGet;
```

###### Returns

[`ConfigDefaultTtlGet`](#configdefaultttlget)

###### Inherited from

```ts
AcknowledgedConfigMessage.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x800c;
```

The message Op Code.

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

##### responseType

```ts
responseType: typeof ConfigDefaultTtlStatus = ConfigDefaultTtlStatus;
```

The Type of the response message.

###### Overrides

```ts
AcknowledgedConfigMessage.responseType
```

##### opCode

```ts
readonly static opCode: number = 0x800c;
```

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigDefaultTtlGet | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigDefaultTtlGet`](#configdefaultttlget) \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.fromData
```

***

### ConfigDefaultTtlStatus

#### Extends

- `ConfigResponse`

#### Constructors

##### Constructor

```ts
new ConfigDefaultTtlStatus(ttl: number): ConfigDefaultTtlStatus;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ttl` | `number` | The Time To Live (TTL) value. Valid value is in range 1...127. |

###### Returns

[`ConfigDefaultTtlStatus`](#configdefaultttlstatus)

###### Overrides

```ts
ConfigResponse.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x800e;
```

The message Op Code.

###### Overrides

```ts
ConfigResponse.opCode
```

##### ttl

```ts
ttl: number;
```

The Time To Live (TTL) value. Valid value is in range 1...127.

##### opCode

```ts
readonly static opCode: number = 0x800e;
```

###### Overrides

```ts
ConfigResponse.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
ConfigResponse.parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigDefaultTtlStatus | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigDefaultTtlStatus`](#configdefaultttlstatus) \| `undefined`

###### Overrides

```ts
ConfigResponse.fromData
```

***

### ConfigModelAppBind

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigAppKeyMessage`\<`this`\> & `ConfigAnyModelMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigModelAppBind(
   applicationKeyIndex: KeyIndex, 
   elementAddress: Address, 
   modelIdentifier: number, 
   companyIdentifier?: number | undefined
): ConfigModelAppBind;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | `undefined` |
| `elementAddress` | `Address` | `undefined` |
| `modelIdentifier` | `number` | `undefined` |
| `companyIdentifier` | `number` \| `undefined` | `undefined` |

###### Returns

[`ConfigModelAppBind`](#configmodelappbind)

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).applicationKeyIndex
```

##### companyIdentifier

```ts
companyIdentifier: number | undefined = undefined;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).companyIdentifier
```

##### elementAddress

```ts
elementAddress: Address;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).elementAddress
```

##### modelIdentifier

```ts
modelIdentifier: number;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).modelIdentifier
```

##### opCode

```ts
opCode: number = 0x803d;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

##### responseType

```ts
responseType: typeof ConfigModelAppStatus = ConfigModelAppStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x803d;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).parameters
```

#### Methods

##### fromApplicationKey()

```ts
static fromApplicationKey(applicationKey: ApplicationKey, model: Model): ConfigModelAppBind | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) |
| `model` | [`Model`](#model) |

###### Returns

[`ConfigModelAppBind`](#configmodelappbind) \| `undefined`

##### fromData()

```ts
static fromData(parameters: Data): ConfigModelAppBind | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigModelAppBind`](#configmodelappbind) \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).fromData
```

***

### ConfigModelAppStatus

#### Extends

- `ConfigResponse`\<`this`\> & `ConfigStatusMessage`\<`this`\> & `ConfigAppKeyMessage`\<`this`\> & `ConfigAnyModelMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigModelAppStatus(
   applicationKeyIndex: KeyIndex, 
   elementAddress: Address, 
   modelIdentifier: number, 
   status: ConfigMessageStatus, 
   companyIdentifier?: number | undefined
): ConfigModelAppStatus;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | `undefined` |
| `elementAddress` | `Address` | `undefined` |
| `modelIdentifier` | `number` | `undefined` |
| `status` | `ConfigMessageStatus` | `undefined` |
| `companyIdentifier` | `number` \| `undefined` | `undefined` |

###### Returns

[`ConfigModelAppStatus`](#configmodelappstatus)

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).applicationKeyIndex
```

##### companyIdentifier

```ts
companyIdentifier: number | undefined = undefined;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).companyIdentifier
```

##### elementAddress

```ts
elementAddress: Address;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).elementAddress
```

##### modelIdentifier

```ts
modelIdentifier: number;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).modelIdentifier
```

##### opCode

```ts
opCode: number = 0x803e;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

##### status

```ts
status: ConfigMessageStatus;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).status
```

##### opCode

```ts
readonly static opCode: number = 0x803e;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).parameters
```

#### Methods

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### confirm()

```ts
static confirm<T extends ConfigAppKeyMessage & ConfigAnyModelMessage>(request: T): ConfigModelAppStatus;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* `ConfigAppKeyMessage` & `ConfigAnyModelMessage` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | `T` |

###### Returns

[`ConfigModelAppStatus`](#configmodelappstatus)

##### fromData()

```ts
static fromData(parameters: Data): ConfigModelAppStatus | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigModelAppStatus`](#configmodelappstatus) \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).fromData
```

##### responseTo()

```ts
static responseTo<T extends ConfigAppKeyMessage & ConfigAnyModelMessage>(request: T, status: ConfigMessageStatus): ConfigModelAppStatus;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* `ConfigAppKeyMessage` & `ConfigAnyModelMessage` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | `T` |
| `status` | `ConfigMessageStatus` |

###### Returns

[`ConfigModelAppStatus`](#configmodelappstatus)

***

### ConfigModelAppUnbind

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigAppKeyMessage`\<`this`\> & `ConfigAnyModelMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigModelAppUnbind(
   applicationKeyIndex: KeyIndex, 
   elementAddress: Address, 
   modelIdentifier: number, 
   companyIdentifier?: number | undefined
): ConfigModelAppUnbind;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | `undefined` |
| `elementAddress` | `Address` | `undefined` |
| `modelIdentifier` | `number` | `undefined` |
| `companyIdentifier` | `number` \| `undefined` | `undefined` |

###### Returns

[`ConfigModelAppUnbind`](#configmodelappunbind)

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).constructor
```

#### Properties

##### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).applicationKeyIndex
```

##### companyIdentifier

```ts
companyIdentifier: number | undefined = undefined;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).companyIdentifier
```

##### elementAddress

```ts
elementAddress: Address;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).elementAddress
```

##### modelIdentifier

```ts
modelIdentifier: number;
```

###### Inherited from

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).modelIdentifier
```

##### opCode

```ts
opCode: number = 0x803f;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

##### responseType

```ts
responseType: typeof ConfigModelAppStatus = ConfigModelAppStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x803f;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).parameters
```

#### Methods

##### fromApplicationKey()

```ts
static fromApplicationKey(applicationKey: ApplicationKey, model: Model): ConfigModelAppUnbind | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) |
| `model` | [`Model`](#model) |

###### Returns

[`ConfigModelAppUnbind`](#configmodelappunbind) \| `undefined`

##### fromData()

```ts
static fromData(parameters: Data): ConfigModelAppUnbind | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigModelAppUnbind`](#configmodelappunbind) \| `undefined`

###### Overrides

```ts
Mixin(
  AcknowledgedConfigMessage,
  ConfigAppKeyMessage,
  ConfigAnyModelMessage,
).fromData
```

***

### ConfigNetKeyAdd

A `ConfigNetKeyAdd` is an acknowledged message used to add an ``NetworkKey``
to the NetKey List on a Node.

The Network Key is used to authenticate and decrypt messages it receives,
as well as authenticate and encrypt messages it sends.

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigNetKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigNetKeyAdd(networkKeyIndex: KeyIndex, key: Data): ConfigNetKeyAdd;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | - |
| `key` | `Data` | The 128-bit Application Key data. |

###### Returns

[`ConfigNetKeyAdd`](#confignetkeyadd)

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).constructor
```

#### Properties

##### key

```ts
key: Data;
```

The 128-bit Application Key data.

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8040;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

##### responseType

```ts
responseType: typeof ConfigNetKeyStatus = ConfigNetKeyStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8040;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigNetKeyAdd | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigNetKeyAdd`](#confignetkeyadd) \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).fromData
```

##### fromNetworkKey()

```ts
static fromNetworkKey(networkKey: NetworkKey): ConfigNetKeyAdd;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) |

###### Returns

[`ConfigNetKeyAdd`](#confignetkeyadd)

***

### ConfigNetKeyDelete

`ConfigNetKeyDelete` is an acknowledged message used to delete an `NetworkKey`
from the NetKey List on a Node.

To remove the key from the local Node you may use `MeshNetwork.remove()`.

WARN: It is not guaranteed, that the target Node will remove the key from its
           NetKey List. To make sure the Node gets excluded, use ``ConfigNetKeyUpdate``
           to update the value of the key and skip the Node when distributing the new
           value. After the Key Refresh Procedure is complete, the target Node will
           effectively be excluded from the mesh network.

#### Extends

- `AcknowledgedConfigMessage`\<`this`\> & `ConfigNetKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigNetKeyDelete(networkKeyIndex: KeyIndex): ConfigNetKeyDelete;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeyIndex` | `KeyIndex` |

###### Returns

[`ConfigNetKeyDelete`](#confignetkeydelete)

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).constructor
```

#### Properties

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8041;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

##### responseType

```ts
responseType: typeof ConfigNetKeyStatus = ConfigNetKeyStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8041;
```

The message Op Code.

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigNetKeyDelete | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigNetKeyDelete`](#confignetkeydelete) \| `undefined`

###### Overrides

```ts
Mixin(AcknowledgedConfigMessage, ConfigNetKeyMessage).fromData
```

##### fromNetworkKey()

```ts
static fromNetworkKey(networkKey: NetworkKey): ConfigNetKeyDelete;
```

Creates a ``ConfigNetKeyDelete`` message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to be removed. |

###### Returns

[`ConfigNetKeyDelete`](#confignetkeydelete)

***

### ConfigNetKeyStatus

A `ConfigNetKeyStatus` is an unacknowledged message used to report the status of
the operation on the NetKey List.

#### Extends

- `ConfigResponse`\<`this`\> & `ConfigStatusMessage`\<`this`\> & `ConfigNetKeyMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigNetKeyStatus(networkKeyIndex: KeyIndex, status: ConfigMessageStatus): ConfigNetKeyStatus;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKeyIndex` | `KeyIndex` |
| `status` | `ConfigMessageStatus` |

###### Returns

[`ConfigNetKeyStatus`](#confignetkeystatus)

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).constructor
```

#### Properties

##### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).networkKeyIndex
```

##### opCode

```ts
opCode: number = 0x8044;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).opCode
```

##### status

```ts
status: ConfigMessageStatus;
```

###### Inherited from

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).status
```

##### opCode

```ts
readonly static opCode: number = 0x8044;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigNetKeyStatus | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigNetKeyStatus`](#confignetkeystatus) \| `undefined`

###### Overrides

```ts
Mixin(
  ConfigResponse,
  ConfigStatusMessage,
  ConfigNetKeyMessage,
).fromData
```

##### fromNetworkKey()

```ts
static fromNetworkKey(networkKey: NetworkKey): ConfigNetKeyStatus;
```

Creates a ``ConfigNetKeyStatus`` message confirming the request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to confirm. |

###### Returns

[`ConfigNetKeyStatus`](#confignetkeystatus)

##### responseTo()

```ts
static responseTo(request: ConfigNetKeyMessage, status: ConfigMessageStatus): ConfigNetKeyStatus;
```

Creates a ``ConfigNetKeyStatus`` message in case of a failure.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | `ConfigNetKeyMessage` | The request, for which this message is to be sent. |
| `status` | `ConfigMessageStatus` | The response status. |

###### Returns

[`ConfigNetKeyStatus`](#confignetkeystatus)

***

### ConfigNodeReset

#### Extends

- `AcknowledgedConfigMessage`

#### Constructors

##### Constructor

```ts
new ConfigNodeReset(): ConfigNodeReset;
```

###### Returns

[`ConfigNodeReset`](#confignodereset)

###### Inherited from

```ts
AcknowledgedConfigMessage.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x8049;
```

The message Op Code.

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

##### responseType

```ts
responseType: typeof ConfigNodeResetStatus = ConfigNodeResetStatus;
```

The Type of the response message.

###### Overrides

```ts
AcknowledgedConfigMessage.responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8049;
```

###### Overrides

```ts
AcknowledgedConfigMessage.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigNodeReset | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigNodeReset`](#confignodereset) \| `undefined`

###### Overrides

```ts
AcknowledgedConfigMessage.fromData
```

***

### ConfigNodeResetStatus

#### Extends

- `ConfigResponse`

#### Constructors

##### Constructor

```ts
new ConfigNodeResetStatus(): ConfigNodeResetStatus;
```

###### Returns

[`ConfigNodeResetStatus`](#confignoderesetstatus)

###### Inherited from

```ts
ConfigResponse.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x804a;
```

The message Op Code.

###### Overrides

```ts
ConfigResponse.opCode
```

##### opCode

```ts
readonly static opCode: number = 0x804a;
```

###### Overrides

```ts
ConfigResponse.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
ConfigResponse.parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): ConfigNodeResetStatus | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`ConfigNodeResetStatus`](#confignoderesetstatus) \| `undefined`

###### Overrides

```ts
ConfigResponse.fromData
```

***

### Element

An Element is an addressable entity within a `Node`.

Each Node has at least one element, the Primary Element, and may have
one or more additional secondary elements. The number and structure of
elements is static and does not change throughout the lifetime of a node
(that is, as long as the node is part of a network).

The Primary Element is addressed using the first Unicast Address
assigned to the Node during provisioning. Each additional secondary
element is addressed using the subsequent addresses. These unicast
element addresses allow nodes to identify which element within a node
is transmitting or receiving a message.

#### Constructors

##### Constructor

```ts
new Element(
   name: string | undefined, 
   location: Location, 
   models: Model[]
): Element;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `location` | `Location` |
| `models` | [`Model`](#model)[] |

###### Returns

[`Element`](#element)

#### Properties

##### index

```ts
index: number;
```

Numeric order of the Element within this Node.

##### name

```ts
name: string | undefined = undefined;
```

UTF-8 human-readable name of the Element.

##### parentNode

```ts
parentNode: Node | undefined = undefined;
```

Parent Node. This may be `undefined` if the Element was obtained in
Composition Data and has not yet been added to a Node.

#### Accessors

##### isPrimary

###### Get Signature

```ts
get isPrimary(): boolean;
```

Returns whether the Element is a Primary Element on the Node,
that is its index is equal to 0.

###### Returns

`boolean`

##### location

###### Get Signature

```ts
get location(): Location;
```

###### Returns

`Location`

##### models

###### Get Signature

```ts
get models(): Model[];
```

An array of Model objects in the Element.

###### Returns

[`Model`](#model)[]

##### unicastAddress

###### Get Signature

```ts
get unicastAddress(): Address;
```

Returns the Unicast Address of the Element.

For Elements not added to Node this returns the Element index
value as `Address`.

###### Returns

`Address`

##### primaryElement

###### Get Signature

```ts
get static primaryElement(): Element;
```

The primary Element for Provisioner's Node.

The Element will contain all mandatory Models (Configuration Server and Health Server) and supported clients (Configuration Client and Health Client).

###### Returns

[`Element`](#element)

#### Methods

##### addModel()

```ts
addModel(model: Model): void;
```

Adds given model to the Element.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The model to be added. |

###### Returns

`void`

##### addPrimaryElementModels()

```ts
addPrimaryElementModels(meshNetwork: MeshNetwork, _publisher: MeshNetworkManager): void;
```

This methods adds the natively supported Models to the Element.

This method should only be called for the primary Element of the
local Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `meshNetwork` | [`MeshNetwork`](#meshnetwork-3) | The mesh network object. |
| `_publisher` | [`MeshNetworkManager`](#meshnetworkmanager) | - |

###### Returns

`void`

##### containsModelWithSigModelId()

```ts
containsModelWithSigModelId(sigModelId: number): boolean;
```

Returns whether the Element contains a Bluetooth SIG defined Model with
given Model ID.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `sigModelId` | `number` | Bluetooth SIG Model ID. |

###### Returns

`boolean`

`True` if the Element contains a Model with given Model ID,`false` otherwise.

##### insertModel()

```ts
insertModel(model: Model, i: number): void;
```

Inserts the given model to the Element at the specified position.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The model to be added. |
| `i` | `number` | - |

###### Returns

`void`

##### modelWithModelId()

```ts
modelWithModelId(modelId: number): Model | undefined;
```

Returns the first found Model with given identifier.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `modelId` | `number` | The 32-bit Model identifier. |

###### Returns

[`Model`](#model) \| `undefined`

The Model found, or `undefined` if no such exist.

##### removePrimaryElementModels()

```ts
removePrimaryElementModels(): void;
```

Removes the models that are or should be supported natively.

###### Returns

`void`

##### decode()

```ts
static decode(jv: Record<string, unknown>): Element;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`Element`](#element)

##### fromLocation()

```ts
static fromLocation(location: Location): Element;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `location` | `Location` |

###### Returns

[`Element`](#element)

***

### ElementData

#### Constructors

##### Constructor

```ts
new ElementData(
   index: number, 
   location: Location, 
   models: ModelData[]
): ElementData;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `index` | `number` | Numeric order of the Element within this Node. |
| `location` | `Location` | Description of the Element's location. |
| `models` | [`ModelData`](#modeldata)[] | An array of Model objects in the Element. |

###### Returns

[`ElementData`](#elementdata)

#### Properties

##### index

```ts
index: number;
```

Numeric order of the Element within this Node.

##### location

```ts
location: Location;
```

Description of the Element's location.

##### models

```ts
models: ModelData[];
```

An array of Model objects in the Element.

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromCompositionData()

```ts
static fromCompositionData(
   compositionData: Data, 
   offset: number, 
   index: number
): 
  | {
  elementData: ElementData;
  offset: number;
}
  | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `compositionData` | `Data` |
| `offset` | `number` |
| `index` | `number` |

###### Returns

  \| \{
  `elementData`: [`ElementData`](#elementdata);
  `offset`: `number`;
\}
  \| `undefined`

##### fromElement()

```ts
static fromElement(element: Element): ElementData;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | [`Element`](#element) |

###### Returns

[`ElementData`](#elementdata)

***

### GattBearer

The GATT bearer is responsible for sending and receiving mesh messages
to and from the GATT Proxy Node.

#### Extends

- [`BaseGattProxyBearer`](#basegattproxybearer)\<*typeof* `MeshProxyService`, `this`\> & [`MeshBearer`](#abstract-meshbearer)\<`this`\>

#### Constructors

##### Constructor

```ts
new GattBearer(
   name: string | undefined, 
   uuid: UUID, 
   centralManager: CBCentralManager
): GattBearer;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `uuid` | `UUID` |
| `centralManager` | `CBCentralManager` |

###### Returns

[`GattBearer`](#gattbearer)

###### Overrides

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).constructor
```

#### Properties

##### identifier

```ts
identifier: UUID;
```

The UUID associated with the peer.

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).identifier
```

##### isOpen

```ts
isOpen: boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).isOpen
```

##### logger?

```ts
optional logger?: LoggerHandler;
```

The logger receives logs sent from the bearer. The logs will contain
raw data of sent and received packets, as well as connection events.

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).logger
```

#### Accessors

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

The name of the peripheral.

###### Returns

`string` \| `undefined`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).name
```

#### Methods

##### centralManagerDidDisconnectPeripheral()

```ts
centralManagerDidDisconnectPeripheral(
   _: CBCentralManager, 
   peripheral: CBPeripheral, 
   error?: Error
): void;
```

Called when a peripheral is disconnected.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The disconnected peripheral. |
| `error?` | `Error` | Optional error if the disconnection was unexpected. |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).centralManagerDidDisconnectPeripheral
```

##### centralManagerDidDiscoverPeripheral()

```ts
centralManagerDidDiscoverPeripheral(
   _central: CBCentralManager, 
   peripheral: CBPeripheral, 
   rssi?: number, 
   _advertisementData?: unknown
): void;
```

Called when a peripheral is discovered.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The discovered peripheral. |
| `rssi?` | `number` | Optional received signal strength indicator (RSSI) of the peripheral. |
| `_advertisementData?` | `unknown` | - |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).centralManagerDidDiscoverPeripheral
```

##### centralManagerDidUpdateState()

```ts
centralManagerDidUpdateState(_central: CBCentralManager, state: CBCentralManagerState): void;
```

Called when the central manager's state is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `state` | `CBCentralManagerState` | A string representing the new state (e.g., 'poweredOn', 'poweredOff', etc.). |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).centralManagerDidUpdateState
```

##### close()

```ts
close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).close
```

##### didDisconnect()

```ts
didDisconnect(_peripheral: CBPeripheral): void;
```

Called when the peripheral disconnects.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).didDisconnect
```

##### didUpdateState()

```ts
didUpdateState(_peripheral: CBPeripheral): void;
```

Called when the peripheral connection state changes.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).didUpdateState
```

##### didUpdateValueForCharacteristic()

```ts
didUpdateValueForCharacteristic(_peripheral: CBPeripheral, characteristic: CBCharacteristic): void;
```

Called when a characteristic's value is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_peripheral` | `CBPeripheral` | - |
| `characteristic` | `CBCharacteristic` | The characteristic whose value was updated. |

###### Returns

`void`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).didUpdateValueForCharacteristic
```

##### open()

```ts
open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).open
```

##### readRSSI()

```ts
readRSSI(): Promise<number> | undefined;
```

Retrieves the current RSSI value for the peripheral while it is connected
to the central manager.

The result will be returned using `GattBearerDelegate.bearerDidReadRSSI()` callback.

###### Returns

`Promise`\<`number`\> \| `undefined`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).readRSSI
```

##### send()

```ts
send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).send
```

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(BaseGattProxyBearer<typeof MeshProxyService>, MeshBearer).supports
```

##### fromPeripheral()

```ts
static fromPeripheral(peripheral: CBPeripheral, centralManager: CBCentralManager): GattBearer;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `peripheral` | `CBPeripheral` |
| `centralManager` | `CBCentralManager` |

###### Returns

[`GattBearer`](#gattbearer)

***

### GattBearerError

Set of errors that may be thrown from the GATT bearer.

#### Extends

- `Error`

#### Properties

##### deviceNotSupported

```ts
static deviceNotSupported: GattBearerError;
```

The connected device does not have services required
by the Bearer.

***

### `abstract` GattBearerHandler

This class extends `BearerHandler` and adds GATT specific
event handlers.

#### Extends

- [`BearerHandler`](#abstract-bearerhandler)

#### Constructors

##### Constructor

```ts
new GattBearerHandler(): GattBearerHandler;
```

###### Returns

[`GattBearerHandler`](#abstract-gattbearerhandler)

###### Inherited from

[`BearerHandler`](#abstract-bearerhandler).[`constructor`](#constructor-5)

#### Methods

##### bearerDidClose()

```ts
abstract bearerDidClose(bearer: Bearer, error?: Error): void;
```

Callback called when the Bearer is no longer open.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | [`Bearer`](#abstract-bearer) | The Bearer. |
| `error?` | `Error` | The reason of closing the Bearer, or `undefined` if closing was intended. |

###### Returns

`void`

###### Inherited from

[`BearerHandler`](#abstract-bearerhandler).[`bearerDidClose`](#bearerdidclose)

##### bearerDidConnect()

```ts
abstract bearerDidConnect(bearer: Bearer): void;
```

Callback called when the GATT device has connected.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | [`Bearer`](#abstract-bearer) | The Bearer. |

###### Returns

`void`

##### bearerDidDiscoverServices()

```ts
abstract bearerDidDiscoverServices(bearer: Bearer): void;
```

Callback called when the services of the GATT device
have been discovered.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | [`Bearer`](#abstract-bearer) | The Bearer. |

###### Returns

`void`

##### bearerDidOpen()

```ts
abstract bearerDidOpen(bearer: Bearer): void;
```

Callback called when the Bearer is ready for use.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | [`Bearer`](#abstract-bearer) | The Bearer. |

###### Returns

`void`

###### Inherited from

[`BearerHandler`](#abstract-bearerhandler).[`bearerDidOpen`](#bearerdidopen)

##### bearerDidReadRSSI()

```ts
abstract bearerDidReadRSSI(bearer: Bearer, RSSI: Long): void;
```

Callback called periodically when a RSSI value to the
GATT Bearer has been obtained.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | [`Bearer`](#abstract-bearer) | The Bearer. |
| `RSSI` | `Long` | The Received Signal Strength Indication value, from -127 to around 4. |

###### Returns

`void`

***

### GenericOnOffGet

#### Extends

- `StaticAcknowledgedMeshMessage`

#### Constructors

##### Constructor

```ts
new GenericOnOffGet(): GenericOnOffGet;
```

###### Returns

[`GenericOnOffGet`](#genericonoffget)

###### Inherited from

```ts
StaticAcknowledgedMeshMessage.constructor
```

#### Properties

##### opCode

```ts
opCode: number = 0x8201;
```

The message Op Code.

###### Overrides

```ts
StaticAcknowledgedMeshMessage.opCode
```

##### responseType

```ts
responseType: typeof GenericOnOffStatus = GenericOnOffStatus;
```

The Type of the response message.

###### Overrides

```ts
StaticAcknowledgedMeshMessage.responseType
```

##### opCode

```ts
readonly static opCode: number = 0x8201;
```

###### Overrides

```ts
StaticAcknowledgedMeshMessage.opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
StaticAcknowledgedMeshMessage.parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

`undefined`

###### Overrides

```ts
StaticAcknowledgedMeshMessage.fromData
```

***

### GenericOnOffSet

#### Extends

- `StaticAcknowledgedMeshMessage`\<`this`\> & `TransactionMessage`\<`this`\> & `TransitionMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new GenericOnOffSet(
   isOn: boolean, 
   transitionTime?: TransitionTime | undefined, 
   delay?: number | undefined
): GenericOnOffSet;
```

Creates the Generic OnOff Set message.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `isOn` | `boolean` | `undefined` | The target value of the Generic OnOff state. |
| `transitionTime` | `TransitionTime` \| `undefined` | `undefined` | The time that an element will take to transition to the target state from the present state. |
| `delay` | `number` \| `undefined` | `undefined` | Message execution delay in 5 millisecond steps. |

###### Returns

[`GenericOnOffSet`](#genericonoffset)

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).constructor
```

#### Properties

##### delay

```ts
delay: number | undefined = undefined;
```

Message execution delay in 5 millisecond steps.

###### Inherited from

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).delay
```

##### isOn

```ts
isOn: boolean;
```

The new state of Generic OnOff Server.

##### opCode

```ts
opCode: number = 0x8202;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).opCode
```

##### responseType

```ts
responseType: typeof GenericOnOffStatus = GenericOnOffStatus;
```

The Type of the response message.

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).responseType
```

##### tid

```ts
tid: number;
```

Transaction identifier. If not set, this field will automatically
be set when the message is being sent or received.

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).tid
```

##### transitionTime

```ts
transitionTime: TransitionTime | undefined = undefined;
```

The time that an element will take to transition to the target state from the present state.

###### Inherited from

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).transitionTime
```

##### opCode

```ts
readonly static opCode: number = 0x8202;
```

The message Op Code.

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).parameters
```

#### Methods

##### fromData()

```ts
static fromData(parameters: Data): GenericOnOffSet | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`GenericOnOffSet`](#genericonoffset) \| `undefined`

###### Overrides

```ts
Mixin(
  StaticAcknowledgedMeshMessage,
  TransactionMessage,
  TransitionMessage,
).fromData
```

***

### GenericOnOffStatus

#### Extends

- `StaticMeshResponse`\<`this`\> & `TransitionStatusMessage`\<`this`\>

#### Constructors

##### Constructor

```ts
new GenericOnOffStatus(
   isOn: boolean, 
   remainingTime?: TransitionTime | undefined, 
   targetState?: boolean
): GenericOnOffStatus;
```

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `isOn` | `boolean` | `undefined` | The present state of Generic OnOff Server. |
| `remainingTime` | `TransitionTime` \| `undefined` | `undefined` | - |
| `targetState?` | `boolean` | `undefined` | The target state of Generic OnOff Server. |

###### Returns

[`GenericOnOffStatus`](#genericonoffstatus)

###### Overrides

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).constructor
```

#### Properties

##### isOn

```ts
isOn: boolean;
```

The present state of Generic OnOff Server.

##### opCode

```ts
opCode: number = 0x8204;
```

The message Op Code.

###### Overrides

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).opCode
```

##### remainingTime

```ts
remainingTime: TransitionTime | undefined = undefined;
```

###### Inherited from

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).remainingTime
```

##### targetState?

```ts
optional targetState?: boolean;
```

The target state of Generic OnOff Server.

##### opCode

```ts
readonly static opCode: number = 0x8204;
```

The message Op Code.

###### Overrides

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).opCode
```

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

`Data` \| `undefined`

###### Overrides

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).parameters
```

#### Methods

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromData()

```ts
static fromData(parameters: Data): GenericOnOffStatus | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `parameters` | `Data` |

###### Returns

[`GenericOnOffStatus`](#genericonoffstatus) \| `undefined`

###### Overrides

```ts
Mixin(StaticMeshResponse, TransitionStatusMessage).fromData
```

##### fromIsOn()

```ts
static fromIsOn(isOn: boolean): GenericOnOffStatus;
```

Creates the Generic OnOff Status message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `isOn` | `boolean` | The current value of the Generic OnOff state. |

###### Returns

[`GenericOnOffStatus`](#genericonoffstatus)

##### withStateAndTime()

```ts
static withStateAndTime(
   isOn: boolean, 
   targetState: boolean, 
   remainingTime: TransitionTime
): GenericOnOffStatus;
```

Creates the Generic OnOff Status message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `isOn` | `boolean` | The current value of the Generic OnOff state. |
| `targetState` | `boolean` | The target value of the Generic OnOff state. |
| `remainingTime` | `TransitionTime` | The time that an element will take to transition to the target state from the present state. |

###### Returns

[`GenericOnOffStatus`](#genericonoffstatus)

***

### `abstract` Group

The Group object represents a user-defined group of Nodes,
identified by Group Address or Virtual Label.

A group may be given a human-readable name.

In Mesh Configuration Database a Group may have a parent Group,
but this is not reflected in the Mesh Profile specification. Groups
cannot form circle relationships.

#### Constructors

##### Constructor

```ts
new Group(): Group;
```

###### Returns

[`Group`](#abstract-group)

#### Properties

##### address

```ts
abstract address: MeshAddress;
```

The address of the group.

##### groupAddress

```ts
abstract groupAddress: string;
```

The address property contains a 4-character hexadecimal
string from 0xC000 to 0xFEFF or a 32-character hexadecimal
string of virtual label UUID, and is the address of the group.

##### meshNetwork?

```ts
abstract optional meshNetwork?: MeshNetwork;
```

##### parentAddress

```ts
abstract parentAddress: string;
```

The parentAddress property contains a 4-character hexadecimal
string or a 32-character hexadecimal string and represents
an address of a parent Group in which this group is included.
The value of "0000" indicates that the group is not included
in another group (i.e., the group has no parent).

#### Accessors

##### groupName

###### Get Signature

```ts
get abstract groupName(): string;
```

###### Returns

`string`

##### isUsed

###### Get Signature

```ts
get abstract isUsed(): boolean;
```

Returns whether the Group is in use in the given mesh network.

A Group in use may either be a parent of some other Group,
or set as a publication or subscription for any Model or any
Element of any Node belonging to this network.

###### Returns

`boolean`

Whether the Group is in use in the mesh network.

#### Methods

##### equal()

```ts
abstract equal(other: Group): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`Group`](#abstract-group) |

###### Returns

`boolean`

##### isDirectChildOf()

```ts
abstract isDirectChildOf(parent: Group): boolean;
```

Returns whether this Group is a direct child group of the
given one.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `parent` | [`Group`](#abstract-group) | The Group to compare. |

###### Returns

`boolean`

`True` if this Group is a child group of the given one, otherwise `false`.

##### isDirectParentOf()

```ts
abstract isDirectParentOf(child: Group): boolean;
```

Returns whether this Group is the parent group of the
given one.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `child` | [`Group`](#abstract-group) | The Group to compare. |

###### Returns

`boolean`

`True` if the given Group is a child group of this one, otherwise `false`.

***

### HeartbeatMessage

#### Constructors

##### Constructor

```ts
new HeartbeatMessage(
   opCode: number, 
   source: Address, 
   destination: Address, 
   features: NodeFeatures, 
   initialTtl: number, 
   transportPdu: Data, 
   ivIndex: number, 
   receivedTtl?: number
): HeartbeatMessage;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `opCode` | `number` | Message Op Code. |
| `source` | `Address` | The Unicast Address of the originating Node. |
| `destination` | `Address` | The destination Address. This can be either Unicast or Group Address. |
| `features` | `NodeFeatures` | Currently active features of the Node. - If the Relay feature is set, the Relay feature of a Node is in use. - If the Proxy feature is set, the GATT Proxy feature of a Node is in use. - If the Friend feature is set, the Friend feature of a Node is in use. - If the Low Power feature is set, the Node has active relationship with a Friend Node. |
| `initialTtl` | `number` | Initial TTL used when sending the message. |
| `transportPdu` | `Data` | The raw data of Upper Transport Layer PDU. |
| `ivIndex` | `number` | The IV Index used to encode this message. |
| `receivedTtl?` | `number` | TTL value with which the Heartbeat message was received. This is set to `undefined` for outgoing Heartbeat messages. |

###### Returns

[`HeartbeatMessage`](#heartbeatmessage)

#### Properties

##### destination

```ts
destination: Address;
```

The destination Address. This can be either Unicast or Group Address.

##### features

```ts
features: NodeFeatures;
```

Currently active features of the Node.

- If the Relay feature is set, the Relay feature of a Node is in use.
- If the Proxy feature is set, the GATT Proxy feature of a Node is in use.
- If the Friend feature is set, the Friend feature of a Node is in use.
- If the Low Power feature is set, the Node has active relationship with a Friend Node.

##### initialTtl

```ts
initialTtl: number;
```

Initial TTL used when sending the message.

##### ivIndex

```ts
ivIndex: number;
```

The IV Index used to encode this message.

##### opCode

```ts
opCode: number;
```

Message Op Code.

##### receivedTtl?

```ts
optional receivedTtl?: number;
```

TTL value with which the Heartbeat message was received.

This is set to `undefined` for outgoing Heartbeat messages.

##### source

```ts
source: Address;
```

The Unicast Address of the originating Node.

##### transportPdu

```ts
transportPdu: Data;
```

The raw data of Upper Transport Layer PDU.

##### opCode

```ts
static opCode: number = 0x0a;
```

#### Accessors

##### hops

###### Get Signature

```ts
get hops(): number;
```

Number of hops that this message went through.

###### Returns

`number`

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromControlMessage()

```ts
static fromControlMessage(message: ControlMessage): HeartbeatMessage | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `ControlMessage` |

###### Returns

[`HeartbeatMessage`](#heartbeatmessage) \| `undefined`

***

### InputOobActions

A set of supported Input Out-of-band actions.

#### Extends

- `OptionSet`\<`UInt16`\>

#### Constructors

##### Constructor

```ts
new InputOobActions(value?: number): InputOobActions;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value?` | `number` |

###### Returns

[`InputOobActions`](#inputoobactions)

###### Inherited from

```ts
OptionSet<UInt16>.constructor
```

#### Properties

##### inputAlphanumeric

```ts
static inputAlphanumeric: InputOobActions;
```

##### inputNumeric

```ts
static inputNumeric: InputOobActions;
```

##### push

```ts
static push: InputOobActions;
```

##### twist

```ts
static twist: InputOobActions;
```

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

###### Overrides

```ts
OptionSet.toString
```

##### fromData()

```ts
static fromData(data: Data, offset: number): InputOobActions;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `offset` | `number` |

###### Returns

[`InputOobActions`](#inputoobactions)

###### Overrides

```ts
OptionSet.fromData
```

***

### `abstract` IvIndexObserver

An observer class for the IV Index changes in the mesh network.

WARNING: This class is for advanced users and should be used with caution.
The IV Index is managed internally by the library. Tracking changes
to the IV Index is not necessary for most applications.

#### Constructors

##### Constructor

```ts
new IvIndexObserver(): IvIndexObserver;
```

###### Returns

[`IvIndexObserver`](#abstract-ivindexobserver)

#### Methods

##### ivIndexDidChange()

```ts
abstract ivIndexDidChange(ivIndex: IvIndex): void;
```

Called when the IV Index of the mesh network has changed.

NOTE: This method is called from the main dispatch queue.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ivIndex` | `IvIndex` | The new IV Index value. |

###### Returns

`void`

***

### MeshAddress

#### Constructors

##### Constructor

```ts
new MeshAddress(address: Address, virtualLabel: UUID | undefined): MeshAddress;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | 16-bit address. |
| `virtualLabel` | `UUID` \| `undefined` | Virtual Label UUID. |

###### Returns

[`MeshAddress`](#meshaddress)

#### Properties

##### address

```ts
address: Address;
```

16-bit address.

##### virtualLabel

```ts
virtualLabel: UUID | undefined;
```

Virtual Label UUID.

#### Accessors

##### hex

###### Get Signature

```ts
get hex(): string;
```

###### Returns

`string`

#### Methods

##### equal()

```ts
equal(other: unknown): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `unknown` |

###### Returns

`boolean`

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromAddress()

```ts
static fromAddress(address: Address): MeshAddress;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `address` | `Address` |

###### Returns

[`MeshAddress`](#meshaddress)

##### fromHex()

```ts
static fromHex(hex: string): MeshAddress | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

###### Returns

[`MeshAddress`](#meshaddress) \| `undefined`

##### fromVirtualLabel()

```ts
static fromVirtualLabel(virtualLabel: UUID): MeshAddress;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `virtualLabel` | `UUID` |

###### Returns

[`MeshAddress`](#meshaddress)

***

### `abstract` MeshBearer

A mesh bearer is used to send mesh messages to provisioned nodes.

#### Extends

- [`Bearer`](#abstract-bearer)

#### Constructors

##### Constructor

```ts
new MeshBearer(): MeshBearer;
```

###### Returns

[`MeshBearer`](#abstract-meshbearer)

###### Inherited from

[`Bearer`](#abstract-bearer).[`constructor`](#constructor-3)

#### Properties

##### isOpen

```ts
abstract isOpen: boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

###### Inherited from

[`Bearer`](#abstract-bearer).[`isOpen`](#isopen-1)

#### Methods

##### close()

```ts
abstract close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

[`Bearer`](#abstract-bearer).[`close`](#close-1)

##### open()

```ts
abstract open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

[`Bearer`](#abstract-bearer).[`open`](#open-1)

##### send()

```ts
abstract send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

[`Bearer`](#abstract-bearer).[`send`](#send-1)

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

###### Inherited from

[`Bearer`](#abstract-bearer).[`supports`](#supports-1)

***

### MeshData

The Mesh Network configuration saved internally.
It contains the Mesh Network and additional data that
are not in the JSON schema, but are used by in the app.

#### Constructors

##### Constructor

```ts
new MeshData(): MeshData;
```

###### Returns

[`MeshData`](#meshdata)

#### Properties

##### meshNetwork

```ts
meshNetwork: MeshNetwork | undefined = undefined;
```

Mesh Network state.

#### Methods

##### decode()

```ts
decode(json: Record<string, unknown>, storage: Storage): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `json` | `Record`\<`string`, `unknown`\> |
| `storage` | `Storage` |

###### Returns

`boolean`

##### encode()

```ts
encode(): 
  | SerializeObjectProperties<MeshNetwork>
  | undefined;
```

###### Returns

  \| `SerializeObjectProperties`\<[`MeshNetwork`](#meshnetwork-3)\>
  \| `undefined`

***

### MeshNetwork

#### Constructors

##### Constructor

```ts
new MeshNetwork(
   name: string, 
   $storage: Storage, 
   uuid?: UUID
): MeshNetwork;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `$storage` | `Storage` |
| `uuid` | `UUID` |

###### Returns

[`MeshNetwork`](#meshnetwork-3)

#### Properties

##### applicationKeys

```ts
applicationKeys: ApplicationKey[];
```

##### groups

```ts
groups: Group[];
```

An array of Groups in the network.

##### isPartial

```ts
isPartial: boolean;
```

Whether the configuration contains full information about the mesh network,
or only partial. In partial configuration Nodes' Device Keys can be `nil`.

##### ivIndexObserver?

```ts
optional ivIndexObserver?: IvIndexObserver;
```

##### networkExclusions?

```ts
optional networkExclusions?: ExclusionList[];
```

##### networkKeys

```ts
networkKeys: NetworkKey[];
```

##### nodes

```ts
nodes: Node[];
```

An array of Nodes in the network.

##### provisioners

```ts
provisioners: Provisioner[];
```

An array of provisioner objects that includes information about known
Provisioners and ranges of addresses and scenes that have been allocated
to these Provisioners.

##### scenes

```ts
scenes: Scene[];
```

An array of Scenes in the network.

##### uuid

```ts
uuid: UUID;
```

Random 128-bit UUID allows differentiation among multiple mesh networks.

#### Accessors

##### ivIndex

###### Get Signature

```ts
get ivIndex(): IvIndex;
```

###### Returns

`IvIndex`

###### Set Signature

```ts
set ivIndex(newValue: IvIndex): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `IvIndex` |

###### Returns

`void`

##### localElements

###### Get Signature

```ts
get localElements(): Element[];
```

###### Returns

[`Element`](#element)[]

###### Set Signature

```ts
set localElements(elements: Element[]): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `elements` | [`Element`](#element)[] |

###### Returns

`void`

##### localProvisioner

###### Get Signature

```ts
get localProvisioner(): Provisioner | undefined;
```

Returns the local Provisioner, or `undefined` if the mesh network
does not have any.

###### See

`setLocalProvisioner()`

###### Returns

[`Provisioner`](#provisioner) \| `undefined`

##### meshName

###### Get Signature

```ts
get meshName(): string;
```

UTF-8 string, which should be human readable name for this mesh network.

###### Returns

`string`

###### Set Signature

```ts
set meshName(name: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |

###### Returns

`void`

##### nextAvailableApplicationKeyIndex

###### Get Signature

```ts
get nextAvailableApplicationKeyIndex(): KeyIndex | undefined;
```

Next available Key Index that can be assigned to a new Application Key.

NOTE: This method searches for any available key index that is not used,
        looking for gaps in the key indexes. If there are no gaps, the
        next available key index will be the first one after the last one.

###### Returns

`KeyIndex` \| `undefined`

##### nextAvailableNetworkKeyIndex

###### Get Signature

```ts
get nextAvailableNetworkKeyIndex(): KeyIndex | undefined;
```

Next available Key Index that can be assigned to a new Network Key.

NOTE: This method searches for any available key index that is not used,
        looking for gaps in the key indexes. If there are no gaps, the
        next available key index will be the first one after the last one.

###### Returns

`KeyIndex` \| `undefined`

##### timestamp

###### Get Signature

```ts
get timestamp(): number;
```

###### Returns

`number`

#### Methods

##### addApplicationKey()

```ts
addApplicationKey(key: ApplicationKey): void;
```

Adds the given Application Key to the network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | [`ApplicationKey`](#applicationkey) | The new Application Key to be added. |

###### Returns

`void`

##### addApplicationKeyWithProperties()

```ts
addApplicationKeyWithProperties(
   applicationKey: Data, 
   name: string, 
   index?: KeyIndex
): ApplicationKey | MeshNetworkError;
```

Adds a new Application Key and binds it to the first Network Key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | `Data` | The 128-bit Application Key. |
| `name` | `string` | The human readable name. |
| `index?` | `KeyIndex` | An optional Key Index to assign. If `undefined`, the next available Key Index will be assigned automatically. |

###### Returns

[`ApplicationKey`](#applicationkey) \| `MeshNetworkError`

This method returns an error if the key is not 128-bit long,
          there isn't any Network Key to bind the new key to
          or the assigned Key Index is out of range.

##### addGroup()

```ts
addGroup(_group: Group): MeshNetworkError | undefined;
```

Adds a new Group to the network.

If the mesh network already contains a Group with the same address,
this method returns an error.

Groups with predefined addresses (i.e. All Nodes) cannot be added as
custom groups.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_group` | [`Group`](#abstract-group) | The Group to be added. |

###### Returns

`MeshNetworkError` \| `undefined`

This method returns an error if a Group with the same address already exists in the mesh network, or it is a Special Group.

##### addNetworkKey()

```ts
addNetworkKey(key: NetworkKey): void;
```

Adds the given Network Key to the network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | [`NetworkKey`](#networkkey) | The new Network Key to be added. |

###### Returns

`void`

##### addNetworkKeyWithName()

```ts
addNetworkKeyWithName(
   networkKey: Data, 
   name: string, 
   index?: KeyIndex
): NetworkKey | MeshNetworkError;
```

Adds a new Network Key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | `Data` | The 128-bit Application Key. |
| `name` | `string` | The human readable name. |
| `index?` | `KeyIndex` | The optional Key Index to assign. If `undefined`, the next available Key Index will be assigned automatically. |

###### Returns

[`NetworkKey`](#networkkey) \| `MeshNetworkError`

This method returns an error if the key is not 128-bit long or the assigned Key Index is out of range.

###### See

`MeshNetwork.nextAvailableNetworkKeyIndex`

##### addNode()

```ts
addNode(node: Node): void | MeshNetworkError;
```

Adds the Node to the local database.

NOTE: This method should only be used to add debug Nodes, or Nodes that have already been provisioned.
Use `MeshNetworkManager.provisionUnprovisionedDevice()` to provision a Node to the mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | A Node to be added. |

###### Returns

`void` \| `MeshNetworkError`

This method returns an error if the Node's address is not available, the Node does not have a Network Key, the Network Key does not belong to the mesh network, or a Node with the same UUID already exists in the network.

##### addProvisioner()

```ts
addProvisioner(provisioner: Provisioner): Promise<void>;
```

Adds the Provisioner and assigns a Unicast Address to it.

This method does nothing if the Provisioner is already added to the
mesh network.

NOTE: To add a Provisioner object without assigning it a Unicast Address
use `addProvisionerWithAddress()`` passing `undefined` as the `address`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to be added. |

###### Returns

`Promise`\<`void`\>

###### Throws

`MeshNetworkError` - if the ranges allocated to the Provisioner are invalid ranges or ranges overlapping with an existing Provisioner.

##### addProvisionerWithAddress()

```ts
addProvisionerWithAddress(provisioner: Provisioner, unicastAddress?: Address): Promise<void>;
```

Adds the Provisioner and assigns the given Unicast Address to it.

This method does nothing if the Provisioner is already added to the
mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to be added. |
| `unicastAddress?` | `Address` | The Unicast Address to be used by the Provisioner. A `undefined` address means that the Provisioner is not able to perform configuration operations. |

###### Returns

`Promise`\<`void`\>

###### Throws

`MeshNetworkError` - if validation of the Provisioner has failed.

##### containsGroup()

```ts
containsGroup(group: Group): boolean;
```

Checks if a Group with the same address as the given one exists in the network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `group` | [`Group`](#abstract-group) | A Group to look for. |

###### Returns

`boolean`

`True` if the Group was found, `false` otherwise.

##### containsNode()

```ts
containsNode(node: Node): boolean;
```

Returns whether the given Node is in the mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to look for. |

###### Returns

`boolean`

`True` if the Node was found, `false` otherwise.

##### containsNodeWithUuid()

```ts
containsNodeWithUuid(uuid: UUID): boolean;
```

Returns whether the Node with given UUID is in the
mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `uuid` | `UUID` | The Node's UUID to look for. |

###### Returns

`boolean`

`True` if the Node was found, `false` otherwise.

##### containsProvisioner()

```ts
containsProvisioner(provisioner: Provisioner): boolean;
```

Returns whether the Provisioner is in the mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to look for. |

###### Returns

`boolean`

`True` if the Provisioner was found, `false` otherwise.

##### containsProvisionerWithUuid()

```ts
containsProvisionerWithUuid(uuid: UUID): boolean;
```

Returns whether the Provisioner with given UUID is in the
mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `uuid` | `UUID` | The Provisioner's UUID to look for. |

###### Returns

`boolean`

`True` if the Provisioner was found, `false` otherwise.

##### copy()

```ts
copy(configuration: ExportConfiguration): MeshNetwork;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `configuration` | `ExportConfiguration` |

###### Returns

[`MeshNetwork`](#meshnetwork-3)

##### encode()

```ts
encode(): SerializeObjectProperties<MeshNetwork>;
```

###### Returns

`SerializeObjectProperties`\<[`MeshNetwork`](#meshnetwork-3)\>

##### isAddressAvailableForNode()

```ts
isAddressAvailableForNode(address: Address, node: Node): boolean;
```

Returns whether the given address can be reassigned to the given Node.

The Unicast Addresses already assigned to the given Node are excluded from
checking address collisions, that is `true` is returned as if they were available.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | The first address to check. |
| `node` | [`Node`](#node) | The Node, which address is to change. It will be excluded from checking address collisions. |

###### Returns

`boolean`

`True`, if the address is available, `false` otherwise.

##### isAddressRangeAvailable()

```ts
isAddressRangeAvailable(range: AddressRange): boolean;
```

Returns whether the given address range can be assigned to a new Node.

This method does not check if the range is allocated to the current Provisioner.
For that, use ``Provisioner/hasAllocated(addressRange:)``.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`AddressRange`](#addressrange) | The address range to check. |

###### Returns

`boolean`

`True`, if the address is available, `false` otherwise.

##### matchesNetworkId()

```ts
matchesNetworkId(networkId: Data): boolean;
```

Returns whether any of the Network Keys in the mesh network
matches the given Network ID.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkId` | `Data` | The Network ID. |

###### Returns

`boolean`

`True` if the Network ID matches any subnetwork of this mesh network, `false` otherwise.

##### matchesNetworkIdentity()

```ts
matchesNetworkIdentity(networkIdentity: NetworkIdentity): boolean;
```

Returns whether any of the Network Keys in the mesh network
matches the given Network Identity.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkIdentity` | [`NetworkIdentity`](#networkidentity) | The Network Identity. |

###### Returns

`boolean`

`True` if the Network ID matches any subnetwork of this mesh network, `false` otherwise.

##### matchesNodeIdentity()

```ts
matchesNodeIdentity(nodeIdentity: NodeIdentity): boolean;
```

Returns whether any of the Nodes in the mesh network matches
the given Node Identity.

This method may be used to match the Node Identity or Private Node Identity beacons.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodeIdentity` | [`NodeIdentity`](#nodeidentity) | Node Identity obtained from the advertising packet. |

###### Returns

`boolean`

`True` if the given Node Identity match any Node of this mesh network; `false` otherwise.

##### nextAvailableGroupAddress()

```ts
nextAvailableGroupAddress(provisioner: Provisioner): Address | undefined;
```

Returns the next available Group Address from the Group Address range
assigned to the given Provisioner that can be assigned to a new Group.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner, which range is to be used for address generation. |

###### Returns

`Address` \| `undefined`

The next available Group Address that can be assigned to a new Group, or `undefined`, if there are no more available addresses in the allocated range.

##### nextAvailableUnicastAddressForProvisioner()

```ts
nextAvailableUnicastAddressForProvisioner(provisioner: Provisioner): Address | undefined;
```

Returns the next available Unicast Address from the Unicast Address range
assigned to the given Provisioner that can be assigned to that Provisioner's Node.

This method is assuming that the Provisioner has only 1 element.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner that is creating the Node for itself. The address will be taken from it's allocated range. |

###### Returns

`Address` \| `undefined`

The next available Unicast Address that can be assigned to a node, or `undefined`, if there are no more available addresses in the allocated range.

###### See

`nextAvailableUnicastAddressStartingFrom()``

##### nextAvailableUnicastAddressStartingFrom()

```ts
nextAvailableUnicastAddressStartingFrom(
   elementsCount: number, 
   provisioner: Provisioner, 
   offset?: Address
): Address | undefined;
```

Returns the next available Unicast Address from the Unicast Address range
assigned to the given Provisioner that can be assigned to a new Node with the given
number of Elements.

The returned address can be set as the primary Unicast Address of the Node.
Each following Element will be identified by a subsequent Unicast Address.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `elementsCount` | `number` | `undefined` | The number of Node's Elements. |
| `provisioner` | [`Provisioner`](#provisioner) | `undefined` | The Provisioner that is creating the node. The address will be taken from it's allocated range. |
| `offset` | `Address` | `Address.minUnicastAddress` | The primary Unicast Address to be assigned. |

###### Returns

`Address` \| `undefined`

The next available Unicast Address that can be assigned to a Node, or `undefined`, if there are no more available addresses in the allocated range.

##### nodeForProvisioner()

```ts
nodeForProvisioner(provisioner: Provisioner): Node | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) |

###### Returns

[`Node`](#node) \| `undefined`

##### nodeMatchingNodeIdentity()

```ts
nodeMatchingNodeIdentity(nodeIdentity: NodeIdentity): Node | undefined;
```

Returns a Node that matches the Node Identity, or `nil`.

This method may be used to match the Node Identity or Private Node Identity beacons.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `nodeIdentity` | [`NodeIdentity`](#nodeidentity) | Node Identity obtained from the advertising packet. |

###### Returns

[`Node`](#node) \| `undefined`

A Node that matches the given Node Identity; or `undefined` otherwise.

##### nodeWithAddress()

```ts
nodeWithAddress(address: Address): Node | undefined;
```

Returns the Node with the given Unicast Address. The address may
be belong to any of the Node's Elements.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | A Unicast Address to look for. |

###### Returns

[`Node`](#node) \| `undefined`

The Node found, or `undefined` if no such exists.

##### nodeWithUuid()

```ts
nodeWithUuid(uuid: UUID): Node | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuid` | `UUID` |

###### Returns

[`Node`](#node) \| `undefined`

##### removeApplicationKeyWithKeyIndex()

```ts
removeApplicationKeyWithKeyIndex(index: KeyIndex, force?: boolean): ApplicationKey | MeshNetworkError;
```

Removes Application Key with given Key Index.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `index` | `KeyIndex` | `undefined` | The Key Index of a key to be removed. |
| `force` | `boolean` | `false` | If set to `true`, the key will be deleted even if there are other Nodes known to use this key. |

###### Returns

[`ApplicationKey`](#applicationkey) \| `MeshNetworkError`

The removed key.

##### removeGroup()

```ts
removeGroup(_group: Group): MeshNetworkError | undefined;
```

Removes the given Group from the network.

The Group must not be in use, i.e. it may not be a parent of
another Group.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_group` | [`Group`](#abstract-group) | The Group to be removed. |

###### Returns

`MeshNetworkError` \| `undefined`

This method returns ``MeshNetworkError/groupInUse`` when the Group is in use in this mesh network.

##### removeNetworkKeyWithKeyIndex()

```ts
removeNetworkKeyWithKeyIndex(index: KeyIndex, force?: boolean): NetworkKey | MeshNetworkError;
```

Removes Network Key with given Key Index.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `index` | `KeyIndex` | `undefined` | The Key Index of a key to be removed. |
| `force` | `boolean` | `false` | If set to `true`, the key will be deleted even if there are other Nodes known to use this key. |

###### Returns

[`NetworkKey`](#networkkey) \| `MeshNetworkError`

The method returns an error if the key is in use and cannot be
          removed (unless `force` was set to `true`).

##### removeNode()

```ts
removeNode(node: Node): void;
```

Removes the Node from the local database.

This method only removes the Node from the local database, but the Node
may still be able to interact with the network. To reset the Node
send a ``ConfigNodeReset`` message to the remote Node.
It will be removed from the local database automatically when
``ConfigNodeResetStatus`` message is received.

NOTE: Sending Config Node Reset message does not guarantee that the
             Node won't be able to communicate with the network. To make sure
             that the Node will not be able to send and receive messages from
             the network all the Network Keys (and optionally Application Keys)
             known by the Node must to be updated using Key Refresh Procedure,
             or removed from other Nodes.
             See Bluetooth Mesh Profile 1.0.1, chapter: 3.10.7 Node Removal
             procedure.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to be removed. |

###### Returns

`void`

##### removeNodeWithUuid()

```ts
removeNodeWithUuid(uuid: UUID): Node | undefined;
```

Removes the Node with given UUID from the mesh network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `uuid` | `UUID` | The UUID of a Node to remove. |

###### Returns

[`Node`](#node) \| `undefined`

##### updateTimestamp()

```ts
updateTimestamp(): void;
```

###### Returns

`void`

##### copy()

```ts
static copy(network: MeshNetwork, configuration: ExportConfiguration): MeshNetwork;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `network` | [`MeshNetwork`](#meshnetwork-3) |
| `configuration` | `ExportConfiguration` |

###### Returns

[`MeshNetwork`](#meshnetwork-3)

##### decode()

```ts
static decode(jv: Record<string, unknown>, storage: Storage): MeshNetwork;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |
| `storage` | `Storage` |

###### Returns

[`MeshNetwork`](#meshnetwork-3)

***

### MeshNetworkManager

The main object responsible for managing the mesh network.

#### Extends

- `BindableTinyEmitter`\<`AttentionTimerHandler` & `MeshNetworkHandler`\>

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `MD` *extends* [`MeshData`](#meshdata) | [`MeshData`](#meshdata) |
| `MN` *extends* [`MeshNetwork`](#meshnetwork-3) | [`MeshNetwork`](#meshnetwork-3) |

#### Implements

- `NetworkParametersProvider`

#### Constructors

##### Constructor

```ts
new MeshNetworkManager<MD extends MeshData = MeshData, MN extends MeshNetwork = MeshNetwork>(
   storage: Storage, 
   MeshDataClass: MeshDataCtor<MD>, 
   MeshNetworkClass: MeshNetworkCtor<MN>, 
   handlerQueue?: DispatchQueue
): MeshNetworkManager<MD, MN>;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `storage` | `Storage` | `undefined` |
| `MeshDataClass` | [`MeshDataCtor`](#meshdatactor)\<`MD`\> | `undefined` |
| `MeshNetworkClass` | [`MeshNetworkCtor`](#meshnetworkctor)\<`MN`\> | `undefined` |
| `handlerQueue` | `DispatchQueue` | `DispatchQueue.main` |

###### Returns

[`MeshNetworkManager`](#meshnetworkmanager)\<`MD`, `MN`\>

###### Overrides

```ts
BindableTinyEmitter<AttentionTimerHandler & MeshNetworkHandler>.constructor
```

#### Properties

##### handlerQueue

```ts
handlerQueue: DispatchQueue;
```

A queue to call handler methods on.

##### networkParameters

```ts
networkParameters: NetworkParameters = NetworkParameters.default;
```

Network parameters.

###### Implementation of

```ts
NetworkParametersProvider.networkParameters
```

##### proxyFilter

```ts
proxyFilter: ProxyFilter;
```

The Proxy Filter state.

#### Accessors

##### isNetworkCreated

###### Get Signature

```ts
get isNetworkCreated(): boolean;
```

###### Returns

`boolean`

##### localElements

###### Get Signature

```ts
get localElements(): Element[];
```

###### Returns

[`Element`](#element)[]

###### Set Signature

```ts
set localElements(elements: Element[]): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `elements` | [`Element`](#element)[] |

###### Returns

`void`

##### logger

###### Get Signature

```ts
get logger(): LoggerHandler | undefined;
```

The logger handler will be called whenever a new log entry is created.

###### Returns

`LoggerHandler` \| `undefined`

###### Set Signature

```ts
set logger(value: LoggerHandler): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `LoggerHandler` |

###### Returns

`void`

##### meshNetwork

###### Get Signature

```ts
get meshNetwork(): MeshNetwork | undefined;
```

###### Returns

[`MeshNetwork`](#meshnetwork-3) \| `undefined`

##### networkManager

###### Get Signature

```ts
get networkManager(): NetworkManager | undefined;
```

###### Returns

`NetworkManager` \| `undefined`

##### networkManagerHandler

###### Get Signature

```ts
get networkManagerHandler(): $NetworkManagerHandler;
```

###### Returns

`$NetworkManagerHandler`

##### storage

###### Get Signature

```ts
get storage(): Storage;
```

###### Returns

`Storage`

##### transmitter

###### Get Signature

```ts
get transmitter(): Transmitter | undefined;
```

The sender object should send PDUs created by the manager
using any Bearer.

###### Returns

[`Transmitter`](#abstract-transmitter) \| `undefined`

###### Set Signature

```ts
set transmitter(value: Transmitter): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | [`Transmitter`](#abstract-transmitter) |

###### Returns

`void`

#### Methods

##### bearerDidDeliverData()

```ts
bearerDidDeliverData(
   _bearer: Bearer, 
   data: Data, 
   type: PduType
): void;
```

This method should be called whenever a PDU has been received from the mesh
network using any bearer.

When a complete Mesh Message is received and reassembled, the delegate's
`MeshNetworkDelegate.meshNetworkManager()`
will be called.

For easier integration with `GattBearer`, instead of calling this method,
set the manager as Bearer's `Bearer.dataDelegate`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_bearer` | [`Bearer`](#abstract-bearer) | - |
| `data` | `Data` | The PDU received. |
| `type` | [`PduType`](#pdutype) | The PDU type. |

###### Returns

`void`

##### clear()

```ts
clear(): Promise<void>;
```

Forgets the currently loaded network and saves the state.

The manager gets to the state as if no ``load()`` or ``createNewMeshNetwork()``
was called.

###### Returns

`Promise`\<`void`\>

##### createNewMeshNetworkWithNameAndProvisioner()

```ts
createNewMeshNetworkWithNameAndProvisioner(name: string, provisioner: Provisioner): MeshNetwork | MeshNetworkError;
```

Generates a new Mesh Network configuration with default values.

This method will override the existing configuration, if such exists.
The mesh network will contain the given ``Provisioner``
and randomly generated Primary Network Key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `name` | `string` | The user given network name. |
| `provisioner` | [`Provisioner`](#provisioner) | The default Provisioner. |

###### Returns

[`MeshNetwork`](#meshnetwork-3) \| `MeshNetworkError`

##### createNewMeshNetworkWithNameAndProvisionerName()

```ts
createNewMeshNetworkWithNameAndProvisionerName(name: string, provisionerName: string): MeshNetwork | MeshNetworkError;
```

Generates a new Mesh Network configuration with default values.

This method will override the existing configuration, if such exists.
The mesh network will contain one `Provisioner` with the given name
and randomly generated Primary Network Key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `name` | `string` | The user given network name. |
| `provisionerName` | `string` | The user given local provisioner name. |

###### Returns

[`MeshNetwork`](#meshnetwork-3) \| `MeshNetworkError`

##### ensureNetworkKey()

```ts
ensureNetworkKey(networkKey: NetworkKey): void;
```

This message checks whether a message encrypted with the given Network Key
can be relayed using the connected GATT Proxy Node.

A message may be sent to a local Node, or using a GATT Proxy Node.
Check if the message can be relayed to the destination using a Proxy Node.
The Proxy Node must know the Network Key; otherwise it will not be able to
decode the destination and decrement TTL.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to be used to send the message. |

###### Returns

`void`

##### export()

```ts
export(): Promise<Data>;
```

###### Returns

`Promise`\<`Data`\>

##### load()

```ts
load(): Promise<void>;
```

Loads the Mesh Network configuration from the `Storage` set in the initiator
of the manager.

###### Returns

`Promise`\<`void`\>

##### provisionUnprovisionedDevice()

```ts
provisionUnprovisionedDevice(unprovisionedDevice: UnprovisionedDevice, bearer: ProvisioningBearer): MeshNetworkError | ProvisioningManager;
```

This method returns the `ProvisioningManager` that can be used
to provision the given device.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `unprovisionedDevice` | [`UnprovisionedDevice`](#unprovisioneddevice) | The device to be added to mesh network. |
| `bearer` | [`ProvisioningBearer`](#abstract-provisioningbearer) | The Provisioning Bearer to be used for sending provisioning PDUs. |

###### Returns

`MeshNetworkError` \| [`ProvisioningManager`](#provisioningmanager)

The Provisioning manager that should be used to continue provisioning process after identification.

##### publish()

```ts
publish(message: MeshMessage, model: Model): Promise<MessageHandle | undefined>;
```

This method tries to publish the given message using the
publication information set in the ``Model``.

If the retransmission is set to a value greater than 0, and the message
is unacknowledged, this method will retransmit it number of times
with the count and interval specified in the retransmission object.

If the publication is not configured for the given Model, this method
does nothing.

- note: This method does not check whether the given Model does support
        the given message. It will publish whatever message is given using
        the publication configuration of the given Model.

An appropriate callback of the ``MeshNetworkDelegate`` will be called when
the message has been sent successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `MeshMessage` | The message to be sent. |
| `model` | [`Model`](#model) | The model from which to send the message. |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle) \| `undefined`\>

Message handle that can be used to cancel sending.

##### save()

```ts
save(): Promise<void>;
```

Saves the Mesh Network configuration in the ``Storage`` given in the initiator
of the manager.

If storage was not specified, the local file will be used.

###### Returns

`Promise`\<`void`\>

##### sendAcknowledgedConfigMessageToAddress()

```ts
sendAcknowledgedConfigMessageToAddress(message: {
  completion?: (result: Result<ConfigResponse, Error>) => void;
  destination: Address;
  initialTtl?: number;
  message: AcknowledgedConfigMessage;
  networkKey?: NetworkKey;
}): Promise<MessageHandle>;
```

Sends a Configuration Message to the Node with given destination Address.

The `destination` must be a Unicast Address, otherwise the method
throws an `AccessError.invalidDestination` error.

Apart from the `completion` callback, an appropriate callback of the
`MeshNetworkHandler` will be called when the message has been sent
successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | \{ `completion?`: (`result`: `Result`\<`ConfigResponse`, `Error`\>) => `void`; `destination`: `Address`; `initialTtl?`: `number`; `message`: `AcknowledgedConfigMessage`; `networkKey?`: [`NetworkKey`](#networkkey); \} | The message to be sent. |
| `message.completion?` | (`result`: `Result`\<`ConfigResponse`, `Error`\>) => `void` | - |
| `message.destination` | `Address` | - |
| `message.initialTtl?` | `number` | - |
| `message.message` | `AcknowledgedConfigMessage` | - |
| `message.networkKey?` | [`NetworkKey`](#networkkey) | - |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle)\>

Message handle that can be used to cancel sending.

###### Throws

This method throws when the mesh network has not been created,
          the local Node does not have configuration capabilities
          (no Unicast Address assigned), or the destination address
          is not a Unicast Address or it belongs to an unknown Node.
          Error `AccessError.cannotDelete` is sent when trying to
          delete the last Network Key on the device.

##### sendAcknowledgedConfigMessageToNode()

```ts
sendAcknowledgedConfigMessageToNode(message: {
  completion?: (result: Result<ConfigResponse, Error>) => void;
  initialTtl?: number;
  message: AcknowledgedConfigMessage;
  networkKey?: NetworkKey;
  node: Node;
}): Promise<MessageHandle>;
```

Sends a Configuration Message to the primary Element on the given `Node`.

Apart from the `completion` callback, an appropriate callback of the
`MeshNetworkHandler` will be called when the message has been sent
successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | \{ `completion?`: (`result`: `Result`\<`ConfigResponse`, `Error`\>) => `void`; `initialTtl?`: `number`; `message`: `AcknowledgedConfigMessage`; `networkKey?`: [`NetworkKey`](#networkkey); `node`: [`Node`](#node); \} | The message to be sent. |
| `message.completion?` | (`result`: `Result`\<`ConfigResponse`, `Error`\>) => `void` | - |
| `message.initialTtl?` | `number` | - |
| `message.message` | `AcknowledgedConfigMessage` | - |
| `message.networkKey?` | [`NetworkKey`](#networkkey) | - |
| `message.node` | [`Node`](#node) | - |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle)\>

This method throws when the mesh network has not been created,
          the local Node does not have configuration capabilities
          (no Unicast Address assigned), or the destination address
          is not a Unicast Address or it belongs to an unknown Node.
          Error `AccessError.cannotDelete` is sent when trying to
          delete the last Network Key on the device.

##### sendAcknowledgedMeshMessageToModel()

```ts
sendAcknowledgedMeshMessageToModel(message: {
  applicationKey?: ApplicationKey;
  initialTtl?: number;
  localElement?: Element;
  message: AcknowledgedMeshMessage;
  model: Model;
}): Promise<MeshResponse>;
```

Encrypts the message with the given Application Key and the Network Key
bound to it, and sends it to the Node to which the Model belongs to.

The key must be bound to the given Model. If the key is not provided, the first
Application Key bound to the target Model, which is also known by the GATT Proxy
Node, will be used.

An appropriate callback of the ``MeshNetworkDelegate`` will be called when
the message has been sent successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | \{ `applicationKey?`: [`ApplicationKey`](#applicationkey); `initialTtl?`: `number`; `localElement?`: [`Element`](#element); `message`: `AcknowledgedMeshMessage`; `model`: [`Model`](#model); \} | The message to be sent. |
| `message.applicationKey?` | [`ApplicationKey`](#applicationkey) | - |
| `message.initialTtl?` | `number` | - |
| `message.localElement?` | [`Element`](#element) | - |
| `message.message` | `AcknowledgedMeshMessage` | - |
| `message.model` | [`Model`](#model) | - |

###### Returns

`Promise`\<`MeshResponse`\>

The response with the expected ``AcknowledgedMeshMessage/responseOpCode``
           received from the target Node.

###### Throws

This method throws when the mesh network has not been created,
          the target Model does not belong to any Element, or has
          no Application Key bound to it, or when
          the local Node does not have configuration capabilities
          (no Unicast Address assigned), or the given local Element
          does not belong to the local Node.

##### sendMeshMessageToMeshAddress()

```ts
sendMeshMessageToMeshAddress(message: {
  applicationKey: ApplicationKey;
  destination: MeshAddress;
  initialTtl?: number;
  localElement?: Element;
  message: MeshMessage;
}): Promise<void>;
```

Encrypts the message with the Application Key and the Network Key
bound to it, and sends to the given destination address.

The method completes when the message has been sent or an error occurred.

An appropriate callback of the ``MeshNetworkDelegate`` will be called when
the message has been sent successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | \{ `applicationKey`: [`ApplicationKey`](#applicationkey); `destination`: [`MeshAddress`](#meshaddress); `initialTtl?`: `number`; `localElement?`: [`Element`](#element); `message`: `MeshMessage`; \} | The message to be sent. |
| `message.applicationKey` | [`ApplicationKey`](#applicationkey) | - |
| `message.destination` | [`MeshAddress`](#meshaddress) | - |
| `message.initialTtl?` | `number` | - |
| `message.localElement?` | [`Element`](#element) | - |
| `message.message` | `MeshMessage` | - |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws when the mesh network has not been created,
          the local Node does not have configuration capabilities
          (no Unicast Address assigned), or the given local Element
          does not belong to the local Node, or the manager failed to
          send the message.

##### sendProxyConfigurationMessage()

```ts
sendProxyConfigurationMessage(message: ProxyConfigurationMessage): Promise<void>;
```

Sends the Proxy Configuration Message to the connected Proxy Node.

This method will only work if the bearer uses is GATT Proxy.

The message will be encrypted and sent to the `Transmitter`, which
should deliver the PDU to the connected Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `ProxyConfigurationMessage` | The Proxy Configuration message to be sent. |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws when the mesh network has not been created.

##### sendUnacknowledgedMeshMessageToModel()

```ts
sendUnacknowledgedMeshMessageToModel(message: {
  applicationKey?: ApplicationKey;
  initialTtl?: number;
  localElement?: Element;
  message: UnacknowledgedMeshMessage;
  model: Model;
}): Promise<void>;
```

Encrypts the message with the given Application Key and the Network Key
bound to it, and sends it to the Node to which the Model belongs to.

The key must be bound to the given Model. If the key is not provided, the first
Application Key bound to the target Model, which is also known by the GATT Proxy
Node, will be used.

The method completes when the message has been sent or an error occurred.

An appropriate callback of the ``MeshNetworkHandler`` will be called when
the message has been sent successfully or a problem occurred.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | \{ `applicationKey?`: [`ApplicationKey`](#applicationkey); `initialTtl?`: `number`; `localElement?`: [`Element`](#element); `message`: `UnacknowledgedMeshMessage`; `model`: [`Model`](#model); \} | The message to be sent. |
| `message.applicationKey?` | [`ApplicationKey`](#applicationkey) | - |
| `message.initialTtl?` | `number` | - |
| `message.localElement?` | [`Element`](#element) | - |
| `message.message` | `UnacknowledgedMeshMessage` | - |
| `message.model` | [`Model`](#model) | - |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws when the mesh network has not been created,
          the target Model does not belong to any Element, or has
          no Application Key bound to it, or when
          the local Node does not have configuration capabilities
          (no Unicast Address assigned), or the given local Element
          does not belong to the local Node, or the manager failed to
          send the message.

***

### MessageHandle

#### Constructors

##### Constructor

```ts
new MessageHandle(
   message: MeshMessage, 
   source: Address, 
   destination: MeshAddress, 
   manager: NetworkManager
): MessageHandle;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `MeshMessage` |
| `source` | `Address` |
| `destination` | [`MeshAddress`](#meshaddress) |
| `manager` | `NetworkManager` |

###### Returns

[`MessageHandle`](#messagehandle)

#### Properties

##### destination

```ts
destination: MeshAddress;
```

The destination Address.

This can be any type of Address.

##### manager

```ts
manager: NetworkManager;
```

##### opCode

```ts
opCode: number;
```

The Op Code of the message.

##### source

```ts
source: Address;
```

The source Unicast Address.

#### Methods

##### cancel()

```ts
cancel(): Promise<void>;
```

Cancels sending the message.

Only segmented or acknowledged messages may be cancelled.

Unsegmented unacknowledged messages are sent almost instantaneously
(depending on the connection interval and message size)
and therefore cannot be cancelled.

###### Returns

`Promise`\<`void`\>

***

### Model

A Model defines the basic functionality of a `Node`.

A Node may include one or more `Element`, each with one or mode models.
A model defines the required states, the messages that act upon those states,
and any associated behavior.

Two Models with the same `Model.modelId` cannot be located on the same Element.

A Model may extend another Model. Models in Extend relationship may share states.
A Model which does not extend any other Model is called a *base* Model.

Models in Extend relationship located on the same Element share the Subscription List.

#### Constructors

##### Constructor

```ts
new Model(
   modelId: number, 
   $subscribe?: string[], 
   $bind?: KeyIndex[], 
   handler?: ModelHandler
): Model;
```

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `modelId` | `number` | `undefined` | - |
| `$subscribe` | `string`[] | `[]` | - |
| `$bind` | `KeyIndex`[] | `[]` | - |
| `handler?` | [`ModelHandler`](#abstract-modelhandler) | `undefined` | The model message handler. This is non-`undefined` for supported local Models and `undefined` for Models of remote Nodes. |

###### Returns

[`Model`](#model)

#### Properties

##### handler?

```ts
optional handler?: ModelHandler;
```

The model message handler. This is non-`undefined` for supported local Models
and `undefined` for Models of remote Nodes.

##### modelId

```ts
modelId: SigModelId;
```

Bluetooth SIG or vendor-assigned model identifier.

In case of vendor models the 2 most significant bytes of this property are
the Company Identifier, as registered in Bluetooth SIG Assigned Numbers database.

For Bluetooth SIG defined models these 2 bytes are `0x0000`.

Use `Model.modelIdentifier` to get the 16-bit model identifier and
`Model.companyIdentifier` to obtain the Company Identifier.

Use `Model.isBluetoothSIGAssigned` to check whether the Model is defined by
Bluetooth SIG.

##### parentElement

```ts
parentElement: Element | undefined = undefined;
```

Parent Element.

#### Accessors

##### applicationKeys

###### Get Signature

```ts
get applicationKeys(): ApplicationKey[];
```

###### Returns

[`ApplicationKey`](#applicationkey)[]

##### bind

###### Get Signature

```ts
get bind(): KeyIndex[];
```

An array of Application Key indexes to which this model is bound.

###### Returns

`KeyIndex`[]

##### boundApplicationKeys

###### Get Signature

```ts
get boundApplicationKeys(): ApplicationKey[];
```

List of Application Keys bound to this Model.

The list will not contain unknown Application Keys bound
to this Model, possibly bound by other Provisioner.

If the Node does not belong to any mesh network, this method returns an empty array.
In that case use ``Model.isBoundTo()`` instead.

###### Returns

[`ApplicationKey`](#applicationkey)[]

##### companyIdentifier

###### Get Signature

```ts
get companyIdentifier(): number | undefined;
```

The Company Identifier or `undefined`, if the model is Bluetooth SIG-assigned.

###### Returns

`number` \| `undefined`

##### companyName

###### Get Signature

```ts
get companyName(): string;
```

###### Returns

`string`

##### isBluetoothSIGAssigned

###### Get Signature

```ts
get isBluetoothSIGAssigned(): boolean;
```

Returns `true` for Models with identifiers assigned by Bluetooth SIG,
`false` otherwise.

###### Returns

`boolean`

##### isBridgeConfigurationClient

###### Get Signature

```ts
get isBridgeConfigurationClient(): boolean;
```

###### Returns

`boolean`

##### isBridgeConfigurationServer

###### Get Signature

```ts
get isBridgeConfigurationServer(): boolean;
```

###### Returns

`boolean`

##### isConfigurationClient

###### Get Signature

```ts
get isConfigurationClient(): boolean;
```

###### Returns

`boolean`

##### isConfigurationServer

###### Get Signature

```ts
get isConfigurationServer(): boolean;
```

###### Returns

`boolean`

##### isDirectedForwardingConfigurationClient

###### Get Signature

```ts
get isDirectedForwardingConfigurationClient(): boolean;
```

###### Returns

`boolean`

##### isDirectedForwardingConfigurationServer

###### Get Signature

```ts
get isDirectedForwardingConfigurationServer(): boolean;
```

###### Returns

`boolean`

##### isGenericOnOffServer

###### Get Signature

```ts
get isGenericOnOffServer(): boolean;
```

###### Returns

`boolean`

##### isHealthClient

###### Get Signature

```ts
get isHealthClient(): boolean;
```

###### Returns

`boolean`

##### isHealthServer

###### Get Signature

```ts
get isHealthServer(): boolean;
```

###### Returns

`boolean`

##### isLargeCompositionDataClient

###### Get Signature

```ts
get isLargeCompositionDataClient(): boolean;
```

###### Returns

`boolean`

##### isLargeCompositionDataServer

###### Get Signature

```ts
get isLargeCompositionDataServer(): boolean;
```

###### Returns

`boolean`

##### isOnDemandPrivateProxyClient

###### Get Signature

```ts
get isOnDemandPrivateProxyClient(): boolean;
```

###### Returns

`boolean`

##### isOnDemandPrivateProxyServer

###### Get Signature

```ts
get isOnDemandPrivateProxyServer(): boolean;
```

###### Returns

`boolean`

##### isOpcodesAggregatorClient

###### Get Signature

```ts
get isOpcodesAggregatorClient(): boolean;
```

###### Returns

`boolean`

##### isOpcodesAggregatorServer

###### Get Signature

```ts
get isOpcodesAggregatorServer(): boolean;
```

###### Returns

`boolean`

##### isPrivateBeaconClient

###### Get Signature

```ts
get isPrivateBeaconClient(): boolean;
```

###### Returns

`boolean`

##### isPrivateBeaconServer

###### Get Signature

```ts
get isPrivateBeaconServer(): boolean;
```

###### Returns

`boolean`

##### isRemoteProvisioningClient

###### Get Signature

```ts
get isRemoteProvisioningClient(): boolean;
```

###### Returns

`boolean`

##### isRemoteProvisioningServer

###### Get Signature

```ts
get isRemoteProvisioningServer(): boolean;
```

###### Returns

`boolean`

##### isSarConfigurationClient

###### Get Signature

```ts
get isSarConfigurationClient(): boolean;
```

###### Returns

`boolean`

##### isSarConfigurationServer

###### Get Signature

```ts
get isSarConfigurationServer(): boolean;
```

###### Returns

`boolean`

##### isSceneClient

###### Get Signature

```ts
get isSceneClient(): boolean;
```

###### Returns

`boolean`

##### modelIdentifier

###### Get Signature

```ts
get modelIdentifier(): number;
```

Bluetooth SIG or vendor-assigned model identifier.

###### Returns

`number`

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

The Model name as defined in Bluetooth Mesh Model Specification.

###### Returns

`string` \| `undefined`

##### publish

###### Get Signature

```ts
get publish(): Publish | undefined;
```

###### Returns

[`Publish`](#abstract-publish) \| `undefined`

##### relatedModels

###### Get Signature

```ts
get relatedModels(): Model[];
```

Returns all ``Model`` instances that are in a hierarchy of *Extend* relationship with this Model.

The *Extend* relationship is explained in Mesh Profile 1.0.1, chapter 2.3.6.

NOTE: Models that operate on bound states share a single instance of a Subscription List per Element.

NOTE: Model extension is only defined for SIG Models. Currently it is not possible to
        get relationships between Vendor Models, and for those this method returns an empty list.

###### Returns

[`Model`](#model)[]

##### requiresDeviceKey

###### Get Signature

```ts
get requiresDeviceKey(): boolean;
```

Returns whether the access layer security on the Model shall use the Device Key.

###### Returns

`boolean`

##### subscribe

###### Get Signature

```ts
get subscribe(): string[];
```

The array of Group Addresses (4-character hexadecimal string),
or Virtual Label UUIDs (32-character hexadecimal string).

###### Returns

`string`[]

##### subscriptions

###### Get Signature

```ts
get subscriptions(): Group[];
```

Returns the list of known Groups that this Model is subscribed to.
Models on the Primary Element are also subscribed to the All Nodes address.

It may be that the Model is subscribed to some other Groups, which are
not known to the local database, and those are not returned.

Use `Model.isSubscribed()` to check other Groups.

###### Returns

[`Group`](#abstract-group)[]

##### supportsDeviceKey

###### Get Signature

```ts
get supportsDeviceKey(): boolean;
```

Returns whether the access layer security on the Model can use the Device Key.

###### Returns

`boolean`

#### Methods

##### bindApplicationKeyWithIndex()

```ts
bindApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Adds the given Application Key Index to the bound keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key index to bind. |

###### Returns

`void`

##### copyFrom()

```ts
copyFrom(model: Model): void;
```

Copies the properties from the given Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model to copy from. |

###### Returns

`void`

##### extendsDirectly()

```ts
extendsDirectly(_other: Model): boolean;
```

Returns whether that Model directly extends the given ``Model``.

This method only checks direct Extend relationship, not hierarchical. If a Model A extends B,
which extends C, this method will return `false` if checked with A and C. Base Models
may be on the same Element or on an Element with a lower index.

The *Extend* relationship is explained in Mesh Profile 1.0.1, chapter 2.3.6.

NOTE: Models in Extend relationship share their Subscription List if they are on the same Element.

NOTE: Model extension is only defined for SIG Models. Currently it is not possible to
        get relationships between Vendor Models, and for those this method returns `false`.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_other` | [`Model`](#model) |

###### Returns

`boolean`

`True` if the given Model is a *base* Model of that one, `false` otherwise.

##### isSubscribedToGroup()

```ts
isSubscribedToGroup(group: Group): boolean;
```

Returns whether the Model is subscribed to the given `Group`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `group` | [`Group`](#abstract-group) | The Group to check subscription to. |

###### Returns

`boolean`

`True` if the Model is subscribed to the Group, `false` otherwise.

##### isSubscribedToMeshAddress()

```ts
isSubscribedToMeshAddress(address: MeshAddress): boolean;
```

Returns whether the Model is subscribed to the given `MeshAddress`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | [`MeshAddress`](#meshaddress) | The address to check subscription to. |

###### Returns

`boolean`

`True` if the Model is subscribed to a `Group` with given, address, `false` otherwise.

##### unbindApplicationKeyWithIndex()

```ts
unbindApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Removes the Application Key binding to with the given Key Index
and clears the publication, if it was set to use the same key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key index to unbind. |

###### Returns

`void`

##### decode()

```ts
static decode(jv: Record<string, unknown>): Model;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`Model`](#model)

##### fromSigModelId()

```ts
static fromSigModelId(sigModelId: SigModelId): Model;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `sigModelId` | `SigModelId` |

###### Returns

[`Model`](#model)

##### fromSigModelIdAndHandler()

```ts
static fromSigModelIdAndHandler(sigModelId: number, handler: ModelHandler): Model;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `sigModelId` | `number` |
| `handler` | [`ModelHandler`](#abstract-modelhandler) |

###### Returns

[`Model`](#model)

***

### ModelData

#### Constructors

##### Constructor

```ts
new ModelData(modelId: number): ModelData;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `modelId` | `number` |

###### Returns

[`ModelData`](#modeldata)

#### Properties

##### modelId

```ts
modelId: number;
```

#### Accessors

##### companyIdentifier

###### Get Signature

```ts
get companyIdentifier(): number | undefined;
```

The Company Identifier or `undefined`, if the model is Bluetooth SIG-assigned.

###### Returns

`number` \| `undefined`

##### isBluetoothSIGAssigned

###### Get Signature

```ts
get isBluetoothSIGAssigned(): boolean;
```

Returns `true` for Models with identifiers assigned by Bluetooth SIG, `false` otherwise.

###### Returns

`boolean`

##### modelIdentifier

###### Get Signature

```ts
get modelIdentifier(): number;
```

Bluetooth SIG or vendor-assigned model identifier.

###### Returns

`number`

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromModel()

```ts
static fromModel(model: Model): ModelData;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `model` | [`Model`](#model) |

###### Returns

[`ModelData`](#modeldata)

##### fromSigModelId()

```ts
static fromSigModelId(sigModelId: number): ModelData;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `sigModelId` | `number` |

###### Returns

[`ModelData`](#modeldata)

##### fromVendorModelId()

```ts
static fromVendorModelId(vendorModelId: number, companyId: number): ModelData;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `vendorModelId` | `number` |
| `companyId` | `number` |

###### Returns

[`ModelData`](#modeldata)

***

### `abstract` ModelHandler

Model handler defines the functionality of a `Model` on the
Local Node.

Model Delegates are assigned to the Models during setting up
the `MeshNetworkManager.localElements`.

The Model Delegate must declare a map of mesh message type
supported by this Model. Whenever a mesh message matching any
of the declared Op Codes is received, and the Model instance is bound
to the Application Key used to encrypt the message, one of the message
handlers will be called:
* `ModelDelegate.modelDidReceiveUnacknowledgedMessage()`
* `ModelDelegate.modelDidReceiveAcknowledgedMessage()`
* `ModelDelegate.modelDidReceiveResponse()`

The Model Delegate also specifies should the Model support subscription
and defines publication composer for automatic publications.

#### Extended by

- [`StoredWithSceneModelHandler`](#abstract-storedwithscenemodelhandler)

#### Constructors

##### Constructor

```ts
new ModelHandler(): ModelHandler;
```

###### Returns

[`ModelHandler`](#abstract-modelhandler)

#### Accessors

##### isSubscriptionSupported

###### Get Signature

```ts
get isSubscriptionSupported(): boolean;
```

###### Returns

`boolean`

##### messageTypes

###### Get Signature

```ts
get messageTypes(): Map<number, {
  fromData: (_: Data) => MeshMessage | undefined;
}>;
```

###### Returns

`Map`\<`number`, \{
  `fromData`: (`_`: `Data`) => `MeshMessage` \| `undefined`;
\}\>

##### publicationMessageComposer

###### Get Signature

```ts
get abstract publicationMessageComposer(): MessageComposer | undefined;
```

The message composer that will be used to create a Mesh Message.

The composer will be used whenever model is about to publish its
state using the publishing information specified in the Model.

When set to `undefined`, the library will return error
`ConfigMessageStatus.invalidPublishParameters` for each Config
Model Publication Set and Config Model Publication Virtual Address Set.

###### Returns

`MessageComposer` \| `undefined`

#### Methods

##### decode()

```ts
decode(accessPdu: AccessPdu): MeshMessage | undefined;
```

This method tries to decode the Access PDU into a Message.

The Model Handler must support the opcode and specify to
which type should the message be decoded.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `accessPdu` | `AccessPdu` | The Access PDU received. |

###### Returns

`MeshMessage` \| `undefined`

The decoded message, or `undefined`, if the message is not supported or invalid.

##### modelDidReceiveAcknowledgedMessage()

```ts
abstract modelDidReceiveAcknowledgedMessage(
   model: Model, 
   request: AcknowledgedMeshMessage, 
   source: Address, 
   destination: MeshAddress
): Error | MeshResponse;
```

This method should handle the received Acknowledged Message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `request` | `AcknowledgedMeshMessage` | The Acknowledged Message received. |
| `source` | `Address` | The source Unicast Address. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |

###### Returns

`Error` \| `MeshResponse`

The response message to be sent to the sender or The method should returns a `ModelError` if the receive message is invalid and no response should be replied.

##### modelDidReceiveMessage()

```ts
modelDidReceiveMessage(
   model: Model, 
   message: MeshMessage, 
   source: Address, 
   destination: MeshAddress, 
   request?: AcknowledgedMeshMessage
): MeshResponse | undefined;
```

This method handles the decoded message and passes it to
the proper handle method, depending on its type or whether
it is a response to a previously sent request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The local Model that received the message. |
| `message` | `MeshMessage` | The decoded message. |
| `source` | `Address` | The Unicast Address of the Element that the message originates from. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |
| `request?` | `AcknowledgedMeshMessage` | The request message sent previously that this message replies to, or `undefined`, if this is not a response. |

###### Returns

`MeshResponse` \| `undefined`

The response message, if the received message is an Acknowledged Mesh Message that needs to be replied.

##### modelDidReceiveResponse()

```ts
abstract modelDidReceiveResponse(
   model: Model, 
   response: MeshResponse, 
   request: AcknowledgedMeshMessage, 
   source: Address
): void;
```

This method should handle the received response to the
previously sent request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `response` | `MeshResponse` | The response received. |
| `request` | `AcknowledgedMeshMessage` | The Acknowledged Message sent. |
| `source` | `Address` | The Unicast Address of the Element that sent the response. |

###### Returns

`void`

##### modelDidReceiveUnacknowledgedMessage()

```ts
abstract modelDidReceiveUnacknowledgedMessage(
   model: Model, 
   message: UnacknowledgedMeshMessage, 
   source: Address, 
   destination: MeshAddress
): void;
```

This method should handle the received Unacknowledged Message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `message` | `UnacknowledgedMeshMessage` | The Unacknowledged Message received. |
| `source` | `Address` | The source Unicast Address. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |

###### Returns

`void`

##### publish()

```ts
publish(message: MeshMessage, manager: MeshNetworkManager): Promise<MessageHandle | undefined>;
```

Publishes a single message given as a parameter using the
Publish information set in the underlying Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `MeshMessage` | The message to be published. |
| `manager` | [`MeshNetworkManager`](#meshnetworkmanager) | The manager to be used for publishing. |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle) \| `undefined`\>

The Message Handler that can be used to cancel sending.

##### publishUsing()

```ts
publishUsing(manager: MeshNetworkManager): Promise<MessageHandle | undefined>;
```

Publishes a single message created by Model's message composer using
the Publish information set in the underlying Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `manager` | [`MeshNetworkManager`](#meshnetworkmanager) | The manager to be used for publishing. |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle) \| `undefined`\>

The Message Handler that can be used to cancel sending.

***

### NetworkKey

#### Extends

- `Key`

#### Constructors

##### Constructor

```ts
new NetworkKey(
   name: string, 
   index: KeyIndex, 
   key: Data
): NetworkKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `index` | `KeyIndex` |
| `key` | `Data` |

###### Returns

[`NetworkKey`](#networkkey)

###### Overrides

```ts
Key.constructor
```

#### Accessors

##### index

###### Get Signature

```ts
get index(): KeyIndex;
```

Index of this key, in range from 0 through to 4095.

###### Returns

`KeyIndex`

###### Set Signature

```ts
set index(newValue: KeyIndex): void;
```

Index of this key, in range from 0 through to 4095.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `KeyIndex` |

###### Returns

`void`

###### Overrides

```ts
Key.index
```

##### isPrimary

###### Get Signature

```ts
get isPrimary(): boolean;
```

Returns whether the Network Key is the Primary Network Key.
The Primary key is the one which Key Index is equal to 0.

A Primary Network Key may not be removed from the mesh network,
but can be removed from any Node using Config Net Key Delete
messages encrypted using an Application Key bound to a different
Network Key.

###### Returns

`boolean`

##### isSecondary

###### Get Signature

```ts
get isSecondary(): boolean;
```

Returns whether the Network Key is a secondary Network Key,
that is the Key Index is NOT equal to 0.

###### Returns

`boolean`

##### key

###### Get Signature

```ts
get key(): Data;
```

###### Returns

`Data`

###### Set Signature

```ts
set key(newValue: Data): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `Data` |

###### Returns

`void`

###### Overrides

```ts
Key.key
```

##### keys

###### Get Signature

```ts
get keys(): NetworkKeyDerivatives;
```

###### Returns

[`NetworkKeyDerivatives`](#networkkeyderivatives)

##### minSecurity

###### Get Signature

```ts
get minSecurity(): Security;
```

Minimum security level for a subnet associated with this Network Key.

If all Nodes on the subnet associated with this network key have been
provisioned using the Secure Provisioning procedure, then
the value of this property for the subnet is set to `Security.secure`;
otherwise the value is set to `Security.insecure` and the subnet
is considered less secure.

###### Returns

`Security`

###### Set Signature

```ts
set minSecurity(newValue: Security): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `Security` |

###### Returns

`void`

##### networkId

###### Get Signature

```ts
get networkId(): Data;
```

The Network ID derived from this Network Key. This identifier
is public information.

###### Returns

`Data`

###### Set Signature

```ts
set networkId(newValue: Data): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `Data` |

###### Returns

`void`

##### oldKey

###### Get Signature

```ts
get oldKey(): Data | undefined;
```

The old Network Key is present when the phase property has a different
value than `KeyRefreshPhase.normalOperation`, such as when a Key Refresh
procedure is in progress.

###### Returns

`Data` \| `undefined`

###### Set Signature

```ts
set oldKey(newValue: Data | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `Data` \| `undefined` |

###### Returns

`void`

##### oldKeys

###### Get Signature

```ts
get oldKeys(): NetworkKeyDerivatives | undefined;
```

###### Returns

[`NetworkKeyDerivatives`](#networkkeyderivatives) \| `undefined`

##### oldNetworkId

###### Get Signature

```ts
get oldNetworkId(): Data | undefined;
```

The Network ID derived from the old Network Key. This identifier
is public information. It is set when `NetworkKey.oldKey` is set.

###### Returns

`Data` \| `undefined`

###### Set Signature

```ts
set oldNetworkId(newValue: Data | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `Data` \| `undefined` |

###### Returns

`void`

##### phase

###### Get Signature

```ts
get phase(): KeyRefreshPhase;
```

###### Returns

`KeyRefreshPhase`

###### Set Signature

```ts
set phase(newValue: KeyRefreshPhase): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `KeyRefreshPhase` |

###### Returns

`void`

##### timestamp

###### Get Signature

```ts
get timestamp(): number;
```

###### Returns

`number`

###### Set Signature

```ts
set timestamp(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### transmitKeys

###### Get Signature

```ts
get transmitKeys(): NetworkKeyDerivatives;
```

Returns the key set that should be used for encrypting outgoing packets.

###### Returns

[`NetworkKeyDerivatives`](#networkkeyderivatives)

#### Methods

##### equals()

```ts
equals(other: NetworkKey): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`NetworkKey`](#networkkey) |

###### Returns

`boolean`

##### isUsedInMeshNetwork()

```ts
isUsedInMeshNetwork(meshNetwork: MeshNetwork): boolean;
```

Return whether the Network Key is used in the given mesh network.

A `true` is returned when the Network Key is added to Network Keys
array of the network and is known to at least one node, or bound
to an existing Application Key.

A used Network Key may not be removed from the network.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `meshNetwork` | [`MeshNetwork`](#meshnetwork-3) | The mesh network to look the key in. |

###### Returns

`boolean`

`True` if the key is used in the given network, `false` otherwise.

##### lowerSecurity()

```ts
lowerSecurity(): void;
```

This method lowers the minimum security level of the Network Key to
`Security.insecure`.

###### Returns

`void`

###### See

 - `Security`
 - `NetworkKey.minSecurity`

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### decode()

```ts
static decode(jv: Record<string, unknown>): NetworkKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`NetworkKey`](#networkkey)

##### fromName()

```ts
static fromName(
   name: string, 
   index: KeyIndex, 
   key: Data
): NetworkKey | MeshNetworkError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `index` | `KeyIndex` |
| `key` | `Data` |

###### Returns

[`NetworkKey`](#networkkey) \| `MeshNetworkError`

##### primaryNetworkKey()

```ts
static primaryNetworkKey(): NetworkKey;
```

Creates the primary Network Key for a mesh network.

###### Returns

[`NetworkKey`](#networkkey)

***

### NetworkKeyDerivatives

#### Constructors

##### Constructor

```ts
new NetworkKeyDerivatives(key: Data): NetworkKeyDerivatives;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Data` |

###### Returns

[`NetworkKeyDerivatives`](#networkkeyderivatives)

#### Properties

##### beaconKey

```ts
beaconKey: Data;
```

The Beacon Key.

##### encryptionKey

```ts
encryptionKey: Data;
```

The Encryption Key.

##### identityKey

```ts
identityKey: Data;
```

The Identity Key.

##### nid

```ts
nid: number;
```

Network identifier.

##### privacyKey

```ts
privacyKey: Data;
```

The Privacy Key.

##### privateBeaconKey

```ts
privateBeaconKey: Data;
```

The Private Beacon Key.

***

### NetworkParameters

#### Constructors

##### Constructor

```ts
new NetworkParameters(): NetworkParameters;
```

###### Returns

[`NetworkParameters`](#networkparameters-1)

#### Properties

##### allowIvIndexRecoveryOver42

```ts
allowIvIndexRecoveryOver42: boolean = false;
```

According to Bluetooth Mesh Profile 1.0.1, section 3.10.5, if the IV Index
of the mesh network increased by more than 42 since the last connection
(which can take at least 48 weeks), the Node should be re-provisioned.
However, as this library can be used to provision other Nodes, it should not
be blocked from sending messages to the network only because the phone wasn't
connected to the network for that time. This flag can disable this check,
effectively allowing such connection.

The same can be achieved by clearing the app data (uninstalling and reinstalling
the app) and importing the mesh network. With no "previous" IV Index, the
library will accept any IV Index received in the Secure Network beacon upon
connection to the GATT Proxy Node.

##### ivUpdateTestMode

```ts
ivUpdateTestMode: boolean = false;
```

IV Update Test Mode enables efficient testing of the IV Update procedure.
The IV Update test mode removes the 96-hour limit; all other behavior of the device
are unchanged.

###### See

Bluetooth Mesh Profile 1.0.1, section 3.10.5.1.

##### default

```ts
readonly static default: NetworkParameters;
```

A set of default network parameters.

Example:
```ts
meshNetworkManager.networkParameters = NetworkParameters.default
```

#### Accessors

##### acknowledgmentMessageInterval

###### Get Signature

```ts
get acknowledgmentMessageInterval(): number;
```

The base time after which the acknowledged message will be repeated.

The repeat timer will be set using the following formula:
```
acknowledgment message interval + 50 ms * TTL + 50 ms * number of segments
```
The TTL and segment count dependent parts are added
automatically, and this value shall specify only the constant part.

The interval is doubled each time a request is retransmitted.

The default value is 2 seconds.

###### Returns

`number`

###### Set Signature

```ts
set acknowledgmentMessageInterval(value: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

`void`

##### acknowledgmentMessageTimeout

###### Get Signature

```ts
get acknowledgmentMessageTimeout(): number;
```

If the Element does not receive a response within a period of time known
as the acknowledged message timeout, then the Element may consider the
message has not been delivered, without sending any additional messages.

The `MeshNetworkHandler.meshNetworkManagerFailedToSendMessage()`
callback will be called on timeout.

The acknowledged message timeout should be set to a minimum of 30 seconds,
which is the default value.

###### Returns

`number`

###### Set Signature

```ts
set acknowledgmentMessageTimeout(value: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

`void`

##### completeAcknowledgmentTimerInterval

###### Get Signature

```ts
get completeAcknowledgmentTimerInterval(): number;
```

The initial value of the timer ensuring that no more than one Segment Acknowledgment message
is sent for the same SeqAuth value in a period of:
```
acknowledgment delay increment * segment reception interval (ms)
```

###### Returns

`number`

##### defaultTtl

###### Get Signature

```ts
get defaultTtl(): number;
```

The default value of Time To Live (TTL), which is used for sending messages if the
value is not set for the Provisioner's Node.

In Bluetooth Mesh each message is sent with a given TTL value. When a relay
Node receives such message it decrements the TTL value by 1, re-encrypts it
using the same Network Key and retransmits further. If the received TTL value is
1 or 0 the message is no longer retransmitted.

By default TTL is set to 5, which is a reasonable value. The TTL shall be in range 2...127.

###### Returns

`number`

###### Set Signature

```ts
set defaultTtl(value: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

`void`

##### discardTimeout

###### Get Signature

```ts
get discardTimeout(): number;
```

The Discard Timeout is the time that the Lower Transport layer waits
after receiving a new segment of a segmented message before
discarding that segmented message.

Valid range for this timeout is from 5 seconds to 1 minute and 20 seconds
(80 seconds) with 5 second step. The default value is 10 seconds.

The Discard Timeout is reset every time a new segment of a message
is received.

The value of this timeout is controlled by `sarDiscardTimeout`
state and is calculated the following way:
```
(SAR Discard Timeout + 1) * 5 ms
```

###### Returns

`number`

##### multicastRetransmissionsInterval

###### Get Signature

```ts
get multicastRetransmissionsInterval(): number;
```

The interval between retransmissions of segments of a segmented message for
a destination that is a Group Address or a Virtual Address.

Valid range is from 25 ms to 400 ms with 25 ms interval.

###### See

`sarMulticastRetransmissionsIntervalStep`

###### Returns

`number`

##### sarAcknowledgmentDelayIncrement

###### Get Signature

```ts
get sarAcknowledgmentDelayIncrement(): number;
```

The **SAR Acknowledgment Delay Increment state** is a 3-bit value that controls
the interval between the reception of a new segment of a segmented message
for a destination that is a Unicast Address and the transmission of the
Segment Acknowledgment for that message.

The default value of the **SAR Acknowledgment Delay Increment state** is `0b001`
(2.5 segment transmission interval steps).

@see`sarReceiverSegmentIntervalStep`

###### Returns

`number`

###### Set Signature

```ts
set sarAcknowledgmentDelayIncrement(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarAcknowledgmentRetransmissionsCount

###### Get Signature

```ts
get sarAcknowledgmentRetransmissionsCount(): number;
```

The **SAR Acknowledgment Retransmissions Count** state is a 2-bit value
that controls the number of retransmissions of Segment Acknowledgment messages
sent by the lower transport layer.

Retransmission of Segment Acknowledgment messages is only enabled for messages
composed of more segments then the value of `sarSegmentsThreshold`.

The maximum number of transmissions of a Segment Acknowledgment message is
```
SAR Acknowledgment Retransmissions Count + 1
```
For example, `0b00` represents a limit of 1 transmission, and `0b11` represents a limit of 4 transmissions.

The default value of the **SAR Acknowledgment Retransmissions Count state** is `0b00`
(1 transmission, retransmissions disabled).

NOTE: Retransmission of Segment Acknowledgment messages is controlled by `sarSegmentsThreshold`.

###### See

`sarSegmentsThreshold`

###### Returns

`number`

###### Set Signature

```ts
set sarAcknowledgmentRetransmissionsCount(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarDiscardTimeout

###### Get Signature

```ts
get sarDiscardTimeout(): number;
```

The **SAR Discard Timeout state** is a 4-bit value that controls the time that the
Lower Transport layer waits after receiving unique segments of a segmented
message before discarding that segmented message.

The default value of the **SAR Discard Timeout state** is `0b0001` (10 seconds).

The Discard Timeout initial value is set using the following formula:
```
(SAR Discard Timeout + 1) * 5 ms
```

###### Returns

`number`

###### Set Signature

```ts
set sarDiscardTimeout(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarMulticastRetransmissionsCount

###### Get Signature

```ts
get sarMulticastRetransmissionsCount(): number;
```

The **SAR Multicast Retransmissions Count state** is a 4-bit value that
controls the maximum number of transmissions of segments of segmented
messages to a group address or a virtual address.

The maximum number of transmissions of a segment is calculated with the
following formula:
```
SAR Multicast Retransmissions Count + 1
```
For example, `0b0000` represents a single transmission, and `0b0111`
represents 8 transmissions.

The default value of the **SAR Multicast Retransmissions Count state** is
`0b0010` (3 transmissions).

###### Returns

`number`

###### Set Signature

```ts
set sarMulticastRetransmissionsCount(value: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

`void`

##### sarMulticastRetransmissionsIntervalStep

###### Get Signature

```ts
get sarMulticastRetransmissionsIntervalStep(): number;
```

The SAR Multicast Retransmissions Interval Step state** is a 4-bit
value that controls the interval between retransmissions of segments of a
segmented message for a destination that is a group address or a virtual address.

The multicast retransmissions interval is the number of milliseconds
calculated using the following formula:
```
SAR Multicast Retransmissions Interval Step + 1
```
The default value of the **SAR Multicast Retransmissions Interval Step state** is
`0b1001` (250 milliseconds).

###### Returns

`number`

###### Set Signature

```ts
set sarMulticastRetransmissionsIntervalStep(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarReceiverSegmentIntervalStep

###### Get Signature

```ts
get sarReceiverSegmentIntervalStep(): number;
```

The **SAR Receiver Segment Interval Step state** is a 4-bit value that indicates
the interval between received segments of a segmented message.
This is used to control rate of transmission of Segment Acknowledgment messages.

The default value of the **SAR Receiver Segment Interval Step state** is `0b0101`
(60 milliseconds).

@see`sarAcknowledgmentDelayIncrement`

###### Returns

`number`

###### Set Signature

```ts
set sarReceiverSegmentIntervalStep(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarSegmentIntervalStep

###### Get Signature

```ts
get sarSegmentIntervalStep(): number;
```

The **SAR Segment Interval Step state** is a 4-bit value that controls
the interval between transmissions of segments of a segmented message.

The segment transmission interval is the number of milliseconds calculated
using the following formula:
```
(SAR Segment Interval Step + 1) * 10 ms
```
The default value of the **SAR Segment Interval Step state** is `0b0101`
(60 milliseconds).

###### Returns

`number`

###### Set Signature

```ts
set sarSegmentIntervalStep(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarSegmentsThreshold

###### Get Signature

```ts
get sarSegmentsThreshold(): number;
```

The **SAR Segments Threshold state** is a 5-bit value that represents
the size of a segmented message in number of segments above which the
retransmissions of Segment Acknowledgment messages are enabled.

Example: When a message is composed of 4 segments retransmissions of
Segment Acknowledgment messages is enabled if the **SAR Segments
Threshold state** is set to 3 or less.

NOTE: Retransmissions of Segment Acknowledgment messages is always
        disabled for single-segment segmented messages as they are complete
        after receiving just one segment. The value of 0 and 1 are then
        equivalent, as the shortest message for which Ack retransmissions
        are enabled is 2 segments.

The default value for the **SAR Segments Threshold state** is `0b00011` (3 segments).

###### See

`sarAcknowledgmentRetransmissionsCount`

###### Returns

`number`

###### Set Signature

```ts
set sarSegmentsThreshold(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarUnicastRetransmissionsCount

###### Get Signature

```ts
get sarUnicastRetransmissionsCount(): number;
```

The **SAR Unicast Retransmissions Count state** is a 4-bit value that
controls the maximum number of transmissions of segments of segmented
messages to a Unicast Address destination.

The maximum number of transmissions of a segment is given with the formula:
```
SAR Unicast Retransmissions Count + 1
```
For example, `0b0000` represents a single transmission, and `0b0111`
represents 8 transmissions.

The default value of the **SAR Unicast Retransmissions Count state** is
`0b0010` (3 transmissions).

###### See

`sarUnicastRetransmissionsWithoutProgressCount`

###### Returns

`number`

###### Set Signature

```ts
set sarUnicastRetransmissionsCount(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarUnicastRetransmissionsIntervalIncrement

###### Get Signature

```ts
get sarUnicastRetransmissionsIntervalIncrement(): number;
```

The **SAR Unicast Retransmissions Interval Increment state** is a 4-bit
value that controls the incremental component of the interval between
retransmissions of segments of a segmented message for a destination
that is a Unicast Address.

The unicast retransmissions interval increment is the number of milliseconds
calculated using the following formula:
```
(SAR Unicast Retransmissions Interval Increment + 1) * 25 (ms)
```
The default value of the **SAR Unicast Retransmissions Interval Increment state**
is `0b0001` (50 milliseconds).

###### See

`sarUnicastRetransmissionsIntervalStep`

###### Returns

`number`

###### Set Signature

```ts
set sarUnicastRetransmissionsIntervalIncrement(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarUnicastRetransmissionsIntervalStep

###### Get Signature

```ts
get sarUnicastRetransmissionsIntervalStep(): number;
```

The **SAR Unicast Retransmissions Interval Step state** is a 4-bit value
that controls the interval between retransmissions of segments of a segmented
message for a destination that is a Unicast Address.

The unicast retransmissions interval step is the number of milliseconds calculated
using the following formula:
```
(SAR Unicast Retransmissions Interval Step + 1) * 25 (ms)
```
The default value of the **SAR Unicast Retransmissions Interval Step**
is `0b0111` (200 milliseconds).

###### See

`sarUnicastRetransmissionsIntervalIncrement`

###### Returns

`number`

###### Set Signature

```ts
set sarUnicastRetransmissionsIntervalStep(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### sarUnicastRetransmissionsWithoutProgressCount

###### Get Signature

```ts
get sarUnicastRetransmissionsWithoutProgressCount(): number;
```

The **SAR Unicast Retransmissions Without Progress Count state**
is a 4-bit value that controls the maximum number of transmissions of segments
of segmented messages to a Unicast destination without progress
(i.e., without newly marking any segment as acknowledged).

The maximum number of transmissions of a segment without progress is
calculated using the formula:
```
SAR Unicast Retransmissions Without Progress Count + 1
```
For example, `0b0000` represents a single transmission, and `0b0111`
represents 8 transmissions.

The default value of the **SAR Unicast Retransmissions Without Progress
Count state** is `0b0010` (3 transmissions).

NOTE: The value of this state should be set to a value greater than the
        value of the **SAR Acknowledgement Retransmissions Count**
        on a peer node. This helps prevent the SAR transmitter from
        abandoning the SAR prematurely.

###### See

`sarUnicastRetransmissionsCount`

###### Returns

`number`

###### Set Signature

```ts
set sarUnicastRetransmissionsWithoutProgressCount(newValue: number): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `number` |

###### Returns

`void`

##### segmentReceptionInterval

###### Get Signature

```ts
get segmentReceptionInterval(): number;
```

A value indicated by the **SAR Receiver Segment Interval Step state**.

###### See

 - `sarReceiverSegmentIntervalStep`
 - `setAcknowledgmentTimerInterval()`

###### Returns

`number`

##### segmentTransmissionInterval

###### Get Signature

```ts
get segmentTransmissionInterval(): number;
```

The interval between transmissions of segments of a segmented message.

The value of this interval is indicated by **SAR Segment Interval Step state**.

###### See

`sarSegmentIntervalStep`

###### Returns

`number`

#### Methods

##### acknowledgmentMessageIntervalForTtl()

```ts
acknowledgmentMessageIntervalForTtl(ttl: number, segmentCount: Long): number;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` |
| `segmentCount` | `Long` |

###### Returns

`number`

##### acknowledgmentTimerInterval()

```ts
acknowledgmentTimerInterval(segN: number): number;
```

The initial value of the SAR Acknowledgment timer for a given `segN`.

The value depends on the number of segments in a segmented message.

The initial value of the SAR Acknowledgment timer is calculated using the following
formula:
```
min(SegN + 0.5 , acknowledgment delay increment) * segment reception interval (ms)
```
where
```
acknowledgment delay increment = SAR Acknowledgment Delay Increment + 1.5

segment reception interval = (SAR Receiver Segment Interval Step + 1) × 10 ms
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `segN` | `number` |

###### Returns

`number`

##### unicastRetransmissionsInterval()

```ts
unicastRetransmissionsInterval(ttl: number): number;
```

The initial value of the SAR Unicast Retransmissions timer.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ttl` | `number` | The TTL value with the message is being sent. |

###### Returns

`number`

The initial value of the SAR Unicast Retransmissions timer.

##### basic()

```ts
static basic(builder: (config: NetworkParametersConfig) => void): NetworkParameters;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `builder` | (`config`: [`NetworkParametersConfig`](#networkparametersconfig)) => `void` |

###### Returns

[`NetworkParameters`](#networkparameters-1)

***

### NetworkParametersConfig

The builder allows easy configuration of `NetworkParameters`.

#### Constructors

##### Constructor

```ts
new NetworkParametersConfig(): NetworkParametersConfig;
```

###### Returns

[`NetworkParametersConfig`](#networkparametersconfig)

#### Properties

##### networkParameters

```ts
networkParameters: NetworkParameters = NetworkParameters.default;
```

#### Methods

##### build()

```ts
build(): NetworkParameters;
```

Builds the `NetworkParameters` structure.

###### Returns

[`NetworkParameters`](#networkparameters-1)

##### discardAcknowledgedMessages()

```ts
discardAcknowledgedMessages(timeout: number): void;
```

Sets the timeout for receiving a response to an acknowledged access message.

The ``MeshNetworkDelegate/meshNetworkManager(_:failedToSendMessage:from:to:error:)-9gepm``
callback will be called when the response is not received before the timeout expires..

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `timeout` | `number` | The timeout after which the `AccessError.timeout` is reported. This shall be set to a minimum of 30 seconds, which is also the default value. |

###### Returns

`void`

###### See

`NetworkParameters.acknowledgmentMessageTimeout`

##### discardIncompleteSegmentedMessages()

```ts
discardIncompleteSegmentedMessages(timeout: number): void;
```

Sets the time after which an incomplete segmented message is discarded when no new segment
is received. The timer is restarted each time a new segment is received.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `timeout` | `number` | The time since last received segment, after which segmented message is discarded. Valid range for the timeout is from 5 seconds to 1 minute and 20 seconds (80 seconds) with 5 second step. Default value is 10 seconds. |

###### Returns

`void`

###### See

`NetworkParameters.sarDiscardTimeout`

##### retransmitAcknowledgedMessage()

```ts
retransmitAcknowledgedMessage(interval: number): void;
```

Sets the base time after which the acknowledged message is repeated.

The repeat timer will be set using the following formula:
```
acknowledgment message interval + 50 ms * TTL + 50 ms * number of segments
```
TTL and the component dependent on number of segments are added
automatically. This method adjusts only the constant component.

The interval is doubled each time the request is retransmitted until the
response is received or the timeout set using `discardAcknowledgedMessages()`
expires.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `interval` | `number` |

###### Returns

`void`

###### See

`NetworkParameters.acknowledgmentMessageInterval`

##### retransmitAllSegmentsToGroupAddress()

```ts
retransmitAllSegmentsToGroupAddress(total: Long, interval: number): void;
```

Sets number and interval of retransmissions of segments of a segmented message for
a destination that is a Group Address or a Virtual Address.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `total` | `Long` | Number of retransmissions of segments of a segmented message for a multicast destination. The default value is 3. |
| `interval` | `number` | The interval between retransmissions of segments. The default interval is 250 ms. |

###### Returns

`void`

###### See

 - `NetworkParameters.sarMulticastRetransmissionsCount`
 - `NetworkParameters.sarMulticastRetransmissionsIntervalStep`

##### retransmitSegmentAcknowledgmentMessages()

```ts
retransmitSegmentAcknowledgmentMessages(count: number, threshold: number): void;
```

Sets the parameters controlling retransmission of Segment Acknowledgment messages
for incomplete messages.

When a receiver receives a segment of a segmented message composed of 2 or more
segments it starts the SAR Acknowledgment timer. The initial value of this timer
is controller by `transmitSegmentAcknowledgmentMessage`
and depends on the number of segments. Each time a new segment is received, the timer
is restarted. When the timer expires, a Segment Acknowledgment message is sent to the
transmitter indicating which segments were received until that point.

When the number of segments of the message is greater than the `threshold` and
the `count` parameter is greater than 0 the Segment Acknowledgment message is
retransmitted `count` times.

By default retransmissions of Segment Acknowledgment messages are disabled.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `count` | `number` | Number of retransmissions of Segment Acknowledgment. Valid values are 0-3, where 0 disables retransmissions. By default retransmissions are disabled. |
| `threshold` | `number` | The number of segments above which the retransmissions of Segment Acknowledgment messages are enabled. By default, the threshold is set to 3 segments. |

###### Returns

`void`

###### See

 - `NetworkParameters.sarAcknowledgmentRetransmissionsCount`
 - `NetworkParameters.sarSegmentsThreshold`

##### retransmitUnacknowledgedSegmentsToUnicastAddress()

```ts
retransmitUnacknowledgedSegmentsToUnicastAddress(
   retransmissionsCount: number, 
   retransmissionsWithoutProgressCount: number, 
   interval: number, 
   increment: number
): void;
```

Sets the parameters of retransmissions of segments of a segmented message
for a destination that is a Unicast Address.

The number of retransmissions and number of retransmissions without progress  indicate the
maximum number of retransmissions before sending the message is cancelled.
The count without progress is reset each time a Segment Acknowledgment message indicating
a progress in transfer is received.

The `interval` and `increment`define the interval between retransmissions in case no
Segment Acknowledgment message is received. When an acknowledgment is received, the
missing segments are transmitted immediately.

The `interval` indicates the fixed interval added to a product of the `increment` and a value
calculated using the formula: `TTL - 1`, where the TTL is the Time To Live value with which the
message is sent.

- parameters:
  - retransmissionsCount: Maximum number of retransmissions of segments of a segmented
           message. Default value is 2 retransmissions (3 transmissions, including the initial one).
  - retransmissionsWithoutProgressCount: Maximum number of retransmissions of segments
           of a segmented message in case no new segments were acknowledged.
           Default value is 2 retransmissions (3 transmissions, including the initial one).
  - interval: The constant component of the interval between retransmissions.
              Default interval is 200 ms. Valid range is from 25 ms to 400 ms with 25 ms interval.
  - increment: The increment component the the interval, which is multiplied by `TTL - 1`.
               Default increment is 50 ms. Valid range is from 25 ms to 400 ms with 25 ms interval.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `retransmissionsCount` | `number` |
| `retransmissionsWithoutProgressCount` | `number` |
| `interval` | `number` |
| `increment` | `number` |

###### Returns

`void`

###### See

 - `NetworkParameters.sarUnicastRetransmissionsCount`
 - `NetworkParameters.sarUnicastRetransmissionsWithoutProgressCount`
 - `NetworkParameters.sarUnicastRetransmissionsIntervalStep`
 - `NetworkParameters.sarUnicastRetransmissionsIntervalIncrement`

##### setDefaultTtl()

```ts
setDefaultTtl(ttl: number): void;
```

Sets the default Time To Live (TTL) which will be used for sending messages if the value has
not been set for the Provisioner's Node.

In Bluetooth Mesh each message is sent with a TTL value. When a relay
Node receives such message, it decrements the TTL value by 1, re-encrypts it
using the same Network Key and retransmits further. If the received TTL value is
1 or 0 the message is no longer retransmitted.

By default default TTL is set to 5, which is a reasonable value. The TTL shall be in range 2...127.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` |

###### Returns

`void`

###### See

`NetworkParameters.defaultTtl`

##### transmitSegmentAcknowledgmentMessage()

```ts
transmitSegmentAcknowledgmentMessage(segmentReceptionInterval: number, acknowledgmentDelayIncrement: number): void;
```

Sets the parameters for calculating the interval between receiving a new segment of a segmented
message for a destination that is a Unicast Address and sending a Segment Acknowledgment message.

The Segment Acknowledgment message contains information about which segments have been
received until the moment of sending the message. Upon receiving, the transmitter should retransmit
all missing segments.

The initial value of the timer for a given message depends on number of segments and is calculated
using the following formula:
```
min(number of segment - 0.5, acknowledgment delay increment) * segment reception interval (ms)
```

Number of retransmissions of the Segment Acknowledgment message can be set using
`retransmitSegmentAcknowledgmentMessages`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `segmentReceptionInterval` | `number` | A value that indicates the interval between received segments of a segmented message. Available values are in range 10 ms - 160 ms with 10 ms step with default value 60 ms. |
| `acknowledgmentDelayIncrement` | `number` | The minimum delay increment is a value that controls the interval between the reception of a new segment of a segmented message for a destination that is a Unicast Address and the transmission of the Segment Acknowledgment for that message. Valid values are 1.5, 2.5, ... until 8.5 with the default value being 1.5. |

###### Returns

`void`

###### See

 - `NetworkParameters.sarReceiverSegmentIntervalStep`
 - `NetworkParameters.sarAcknowledgmentDelayIncrement`

##### transmitSegments()

```ts
transmitSegments(interval: number): void;
```

Sets the interval between transmissions of segments of a segmented message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `interval` | `number` | The interval in seconds, in range 10 - 160 ms with 10 ms step. The default interval is 60 ms. |

###### Returns

`void`

###### See

`NetworkParameters.sarSegmentIntervalStep`

***

### NetworkTransmit

The object represents parameters of the transmissions of network
layer messages originating from a mesh node.

#### Constructors

##### Constructor

```ts
new NetworkTransmit(count: number, interval: number): NetworkTransmit;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `count` | `number` |
| `interval` | `number` |

###### Returns

[`NetworkTransmit`](#networktransmit)

#### Properties

##### count

```ts
count: number;
```

Number of transmissions for network messages.
The value is in range from 1 to 8.

##### interval

```ts
interval: number;
```

The interval (in milliseconds) between retransmissions
(from 10 to 320 ms in 10 ms steps).

#### Accessors

##### steps

###### Get Signature

```ts
get steps(): number;
```

Number of 10-millisecond steps between transmissions.

###### Returns

`number`

##### timeInterval

###### Get Signature

```ts
get timeInterval(): number;
```

The interval in as `TimeInterval` in seconds.

###### Returns

`number`

#### Methods

##### decode()

```ts
static decode(jv: Record<string, unknown>): NetworkTransmit;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`NetworkTransmit`](#networktransmit)

##### fromFixingIncorrectNetworkTransmit()

```ts
static fromFixingIncorrectNetworkTransmit(incorrectNetworkTransmit: NetworkTransmit): NetworkTransmit;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `incorrectNetworkTransmit` | [`NetworkTransmit`](#networktransmit) |

###### Returns

[`NetworkTransmit`](#networktransmit)

##### fromRequest()

```ts
static fromRequest(request: ConfigNetworkTransmitSet): NetworkTransmit;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `request` | `ConfigNetworkTransmitSet` |

###### Returns

[`NetworkTransmit`](#networktransmit)

##### fromStatus()

```ts
static fromStatus(status: ConfigNetworkTransmitStatus): NetworkTransmit;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `status` | `ConfigNetworkTransmitStatus` |

###### Returns

[`NetworkTransmit`](#networktransmit)

***

### Node

#### Extended by

- [`UnknownNode`](#unknownnode)

#### Constructors

##### Constructor

```ts
new Node(
   name: string | undefined, 
   unicastAddress: Address, 
   elements: number
): Node;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `unicastAddress` | `Address` |
| `elements` | `number` |

###### Returns

[`Node`](#node)

#### Properties

##### companyIdentifier

```ts
companyIdentifier: number | undefined = undefined;
```

The 16-bit Company Identifier (CID) assigned by the Bluetooth SIG.
The value of this property is obtained from node composition data.

##### deviceKey

```ts
deviceKey: Data | undefined = undefined;
```

128-bit device key for this Node.

##### features

```ts
features: NodeFeaturesState | undefined = undefined;
```

Node's features.

##### heartbeatSubscription

```ts
heartbeatSubscription: HeartbeatSubscription | undefined = undefined;
```

The Heartbeat Subscription object represents parameters that define
receiving of periodical Heartbeat transport control messages.

##### meshNetwork

```ts
meshNetwork: MeshNetwork | undefined = undefined;
```

##### minimumNumberOfReplayProtectionList?

```ts
optional minimumNumberOfReplayProtectionList?: number;
```

The minimum number of Replay Protection List (RPL) entries for this
node. The value of this property is obtained from node composition
data.

##### productIdentifier

```ts
productIdentifier: number | undefined = undefined;
```

The 16-bit vendor-assigned Product Identifier (PID).
The value of this property is obtained from node composition data.

##### security

```ts
security: Security;
```

The level of security for the subnet on which the node has been
originally provisioner.

##### uuid

```ts
uuid: UUID;
```

Unique Node Identifier.

##### versionIdentifier

```ts
versionIdentifier: number | undefined = undefined;
```

The 16-bit vendor-assigned Version Identifier (VID).
The value of this property is obtained from node composition data.

#### Accessors

##### appKeys

###### Get Signature

```ts
get appKeys(): NodeKey[];
```

An array of Node Application Key objects that include information
about the Application Keys known to this node.

###### Returns

[`NodeKey`](#nodekey)[]

##### applicationKeys

###### Get Signature

```ts
get applicationKeys(): ApplicationKey[];
```

Returns list of Application Keys known to this Node.

NOTE: If the Node has been removed from the mesh network this property returns an empty array.

###### Returns

[`ApplicationKey`](#applicationkey)[]

##### defaultTtl

###### Get Signature

```ts
get defaultTtl(): number | undefined;
```

The default Time To Live (TTL) value used when sending messages.
The TTL may only be set for a Provisioner's Node, or for a Node
that has not been added to a mesh network.

Use `ConfigDefaultTtlGet` and `ConfigDefaultTtlSet` messages to
read or set the default TTL value of a remote Node.

###### Returns

`number` \| `undefined`

###### Set Signature

```ts
set defaultTtl(ttl: number | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` \| `undefined` |

###### Returns

`void`

##### elements

###### Get Signature

```ts
get elements(): Element[];
```

###### Returns

[`Element`](#element)[]

##### elementsCount

###### Get Signature

```ts
get elementsCount(): number;
```

Number of Node's Elements.

###### Returns

`number`

##### isCompositionDataReceived

###### Get Signature

```ts
get isCompositionDataReceived(): boolean;
```

Returns weather Composition Data has been applied to the Node.

###### Returns

`boolean`

##### isConfigComplete

###### Get Signature

```ts
get isConfigComplete(): boolean;
```

The boolean value represents whether the Mesh Manager
has finished configuring this Node. The property is set to `true`
once a Mesh Manager is done completing this node's
configuration, otherwise it is set to `false`.

###### Returns

`boolean`

###### Set Signature

```ts
set isConfigComplete(value: boolean): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `boolean` |

###### Returns

`void`

##### isExcluded

###### Get Signature

```ts
get isExcluded(): boolean;
```

The flag is set to `true` when the Node is in the process of being
deleted and is excluded from the new network key distribution
during the key refresh procedure; otherwise is set to `false`.

###### Returns

`boolean`

###### Set Signature

```ts
set isExcluded(value: boolean): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `boolean` |

###### Returns

`void`

##### isLocalProvisioner

###### Get Signature

```ts
get isLocalProvisioner(): boolean;
```

Returns whether the Node belongs to the main Provisioner.
The main Provisioner will be used to perform all
provisioning and communication on this device. Every device
should use a different Provisioner to set up devices in the
same mesh network to avoid conflicts with addressing nodes.

###### Returns

`boolean`

##### isProvisioner

###### Get Signature

```ts
get isProvisioner(): boolean;
```

Returns whether the Node belongs to one of the Provisioners
of the mesh network.

###### Returns

`boolean`

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

###### Returns

`string` \| `undefined`

###### Set Signature

```ts
set name(name: string | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |

###### Returns

`void`

##### netKeys

###### Get Signature

```ts
get netKeys(): NodeKey[];
```

###### Returns

[`NodeKey`](#nodekey)[]

##### networkKeys

###### Get Signature

```ts
get networkKeys(): NetworkKey[];
```

Returns list of Network Keys known to this Node.

NOTE: If the Node has been removed from the mesh network this property returns an empty array.

###### Returns

[`NetworkKey`](#networkkey)[]

##### networkTransmit

###### Get Signature

```ts
get networkTransmit(): NetworkTransmit | undefined;
```

The object represents parameters of the transmissions of network
layer messages originating from a mesh node.

###### Returns

[`NetworkTransmit`](#networktransmit) \| `undefined`

###### Set Signature

```ts
set networkTransmit(networkTransmit: NetworkTransmit | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkTransmit` | [`NetworkTransmit`](#networktransmit) \| `undefined` |

###### Returns

`void`

##### primaryElement

###### Get Signature

```ts
get primaryElement(): Element | undefined;
```

The Primary Element of the Node.

`undefined` is returned if Composition Data has not been received yet.

###### Returns

[`Element`](#element) \| `undefined`

##### primaryUnicastAddress

###### Get Signature

```ts
get primaryUnicastAddress(): Address;
```

Primary Unicast Address of the Node.

###### Returns

`Address`

##### ttl

###### Get Signature

```ts
get ttl(): number | undefined;
```

The default Time To Live (TTL) value used when sending messages.

###### Returns

`number` \| `undefined`

###### Set Signature

```ts
set ttl(ttl: number | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` \| `undefined` |

###### Returns

`void`

##### unicastAddressRange

###### Get Signature

```ts
get unicastAddressRange(): AddressRange;
```

The Unicast Address range assigned to all Elements of the Node.

The address range is continuous and starts with `primaryUnicastAddress`
and ends with `lastUnicastAddress`.

###### Returns

[`AddressRange`](#addressrange)

#### Methods

##### addApplicationKey()

```ts
addApplicationKey(applicationKey: ApplicationKey): void;
```

Adds the Application Key to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to add. |

###### Returns

`void`

##### addApplicationKeyWithIndex()

```ts
addApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Adds the Application Key with given index to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key index to add. |

###### Returns

`void`

##### addElement()

```ts
addElement(element: Element): void;
```

Adds the given Element to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `element` | [`Element`](#element) | The Element to be added. |

###### Returns

`void`

##### addElements()

```ts
addElements(elements: Element[]): void;
```

Adds given list of Elements to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `elements` | [`Element`](#element)[] | The list of Elements to be added. |

###### Returns

`void`

##### addNetworkKey()

```ts
addNetworkKey(networkKey: NetworkKey): void;
```

Adds the Network Key to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to add. |

###### Returns

`void`

##### addNetworkKeyWithIndex()

```ts
addNetworkKeyWithIndex(networkKeyIndex: KeyIndex): void;
```

Adds the Network Key with given index to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Network Key index to add. |

###### Returns

`void`

##### applyCompositionData()

```ts
applyCompositionData(compositionData: ConfigCompositionDataStatus): void;
```

Applies the result of Composition Data to the Node.

This method does nothing if the Node already was configured
or the Composition Data Status does not have Page 0.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `compositionData` | [`ConfigCompositionDataStatus`](#configcompositiondatastatus) | The result of Config Composition Data Get with page 0. |

###### Returns

`void`

##### containsElementsWithAddressesOverlapping()

```ts
containsElementsWithAddressesOverlapping(range: AddressRange): boolean;
```

Returns whether any of the Node's Elements has a Unicast Address from the given
range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`AddressRange`](#addressrange) | Address range to check. |

###### Returns

`boolean`

`True`, if the node address range overlaps with the given range, `false` otherwise.

##### containsElementWithAddress()

```ts
containsElementWithAddress(address: Address): boolean;
```

Returns whether the Node has the given Unicast Address assigned to one
of its Elements.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | Address to check. |

###### Returns

`boolean`

`True` if any of node's elements (or the node itself) was assigned the given address, `false` otherwise.

##### elementWithAddress()

```ts
elementWithAddress(address: Address): Element | undefined;
```

Returns the Element that belongs to this Node with the given
Unicast Address, or `undefined`, if such does not exist.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | The Unicast Address of an Element to get. |

###### Returns

[`Element`](#element) \| `undefined`

The Element found, or `undefined`, if no such exist.

##### equals()

```ts
equals(other: Node): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`Node`](#node) |

###### Returns

`boolean`

##### knowsApplicationKey()

```ts
knowsApplicationKey(applicationKey: ApplicationKey): boolean;
```

Returns whether the Node has knowledge about the given Application Key.
The Application Key comparison bases only on the Key Index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Application Key with the same Key Index as given key, `false` otherwise.

##### knowsApplicationKeyIndex()

```ts
knowsApplicationKeyIndex(applicationKeyIndex: KeyIndex): boolean;
```

Returns whether the Node has knowledge about Application Key with the
given index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key Index to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Application Key index, `false` otherwise.

##### knowsNetworkKey()

```ts
knowsNetworkKey(networkKey: NetworkKey): boolean;
```

Returns whether the Node has knowledge about the given Network Key.
The Network Key comparison bases only on the Key Index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Network Key with the same Key Index as given key, `false` otherwise.

##### knowsNetworkKeyIndex()

```ts
knowsNetworkKeyIndex(networkKeyIndex: KeyIndex): boolean;
```

Returns whether the Node has knowledge about Network Key with the
given index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Network Key Index to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Network Key index, `false` otherwise.

##### removeApplicationKeyWithIndex()

```ts
removeApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Removes the Application Key with given index and all Model bindings
that point to it and the publications that are using this key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Key Index of Application Key to be removed. |

###### Returns

`void`

##### removeNetworkKeyWithIndex()

```ts
removeNetworkKeyWithIndex(networkKeyIndex: KeyIndex): void;
```

Removes the Network Key with given index and all Application Keys
bound to it from the Node. This method also removes all Model bindings
that point any of the removed Application Keys and the publications
that are using this key, including Heartbeat publication.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Key Index of Network Key to be removed. |

###### Returns

`void`

##### setApplicationKeys()

```ts
setApplicationKeys(applicationKeys: ApplicationKey[]): void;
```

Sets the Application Keys to the Node.
This will overwrite the previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](#applicationkey)[] | The Application Keys to set. |

###### Returns

`void`

##### setApplicationKeysWithIndexes()

```ts
setApplicationKeysWithIndexes(applicationKeyIndexes: KeyIndex[]): void;
```

Sets the Application Keys with given indexes to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndexes` | `KeyIndex`[] | The Application Key indexes to set. |

###### Returns

`void`

##### setElements()

```ts
setElements(elements: Element[]): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `elements` | [`Element`](#element)[] |

###### Returns

`void`

##### setNetworkKeys()

```ts
setNetworkKeys(networkKeys: NetworkKey[]): void;
```

Sets the Network Keys to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeys` | [`NetworkKey`](#networkkey)[] | The Network Keys to set. |

###### Returns

`void`

##### setNetworkKeysWithIndexes()

```ts
setNetworkKeysWithIndexes(networkKeyIndexes: KeyIndex[]): void;
```

Sets the Network Keys with given indexes to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndexes` | `KeyIndex`[] | The Network Key indexes to set. |

###### Returns

`void`

##### decode()

```ts
static decode(jv: Record<string, unknown>): Node;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`Node`](#node)

##### forProvisionerWithAddress()

```ts
static forProvisionerWithAddress(provisioner: Provisioner, address: Address): Node;
```

Initializes the Provisioner's Node.

The Provisioner's node has the same name and node UUID as the Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner for which the node is added. |
| `address` | `Address` | The unicast address to be assigned to the Node. |

###### Returns

[`Node`](#node)

##### forUnprovisionedDevice()

```ts
static forUnprovisionedDevice(
   unprovisionedDevice: UnprovisionedDevice, 
   n: number, 
   deviceKey: Data, 
   security: Security, 
   networkKey: NetworkKey, 
   address: Address
): Node;
```

Initializes a Node for given unprovisioned device.

The Node will have the same UUID as the device in the advertising
packet.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `unprovisionedDevice` | [`UnprovisionedDevice`](#unprovisioneddevice) | The newly provisioned device. |
| `n` | `number` | Number of Elements on the new Node. |
| `deviceKey` | `Data` | The Device Key. |
| `security` | `Security` | The Node's security. A Node is considered secure if it was provisioned using a OOB Public Key. |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key. |
| `address` | `Address` | The Unicast Address to be assigned to the Node. |

###### Returns

[`Node`](#node)

##### withAssignedNetworkKeyAndAddress()

```ts
static withAssignedNetworkKeyAndAddress(
   name: string | undefined, 
   uuid: UUID, 
   deviceKey: Data, 
   security: Security, 
   networkKey: NetworkKey, 
   address: Address
): Node;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `uuid` | `UUID` |
| `deviceKey` | `Data` |
| `security` | `Security` |
| `networkKey` | [`NetworkKey`](#networkkey) |
| `address` | `Address` |

###### Returns

[`Node`](#node)

***

### NodeKey

#### Constructors

##### Constructor

```ts
new NodeKey($index: KeyIndex, $updated: boolean): NodeKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `$index` | `KeyIndex` |
| `$updated` | `boolean` |

###### Returns

[`NodeKey`](#nodekey)

#### Accessors

##### index

###### Get Signature

```ts
get index(): KeyIndex;
```

The Key index for this network key.

###### Returns

`KeyIndex`

##### updated

###### Get Signature

```ts
get updated(): boolean;
```

This flag contains value set to `false`, unless a Key Refresh
procedure is in progress and the network has been successfully
updated.

###### Returns

`boolean`

#### Methods

##### decode()

```ts
static decode(jv: Record<string, unknown>): NodeKey;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`NodeKey`](#nodekey)

***

### OobType

A set of supported Out-Of-Band types.

#### Extends

- `OptionSet`\<`UInt8`\>

#### Constructors

##### Constructor

```ts
new OobType(value?: number): OobType;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value?` | `number` |

###### Returns

[`OobType`](#oobtype)

###### Inherited from

```ts
OptionSet<UInt8>.constructor
```

#### Properties

##### onlyOobAuthenticatedProvisioningSupported

```ts
static onlyOobAuthenticatedProvisioningSupported: OobType;
```

Only OOB authenticated provisioning supported.

##### staticOobInformationAvailable

```ts
static staticOobInformationAvailable: OobType;
```

Static OOB Information is available.

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

###### Overrides

```ts
OptionSet.toString
```

***

### OutputOobActions

A set of supported Output Out-of-band actions.

#### Extends

- `OptionSet`\<`UInt16`\>

#### Constructors

##### Constructor

```ts
new OutputOobActions(value?: number): OutputOobActions;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value?` | `number` |

###### Returns

[`OutputOobActions`](#outputoobactions)

###### Inherited from

```ts
OptionSet<UInt16>.constructor
```

#### Properties

##### beep

```ts
static beep: OutputOobActions;
```

##### blink

```ts
static blink: OutputOobActions;
```

##### outputAlphanumeric

```ts
static outputAlphanumeric: OutputOobActions;
```

##### outputNumeric

```ts
static outputNumeric: OutputOobActions;
```

##### vibrate

```ts
static vibrate: OutputOobActions;
```

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

###### Overrides

```ts
OptionSet.toString
```

##### fromData()

```ts
static fromData(data: Data, offset: number): OutputOobActions;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `offset` | `number` |

###### Returns

[`OutputOobActions`](#outputoobactions)

###### Overrides

```ts
OptionSet.fromData
```

***

### Page0

Composition Data Page 0 shall be present on a Node.

Composition Data Page 0 shall not change during a term of a Node
on the network.

#### Extends

- [`CompositionDataPage`](#abstract-compositiondatapage)

#### Constructors

##### Constructor

```ts
new Page0(
   page: number, 
   companyIdentifier: number, 
   productIdentifier: number, 
   versionIdentifier: number, 
   minimumNumberOfReplayProtectionList: number, 
   features: NodeFeaturesState, 
   elements: ElementData[]
): Page0;
```

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `page` | `number` | - |
| `companyIdentifier` | `number` | The 16-bit Company Identifier (CID) assigned by the Bluetooth SIG. Company Identifiers are published in [Assigned Numbers](https://www.bluetooth.com/specifications/assigned-numbers/). |
| `productIdentifier` | `number` | The 16-bit vendor-assigned Product Identifier (PID). |
| `versionIdentifier` | `number` | The 16-bit vendor-assigned Version Identifier (VID). |
| `minimumNumberOfReplayProtectionList` | `number` | The minimum number of Replay Protection List (RPL) entries for this node. |
| `features` | `NodeFeaturesState` | Node's features. The Page 0 of the Composition Data does not provide information whether a feature is enabled or disabled, just whether it is supported or not. Read the state of each feature using corresponding Config message. |
| `elements` | [`ElementData`](#elementdata)[] | An array of Node's Elements. |

###### Returns

[`Page0`](#page0)

###### Overrides

[`CompositionDataPage`](#abstract-compositiondatapage).[`constructor`](#constructor-6)

#### Properties

##### companyIdentifier

```ts
companyIdentifier: number;
```

The 16-bit Company Identifier (CID) assigned by the Bluetooth SIG.

Company Identifiers are published in
[Assigned Numbers](https://www.bluetooth.com/specifications/assigned-numbers/).

##### elements

```ts
elements: ElementData[];
```

An array of Node's Elements.

##### features

```ts
features: NodeFeaturesState;
```

Node's features.

The Page 0 of the Composition Data does not provide information
whether a feature is enabled or disabled, just whether it is supported
or not. Read the state of each feature using corresponding Config
message.

##### minimumNumberOfReplayProtectionList

```ts
minimumNumberOfReplayProtectionList: number;
```

The minimum number of Replay Protection List (RPL) entries for this
node.

##### page

```ts
page: number;
```

###### Inherited from

[`CompositionDataPage`](#abstract-compositiondatapage).[`page`](#page)

##### productIdentifier

```ts
productIdentifier: number;
```

The 16-bit vendor-assigned Product Identifier (PID).

##### versionIdentifier

```ts
versionIdentifier: number;
```

The 16-bit vendor-assigned Version Identifier (VID).

#### Accessors

##### parameters

###### Get Signature

```ts
get parameters(): Data | undefined;
```

Composition Data parameters as Data.

###### Returns

`Data` \| `undefined`

###### Overrides

[`CompositionDataPage`](#abstract-compositiondatapage).[`parameters`](#parameters)

#### Methods

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromData()

```ts
static fromData(parameters: Data): Page0 | undefined;
```

This initializer should construct the message based on the
received parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `parameters` | `Data` | The Access Layer parameters. |

###### Returns

[`Page0`](#page0) \| `undefined`

##### fromNode()

```ts
static fromNode(node: Node): Page0;
```

This initializer constructs the Page 0 of Composition Data from
the given Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to construct the Page 0 from. |

###### Returns

[`Page0`](#page0)

***

### PBGattBearer

The PB GATT bearer is responsible for sending and receiving mesh
provisioning messages to and from the GATT Proxy Node.

#### Extends

- [`BaseGattProxyBearer`](#basegattproxybearer)\<*typeof* `MeshProvisioningService`, `this`\> & [`ProvisioningBearer`](#abstract-provisioningbearer)\<`this`\>

#### Constructors

##### Constructor

```ts
new PBGattBearer(
   name: string | undefined, 
   uuid: UUID, 
   centralManager: CBCentralManager
): PBGattBearer;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `uuid` | `UUID` |
| `centralManager` | `CBCentralManager` |

###### Returns

[`PBGattBearer`](#pbgattbearer)

###### Overrides

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).constructor
```

#### Properties

##### identifier

```ts
identifier: UUID;
```

The UUID associated with the peer.

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).identifier
```

##### isOpen

```ts
isOpen: boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).isOpen
```

##### logger?

```ts
optional logger?: LoggerHandler;
```

The logger receives logs sent from the bearer. The logs will contain
raw data of sent and received packets, as well as connection events.

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).logger
```

#### Accessors

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

The name of the peripheral.

###### Returns

`string` \| `undefined`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).name
```

#### Methods

##### centralManagerDidDisconnectPeripheral()

```ts
centralManagerDidDisconnectPeripheral(
   _: CBCentralManager, 
   peripheral: CBPeripheral, 
   error?: Error
): void;
```

Called when a peripheral is disconnected.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The disconnected peripheral. |
| `error?` | `Error` | Optional error if the disconnection was unexpected. |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).centralManagerDidDisconnectPeripheral
```

##### centralManagerDidDiscoverPeripheral()

```ts
centralManagerDidDiscoverPeripheral(
   _central: CBCentralManager, 
   peripheral: CBPeripheral, 
   rssi?: number, 
   _advertisementData?: unknown
): void;
```

Called when a peripheral is discovered.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `peripheral` | `CBPeripheral` | The discovered peripheral. |
| `rssi?` | `number` | Optional received signal strength indicator (RSSI) of the peripheral. |
| `_advertisementData?` | `unknown` | - |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).centralManagerDidDiscoverPeripheral
```

##### centralManagerDidUpdateState()

```ts
centralManagerDidUpdateState(_central: CBCentralManager, state: CBCentralManagerState): void;
```

Called when the central manager's state is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_central` | `CBCentralManager` | - |
| `state` | `CBCentralManagerState` | A string representing the new state (e.g., 'poweredOn', 'poweredOff', etc.). |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).centralManagerDidUpdateState
```

##### close()

```ts
close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).close
```

##### didDisconnect()

```ts
didDisconnect(_peripheral: CBPeripheral): void;
```

Called when the peripheral disconnects.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).didDisconnect
```

##### didUpdateState()

```ts
didUpdateState(_peripheral: CBPeripheral): void;
```

Called when the peripheral connection state changes.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_peripheral` | `CBPeripheral` |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).didUpdateState
```

##### didUpdateValueForCharacteristic()

```ts
didUpdateValueForCharacteristic(_peripheral: CBPeripheral, characteristic: CBCharacteristic): void;
```

Called when a characteristic's value is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_peripheral` | `CBPeripheral` | - |
| `characteristic` | `CBCharacteristic` | The characteristic whose value was updated. |

###### Returns

`void`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).didUpdateValueForCharacteristic
```

##### open()

```ts
open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).open
```

##### readRSSI()

```ts
readRSSI(): Promise<number> | undefined;
```

Retrieves the current RSSI value for the peripheral while it is connected
to the central manager.

The result will be returned using `GattBearerDelegate.bearerDidReadRSSI()` callback.

###### Returns

`Promise`\<`number`\> \| `undefined`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).readRSSI
```

##### send()

```ts
send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).send
```

##### sendProvisioningRequest()

```ts
sendProvisioningRequest(request: ProvisioningRequest): Promise<void>;
```

This method sends the given Provisioning Request over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | `ProvisioningRequest` | The Provisioning request to be sent over the Bearer. |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).sendProvisioningRequest
```

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  BaseGattProxyBearer<typeof MeshProvisioningService>,
  ProvisioningBearer,
).supports
```

##### fromPeripheral()

```ts
static fromPeripheral(peripheral: CBPeripheral, centralManager: CBCentralManager): PBGattBearer;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `peripheral` | `CBPeripheral` |
| `centralManager` | `CBCentralManager` |

###### Returns

[`PBGattBearer`](#pbgattbearer)

***

### PduTypes

A set of supported PDU types by the bearer object.

#### Extends

- `Number`

#### Properties

##### meshBeacon

```ts
static meshBeacon: PduTypes;
```

Set, if the bearer supports Mesh Beacons.

##### networkPdu

```ts
static networkPdu: PduTypes;
```

Set, if the bearer supports Network PDUs.

##### provisioningPdu

```ts
static provisioningPdu: PduTypes;
```

Set, if the bearer supports Provisioning PDUs.

##### proxyConfiguration

```ts
static proxyConfiguration: PduTypes;
```

Set, if the bearer supports proxy filter configuration.

***

### `abstract` Period

The Publishing Period state determines the interval at which status messages
are periodically published by a Model.

#### Constructors

##### Constructor

```ts
new Period(): Period;
```

###### Returns

[`Period`](#abstract-period)

#### Properties

##### interval

```ts
abstract interval: number;
```

The interval between subsequent publications in seconds.

##### numberOfSteps

```ts
abstract numberOfSteps: number;
```

The number of steps, in range 0...63.

##### resolution

```ts
abstract resolution: StepResolution;
```

The resolution of the number of steps.

***

### PrivateNetworkIdentity

Representation of Private Network Identity advertising packet.

#### Implements

- [`NetworkIdentity`](#networkidentity)

#### Constructors

##### Constructor

```ts
new PrivateNetworkIdentity(hash: Data, random: Data): PrivateNetworkIdentity;
```

Creates the Network Identity object from Hash and Random values.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `hash` | `Data` | Function of the included random number and identity information. |
| `random` | `Data` | 64-bit random number. |

###### Returns

[`PrivateNetworkIdentity`](#privatenetworkidentity)

#### Properties

##### hash

```ts
hash: Data;
```

Function of the included random number and identity information.

##### random

```ts
random: Data;
```

64-bit random number.

#### Methods

##### matches()

```ts
matches(networkKey: NetworkKey): boolean;
```

Returns whether the identity matches given `NetworkKey`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to check. |

###### Returns

`boolean`

True, if the identity matches the Network Key; false otherwise.

###### Implementation of

[`NetworkIdentity`](#networkidentity).[`matches`](#matches-4)

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromAdvertisementData()

```ts
static fromAdvertisementData(advertisementData: Record<string, unknown>): PrivateNetworkIdentity | null;
```

Creates the Network Identity object from the received advertisement data.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | Received advertisement data. |

###### Returns

[`PrivateNetworkIdentity`](#privatenetworkidentity) \| `null`

***

### PrivateNodeIdentity

Representation of Private Node Identity advertising packet.

#### Implements

- [`NodeIdentity`](#nodeidentity)

#### Constructors

##### Constructor

```ts
new PrivateNodeIdentity(hash: Data, random: Data): PrivateNodeIdentity;
```

Creates the Private Node Identity object from Hash and Random values.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `hash` | `Data` | Function of the included random number and identity information. |
| `random` | `Data` | 64-bit random number. |

###### Returns

[`PrivateNodeIdentity`](#privatenodeidentity)

#### Properties

##### hash

```ts
hash: Data;
```

Function of the included random number and identity information.

##### random

```ts
random: Data;
```

64-bit random number.

#### Methods

##### matches()

```ts
matches(node: Node): boolean;
```

Returns whether the identity matches given `Node`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to check. |

###### Returns

`boolean`

True, if the identity matches the Node; false otherwise.

###### Implementation of

[`NodeIdentity`](#nodeidentity).[`matches`](#matches-5)

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromAdvertisementData()

```ts
static fromAdvertisementData(advertisementData: Record<string, unknown>): PrivateNodeIdentity | null;
```

Creates the Private Node Identity object from the received advertisement data.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | Received advertisement data. |

###### Returns

[`PrivateNodeIdentity`](#privatenodeidentity) \| `null`

***

### Provisioner

Representation of a Provisioner in the mesh network.

A Provisioner is an entity that is able to provision other device to
the mesh network by assigning them Unicast Addresses and providing
a Network Key in a secure way. A Provisioner with a Unicast Address
assigned may also configure devices and becomes a Node of a network
on its own.

Each Provisioner has assigned 3 ranges:
* Unicast Address range - set of Unicast Addresses which may assign to new
  Nodes during provisioning,
* Group Address range - set of Group Addresses which the Provisioner may use
  to define new groups.
* Scene range - set of Scene numbers which it may use to define new scenes.

The Provisioner should not assign addresses from outside of its ranges to avoid
conflicts with other Provisioners.

#### Constructors

##### Constructor

```ts
new Provisioner(
   name: string, 
   uuid: UUID, 
   allocatedUnicastRange: AddressRange[], 
   allocatedGroupRange: AddressRange[], 
   allocatedSceneRange: SceneRange[]
): Provisioner;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `uuid` | `UUID` |
| `allocatedUnicastRange` | [`AddressRange`](#addressrange)[] |
| `allocatedGroupRange` | [`AddressRange`](#addressrange)[] |
| `allocatedSceneRange` | [`SceneRange`](#scenerange)[] |

###### Returns

[`Provisioner`](#provisioner)

#### Properties

##### allocatedGroupRange

```ts
allocatedGroupRange: AddressRange[];
```

An array of group range objects.

##### allocatedSceneRange

```ts
allocatedSceneRange: SceneRange[];
```

An array of scene range objects.

##### allocatedUnicastRange

```ts
allocatedUnicastRange: AddressRange[];
```

An array of unicast range objects.

##### meshNetwork?

```ts
optional meshNetwork?: MeshNetwork;
```

##### uuid

```ts
uuid: UUID;
```

Provisioner's UUID. If the Provisioner has a corresponding Node,
the Node's UUID will be equal to this one.

#### Accessors

##### isValid

###### Get Signature

```ts
get isValid(): boolean;
```

Returns `true` if all defined ranges are valid.

The Unicast Address range may not be empty, as it needs to assign addresses
during provisioning.

###### Returns

`boolean`

##### name

###### Get Signature

```ts
get name(): string;
```

UTF-8 string, which should be a human readable name of the Provisioner.

###### Returns

`string`

###### Set Signature

```ts
set name(newValue: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `string` |

###### Returns

`void`

##### node

###### Get Signature

```ts
get node(): Node | undefined;
```

###### Returns

[`Node`](#node) \| `undefined`

##### primaryUnicastAddress

###### Get Signature

```ts
get primaryUnicastAddress(): Address | undefined;
```

The Primary Unicast Address of the Provisioner.

The Provisioner must be added to a mesh network and
must have a Unicast Address assigned, otherwise `undefined`
is returned instead.

###### Returns

`Address` \| `undefined`

#### Methods

##### hasAllocatedAddressRange()

```ts
hasAllocatedAddressRange(range: AddressRange): boolean;
```

Returns whether given address range is within any of the ranges allocated
to the Provisioner.

The address may be a Unicast or a Group Address range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`AddressRange`](#addressrange) | The address range to be checked. |

###### Returns

`boolean`

`True` if the address is in allocated ranges, `false` otherwise.

##### hasOverlappingGroupRanges()

```ts
hasOverlappingGroupRanges(provisioner: Provisioner): boolean;
```

Returns `true` if at least one Group Address range overlaps with address
ranges of the given Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to check ranges with. |

###### Returns

`boolean`

`True` if this and the given Provisioner have overlapping group ranges, `false` otherwise.

##### hasOverlappingRanges()

```ts
hasOverlappingRanges(provisioner: Provisioner): boolean;
```

Returns `true` if at least one range overlaps with the given Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to check ranges with. |

###### Returns

`boolean`

`True` if this and the given Provisioner have overlapping ranges, `false` otherwise.

##### hasOverlappingSceneRanges()

```ts
hasOverlappingSceneRanges(provisioner: Provisioner): boolean;
```

Returns `true` if at least one Scene range overlaps with scene ranges of
the given Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to check ranges with. |

###### Returns

`boolean`

`True` if this and the given Provisioner have overlapping scene ranges, `false` otherwise.

##### hasOverlappingUnicastRanges()

```ts
hasOverlappingUnicastRanges(provisioner: Provisioner): boolean;
```

Returns `true` if at least one Unicast Address range overlaps with address
ranges of the given Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner to check ranges with. |

###### Returns

`boolean`

`True` if this and the given Provisioner have overlapping unicast ranges, `false` otherwise.

##### maxElementCount()

```ts
maxElementCount(address: Address): Long;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `address` | `Address` |

###### Returns

`Long`

##### decode()

```ts
static decode(jv: Record<string, unknown>): Provisioner;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`Provisioner`](#provisioner)

##### fromName()

```ts
static fromName(name: string): Provisioner;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |

###### Returns

[`Provisioner`](#provisioner)

##### fromNameWithRanges()

```ts
static fromNameWithRanges(
   name: string, 
   allocatedUnicastRange: AddressRange[], 
   allocatedGroupRange: AddressRange[], 
   allocatedSceneRange: SceneRange[]
): Provisioner;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` |
| `allocatedUnicastRange` | [`AddressRange`](#addressrange)[] |
| `allocatedGroupRange` | [`AddressRange`](#addressrange)[] |
| `allocatedSceneRange` | [`SceneRange`](#scenerange)[] |

###### Returns

[`Provisioner`](#provisioner)

***

### `abstract` ProvisioningBearer

A provisioning bearer is used to send provisioning PDUs to unprovisioned
devices.

#### Extends

- [`Bearer`](#abstract-bearer)

#### Constructors

##### Constructor

```ts
new ProvisioningBearer(): ProvisioningBearer;
```

###### Returns

[`ProvisioningBearer`](#abstract-provisioningbearer)

###### Inherited from

[`Bearer`](#abstract-bearer).[`constructor`](#constructor-3)

#### Properties

##### isOpen

```ts
abstract isOpen: boolean;
```

This property returns `true` if the Bearer is open, otherwise `false`.

###### Inherited from

[`Bearer`](#abstract-bearer).[`isOpen`](#isopen-1)

#### Methods

##### close()

```ts
abstract close(): Promise<void>;
```

This method closes the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

[`Bearer`](#abstract-bearer).[`close`](#close-1)

##### open()

```ts
abstract open(): Promise<void>;
```

This method opens the Bearer.

###### Returns

`Promise`\<`void`\>

###### Inherited from

[`Bearer`](#abstract-bearer).[`open`](#open-1)

##### send()

```ts
abstract send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

###### Inherited from

[`Bearer`](#abstract-bearer).[`send`](#send-1)

##### sendProvisioningRequest()

```ts
sendProvisioningRequest(request: ProvisioningRequest): Promise<void>;
```

This method sends the given Provisioning Request over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `request` | `ProvisioningRequest` | The Provisioning request to be sent over the Bearer. |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

##### supports()

```ts
supports(pduType: PduType): boolean;
```

Returns whether the Bearer supports the given message type.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pduType` | [`PduType`](#pdutype) |

###### Returns

`boolean`

###### Inherited from

[`Bearer`](#abstract-bearer).[`supports`](#supports-1)

***

### ProvisioningError

Set of errors which may be thrown during provisioning a device.

#### Extends

- `Error`

#### Properties

##### cause?

```ts
readonly optional cause?: unknown;
```

##### code

```ts
readonly code: string;
```

##### addressNotSpecified

```ts
static addressNotSpecified: ProvisioningError;
```

Throws when the Unicast Address has not been set.

##### confirmationFailed

```ts
static confirmationFailed: ProvisioningError;
```

Thrown when confirmation value received from the device does not match
calculated value. Authentication failed.

##### invalidOobValueFormat

```ts
static invalidOobValueFormat: ProvisioningError;
```

Thrown when the provided alphanumeric value could not be converted into
bytes using ASCII encoding.

##### invalidPdu

```ts
static invalidPdu: ProvisioningError;
```

The received PDU is invalid.

##### invalidPublicKey

```ts
static invalidPublicKey: ProvisioningError;
```

The received Public Key is invalid or equal to Provisioner's Public Key.

##### invalidState

```ts
static invalidState: ProvisioningError;
```

Thrown when the ProvisioningManager is in invalid state.

##### networkKeyNotSpecified

```ts
static networkKeyNotSpecified: ProvisioningError;
```

Throws when the Network Key has not been set.

##### noAddressAvailable

```ts
static noAddressAvailable: ProvisioningError;
```

Thrown when no available Unicast Address was found in the Provisioner's
range that could be allocated for the device.

##### unsupportedAlgorithm

```ts
static unsupportedAlgorithm: ProvisioningError;
```

Thrown when an unsupported algorithm has been selected for provisioning.

##### unsupportedDevice

```ts
static unsupportedDevice: ProvisioningError;
```

Thrown when the Unprovisioned Device is not supported by the manager.

#### Methods

##### keyGenerationFailed()

```ts
static keyGenerationFailed(error: Error): ProvisioningError;
```

Thrown when the key pair generation has failed.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | `Error` |

###### Returns

[`ProvisioningError`](#provisioningerror)

##### remoteError()

```ts
static remoteError(error: RemoteProvisioningError): ProvisioningError;
```

Thrown when the remote device sent a failure indication.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | [`RemoteProvisioningError`](#remoteprovisioningerror) |

###### Returns

[`ProvisioningError`](#provisioningerror)

***

### ProvisioningManager

The manager responsible for provisioning a new device into the mesh network.

To create an instance of a `ProvisioningManager` use `MeshNetworkManager.provisionUnprovisionedDevice()`.

Provisioning is initiated by calling `identifyAndAttractFor()`. This method will make the
provisioned device to blink, make sound or attract in any supported way, so that the user could
verify which device is being provisioned. The target device will return `ProvisioningCapabilities`,
returned to `handler` as `ProvisioningState.capabilitiesReceived()`.

User needs to set the `unicastAddress` (by default set to `suggestedUnicastAddress`), `networkKey`
and call `provision()`. If user interaction is required
during provisioning process corresponding delegate callbacks will be invoked.

The provisioning is completed when `ProvisioningState.complete` state is returned.

#### Extends

- `BindableTinyEmitter`\<[`ProvisioningHandler`](#provisioninghandler)\>

#### Implements

- [`BearerHandler`](#abstract-bearerhandler)
- [`BearerDataHandler`](#abstract-bearerdatahandler)

#### Constructors

##### Constructor

```ts
new ProvisioningManager(
   unprovisionedDevice: UnprovisionedDevice, 
   bearer: ProvisioningBearer, 
   meshNetwork: MeshNetwork
): ProvisioningManager;
```

Creates the Provisioning Manager that will handle provisioning of the
Unprovisioned Device over the given Provisioning Bearer.

To initiate provisioning process `ProvisioningManager.identify()`
method shall be called.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `unprovisionedDevice` | [`UnprovisionedDevice`](#unprovisioneddevice) | The device to provision into the network. |
| `bearer` | [`ProvisioningBearer`](#abstract-provisioningbearer) | The Bearer used for sending Provisioning PDUs. |
| `meshNetwork` | [`MeshNetwork`](#meshnetwork-3) | The mesh network to provision the device to. |

###### Returns

[`ProvisioningManager`](#provisioningmanager)

###### Overrides

```ts
BindableTinyEmitter<ProvisioningHandler>.constructor
```

#### Properties

##### $provisioningCapabilities?

```ts
optional $provisioningCapabilities?: ProvisioningCapabilities;
```

##### logger?

```ts
optional logger?: LoggerHandler;
```

The logger handler will be called whenever a new log entry is created.

##### networkKey?

```ts
optional networkKey?: NetworkKey;
```

The Network Key to be sent to the device during provisioning.
Setting this property is mandatory before calling
`provision()`.

##### unicastAddress?

```ts
optional unicastAddress?: Address;
```

The Unicast Address that will be assigned to the device.
After device capabilities are received, the address is automatically set to
the first available unicast address from Provisioner's range.

#### Accessors

##### isDeviceSupported

###### Get Signature

```ts
get isDeviceSupported(): boolean | undefined;
```

Returns whether the Unprovisioned Device can be provisioned using this
Provisioner Manager.

If `identify()` has not been called, and the Provisioning
Capabilities are not known, this property returns `undefined`.

###### Returns

`boolean` \| `undefined`

Whether the device can be provisioned by this manager, that is whether the manager supports at least one of the provisioning algorithms supported by the device.

##### isUnicastAddressValid

###### Get Signature

```ts
get isUnicastAddressValid(): boolean;
```

Returns whether the Unicast Address can be used to provision the device.
The Provisioning Capabilities must be obtained prior to using this property,
otherwise the number of device's elements is unknown. Also, the mesh
network must have the local Provisioner set.

###### Returns

`boolean`

##### provisioningCapabilities

###### Get Signature

```ts
get provisioningCapabilities(): ProvisioningCapabilities | undefined;
```

The provisioning capabilities of the device. This information
is retrieved from the remote device during identification process.

###### Returns

[`ProvisioningCapabilities`](#provisioningcapabilities-2) \| `undefined`

###### Set Signature

```ts
set provisioningCapabilities(capabilities: ProvisioningCapabilities | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `capabilities` | [`ProvisioningCapabilities`](#provisioningcapabilities-2) \| `undefined` |

###### Returns

`void`

##### state

###### Get Signature

```ts
get state(): ProvisioningState;
```

###### Returns

[`ProvisioningState`](#provisioningstate-1)

###### Set Signature

```ts
set state(state: ProvisioningState): void;
```

The current state of the provisioning process.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `state` | [`ProvisioningState`](#provisioningstate-1) |

###### Returns

`void`

##### suggestedUnicastAddress

###### Get Signature

```ts
get suggestedUnicastAddress(): Address | undefined;
```

Automatically assigned Unicast Address. This is the first available
Unicast Address from the Provisioner's range with enough free following
addresses to be assigned to the device. This value is available after
the Provisioning Capabilities have been received and such address was found.

###### Returns

`Address` \| `undefined`

#### Methods

##### authValueReceived()

```ts
authValueReceived(value: Data): void;
```

This method should be called when the OOB value has been received
and Auth Value has been calculated.

It computes and sends the Provisioner Confirmation to the device.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `Data` | The 16 or 32 byte long Auth Value, depending on the selected algorithm. |

###### Returns

`void`

##### bearerDidClose()

```ts
bearerDidClose(_bearer: Bearer, _error?: Error): void;
```

Callback called when the Bearer is no longer open.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_bearer` | [`Bearer`](#abstract-bearer) |
| `_error?` | `Error` |

###### Returns

`void`

###### Implementation of

[`BearerHandler`](#abstract-bearerhandler).[`bearerDidClose`](#bearerdidclose)

##### bearerDidDeliverData()

```ts
bearerDidDeliverData(
   _bearer: Bearer, 
   data: Data, 
   _type: PduType
): void;
```

Callback called when a packet has been received using the Bearer.
Data longer than MTU will automatically be reassembled
using the bearer protocol if bearer implements segmentation.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_bearer` | [`Bearer`](#abstract-bearer) | - |
| `data` | `Data` | The data received. |
| `_type` | [`PduType`](#pdutype) | - |

###### Returns

`void`

###### Implementation of

[`BearerDataHandler`](#abstract-bearerdatahandler).[`bearerDidDeliverData`](#bearerdiddeliverdata)

##### bearerDidOpen()

```ts
bearerDidOpen(_bearer: Bearer): void;
```

Callback called when the Bearer is ready for use.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_bearer` | [`Bearer`](#abstract-bearer) |

###### Returns

`void`

###### Implementation of

[`BearerHandler`](#abstract-bearerhandler).[`bearerDidOpen`](#bearerdidopen)

##### identify()

```ts
identify(attentionTimer: number): Promise<void>;
```

This method initializes the provisioning of the device.

As a result of this method `ProvisioningDelegate.provisioningState()`
method will be called with the state `ProvisioningState.capabilitiesReceived()`.
If the device is supported, `ProvisioningManager.provision()`
shall be called to continue provisioning.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `attentionTimer` | `number` | This value determines for how long (in seconds) the device shall remain attracting human's attention by blinking, flashing, buzzing, etc. The value 0 disables Attention Timer. |

###### Returns

`Promise`\<`void`\>

###### Throws

A `ProvisioningError` | `BearerError` can be returned in case of an error.

##### obtainAuthValue()

```ts
obtainAuthValue(): void;
```

This method asks the user to provide a OOB value based on the
authentication method specified in the provisioning process.

For `AuthenticationMethod.noOob` case, the value is automatically
set to 0s.

This method will call `authValueReceived()` when the value
has been obtained.

###### Returns

`void`

##### provision()

```ts
provision(
   algorithm: Algorithm, 
   publicKey: PublicKey, 
   authenticationMethod: AuthenticationMethod
): Promise<void>;
```

This method starts the provisioning of the Unprovisioned Device.

`identify()` has to be invoked prior to calling this method to receive
the `ProvisioningCapabilities`, which include information regarding supported algorithms,
public key method and authentication method.

For the provisioning process to be considered `Security.secure`, it is required that
the Provisionee's Public Key is provided Out-of-Band using `PublicKey.oobPublicKey()`.
The Public Key information should be available in the Unprovisioned Device beacon.
If the device does not provide OOB Public Key, `PublicKey.noOobPublicKey` shall
be used and the provisioned Node and the Network Key will be considered `Security.insecure`.

If a different authentication method than `AuthenticationMethod.noOob` is
chosen a `ProvisioningDelegate.authenticationActionRequired()` callback
will be called during provisioning to provide the Out-of-Band value in case of
`AuthenticationMethod.staticOob` or `AuthenticationMethod.outputOob()`
or display it to the user for providing it on the Provisionee in case of
`AuthenticationMethod.inputOob()`. In the latter case, an additional
`ProvisioningDelegate.inputComplete()` callback will be called when user has finished
providing the value.

NOTE: Mesh Protocol 1.1 introduced a new, stronger provisioning algorithm ``Algorithm/BTM_ECDH_P256_HMAC_SHA256_AES_CCM``. It is recommended for devices which support it.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `algorithm` | `Algorithm` |
| `publicKey` | [`PublicKey`](#publickey) |
| `authenticationMethod` | [`AuthenticationMethod`](#authenticationmethod) |

###### Returns

`Promise`\<`void`\>

###### Throws

A `ProvisioningError` can be thrown in case of an error.

##### reset()

```ts
reset(): void;
```

Resets the provisioning properties and state.

###### Returns

`void`

***

### ProxyProtocolHandler

This helper class allows segmentation and reassembly (SAR) using
the Proxy Protocol defined in Bluetooth Mesh Profile 1.0.1.

#### Constructors

##### Constructor

```ts
new ProxyProtocolHandler(): ProxyProtocolHandler;
```

###### Returns

[`ProxyProtocolHandler`](#proxyprotocolhandler)

#### Methods

##### reassemble()

```ts
reassemble(data: Data): 
  | {
  data: Data;
  messageType: PduType;
}
  | undefined;
```

This method consumes the given data. If the data were segmented,
they are buffered until the last segment is received.
This method returns the message and its type when the last segment
(or the only one) has been received, otherwise it returns `undefined`.

The packets must be delivered in order. If a new message is
received while the previous one is still reassembled, the old
one will be disregarded. Invalid messages are disregarded.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | `Data` | The data received. |

###### Returns

  \| \{
  `data`: `Data`;
  `messageType`: [`PduType`](#pdutype);
\}
  \| `undefined`

The message and its type, or `undefined`, if more data are expected.

##### segment()

```ts
segment(
   data: Data, 
   messageType: PduType, 
   mtu: Long
): Data[];
```

Segments the given data with given message type to 1+ messages
where all but the last one are of the MTU size and the last one
is MTU size or smaller.

This method implements the Proxy Protocol from Bluetooth Mesh
specification.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | `Data` | The data to be segmented. |
| `messageType` | [`PduType`](#pdutype) | The data type. |
| `mtu` | `Long` | The maximum size of a packet to be sent. |

###### Returns

`Data`[]

***

### PublicKeyType

The type of Public Key information.

#### Extends

- `OptionSet`\<`UInt8`\>

#### Constructors

##### Constructor

```ts
new PublicKeyType(value?: number): PublicKeyType;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value?` | `number` |

###### Returns

[`PublicKeyType`](#publickeytype)

###### Inherited from

```ts
OptionSet<UInt8>.constructor
```

#### Properties

##### publicKeyOobInformationAvailable

```ts
static publicKeyOobInformationAvailable: PublicKeyType;
```

Public Key OOB Information is available.

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

###### Overrides

```ts
OptionSet.toString
```

***

### PublicNetworkIdentity

Representation of Network ID advertising packet.

#### Implements

- [`NetworkIdentity`](#networkidentity)

#### Constructors

##### Constructor

```ts
new PublicNetworkIdentity(networkId: Data): PublicNetworkIdentity;
```

Creates the Network Identity object from Hash and Random values.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkId` | `Data` | Identifies the network. |

###### Returns

[`PublicNetworkIdentity`](#publicnetworkidentity)

#### Properties

##### networkId

```ts
networkId: Data;
```

The Network ID is 64-bit network identifier derived from the Network Key.

#### Methods

##### matches()

```ts
matches(networkKey: NetworkKey): boolean;
```

Returns whether the identity matches given `NetworkKey`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to check. |

###### Returns

`boolean`

True, if the identity matches the Network Key; false otherwise.

###### Implementation of

[`NetworkIdentity`](#networkidentity).[`matches`](#matches-4)

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromAdvertisementData()

```ts
static fromAdvertisementData(advertisementData: Record<string, unknown>): PublicNetworkIdentity | null;
```

Creates the Network Identity object from the received advertisement data.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | Received advertisement data. |

###### Returns

[`PublicNetworkIdentity`](#publicnetworkidentity) \| `null`

***

### PublicNodeIdentity

Representation of Node Identity advertising packet.

#### Implements

- [`NodeIdentity`](#nodeidentity)

#### Constructors

##### Constructor

```ts
new PublicNodeIdentity(hash: Data, random: Data): PublicNodeIdentity;
```

the Node Identity object from Hash and Random values.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `hash` | `Data` | Function of the included random number and identity information. |
| `random` | `Data` | 64-bit random number. |

###### Returns

[`PublicNodeIdentity`](#publicnodeidentity)

#### Properties

##### hash

```ts
hash: Data;
```

Function of the included random number and identity information.

##### random

```ts
random: Data;
```

64-bit random number.

#### Methods

##### matches()

```ts
matches(node: Node): boolean;
```

Returns whether the identity matches given `Node`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to check. |

###### Returns

`boolean`

True, if the identity matches the Node; false otherwise.

###### Implementation of

[`NodeIdentity`](#nodeidentity).[`matches`](#matches-5)

##### toString()

```ts
toString(): string;
```

Returns a string representation of an object.

###### Returns

`string`

##### fromAdvertisementData()

```ts
static fromAdvertisementData(advertisementData: Record<string, unknown>): PublicNodeIdentity | null;
```

Creates the Node Identity object from the received advertisement data.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | Received advertisement data. |

###### Returns

[`PublicNodeIdentity`](#publicnodeidentity) \| `null`

***

### `abstract` Publish

The Publishing object defines the publication configuration for a Model.

When a Model is configured for publishing, it will sent messages whenever a state
of the Model has changed, or periodically. The Publish object defines the destination
address and the Application Key to encrypt the messages, together with other settings.

To set the publication on a Model, send the `ConfigModelPublicationSet` or
`ConfigModelPublicationVirtualAddressSet` messages to the Configuration Server model
on the Node. The *Set* messages are confirmed with a `ConfigModelPublicationStatus`.

#### Constructors

##### Constructor

```ts
new Publish(): Publish;
```

###### Returns

[`Publish`](#abstract-publish)

#### Properties

##### address

```ts
abstract address: string;
```

Publication address for the Model. It's 4 or 32-character long
hexadecimal string.

##### credentials

```ts
abstract credentials: Long;
```

An integer 0 o 1 that represents whether master security
(0) materials or friendship security material (1) are used.

##### index

```ts
abstract index: KeyIndex;
```

An Application Key index, indicating which Application Key to
use for the publication.

##### period

```ts
abstract period: Period;
```

The interval between subsequent publications.

##### retransmit

```ts
abstract retransmit: Retransmit;
```

The object describes the number of times a message is published and the
interval between retransmissions of the published message.

##### ttl

```ts
abstract ttl: number;
```

An integer from 0 to 127 that represents the Time To Live (TTL)
value for the outgoing publish message. 255 means default TTL value.

#### Accessors

##### publicationAddress

###### Get Signature

```ts
get abstract publicationAddress(): MeshAddress;
```

Publication address for the model.

###### Returns

[`MeshAddress`](#meshaddress)

***

### `abstract` Scene

A Scene represents a set of states stored with a Scene Number.

A Scene is identified by a `SceneNumber` and may have a
human-readable name associated.

A Node having a Scene Server model can store the states of other
models and restore them on demand.

A Node with a Scene Client can recall Scenes on other Nodes.

Use `Scene.elements` to get list of `Element's with
the given Scene in their Scene Register.

#### Constructors

##### Constructor

```ts
new Scene(): Scene;
```

###### Returns

[`Scene`](#abstract-scene)

#### Properties

##### meshNetwork?

```ts
abstract optional meshNetwork?: MeshNetwork;
```

##### name

```ts
abstract name: string;
```

UTF-8 human-readable name of the Scene.

##### number

```ts
abstract number: SceneNumber;
```

Scene number.

#### Accessors

##### addresses

###### Get Signature

```ts
get abstract addresses(): Address[];
```

###### Returns

`Address`[]

#### Methods

##### removeNode()

```ts
abstract removeNode(node: Node): void;
```

Removes all Unicast Addresses assigned to the given Node from the
Scene object.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node that is may have the Scene in any of its Scene Registers. |

###### Returns

`void`

***

### SceneRange

The range of `SceneNumber`s assigned to a `Provisioner`.

#### Extends

- `RangeObject`\<`SceneNumber`\>

#### Constructors

##### Constructor

```ts
new SceneRange(range: ClosedRange<SceneNumber>): SceneRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `range` | `ClosedRange`\<`SceneNumber`\> |

###### Returns

[`SceneRange`](#scenerange)

###### Inherited from

```ts
RangeObject<SceneNumber>.constructor
```

#### Properties

##### allScenes

```ts
static allScenes: SceneRange;
```

A range containing all valid Scene Numbers.

#### Accessors

##### firstScene

###### Get Signature

```ts
get firstScene(): SceneNumber;
```

The first Scene Number of the range.

###### Returns

`SceneNumber`

##### isValid

###### Get Signature

```ts
get isValid(): boolean;
```

Returns `true` if the scene range is valid.

###### Returns

`boolean`

`True` if the scene range is valid, `false` otherwise.

##### lastScene

###### Get Signature

```ts
get lastScene(): SceneNumber;
```

The last Scene Number of the range.

###### Returns

`SceneNumber`

#### Methods

##### decode()

```ts
static decode(jv: Record<string, unknown>): SceneRange;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`SceneRange`](#scenerange)

***

### `abstract` StoredWithSceneModelHandler

The Model Handler which should be used for Models that allow storing
the state with a Scene.

In addition to handling messages, the Model Handler should also
store and recall the current state whenever
``StoredWithSceneModelHandler.store()``
and ``StoredWithSceneModelHandler.recall()``
calls are received.

Whenever the state changes due to any other reason than receiving
a Scene Recall message, the handler should call
``StoredWithSceneModelHandler.networkDidExitStoredWithSceneState()``
to clear the Current State in the Scene Server model.

#### Extends

- [`ModelHandler`](#abstract-modelhandler)

#### Constructors

##### Constructor

```ts
new StoredWithSceneModelHandler(): StoredWithSceneModelHandler;
```

###### Returns

[`StoredWithSceneModelHandler`](#abstract-storedwithscenemodelhandler)

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`constructor`](#constructor-43)

#### Accessors

##### isSubscriptionSupported

###### Get Signature

```ts
get isSubscriptionSupported(): boolean;
```

###### Returns

`boolean`

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`isSubscriptionSupported`](#issubscriptionsupported)

##### messageTypes

###### Get Signature

```ts
get messageTypes(): Map<number, {
  fromData: (_: Data) => MeshMessage | undefined;
}>;
```

###### Returns

`Map`\<`number`, \{
  `fromData`: (`_`: `Data`) => `MeshMessage` \| `undefined`;
\}\>

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`messageTypes`](#messagetypes)

##### publicationMessageComposer

###### Get Signature

```ts
get abstract publicationMessageComposer(): MessageComposer | undefined;
```

The message composer that will be used to create a Mesh Message.

The composer will be used whenever model is about to publish its
state using the publishing information specified in the Model.

When set to `undefined`, the library will return error
`ConfigMessageStatus.invalidPublishParameters` for each Config
Model Publication Set and Config Model Publication Virtual Address Set.

###### Returns

`MessageComposer` \| `undefined`

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`publicationMessageComposer`](#publicationmessagecomposer)

#### Methods

##### decode()

```ts
decode(accessPdu: AccessPdu): MeshMessage | undefined;
```

This method tries to decode the Access PDU into a Message.

The Model Handler must support the opcode and specify to
which type should the message be decoded.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `accessPdu` | `AccessPdu` | The Access PDU received. |

###### Returns

`MeshMessage` \| `undefined`

The decoded message, or `undefined`, if the message is not supported or invalid.

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`decode`](#decode-6)

##### modelDidReceiveAcknowledgedMessage()

```ts
abstract modelDidReceiveAcknowledgedMessage(
   model: Model, 
   request: AcknowledgedMeshMessage, 
   source: Address, 
   destination: MeshAddress
): Error | MeshResponse;
```

This method should handle the received Acknowledged Message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `request` | `AcknowledgedMeshMessage` | The Acknowledged Message received. |
| `source` | `Address` | The source Unicast Address. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |

###### Returns

`Error` \| `MeshResponse`

The response message to be sent to the sender or The method should returns a `ModelError` if the receive message is invalid and no response should be replied.

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`modelDidReceiveAcknowledgedMessage`](#modeldidreceiveacknowledgedmessage)

##### modelDidReceiveMessage()

```ts
modelDidReceiveMessage(
   model: Model, 
   message: MeshMessage, 
   source: Address, 
   destination: MeshAddress, 
   request?: AcknowledgedMeshMessage
): MeshResponse | undefined;
```

This method handles the decoded message and passes it to
the proper handle method, depending on its type or whether
it is a response to a previously sent request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The local Model that received the message. |
| `message` | `MeshMessage` | The decoded message. |
| `source` | `Address` | The Unicast Address of the Element that the message originates from. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |
| `request?` | `AcknowledgedMeshMessage` | The request message sent previously that this message replies to, or `undefined`, if this is not a response. |

###### Returns

`MeshResponse` \| `undefined`

The response message, if the received message is an Acknowledged Mesh Message that needs to be replied.

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`modelDidReceiveMessage`](#modeldidreceivemessage)

##### modelDidReceiveResponse()

```ts
abstract modelDidReceiveResponse(
   model: Model, 
   response: MeshResponse, 
   request: AcknowledgedMeshMessage, 
   source: Address
): void;
```

This method should handle the received response to the
previously sent request.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `response` | `MeshResponse` | The response received. |
| `request` | `AcknowledgedMeshMessage` | The Acknowledged Message sent. |
| `source` | `Address` | The Unicast Address of the Element that sent the response. |

###### Returns

`void`

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`modelDidReceiveResponse`](#modeldidreceiveresponse)

##### modelDidReceiveUnacknowledgedMessage()

```ts
abstract modelDidReceiveUnacknowledgedMessage(
   model: Model, 
   message: UnacknowledgedMeshMessage, 
   source: Address, 
   destination: MeshAddress
): void;
```

This method should handle the received Unacknowledged Message.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `model` | [`Model`](#model) | The Model associated with this Model Delegate. |
| `message` | `UnacknowledgedMeshMessage` | The Unacknowledged Message received. |
| `source` | `Address` | The source Unicast Address. |
| `destination` | [`MeshAddress`](#meshaddress) | The destination address of the request. |

###### Returns

`void`

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`modelDidReceiveUnacknowledgedMessage`](#modeldidreceiveunacknowledgedmessage)

##### publish()

```ts
publish(message: MeshMessage, manager: MeshNetworkManager): Promise<MessageHandle | undefined>;
```

Publishes a single message given as a parameter using the
Publish information set in the underlying Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `MeshMessage` | The message to be published. |
| `manager` | [`MeshNetworkManager`](#meshnetworkmanager) | The manager to be used for publishing. |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle) \| `undefined`\>

The Message Handler that can be used to cancel sending.

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`publish`](#publish-2)

##### publishUsing()

```ts
publishUsing(manager: MeshNetworkManager): Promise<MessageHandle | undefined>;
```

Publishes a single message created by Model's message composer using
the Publish information set in the underlying Model.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `manager` | [`MeshNetworkManager`](#meshnetworkmanager) | The manager to be used for publishing. |

###### Returns

`Promise`\<[`MessageHandle`](#messagehandle) \| `undefined`\>

The Message Handler that can be used to cancel sending.

###### Inherited from

[`ModelHandler`](#abstract-modelhandler).[`publishUsing`](#publishusing)

##### recall()

```ts
abstract recall(
   scene: SceneNumber, 
   transitionTime?: TransitionTime, 
   delay?: number
): void;
```

This method should recall the States of the Model associated with
the given Scene number.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `scene` | `SceneNumber` | The Scene number. |
| `transitionTime?` | `TransitionTime` | The Transition Time field identifies the time that an element will take to transition to the target state from the present state. |
| `delay?` | `number` | Message execution delay in 5 millisecond steps. |

###### Returns

`void`

##### store()

```ts
abstract store(scene: SceneNumber): void;
```

This method should store the current States of the Model and
associate them with the given Scene number.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `scene` | `SceneNumber` | The Scene number. |

###### Returns

`void`

***

### `abstract` Transmitter

A transmitter is responsible for delivering messages to the mesh network.

#### Constructors

##### Constructor

```ts
new Transmitter(): Transmitter;
```

###### Returns

[`Transmitter`](#abstract-transmitter)

#### Methods

##### send()

```ts
abstract send(data: Data, type: PduType): Promise<void>;
```

This method sends the given data over the bearer.

Data longer than MTU will automatically be segmented if bearer
implements segmentation.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | [`PduType`](#pdutype) |

###### Returns

`Promise`\<`void`\>

###### Throws

This method throws an error if the PDU type is not supported, or data could not be sent for some other reason.

***

### UnknownNode

A class representing an unknown Node connected as a GATT Proxy Node.

An Unknown Node is a Node from which a message Proxy Configuration has been received,
but the mesh network has no information about it.

The Unknown Node has fixed security level set to `Security.insecure`,
a single empty Element and only one `NetworkKey`, the one used to encrypt
the Proxy Configuration message. All other properties are unknown.

NOTE: It is not possible to request its Composition Data, as the Device Key is not known.

#### Extends

- [`Node`](#node)

#### Constructors

##### Constructor

```ts
new UnknownNode(pdu: NetworkPdu, meshNetwork: MeshNetwork): UnknownNode;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | `NetworkPdu` |
| `meshNetwork` | [`MeshNetwork`](#meshnetwork-3) |

###### Returns

[`UnknownNode`](#unknownnode)

###### Overrides

[`Node`](#node).[`constructor`](#constructor-49)

#### Properties

##### companyIdentifier

```ts
companyIdentifier: number | undefined = undefined;
```

The 16-bit Company Identifier (CID) assigned by the Bluetooth SIG.
The value of this property is obtained from node composition data.

###### Inherited from

[`Node`](#node).[`companyIdentifier`](#companyidentifier-5)

##### deviceKey

```ts
deviceKey: Data | undefined = undefined;
```

128-bit device key for this Node.

###### Inherited from

[`Node`](#node).[`deviceKey`](#devicekey)

##### features

```ts
features: NodeFeaturesState | undefined = undefined;
```

Node's features.

###### Inherited from

[`Node`](#node).[`features`](#features-1)

##### heartbeatSubscription

```ts
heartbeatSubscription: HeartbeatSubscription | undefined = undefined;
```

The Heartbeat Subscription object represents parameters that define
receiving of periodical Heartbeat transport control messages.

###### Inherited from

[`Node`](#node).[`heartbeatSubscription`](#heartbeatsubscription)

##### meshNetwork

```ts
meshNetwork: MeshNetwork | undefined = undefined;
```

###### Inherited from

[`Node`](#node).[`meshNetwork`](#meshnetwork-5)

##### minimumNumberOfReplayProtectionList?

```ts
optional minimumNumberOfReplayProtectionList?: number;
```

The minimum number of Replay Protection List (RPL) entries for this
node. The value of this property is obtained from node composition
data.

###### Inherited from

[`Node`](#node).[`minimumNumberOfReplayProtectionList`](#minimumnumberofreplayprotectionlist)

##### productIdentifier

```ts
productIdentifier: number | undefined = undefined;
```

The 16-bit vendor-assigned Product Identifier (PID).
The value of this property is obtained from node composition data.

###### Inherited from

[`Node`](#node).[`productIdentifier`](#productidentifier)

##### security

```ts
security: Security;
```

The level of security for the subnet on which the node has been
originally provisioner.

###### Inherited from

[`Node`](#node).[`security`](#security)

##### uuid

```ts
uuid: UUID;
```

Unique Node Identifier.

###### Inherited from

[`Node`](#node).[`uuid`](#uuid-1)

##### versionIdentifier

```ts
versionIdentifier: number | undefined = undefined;
```

The 16-bit vendor-assigned Version Identifier (VID).
The value of this property is obtained from node composition data.

###### Inherited from

[`Node`](#node).[`versionIdentifier`](#versionidentifier)

#### Accessors

##### appKeys

###### Get Signature

```ts
get appKeys(): NodeKey[];
```

An array of Node Application Key objects that include information
about the Application Keys known to this node.

###### Returns

[`NodeKey`](#nodekey)[]

###### Inherited from

[`Node`](#node).[`appKeys`](#appkeys)

##### applicationKeys

###### Get Signature

```ts
get applicationKeys(): ApplicationKey[];
```

Returns list of Application Keys known to this Node.

NOTE: If the Node has been removed from the mesh network this property returns an empty array.

###### Returns

[`ApplicationKey`](#applicationkey)[]

###### Inherited from

[`Node`](#node).[`applicationKeys`](#applicationkeys-2)

##### defaultTtl

###### Get Signature

```ts
get defaultTtl(): number | undefined;
```

The default Time To Live (TTL) value used when sending messages.
The TTL may only be set for a Provisioner's Node, or for a Node
that has not been added to a mesh network.

Use `ConfigDefaultTtlGet` and `ConfigDefaultTtlSet` messages to
read or set the default TTL value of a remote Node.

###### Returns

`number` \| `undefined`

###### Set Signature

```ts
set defaultTtl(ttl: number | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` \| `undefined` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`defaultTtl`](#defaultttl-1)

##### elements

###### Get Signature

```ts
get elements(): Element[];
```

###### Returns

[`Element`](#element)[]

###### Inherited from

[`Node`](#node).[`elements`](#elements)

##### elementsCount

###### Get Signature

```ts
get elementsCount(): number;
```

Number of Node's Elements.

###### Returns

`number`

###### Inherited from

[`Node`](#node).[`elementsCount`](#elementscount)

##### isCompositionDataReceived

###### Get Signature

```ts
get isCompositionDataReceived(): boolean;
```

Returns weather Composition Data has been applied to the Node.

###### Returns

`boolean`

###### Inherited from

[`Node`](#node).[`isCompositionDataReceived`](#iscompositiondatareceived)

##### isConfigComplete

###### Get Signature

```ts
get isConfigComplete(): boolean;
```

The boolean value represents whether the Mesh Manager
has finished configuring this Node. The property is set to `true`
once a Mesh Manager is done completing this node's
configuration, otherwise it is set to `false`.

###### Returns

`boolean`

###### Set Signature

```ts
set isConfigComplete(value: boolean): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `boolean` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`isConfigComplete`](#isconfigcomplete)

##### isExcluded

###### Get Signature

```ts
get isExcluded(): boolean;
```

The flag is set to `true` when the Node is in the process of being
deleted and is excluded from the new network key distribution
during the key refresh procedure; otherwise is set to `false`.

###### Returns

`boolean`

###### Set Signature

```ts
set isExcluded(value: boolean): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `boolean` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`isExcluded`](#isexcluded)

##### isLocalProvisioner

###### Get Signature

```ts
get isLocalProvisioner(): boolean;
```

Returns whether the Node belongs to the main Provisioner.
The main Provisioner will be used to perform all
provisioning and communication on this device. Every device
should use a different Provisioner to set up devices in the
same mesh network to avoid conflicts with addressing nodes.

###### Returns

`boolean`

###### Inherited from

[`Node`](#node).[`isLocalProvisioner`](#islocalprovisioner)

##### isProvisioner

###### Get Signature

```ts
get isProvisioner(): boolean;
```

Returns whether the Node belongs to one of the Provisioners
of the mesh network.

###### Returns

`boolean`

###### Inherited from

[`Node`](#node).[`isProvisioner`](#isprovisioner)

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

###### Returns

`string` \| `undefined`

###### Set Signature

```ts
set name(name: string | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`name`](#name-4)

##### netKeys

###### Get Signature

```ts
get netKeys(): NodeKey[];
```

###### Returns

[`NodeKey`](#nodekey)[]

###### Inherited from

[`Node`](#node).[`netKeys`](#netkeys)

##### networkKeys

###### Get Signature

```ts
get networkKeys(): NetworkKey[];
```

Returns list of Network Keys known to this Node.

NOTE: If the Node has been removed from the mesh network this property returns an empty array.

###### Returns

[`NetworkKey`](#networkkey)[]

###### Inherited from

[`Node`](#node).[`networkKeys`](#networkkeys-1)

##### networkTransmit

###### Get Signature

```ts
get networkTransmit(): NetworkTransmit | undefined;
```

The object represents parameters of the transmissions of network
layer messages originating from a mesh node.

###### Returns

[`NetworkTransmit`](#networktransmit) \| `undefined`

###### Set Signature

```ts
set networkTransmit(networkTransmit: NetworkTransmit | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkTransmit` | [`NetworkTransmit`](#networktransmit) \| `undefined` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`networkTransmit`](#networktransmit-1)

##### primaryElement

###### Get Signature

```ts
get primaryElement(): Element | undefined;
```

The Primary Element of the Node.

`undefined` is returned if Composition Data has not been received yet.

###### Returns

[`Element`](#element) \| `undefined`

###### Inherited from

[`Node`](#node).[`primaryElement`](#primaryelement-1)

##### primaryUnicastAddress

###### Get Signature

```ts
get primaryUnicastAddress(): Address;
```

Primary Unicast Address of the Node.

###### Returns

`Address`

###### Inherited from

[`Node`](#node).[`primaryUnicastAddress`](#primaryunicastaddress)

##### ttl

###### Get Signature

```ts
get ttl(): number | undefined;
```

The default Time To Live (TTL) value used when sending messages.

###### Returns

`number` \| `undefined`

###### Set Signature

```ts
set ttl(ttl: number | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ttl` | `number` \| `undefined` |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`ttl`](#ttl-1)

##### unicastAddressRange

###### Get Signature

```ts
get unicastAddressRange(): AddressRange;
```

The Unicast Address range assigned to all Elements of the Node.

The address range is continuous and starts with `primaryUnicastAddress`
and ends with `lastUnicastAddress`.

###### Returns

[`AddressRange`](#addressrange)

###### Inherited from

[`Node`](#node).[`unicastAddressRange`](#unicastaddressrange)

#### Methods

##### addApplicationKey()

```ts
addApplicationKey(applicationKey: ApplicationKey): void;
```

Adds the Application Key to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to add. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addApplicationKey`](#addapplicationkey-1)

##### addApplicationKeyWithIndex()

```ts
addApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Adds the Application Key with given index to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key index to add. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addApplicationKeyWithIndex`](#addapplicationkeywithindex)

##### addElement()

```ts
addElement(element: Element): void;
```

Adds the given Element to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `element` | [`Element`](#element) | The Element to be added. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addElement`](#addelement)

##### addElements()

```ts
addElements(elements: Element[]): void;
```

Adds given list of Elements to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `elements` | [`Element`](#element)[] | The list of Elements to be added. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addElements`](#addelements)

##### addNetworkKey()

```ts
addNetworkKey(networkKey: NetworkKey): void;
```

Adds the Network Key to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to add. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addNetworkKey`](#addnetworkkey-1)

##### addNetworkKeyWithIndex()

```ts
addNetworkKeyWithIndex(networkKeyIndex: KeyIndex): void;
```

Adds the Network Key with given index to the Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Network Key index to add. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`addNetworkKeyWithIndex`](#addnetworkkeywithindex)

##### applyCompositionData()

```ts
applyCompositionData(compositionData: ConfigCompositionDataStatus): void;
```

Applies the result of Composition Data to the Node.

This method does nothing if the Node already was configured
or the Composition Data Status does not have Page 0.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `compositionData` | [`ConfigCompositionDataStatus`](#configcompositiondatastatus) | The result of Config Composition Data Get with page 0. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`applyCompositionData`](#applycompositiondata)

##### containsElementsWithAddressesOverlapping()

```ts
containsElementsWithAddressesOverlapping(range: AddressRange): boolean;
```

Returns whether any of the Node's Elements has a Unicast Address from the given
range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`AddressRange`](#addressrange) | Address range to check. |

###### Returns

`boolean`

`True`, if the node address range overlaps with the given range, `false` otherwise.

###### Inherited from

[`Node`](#node).[`containsElementsWithAddressesOverlapping`](#containselementswithaddressesoverlapping)

##### containsElementWithAddress()

```ts
containsElementWithAddress(address: Address): boolean;
```

Returns whether the Node has the given Unicast Address assigned to one
of its Elements.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | Address to check. |

###### Returns

`boolean`

`True` if any of node's elements (or the node itself) was assigned the given address, `false` otherwise.

###### Inherited from

[`Node`](#node).[`containsElementWithAddress`](#containselementwithaddress)

##### elementWithAddress()

```ts
elementWithAddress(address: Address): Element | undefined;
```

Returns the Element that belongs to this Node with the given
Unicast Address, or `undefined`, if such does not exist.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `address` | `Address` | The Unicast Address of an Element to get. |

###### Returns

[`Element`](#element) \| `undefined`

The Element found, or `undefined`, if no such exist.

###### Inherited from

[`Node`](#node).[`elementWithAddress`](#elementwithaddress)

##### equals()

```ts
equals(other: Node): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`Node`](#node) |

###### Returns

`boolean`

###### Inherited from

[`Node`](#node).[`equals`](#equals-2)

##### knowsApplicationKey()

```ts
knowsApplicationKey(applicationKey: ApplicationKey): boolean;
```

Returns whether the Node has knowledge about the given Application Key.
The Application Key comparison bases only on the Key Index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKey` | [`ApplicationKey`](#applicationkey) | The Application Key to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Application Key with the same Key Index as given key, `false` otherwise.

###### Inherited from

[`Node`](#node).[`knowsApplicationKey`](#knowsapplicationkey)

##### knowsApplicationKeyIndex()

```ts
knowsApplicationKeyIndex(applicationKeyIndex: KeyIndex): boolean;
```

Returns whether the Node has knowledge about Application Key with the
given index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Application Key Index to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Application Key index, `false` otherwise.

###### Inherited from

[`Node`](#node).[`knowsApplicationKeyIndex`](#knowsapplicationkeyindex)

##### knowsNetworkKey()

```ts
knowsNetworkKey(networkKey: NetworkKey): boolean;
```

Returns whether the Node has knowledge about the given Network Key.
The Network Key comparison bases only on the Key Index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Network Key with the same Key Index as given key, `false` otherwise.

###### Inherited from

[`Node`](#node).[`knowsNetworkKey`](#knowsnetworkkey)

##### knowsNetworkKeyIndex()

```ts
knowsNetworkKeyIndex(networkKeyIndex: KeyIndex): boolean;
```

Returns whether the Node has knowledge about Network Key with the
given index.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Network Key Index to look for. |

###### Returns

`boolean`

`True` if the Node has knowledge about the Network Key index, `false` otherwise.

###### Inherited from

[`Node`](#node).[`knowsNetworkKeyIndex`](#knowsnetworkkeyindex)

##### removeApplicationKeyWithIndex()

```ts
removeApplicationKeyWithIndex(applicationKeyIndex: KeyIndex): void;
```

Removes the Application Key with given index and all Model bindings
that point to it and the publications that are using this key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndex` | `KeyIndex` | The Key Index of Application Key to be removed. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`removeApplicationKeyWithIndex`](#removeapplicationkeywithindex)

##### removeNetworkKeyWithIndex()

```ts
removeNetworkKeyWithIndex(networkKeyIndex: KeyIndex): void;
```

Removes the Network Key with given index and all Application Keys
bound to it from the Node. This method also removes all Model bindings
that point any of the removed Application Keys and the publications
that are using this key, including Heartbeat publication.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndex` | `KeyIndex` | The Key Index of Network Key to be removed. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`removeNetworkKeyWithIndex`](#removenetworkkeywithindex)

##### setApplicationKeys()

```ts
setApplicationKeys(applicationKeys: ApplicationKey[]): void;
```

Sets the Application Keys to the Node.
This will overwrite the previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeys` | [`ApplicationKey`](#applicationkey)[] | The Application Keys to set. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`setApplicationKeys`](#setapplicationkeys)

##### setApplicationKeysWithIndexes()

```ts
setApplicationKeysWithIndexes(applicationKeyIndexes: KeyIndex[]): void;
```

Sets the Application Keys with given indexes to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `applicationKeyIndexes` | `KeyIndex`[] | The Application Key indexes to set. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`setApplicationKeysWithIndexes`](#setapplicationkeyswithindexes)

##### setElements()

```ts
setElements(elements: Element[]): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `elements` | [`Element`](#element)[] |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`setElements`](#setelements)

##### setNetworkKeys()

```ts
setNetworkKeys(networkKeys: NetworkKey[]): void;
```

Sets the Network Keys to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeys` | [`NetworkKey`](#networkkey)[] | The Network Keys to set. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`setNetworkKeys`](#setnetworkkeys)

##### setNetworkKeysWithIndexes()

```ts
setNetworkKeysWithIndexes(networkKeyIndexes: KeyIndex[]): void;
```

Sets the Network Keys with given indexes to the Node.

This method overwrites previous keys.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKeyIndexes` | `KeyIndex`[] | The Network Key indexes to set. |

###### Returns

`void`

###### Inherited from

[`Node`](#node).[`setNetworkKeysWithIndexes`](#setnetworkkeyswithindexes)

##### decode()

```ts
static decode(jv: Record<string, unknown>): Node;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `jv` | `Record`\<`string`, `unknown`\> |

###### Returns

[`Node`](#node)

###### Inherited from

[`Node`](#node).[`decode`](#decode-9)

##### forProvisionerWithAddress()

```ts
static forProvisionerWithAddress(provisioner: Provisioner, address: Address): Node;
```

Initializes the Provisioner's Node.

The Provisioner's node has the same name and node UUID as the Provisioner.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `provisioner` | [`Provisioner`](#provisioner) | The Provisioner for which the node is added. |
| `address` | `Address` | The unicast address to be assigned to the Node. |

###### Returns

[`Node`](#node)

###### Inherited from

[`Node`](#node).[`forProvisionerWithAddress`](#forprovisionerwithaddress)

##### forUnprovisionedDevice()

```ts
static forUnprovisionedDevice(
   unprovisionedDevice: UnprovisionedDevice, 
   n: number, 
   deviceKey: Data, 
   security: Security, 
   networkKey: NetworkKey, 
   address: Address
): Node;
```

Initializes a Node for given unprovisioned device.

The Node will have the same UUID as the device in the advertising
packet.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `unprovisionedDevice` | [`UnprovisionedDevice`](#unprovisioneddevice) | The newly provisioned device. |
| `n` | `number` | Number of Elements on the new Node. |
| `deviceKey` | `Data` | The Device Key. |
| `security` | `Security` | The Node's security. A Node is considered secure if it was provisioned using a OOB Public Key. |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key. |
| `address` | `Address` | The Unicast Address to be assigned to the Node. |

###### Returns

[`Node`](#node)

###### Inherited from

[`Node`](#node).[`forUnprovisionedDevice`](#forunprovisioneddevice)

##### withAssignedNetworkKeyAndAddress()

```ts
static withAssignedNetworkKeyAndAddress(
   name: string | undefined, 
   uuid: UUID, 
   deviceKey: Data, 
   security: Security, 
   networkKey: NetworkKey, 
   address: Address
): Node;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `name` | `string` \| `undefined` |
| `uuid` | `UUID` |
| `deviceKey` | `Data` |
| `security` | `Security` |
| `networkKey` | [`NetworkKey`](#networkkey) |
| `address` | `Address` |

###### Returns

[`Node`](#node)

###### Inherited from

[`Node`](#node).[`withAssignedNetworkKeyAndAddress`](#withassignednetworkkeyandaddress)

***

### UnprovisionedDevice

A class representing an unprovisioned device.

#### Constructors

##### Constructor

```ts
new UnprovisionedDevice(
   name: string | undefined, 
   uuid: UUID, 
   oobInformation?: OobInformation
): UnprovisionedDevice;
```

a basic Unprovisioned Device object.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `name` | `string` \| `undefined` | The optional name of the device. |
| `uuid` | `UUID` | The UUID of the Unprovisioned Device. |
| `oobInformation` | `OobInformation` | The information about OOB data. |

###### Returns

[`UnprovisionedDevice`](#unprovisioneddevice)

#### Properties

##### name?

```ts
optional name?: string;
```

Returns the human-readable name of the device.

##### oobInformation

```ts
oobInformation: OobInformation;
```

Information that points to out-of-band (OOB) information
needed for provisioning.

##### uuid

```ts
uuid: UUID;
```

Returns the Mesh Beacon UUID of an Unprovisioned Device.

#### Methods

##### fromAdvertisementData()

```ts
static fromAdvertisementData(name: string | undefined, advertisementData: Record<string, unknown>): UnprovisionedDevice | undefined;
```

Creates the Unprovisioned Device object based on the advertisement
data. The Mesh UUID and OOB Information must be present in the
advertisement data, otherwise `undefined` is returned.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `name` | `string` \| `undefined` | - |
| `advertisementData` | `Record`\<`string`, `unknown`\> | The advertisement data deceived from the device during scanning. |

###### Returns

[`UnprovisionedDevice`](#unprovisioneddevice) \| `undefined`

## Interfaces

### InputOob

#### Properties

##### action

```ts
action: InputAction;
```

##### size

```ts
size: number;
```

##### type

```ts
type: inputOob;
```

***

### NetworkIdentity

The Network Identity contains information from Network Identity or Private Network
Identity beacon.

Network Identities can be matched to Network Keys in the network.

#### Methods

##### matches()

```ts
matches(networkKey: NetworkKey): boolean;
```

Returns whether the identity matches given `NetworkKey`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `networkKey` | [`NetworkKey`](#networkkey) | The Network Key to check. |

###### Returns

`boolean`

True, if the identity matches the Network Key; false otherwise.

***

### NodeIdentity

The Node Identity contains information from Node Identity or Private Node Identity
beacon.

It can be used to match advertising device to a specific `Node` in the network.

#### Methods

##### matches()

```ts
matches(node: Node): boolean;
```

Returns whether the identity matches given `Node`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | [`Node`](#node) | The Node to check. |

###### Returns

`boolean`

True, if the identity matches the Node; false otherwise.

***

### NoOob

#### Properties

##### type

```ts
type: noOob;
```

***

### OutputOob

#### Properties

##### action

```ts
action: OutputAction;
```

##### size

```ts
size: number;
```

##### type

```ts
type: outputOob;
```

***

### ProvisioningCapabilities

The device sends this PDU to indicate its supported provisioning
capabilities to a Provisioner.

#### Properties

##### algorithms

```ts
algorithms: Algorithms;
```

Supported algorithms and other capabilities.

##### inputOobActions

```ts
inputOobActions: InputOobActions;
```

Supported Input OOB Actions.

##### inputOobSize

```ts
inputOobSize: number;
```

Maximum supported size of Input OOB value.

##### numberOfElements

```ts
numberOfElements: number;
```

Number of elements supported by the device.

##### oobType

```ts
oobType: OobType;
```

Supported static OOB Types.

##### outputOobActions

```ts
outputOobActions: OutputOobActions;
```

Supported Output OOB Actions.

##### outputOobSize

```ts
outputOobSize: number;
```

Maximum supported size of Output OOB value.

##### publicKeyType

```ts
publicKeyType: PublicKeyType;
```

Supported public key types.

***

### StaticOob

#### Properties

##### type

```ts
type: staticOob;
```

## Type Aliases

### AuthAction

```ts
type AuthAction = 
  | {
  action: "provideStaticKey";
  callback: (data: Data) => void;
}
  | {
  action: "provideNumeric";
  callback: (val: Long) => void;
  maximumNumberOfDigits: UInt8;
  outputAction: OutputAction;
}
  | {
  action: "provideAlphanumeric";
  callback: (text: string) => void;
  maximumNumberOfCharacters: UInt8;
}
  | {
  action: "displayNumber";
  inputAction: InputAction;
  value: BigUInt;
}
  | {
  action: "displayAlphanumeric";
  text: string;
};
```

A set of authentication actions aiming to strengthen device provisioning
security.

#### Union Members

##### Type Literal

```ts
{
  action: "provideStaticKey";
  callback: (data: Data) => void;
}
```

The user shall provide 16 byte OOB Static Key.

***

##### Type Literal

```ts
{
  action: "provideNumeric";
  callback: (val: Long) => void;
  maximumNumberOfDigits: UInt8;
  outputAction: OutputAction;
}
```

The user shall provide a number.

***

##### Type Literal

```ts
{
  action: "provideAlphanumeric";
  callback: (text: string) => void;
  maximumNumberOfCharacters: UInt8;
}
```

The user shall provide an alphanumeric text.

***

##### Type Literal

```ts
{
  action: "displayNumber";
  inputAction: InputAction;
  value: BigUInt;
}
```

The application should display this number to the user.

User should perform selected action given number of times,
or enter the number on the remote device.

The `inputAction` will NOT be `InputAction.inputAlphanumeric`,
which is handled by `displayAlphanumeric()` case.

***

##### Type Literal

```ts
{
  action: "displayAlphanumeric";
  text: string;
}
```

The application should display the text to the user.

User should enter the text on the provisioning device.

***

### AuthenticationMethod

```ts
type AuthenticationMethod = 
  | InputOob
  | OutputOob
  | StaticOob
  | NoOob;
```

The authentication method chosen for provisioning.

***

### CapabilitiesReceived

```ts
type CapabilitiesReceived = {
  capabilities: ProvisioningCapabilities;
  type: capabilitiesReceived;
};
```

Provisioning Capabilities were received.

#### Properties

##### capabilities

```ts
capabilities: ProvisioningCapabilities;
```

##### type

```ts
type: capabilitiesReceived;
```

***

### Complete

```ts
type Complete = {
  type: complete;
};
```

The provisioning process is complete.

#### Properties

##### type

```ts
type: complete;
```

***

### Failed

```ts
type Failed = {
  error: Error;
  type: failed;
};
```

The provisioning has failed because of a local error.

#### Properties

##### error

```ts
error: Error;
```

##### type

```ts
type: failed;
```

***

### MeshDataCtor

```ts
type MeshDataCtor<T extends MeshData> = () => T;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* [`MeshData`](#meshdata) |

#### Returns

`T`

***

### MeshNetworkCtor

```ts
type MeshNetworkCtor<T extends MeshNetwork> = (...args: any) => T;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* [`MeshNetwork`](#meshnetwork-3) |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| ...`args` | `any` |

#### Returns

`T`

***

### Provisioning

```ts
type Provisioning = {
  type: provisioning;
};
```

Provisioning has been started.

#### Properties

##### type

```ts
type: provisioning;
```

***

### ProvisioningHandler

```ts
type ProvisioningHandler = {
  authenticationActionRequired: void;
  inputComplete: void;
  provisioningState: void;
};
```

The handler for receiving provisioning events.

The handler must also provide user input during the provisioning process
related to Input or Output OOB.

#### Methods

##### authenticationActionRequired()

```ts
authenticationActionRequired(action: AuthAction): void;
```

called when an authentication action is required
from the user.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `action` | [`AuthAction`](#authaction) | The action to be performed. |

###### Returns

`void`

##### inputComplete()

```ts
inputComplete(): void;
```

Callback called when the user finished Input Action on the
device.

###### Returns

`void`

##### provisioningState()

```ts
provisioningState(unprovisionedDevice: UnprovisionedDevice, state: ProvisioningState): void;
```

Callback called whenever the provisioning status changes.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `unprovisionedDevice` | [`UnprovisionedDevice`](#unprovisioneddevice) | The device which state has changed. |
| `state` | [`ProvisioningState`](#provisioningstate-1) | The completed provisioning state. |

###### Returns

`void`

***

### ProvisioningState

```ts
type ProvisioningState = 
  | Failed
  | CapabilitiesReceived
  | Complete
  | Provisioning
  | RequestingCapabilities
  | Ready;
```

***

### PublicKey

```ts
type PublicKey = 
  | {
  method: noOobPublicKey;
}
  | {
  key: Uint8Array;
  method: oobPublicKey;
};
```

***

### Ready

```ts
type Ready = {
  type: ready;
};
```

Provisioning Manager is ready to start.

#### Properties

##### type

```ts
type: ready;
```

***

### RequestingCapabilities

```ts
type RequestingCapabilities = {
  type: requestingCapabilities;
};
```

The manager is requesting Provisioning Capabilities from the device.

#### Properties

##### type

```ts
type: requestingCapabilities;
```

## Functions

### addressRangeContains()

```ts
function addressRangeContains(range: AddressRange[], value: number): boolean;
```

Returns whether the given value is in the range array.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`AddressRange`](#addressrange)[] | The range array to be checked. |
| `value` | `number` | The value to be checked. |

#### Returns

`boolean`

`True` if the value is inside the range array, `false` otherwise.

***

### isGroupRanges()

```ts
function isGroupRanges(ranges: AddressRange[]): boolean;
```

Returns `true` if all the address ranges are of group type.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ranges` | [`AddressRange`](#addressrange)[] | An array of address ranges to check. |

#### Returns

`boolean`

`True` if the all address ranges are of group type, `false` otherwise.

***

### isUnicastRanges()

```ts
function isUnicastRanges(ranges: AddressRange[]): boolean;
```

Returns `true` if all the address ranges are of unicast type.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ranges` | [`AddressRange`](#addressrange)[] | An array of address ranges to check. |

#### Returns

`boolean`

`True` if the all address ranges are of unicast type, `false` otherwise.

***

### isValidRanges()

```ts
function isValidRanges(ranges: (AddressRange | SceneRange)[]): boolean;
```

Returns `true` if all the address ranges are valid. Valid address ranges
are in Unicast or Group ranges.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ranges` | ([`AddressRange`](#addressrange) \| [`SceneRange`](#scenerange))[] | An array of address ranges to check. |

#### Returns

`boolean`

`True` if the all address ranges are in Unicast or Group range, `false` otherwise.

***

### mergedRanges()

```ts
function mergedRanges<K extends Number, T extends RangeObject<K>>(ranges: T[]): T[];
```

Returns a sorted array of ranges. If any ranges were overlapping, they
will be merged.

#### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* `Number` |
| `T` *extends* `RangeObject`\<`K`\> |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ranges` | `T`[] | An array of address ranges to merge. |

#### Returns

`T`[]

Sorted array of ranges with all overlapping ranges merged.

***

### rangesContains()

```ts
function rangesContains<E extends Number>(ranges: RangeObject<E>[], range: RangeObject<E>): boolean;
```

Returns whether the range is within any of the ranges in this array.

#### Type Parameters

| Type Parameter |
| ------ |
| `E` *extends* `Number` |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ranges` | `RangeObject`\<`E`\>[] | An array of address ranges to check. |
| `range` | `RangeObject`\<`E`\> | The range to be checked. |

#### Returns

`boolean`

`True` if the range is within the range array, `false` otherwise.

***

### serviceData()

```ts
function serviceData(advertisementData: Record<string, unknown>): Data | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> |

#### Returns

`Data` \| `undefined`

***

### unprovisionedDeviceUUID()

```ts
function unprovisionedDeviceUUID(data: Record<string, unknown>): UUID | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Record`\<`string`, `unknown`\> |

#### Returns

`UUID` \| `undefined`
