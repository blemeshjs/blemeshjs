---
title: Utils Package
description: Shared identifiers, storage abstractions, enums, message helpers, and low-level support types.
icon: Wrench
---

{/* Generated by apps/docs/scripts/generate-api-docs.mjs. Do not edit by hand. */}
{/* Change the TSDoc comment in `packages/utils/src/index.ts` and run `yarn workspace docs api:generate`. */}

Generated from `packages/utils/src/index.ts`.

## Functions

### from()

```ts
function from(pdu: Data): Algorithm | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `pdu` | [`Data`](../../README.md#data) |

#### Returns

[`Algorithm`](../../README.md#algorithm) \| `undefined`

***

### length()

```ts
function length(algorithm: Algorithm): number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `algorithm` | [`Algorithm`](../../README.md#algorithm) |

#### Returns

`number`

***

### value()

```ts
function value(algorithm: Algorithm): number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `algorithm` | [`Algorithm`](../../README.md#algorithm) |

#### Returns

`number`

---

Source: https://www.bluetooth.com/specifications/assigned-numbers/
Last update date: 7 June 2023

## Functions

### nameForId()

```ts
function nameForId(id?: number): string | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `id?` | `number` |

#### Returns

`string` \| `undefined`

---

## Functions

### toString()

```ts
function toString(status: ConfigMessageStatus): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `status` | [`ConfigMessageStatus`](../../README.md#configmessagestatus) |

#### Returns

`string`

---

Safely parses numeric input into a KeyRefreshPhase.

## Param

**value**

Numeric value to convert.

## Functions

### from()

```ts
function from(value: number): KeyRefreshPhase | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

[`KeyRefreshPhase`](../../README.md#keyrefreshphase) \| `undefined`

---

## Functions

### toString()

```ts
function toString(location: Location): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `location` | [`Location`](../../README.md#location) |

#### Returns

`string`

---

A namespace for Mesh Configuration Database Zod schemas.
This provides a clean, organized, and properly typed structure for validation.

## Variables

### AllNodesAddress

```ts
const AllNodesAddress: ZodString;
```

***

### AnyAddress

```ts
const AnyAddress: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
```

***

### AppKey

```ts
const AppKey: ZodObject<{
  boundNetKey: ZodNumber;
  index: ZodNumber;
  key: ZodString;
  name: ZodString;
  oldKey: ZodOptional<ZodString>;
}, $strict>;
```

***

### Bind

```ts
const Bind: ZodArray<ZodNumber>;
```

***

### Credentials

```ts
const Credentials: ZodNumber;
```

***

### Element

```ts
const Element: ZodObject<{
  index: ZodNumber;
  location: ZodString;
  models: ZodArray<ZodObject<{
     bind: ZodArray<ZodNumber>;
     modelId: ZodUnion<readonly [ZodString, ZodString]>;
     publish: ZodOptional<ZodObject<{
        address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString, ZodUnion<...>]>;
        credentials: ZodNumber;
        index: ZodNumber;
        period: ZodObject<{
           numberOfSteps: ZodNumber;
           resolution: ZodPipe<..., ...>;
        }, $strip>;
        retransmit: ZodObject<{
           count: ZodNumber;
           interval: ZodNumber;
        }, $strict>;
        ttl: ZodUnion<readonly [ZodNumber, ZodLiteral<...>]>;
     }, $strict>>;
     subscribe: ZodArray<ZodUnion<readonly [ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>>;
  }, $strict>>;
  name: ZodOptional<ZodString>;
}, $strict>;
```

***

### ElementIndex

```ts
const ElementIndex: ZodNumber;
```

***

### ExclusionList

```ts
const ExclusionList: ZodObject<{
  addresses: ZodArray<ZodString>;
  ivIndex: ZodNumber;
}, $strict>;
```

***

### FeatureState

```ts
const FeatureState: ZodUnion<readonly [ZodLiteral<0>, ZodLiteral<1>, ZodLiteral<2>]>;
```

***

### Group

```ts
const Group: ZodObject<{
  address: ZodUnion<readonly [ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
  name: ZodString;
  parentAddress: ZodUnion<readonly [ZodUnion<readonly [ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>, ZodLiteral<"0000">]>;
}, $strict>;
```

***

### GroupAddress

```ts
const GroupAddress: ZodString;
```

***

### GroupAddressOrLabelUUID

```ts
const GroupAddressOrLabelUUID: ZodUnion<readonly [ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
```

***

### HeartbeatPeriod

```ts
const HeartbeatPeriod: ZodPipe<ZodEnum<{
  0: "0";
  1: "1";
  1024: "1024";
  128: "128";
  16: "16";
  16384: "16384";
  2: "2";
  2048: "2048";
  256: "256";
  32: "32";
  32768: "32768";
  4: "4";
  4096: "4096";
  512: "512";
  64: "64";
  65536: "65536";
  8: "8";
  8192: "8192";
}>, ZodTransform<number, 
  | "0"
  | "1"
  | "2"
  | "4"
  | "8"
  | "16"
  | "32"
  | "64"
  | "128"
  | "256"
  | "512"
  | "1024"
  | "2048"
  | "4096"
  | "8192"
  | "16384"
  | "32768"
| "65536">>;
```

***

### HeartbeatPublication

```ts
const HeartbeatPublication: ZodObject<{
  address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
  features: ZodArray<ZodEnum<{
     friend: "friend";
     lowPower: "lowPower";
     proxy: "proxy";
     relay: "relay";
  }>>;
  index: ZodNumber;
  period: ZodPipe<ZodEnum<{
     0: "0";
     1: "1";
     1024: "1024";
     128: "128";
     16: "16";
     16384: "16384";
     2: "2";
     2048: "2048";
     256: "256";
     32: "32";
     32768: "32768";
     4: "4";
     4096: "4096";
     512: "512";
     64: "64";
     65536: "65536";
     8: "8";
     8192: "8192";
   }>, ZodTransform<number, 
     | "0"
     | "1"
     | "2"
     | "4"
     | "8"
     | "16"
     | "32"
     | "64"
     | "128"
     | "256"
     | "512"
     | "1024"
     | "2048"
     | "4096"
     | "8192"
     | "16384"
     | "32768"
    | "65536">>;
  ttl: ZodNumber;
}, $strict>;
```

***

### HeartbeatSubscription

```ts
const HeartbeatSubscription: ZodObject<{
  destination: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
  source: ZodString;
}, $strict>;
```

***

### Identifier

```ts
const Identifier: ZodString;
```

***

### Key

```ts
const Key: ZodString;
```

***

### KeyIndex

```ts
const KeyIndex: ZodNumber;
```

***

### KeyRefreshPhase

```ts
const KeyRefreshPhase: ZodNumber;
```

***

### MeshConfigurationDatabase

```ts
const MeshConfigurationDatabase: ZodObject<{
  $schema: ZodString;
  appKeys: ZodArray<ZodObject<{
     boundNetKey: ZodNumber;
     index: ZodNumber;
     key: ZodString;
     name: ZodString;
     oldKey: ZodOptional<ZodString>;
  }, $strict>>;
  groups: ZodArray<ZodObject<{
     address: ZodUnion<readonly [ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
     name: ZodString;
     parentAddress: ZodUnion<readonly [ZodUnion<readonly [ZodString, ZodUnion<readonly [..., ...]>]>, ZodLiteral<"0000">]>;
  }, $strict>>;
  id: ZodString;
  meshName: ZodString;
  meshUUID: ZodUnion<readonly [ZodUUID, ZodString]>;
  netKeys: ZodArray<ZodObject<{
     index: ZodNumber;
     key: ZodString;
     minSecurity: ZodEnum<{
        insecure: "insecure";
        secure: "secure";
     }>;
     name: ZodString;
     oldKey: ZodOptional<ZodString>;
     phase: ZodNumber;
     timestamp: ZodOptional<ZodISODateTime>;
  }, $strict>>;
  networkExclusions: ZodOptional<ZodArray<ZodObject<{
     addresses: ZodArray<ZodString>;
     ivIndex: ZodNumber;
  }, $strict>>>;
  nodes: ZodArray<ZodObject<{
     appKeys: ZodArray<ZodObject<{
        index: ZodNumber;
        updated: ZodBoolean;
     }, $strict>>;
     cid: ZodOptional<ZodString>;
     configComplete: ZodBoolean;
     crpl: ZodOptional<ZodString>;
     defaultTTL: ZodOptional<ZodNumber>;
     deviceKey: ZodOptional<ZodString>;
     elements: ZodArray<ZodObject<{
        index: ZodNumber;
        location: ZodString;
        models: ZodArray<ZodObject<{
           bind: ...;
           modelId: ...;
           publish: ...;
           subscribe: ...;
        }, $strict>>;
        name: ZodOptional<ZodString>;
     }, $strict>>;
     excluded: ZodBoolean;
     features: ZodOptional<ZodObject<{
        friend: ZodOptional<ZodUnion<readonly [..., ..., ...]>>;
        lowPower: ZodOptional<ZodUnion<readonly [..., ..., ...]>>;
        proxy: ZodOptional<ZodUnion<readonly [..., ..., ...]>>;
        relay: ZodOptional<ZodUnion<readonly [..., ..., ...]>>;
     }, $strict>>;
     heartbeatPub: ZodOptional<ZodObject<{
        address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
        features: ZodArray<ZodEnum<{
           friend: ...;
           lowPower: ...;
           proxy: ...;
           relay: ...;
        }>>;
        index: ZodNumber;
        period: ZodPipe<ZodEnum<{
           0: ...;
           1: ...;
           1024: ...;
           128: ...;
           16: ...;
           16384: ...;
           2: ...;
           2048: ...;
           256: ...;
           32: ...;
           32768: ...;
           4: ...;
           4096: ...;
           512: ...;
           64: ...;
           65536: ...;
           8: ...;
           8192: ...;
        }>, ZodTransform<number, ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ... | ...>>;
        ttl: ZodNumber;
     }, $strict>>;
     heartbeatSub: ZodOptional<ZodObject<{
        destination: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
        source: ZodString;
     }, $strict>>;
     name: ZodOptional<ZodString>;
     netKeys: ZodArray<ZodObject<{
        index: ZodNumber;
        updated: ZodBoolean;
     }, $strict>>;
     networkTransmit: ZodOptional<ZodObject<{
        count: ZodNumber;
        interval: ZodNumber;
     }, $strict>>;
     pid: ZodOptional<ZodString>;
     relayRetransmit: ZodOptional<ZodObject<{
        count: ZodNumber;
        interval: ZodNumber;
     }, $strict>>;
     secureNetworkBeacon: ZodOptional<ZodBoolean>;
     security: ZodEnum<{
        insecure: "insecure";
        secure: "secure";
     }>;
     unicastAddress: ZodString;
     UUID: ZodUnion<readonly [ZodUUID, ZodString]>;
     vid: ZodOptional<ZodString>;
  }, $strict>>;
  partial: ZodDefault<ZodBoolean>;
  provisioners: ZodArray<ZodObject<{
     allocatedGroupRange: ZodArray<ZodObject<{
        highAddress: ZodString;
        lowAddress: ZodString;
     }, $strict>>;
     allocatedSceneRange: ZodArray<ZodObject<{
        firstScene: ZodString;
        lastScene: ZodString;
     }, $strict>>;
     allocatedUnicastRange: ZodArray<ZodObject<{
        highAddress: ZodString;
        lowAddress: ZodString;
     }, $strict>>;
     provisionerName: ZodString;
     UUID: ZodUnion<readonly [ZodUUID, ZodString]>;
  }, $strict>>;
  scenes: ZodArray<ZodObject<{
     addresses: ZodArray<ZodString>;
     name: ZodString;
     number: ZodString;
  }, $strict>>;
  timestamp: ZodISODateTime;
  version: ZodString;
}, $strict>;
```

***

### Model

```ts
const Model: ZodObject<{
  bind: ZodArray<ZodNumber>;
  modelId: ZodUnion<readonly [ZodString, ZodString]>;
  publish: ZodOptional<ZodObject<{
     address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
     credentials: ZodNumber;
     index: ZodNumber;
     period: ZodObject<{
        numberOfSteps: ZodNumber;
        resolution: ZodPipe<ZodEnum<{
           100: "100";
           1000: "1000";
           10000: "10000";
           600000: "600000";
        }>, ZodTransform<number, "100" | "1000" | "10000" | "600000">>;
     }, $strip>;
     retransmit: ZodObject<{
        count: ZodNumber;
        interval: ZodNumber;
     }, $strict>;
     ttl: ZodUnion<readonly [ZodNumber, ZodLiteral<255>]>;
  }, $strict>>;
  subscribe: ZodArray<ZodUnion<readonly [ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>>;
}, $strict>;
```

***

### ModelId

```ts
const ModelId: ZodUnion<readonly [ZodString, ZodString]>;
```

***

### NetKey

```ts
const NetKey: ZodObject<{
  index: ZodNumber;
  key: ZodString;
  minSecurity: ZodEnum<{
     insecure: "insecure";
     secure: "secure";
  }>;
  name: ZodString;
  oldKey: ZodOptional<ZodString>;
  phase: ZodNumber;
  timestamp: ZodOptional<ZodISODateTime>;
}, $strict>;
```

***

### NetworkRetransmit

```ts
const NetworkRetransmit: ZodObject<{
  count: ZodNumber;
  interval: ZodNumber;
}, $strict>;
```

***

### Node

```ts
const Node: ZodObject<{
  appKeys: ZodArray<ZodObject<{
     index: ZodNumber;
     updated: ZodBoolean;
  }, $strict>>;
  cid: ZodOptional<ZodString>;
  configComplete: ZodBoolean;
  crpl: ZodOptional<ZodString>;
  defaultTTL: ZodOptional<ZodNumber>;
  deviceKey: ZodOptional<ZodString>;
  elements: ZodArray<ZodObject<{
     index: ZodNumber;
     location: ZodString;
     models: ZodArray<ZodObject<{
        bind: ZodArray<ZodNumber>;
        modelId: ZodUnion<readonly [ZodString, ZodString]>;
        publish: ZodOptional<ZodObject<{
           address: ...;
           credentials: ...;
           index: ...;
           period: ...;
           retransmit: ...;
           ttl: ...;
        }, $strict>>;
        subscribe: ZodArray<ZodUnion<readonly [..., ..., ...]>>;
     }, $strict>>;
     name: ZodOptional<ZodString>;
  }, $strict>>;
  excluded: ZodBoolean;
  features: ZodOptional<ZodObject<{
     friend: ZodOptional<ZodUnion<readonly [ZodLiteral<0>, ZodLiteral<1>, ZodLiteral<2>]>>;
     lowPower: ZodOptional<ZodUnion<readonly [ZodLiteral<0>, ZodLiteral<1>, ZodLiteral<2>]>>;
     proxy: ZodOptional<ZodUnion<readonly [ZodLiteral<0>, ZodLiteral<1>, ZodLiteral<2>]>>;
     relay: ZodOptional<ZodUnion<readonly [ZodLiteral<0>, ZodLiteral<1>, ZodLiteral<2>]>>;
  }, $strict>>;
  heartbeatPub: ZodOptional<ZodObject<{
     address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
     features: ZodArray<ZodEnum<{
        friend: "friend";
        lowPower: "lowPower";
        proxy: "proxy";
        relay: "relay";
     }>>;
     index: ZodNumber;
     period: ZodPipe<ZodEnum<{
        0: "0";
        1: "1";
        1024: "1024";
        128: "128";
        16: "16";
        16384: "16384";
        2: "2";
        2048: "2048";
        256: "256";
        32: "32";
        32768: "32768";
        4: "4";
        4096: "4096";
        512: "512";
        64: "64";
        65536: "65536";
        8: "8";
        8192: "8192";
      }>, ZodTransform<number, 
        | "0"
        | "1"
        | "2"
        | "4"
        | "8"
        | "16"
        | "32"
        | "64"
        | "128"
        | "256"
        | "512"
        | "1024"
        | "2048"
        | "4096"
        | "8192"
        | "16384"
        | "32768"
       | "65536">>;
     ttl: ZodNumber;
  }, $strict>>;
  heartbeatSub: ZodOptional<ZodObject<{
     destination: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString]>;
     source: ZodString;
  }, $strict>>;
  name: ZodOptional<ZodString>;
  netKeys: ZodArray<ZodObject<{
     index: ZodNumber;
     updated: ZodBoolean;
  }, $strict>>;
  networkTransmit: ZodOptional<ZodObject<{
     count: ZodNumber;
     interval: ZodNumber;
  }, $strict>>;
  pid: ZodOptional<ZodString>;
  relayRetransmit: ZodOptional<ZodObject<{
     count: ZodNumber;
     interval: ZodNumber;
  }, $strict>>;
  secureNetworkBeacon: ZodOptional<ZodBoolean>;
  security: ZodEnum<{
     insecure: "insecure";
     secure: "secure";
  }>;
  unicastAddress: ZodString;
  UUID: ZodUnion<readonly [ZodUUID, ZodString]>;
  vid: ZodOptional<ZodString>;
}, $strict>;
```

***

### NodeKey

```ts
const NodeKey: ZodObject<{
  index: ZodNumber;
  updated: ZodBoolean;
}, $strict>;
```

***

### ParentAddress

```ts
const ParentAddress: ZodUnion<readonly [ZodUnion<readonly [ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>, ZodLiteral<"0000">]>;
```

***

### Provisioner

```ts
const Provisioner: ZodObject<{
  allocatedGroupRange: ZodArray<ZodObject<{
     highAddress: ZodString;
     lowAddress: ZodString;
  }, $strict>>;
  allocatedSceneRange: ZodArray<ZodObject<{
     firstScene: ZodString;
     lastScene: ZodString;
  }, $strict>>;
  allocatedUnicastRange: ZodArray<ZodObject<{
     highAddress: ZodString;
     lowAddress: ZodString;
  }, $strict>>;
  provisionerName: ZodString;
  UUID: ZodUnion<readonly [ZodUUID, ZodString]>;
}, $strict>;
```

***

### Publish

```ts
const Publish: ZodObject<{
  address: ZodUnion<readonly [ZodString, ZodString, ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>;
  credentials: ZodNumber;
  index: ZodNumber;
  period: ZodObject<{
     numberOfSteps: ZodNumber;
     resolution: ZodPipe<ZodEnum<{
        100: "100";
        1000: "1000";
        10000: "10000";
        600000: "600000";
     }>, ZodTransform<number, "100" | "1000" | "10000" | "600000">>;
  }, $strip>;
  retransmit: ZodObject<{
     count: ZodNumber;
     interval: ZodNumber;
  }, $strict>;
  ttl: ZodUnion<readonly [ZodNumber, ZodLiteral<255>]>;
}, $strict>;
```

***

### Scene

```ts
const Scene: ZodObject<{
  addresses: ZodArray<ZodString>;
  name: ZodString;
  number: ZodString;
}, $strict>;
```

***

### SecurityLevel

```ts
const SecurityLevel: ZodEnum<{
  insecure: "insecure";
  secure: "secure";
}>;
```

***

### SpecialGroupAddress

```ts
const SpecialGroupAddress: ZodString;
```

***

### Subscribe

```ts
const Subscribe: ZodArray<ZodUnion<readonly [ZodString, ZodString, ZodUnion<readonly [ZodUUID, ZodString]>]>>;
```

***

### Timestamp

```ts
const Timestamp: ZodISODateTime;
```

***

### Ttl

```ts
const Ttl: ZodNumber;
```

***

### TtlDefault

```ts
const TtlDefault: ZodLiteral<255>;
```

***

### UnassignedAddress

```ts
const UnassignedAddress: ZodLiteral<"0000">;
```

***

### UnicastAddress

```ts
const UnicastAddress: ZodString;
```

***

### UUID

```ts
const UUID: ZodUnion<readonly [ZodUUID, ZodString]>;
```

---

## Variables

### automatic

```ts
const automatic: {
  type: automatic;
};
```

#### Type Declaration

##### type

```ts
readonly type: automatic = ProxyFilterSetupType.automatic;
```

## Functions

### acceptList()

```ts
function acceptList(addresses: Map<string, Address>): {
  addresses: Map<string, Address>;
  type: acceptList;
};
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `addresses` | `Map`\<`string`, [`Address`](../../README.md#address)\> |

#### Returns

```ts
{
  addresses: Map<string, Address>;
  type: acceptList;
}
```

##### addresses

```ts
addresses: Map<string, Address>;
```

##### type

```ts
readonly type: acceptList = ProxyFilterSetupType.acceptList;
```

***

### rejectList()

```ts
function rejectList(addresses: Map<string, Address>): {
  addresses: Map<string, Address>;
  type: rejectList;
};
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `addresses` | `Map`\<`string`, [`Address`](../../README.md#address)\> |

#### Returns

```ts
{
  addresses: Map<string, Address>;
  type: rejectList;
}
```

##### addresses

```ts
addresses: Map<string, Address>;
```

##### type

```ts
readonly type: rejectList = ProxyFilterSetupType.rejectList;
```

---

## Functions

### fromRawValue()

```ts
function fromRawValue(rawValue: number): ProxyFilterType | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `rawValue` | `number` |

#### Returns

[`ProxyFilterType`](../../README.md#proxyfiltertype) \| `undefined`

***

### toString()

```ts
function toString(type: ProxyFilterType): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `type` | [`ProxyFilterType`](../../README.md#proxyfiltertype) |

#### Returns

`string`

---

## Functions

### fromResolution()

```ts
function fromResolution(resolution: number): StepResolution | undefined;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `resolution` | `number` |

#### Returns

[`StepResolution`](../../README.md#stepresolution) \| `undefined`

***

### toMilliseconds()

```ts
function toMilliseconds(resolution: StepResolution, steps: number): number;
```

Converts the steps to milliseconds using the step resolution.

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `resolution` | [`StepResolution`](../../README.md#stepresolution) |
| `steps` | `number` |

#### Returns

`number`

***

### toString()

```ts
function toString(resolution: StepResolution): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `resolution` | [`StepResolution`](../../README.md#stepresolution) |

#### Returns

`string`

---

## Namespaces

| Namespace | Description |
| ------ | ------ |
| [Algorithm](@blemeshjs/namespaces/Algorithm.md) | - |
| [CompanyIdentifier](@blemeshjs/namespaces/CompanyIdentifier.md) | Source: https://www.bluetooth.com/specifications/assigned-numbers/ Last update date: 7 June 2023 |
| [ConfigMessageStatus](@blemeshjs/namespaces/ConfigMessageStatus.md) | - |
| [KeyRefreshPhase](@blemeshjs/namespaces/KeyRefreshPhase.md) | Safely parses numeric input into a KeyRefreshPhase. |
| [Location](@blemeshjs/namespaces/Location.md) | - |
| [MeshCDB](@blemeshjs/namespaces/MeshCDB.md) | A namespace for Mesh Configuration Database Zod schemas. This provides a clean, organized, and properly typed structure for validation. |
| [ProxyFilterSetup](@blemeshjs/namespaces/ProxyFilterSetup.md) | - |
| [ProxyFilterType](@blemeshjs/namespaces/ProxyFilterType.md) | - |
| [StepResolution](@blemeshjs/namespaces/StepResolution.md) | - |

## Enumerations

### Algorithm

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-btm_ecdh_p256_cmac_aes128_aes_ccm"></a> `BTM_ECDH_P256_CMAC_AES128_AES_CCM` | `128` |
| <a id="enumeration-member-btm_ecdh_p256_hmac_sha256_aes_ccm"></a> `BTM_ECDH_P256_HMAC_SHA256_AES_CCM` | `256` |

***

### CBCentralManagerState

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-poweredoff"></a> `poweredOff` | `"poweredOff"` |
| <a id="enumeration-member-poweredon"></a> `poweredOn` | `"poweredOn"` |
| <a id="enumeration-member-resetting"></a> `resetting` | `"resetting"` |
| <a id="enumeration-member-unauthorized"></a> `unauthorized` | `"unauthorized"` |
| <a id="enumeration-member-unknown"></a> `unknown` | `"unknown"` |
| <a id="enumeration-member-unsupported"></a> `unsupported` | `"unsupported"` |

***

### CBCharacteristicProperties

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-authenticatedsignedwrites"></a> `authenticatedSignedWrites` | `64` |
| <a id="enumeration-member-broadcast"></a> `broadcast` | `1` |
| <a id="enumeration-member-extendedproperties"></a> `extendedProperties` | `128` |
| <a id="enumeration-member-indicate"></a> `indicate` | `32` |
| <a id="enumeration-member-indicateencryptionrequired"></a> `indicateEncryptionRequired` | `512` |
| <a id="enumeration-member-notify"></a> `notify` | `16` |
| <a id="enumeration-member-notifyencryptionrequired"></a> `notifyEncryptionRequired` | `256` |
| <a id="enumeration-member-read"></a> `read` | `2` |
| <a id="enumeration-member-write"></a> `write` | `8` |
| <a id="enumeration-member-writewithoutresponse"></a> `writeWithoutResponse` | `4` |

***

### CBCharacteristicWriteType

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-withoutresponse"></a> `withoutResponse` | `1` |
| <a id="enumeration-member-withresponse"></a> `withResponse` | `0` |

***

### CBPeripheralState

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-connected"></a> `connected` | `"connected"` |
| <a id="enumeration-member-connecting"></a> `connecting` | `"connecting"` |
| <a id="enumeration-member-disconnected"></a> `disconnected` | `"disconnected"` |
| <a id="enumeration-member-disconnecting"></a> `disconnecting` | `"disconnecting"` |

***

### ConfigMessageStatus

The status of a Config operation.

#### See

`UInt8`

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-cannotbind"></a> `cannotBind` | `13` | Cannot Bind. |
| <a id="enumeration-member-cannotremove"></a> `cannotRemove` | `12` | Cannot Remove. |
| <a id="enumeration-member-cannotset"></a> `cannotSet` | `15` | Cannot Set. |
| <a id="enumeration-member-cannotupdate"></a> `cannotUpdate` | `11` | Cannot Update. |
| <a id="enumeration-member-featurenotsupported"></a> `featureNotSupported` | `10` | Feature Not Supported. |
| <a id="enumeration-member-insufficientresources"></a> `insufficientResources` | `5` | Insufficient Resources. |
| <a id="enumeration-member-invalidaddress"></a> `invalidAddress` | `1` | Invalid Address. |
| <a id="enumeration-member-invalidappkeyindex"></a> `invalidAppKeyIndex` | `3` | Invalid AppKey Index. |
| <a id="enumeration-member-invalidbinding"></a> `invalidBinding` | `17` | Invalid Binding. |
| <a id="enumeration-member-invalidmodel"></a> `invalidModel` | `2` | Invalid Model. |
| <a id="enumeration-member-invalidnetkeyindex"></a> `invalidNetKeyIndex` | `4` | Invalid NetKey Index. |
| <a id="enumeration-member-invalidpublishparameters"></a> `invalidPublishParameters` | `7` | Invalid Publish Parameters. |
| <a id="enumeration-member-keyindexalreadystored"></a> `keyIndexAlreadyStored` | `6` | Key Index Already Stored. |
| <a id="enumeration-member-notasubscribemodel"></a> `notASubscribeModel` | `8` | Not a Subscribe Model. |
| <a id="enumeration-member-storagefailure"></a> `storageFailure` | `9` | Storage Failure. |
| <a id="enumeration-member-success"></a> `success` | `0` | Success. |
| <a id="enumeration-member-temporarilyunabletochangestate"></a> `temporarilyUnableToChangeState` | `14` | Temporarily Unable to Change State. |
| <a id="enumeration-member-unspecifiederror"></a> `unspecifiedError` | `16` | Unspecified Error. |

***

### ExportConfiguration

configuration allows to narrow down the exported configuration to a
required minimum.

When sharing a mesh network configuration a care must be taken not to
share sensitive information to prevent unauthorized use.

When sharing a configuration with a guest, a separate Network Key and
set of Application Keys should be created. The Nodes given to the guest under
control should be configured to use the keys. The guest can be given only
the part of the network related to the guest Network Key, with all other data
excluded. Also, to prevent reconfiguration of the Nodes, only partial Nodes
configuration can be shared, which excludes the Device Keys of those Nodes.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-full"></a> `full` | `0` | This configuration will contain the whole copy of local mesh network. |

***

### KeyRefreshPhase

The enum representing Key Refresh phase.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-keydistribution"></a> `keyDistribution` | `1` | Phase 1: Distributing new keys to all nodes. Nodes will transmit using old keys, but can receive using old and new keys. |
| <a id="enumeration-member-normaloperation"></a> `normalOperation` | `0` | Phase 0: Normal Operation. |
| <a id="enumeration-member-usingnewkeys"></a> `usingNewKeys` | `2` | Phase 2: Nodes will use the new keys when encrypting messages but will still receive using the old or new keys. Nodes shall only receive Secure Network beacons secured using the new Network Key. |

***

### Location

An enum defining options for a location of an `Element`.

The values are defined by Bluetooth SIG.

Imported from:
[Assigned Numbers](https://www.bluetooth.com/specifications/assigned-numbers)
-\> 2.4.2.1 Bluetooth SIG GATT Characteristic Presentation Format Description

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-auxiliary"></a> `auxiliary` | `264` |
| <a id="enumeration-member-back"></a> `back` | `257` |
| <a id="enumeration-member-backup"></a> `backup` | `263` |
| <a id="enumeration-member-bottom"></a> `bottom` | `259` |
| <a id="enumeration-member-eighteenth"></a> `eighteenth` | `18` |
| <a id="enumeration-member-eighth"></a> `eighth` | `8` |
| <a id="enumeration-member-eightieth"></a> `eightieth` | `80` |
| <a id="enumeration-member-eightyeighth"></a> `eightyEighth` | `88` |
| <a id="enumeration-member-eightyfifth"></a> `eightyFifth` | `85` |
| <a id="enumeration-member-eightyfirst"></a> `eightyFirst` | `81` |
| <a id="enumeration-member-eightyfourth"></a> `eightyFourth` | `84` |
| <a id="enumeration-member-eightynineth"></a> `eightyNineth` | `89` |
| <a id="enumeration-member-eightysecond"></a> `eightySecond` | `82` |
| <a id="enumeration-member-eightyseventh"></a> `eightySeventh` | `87` |
| <a id="enumeration-member-eightysixth"></a> `eightySixth` | `86` |
| <a id="enumeration-member-eightythird"></a> `eightyThird` | `83` |
| <a id="enumeration-member-eleventh"></a> `eleventh` | `11` |
| <a id="enumeration-member-external"></a> `external` | `272` |
| <a id="enumeration-member-fifteenth"></a> `fifteenth` | `15` |
| <a id="enumeration-member-fifth"></a> `fifth` | `5` |
| <a id="enumeration-member-fiftieth"></a> `fiftieth` | `50` |
| <a id="enumeration-member-fiftyeighth"></a> `fiftyEighth` | `58` |
| <a id="enumeration-member-fiftyfifth"></a> `fiftyFifth` | `55` |
| <a id="enumeration-member-fiftyfirst"></a> `fiftyFirst` | `51` |
| <a id="enumeration-member-fiftyfourth"></a> `fiftyFourth` | `54` |
| <a id="enumeration-member-fiftynineth"></a> `fiftyNineth` | `59` |
| <a id="enumeration-member-fiftysecond"></a> `fiftySecond` | `52` |
| <a id="enumeration-member-fiftyseventh"></a> `fiftySeventh` | `57` |
| <a id="enumeration-member-fiftysixth"></a> `fiftySixth` | `56` |
| <a id="enumeration-member-fiftythird"></a> `fiftyThird` | `53` |
| <a id="enumeration-member-first"></a> `first` | `1` |
| <a id="enumeration-member-flash"></a> `flash` | `266` |
| <a id="enumeration-member-fortieth"></a> `fortieth` | `40` |
| <a id="enumeration-member-fourteenth"></a> `fourteenth` | `14` |
| <a id="enumeration-member-fourth"></a> `fourth` | `4` |
| <a id="enumeration-member-fourtyeighth"></a> `fourtyEighth` | `48` |
| <a id="enumeration-member-fourtyfifth"></a> `fourtyFifth` | `45` |
| <a id="enumeration-member-fourtyfirst"></a> `fourtyFirst` | `41` |
| <a id="enumeration-member-fourtyfourth"></a> `fourtyFourth` | `44` |
| <a id="enumeration-member-fourtynineth"></a> `fourtyNineth` | `49` |
| <a id="enumeration-member-fourtysecond"></a> `fourtySecond` | `42` |
| <a id="enumeration-member-fourtyseventh"></a> `fourtySeventh` | `47` |
| <a id="enumeration-member-fourtysixth"></a> `fourtySixth` | `46` |
| <a id="enumeration-member-fourtythird"></a> `fourtyThird` | `43` |
| <a id="enumeration-member-front"></a> `front` | `256` |
| <a id="enumeration-member-inside"></a> `inside` | `267` |
| <a id="enumeration-member-internal"></a> `internal` | `271` |
| <a id="enumeration-member-left"></a> `left` | `269` |
| <a id="enumeration-member-lower"></a> `lower` | `261` |
| <a id="enumeration-member-main"></a> `main` | `262` |
| <a id="enumeration-member-nineteenth"></a> `nineteenth` | `19` |
| <a id="enumeration-member-nineth"></a> `nineth` | `9` |
| <a id="enumeration-member-ninetieth"></a> `ninetieth` | `90` |
| <a id="enumeration-member-ninetyeighth"></a> `ninetyEighth` | `98` |
| <a id="enumeration-member-ninetyfifth"></a> `ninetyFifth` | `95` |
| <a id="enumeration-member-ninetyfirst"></a> `ninetyFirst` | `91` |
| <a id="enumeration-member-ninetyfourth"></a> `ninetyFourth` | `94` |
| <a id="enumeration-member-ninetynineth"></a> `ninetyNineth` | `99` |
| <a id="enumeration-member-ninetysecond"></a> `ninetySecond` | `92` |
| <a id="enumeration-member-ninetyseventh"></a> `ninetySeventh` | `97` |
| <a id="enumeration-member-ninetysixth"></a> `ninetySixth` | `96` |
| <a id="enumeration-member-ninetythird"></a> `ninetyThird` | `93` |
| <a id="enumeration-member-onehundredandeighteenth"></a> `oneHundredAndEighteenth` | `118` |
| <a id="enumeration-member-onehundredandeighth"></a> `oneHundredAndEighth` | `108` |
| <a id="enumeration-member-onehundredandeightyeighth"></a> `oneHundredAndEightyEighth` | `188` |
| <a id="enumeration-member-onehundredandeightyfifth"></a> `oneHundredAndEightyFifth` | `185` |
| <a id="enumeration-member-onehundredandeightyfirst"></a> `oneHundredAndEightyFirst` | `181` |
| <a id="enumeration-member-onehundredandeightyfourth"></a> `oneHundredAndEightyFourth` | `184` |
| <a id="enumeration-member-onehundredandeightynineth"></a> `oneHundredAndEightyNineth` | `189` |
| <a id="enumeration-member-onehundredandeightysecond"></a> `oneHundredAndEightySecond` | `182` |
| <a id="enumeration-member-onehundredandeightyseventh"></a> `oneHundredAndEightySeventh` | `187` |
| <a id="enumeration-member-onehundredandeightysixth"></a> `oneHundredAndEightySixth` | `186` |
| <a id="enumeration-member-onehundredandeightythird"></a> `oneHundredAndEightyThird` | `183` |
| <a id="enumeration-member-onehundredandeleventh"></a> `oneHundredAndEleventh` | `111` |
| <a id="enumeration-member-onehundredandfifteenth"></a> `oneHundredAndFifteenth` | `115` |
| <a id="enumeration-member-onehundredandfifth"></a> `oneHundredAndFifth` | `105` |
| <a id="enumeration-member-onehundredandfiftyeighth"></a> `oneHundredAndFiftyEighth` | `158` |
| <a id="enumeration-member-onehundredandfiftyfifth"></a> `oneHundredAndFiftyFifth` | `155` |
| <a id="enumeration-member-onehundredandfiftyfirst"></a> `oneHundredAndFiftyFirst` | `151` |
| <a id="enumeration-member-onehundredandfiftyfourth"></a> `oneHundredAndFiftyFourth` | `154` |
| <a id="enumeration-member-onehundredandfiftynineth"></a> `oneHundredAndFiftyNineth` | `159` |
| <a id="enumeration-member-onehundredandfiftysecond"></a> `oneHundredAndFiftySecond` | `152` |
| <a id="enumeration-member-onehundredandfiftyseventh"></a> `oneHundredAndFiftySeventh` | `157` |
| <a id="enumeration-member-onehundredandfiftysixth"></a> `oneHundredAndFiftySixth` | `156` |
| <a id="enumeration-member-onehundredandfiftythird"></a> `oneHundredAndFiftyThird` | `153` |
| <a id="enumeration-member-onehundredandfirst"></a> `oneHundredAndFirst` | `101` |
| <a id="enumeration-member-onehundredandfourteenth"></a> `oneHundredAndFourteenth` | `114` |
| <a id="enumeration-member-onehundredandfourth"></a> `oneHundredAndFourth` | `104` |
| <a id="enumeration-member-onehundredandfourtyeighth"></a> `oneHundredAndFourtyEighth` | `148` |
| <a id="enumeration-member-onehundredandfourtyfifth"></a> `oneHundredAndFourtyFifth` | `145` |
| <a id="enumeration-member-onehundredandfourtyfirst"></a> `oneHundredAndFourtyFirst` | `141` |
| <a id="enumeration-member-onehundredandfourtyfourth"></a> `oneHundredAndFourtyFourth` | `144` |
| <a id="enumeration-member-onehundredandfourtynineth"></a> `oneHundredAndFourtyNineth` | `149` |
| <a id="enumeration-member-onehundredandfourtysecond"></a> `oneHundredAndFourtySecond` | `142` |
| <a id="enumeration-member-onehundredandfourtyseventh"></a> `oneHundredAndFourtySeventh` | `147` |
| <a id="enumeration-member-onehundredandfourtysixth"></a> `oneHundredAndFourtySixth` | `146` |
| <a id="enumeration-member-onehundredandfourtythird"></a> `oneHundredAndFourtyThird` | `143` |
| <a id="enumeration-member-onehundredandnineteenth"></a> `oneHundredAndNineteenth` | `119` |
| <a id="enumeration-member-onehundredandnineth"></a> `oneHundredAndNineth` | `109` |
| <a id="enumeration-member-onehundredandninetyeighth"></a> `oneHundredAndNinetyEighth` | `198` |
| <a id="enumeration-member-onehundredandninetyfifth"></a> `oneHundredAndNinetyFifth` | `195` |
| <a id="enumeration-member-onehundredandninetyfirst"></a> `oneHundredAndNinetyFirst` | `191` |
| <a id="enumeration-member-onehundredandninetyfourth"></a> `oneHundredAndNinetyFourth` | `194` |
| <a id="enumeration-member-onehundredandninetynineth"></a> `oneHundredAndNinetyNineth` | `199` |
| <a id="enumeration-member-onehundredandninetysecond"></a> `oneHundredAndNinetySecond` | `192` |
| <a id="enumeration-member-onehundredandninetyseventh"></a> `oneHundredAndNinetySeventh` | `197` |
| <a id="enumeration-member-onehundredandninetysixth"></a> `oneHundredAndNinetySixth` | `196` |
| <a id="enumeration-member-onehundredandninetythird"></a> `oneHundredAndNinetyThird` | `193` |
| <a id="enumeration-member-onehundredandsecond"></a> `oneHundredAndSecond` | `102` |
| <a id="enumeration-member-onehundredandseventeenth"></a> `oneHundredAndSeventeenth` | `117` |
| <a id="enumeration-member-onehundredandseventh"></a> `oneHundredAndSeventh` | `107` |
| <a id="enumeration-member-onehundredandseventyeighth"></a> `oneHundredAndSeventyEighth` | `178` |
| <a id="enumeration-member-onehundredandseventyfifth"></a> `oneHundredAndSeventyFifth` | `175` |
| <a id="enumeration-member-onehundredandseventyfirst"></a> `oneHundredAndSeventyFirst` | `171` |
| <a id="enumeration-member-onehundredandseventyfourth"></a> `oneHundredAndSeventyFourth` | `174` |
| <a id="enumeration-member-onehundredandseventynineth"></a> `oneHundredAndSeventyNineth` | `179` |
| <a id="enumeration-member-onehundredandseventysecond"></a> `oneHundredAndSeventySecond` | `172` |
| <a id="enumeration-member-onehundredandseventyseventh"></a> `oneHundredAndSeventySeventh` | `177` |
| <a id="enumeration-member-onehundredandseventysixth"></a> `oneHundredAndSeventySixth` | `176` |
| <a id="enumeration-member-onehundredandseventythird"></a> `oneHundredAndSeventyThird` | `173` |
| <a id="enumeration-member-onehundredandsixteenth"></a> `oneHundredAndSixteenth` | `116` |
| <a id="enumeration-member-onehundredandsixth"></a> `oneHundredAndSixth` | `106` |
| <a id="enumeration-member-onehundredandsixtyeighth"></a> `oneHundredAndSixtyEighth` | `168` |
| <a id="enumeration-member-onehundredandsixtyfifth"></a> `oneHundredAndSixtyFifth` | `165` |
| <a id="enumeration-member-onehundredandsixtyfirst"></a> `oneHundredAndSixtyFirst` | `161` |
| <a id="enumeration-member-onehundredandsixtyfourth"></a> `oneHundredAndSixtyFourth` | `164` |
| <a id="enumeration-member-onehundredandsixtynineth"></a> `oneHundredAndSixtyNineth` | `169` |
| <a id="enumeration-member-onehundredandsixtysecond"></a> `oneHundredAndSixtySecond` | `162` |
| <a id="enumeration-member-onehundredandsixtyseventh"></a> `oneHundredAndSixtySeventh` | `167` |
| <a id="enumeration-member-onehundredandsixtysixth"></a> `oneHundredAndSixtySixth` | `166` |
| <a id="enumeration-member-onehundredandsixtythird"></a> `oneHundredAndSixtyThird` | `163` |
| <a id="enumeration-member-onehundredandtenth"></a> `oneHundredAndTenth` | `110` |
| <a id="enumeration-member-onehundredandthird"></a> `oneHundredAndThird` | `103` |
| <a id="enumeration-member-onehundredandthirteenth"></a> `oneHundredAndThirteenth` | `113` |
| <a id="enumeration-member-onehundredandthirtyeighth"></a> `oneHundredAndThirtyEighth` | `138` |
| <a id="enumeration-member-onehundredandthirtyfifth"></a> `oneHundredAndThirtyFifth` | `135` |
| <a id="enumeration-member-onehundredandthirtyfirst"></a> `oneHundredAndThirtyFirst` | `131` |
| <a id="enumeration-member-onehundredandthirtyfourth"></a> `oneHundredAndThirtyFourth` | `134` |
| <a id="enumeration-member-onehundredandthirtynineth"></a> `oneHundredAndThirtyNineth` | `139` |
| <a id="enumeration-member-onehundredandthirtysecond"></a> `oneHundredAndThirtySecond` | `132` |
| <a id="enumeration-member-onehundredandthirtyseventh"></a> `oneHundredAndThirtySeventh` | `137` |
| <a id="enumeration-member-onehundredandthirtysixth"></a> `oneHundredAndThirtySixth` | `136` |
| <a id="enumeration-member-onehundredandthirtythird"></a> `oneHundredAndThirtyThird` | `133` |
| <a id="enumeration-member-onehundredandtwelveth"></a> `oneHundredAndTwelveth` | `112` |
| <a id="enumeration-member-onehundredandtwentyeighth"></a> `oneHundredAndTwentyEighth` | `128` |
| <a id="enumeration-member-onehundredandtwentyfifth"></a> `oneHundredAndTwentyFifth` | `125` |
| <a id="enumeration-member-onehundredandtwentyfirst"></a> `oneHundredAndTwentyFirst` | `121` |
| <a id="enumeration-member-onehundredandtwentyfourth"></a> `oneHundredAndTwentyFourth` | `124` |
| <a id="enumeration-member-onehundredandtwentynineth"></a> `oneHundredAndTwentyNineth` | `129` |
| <a id="enumeration-member-onehundredandtwentysecond"></a> `oneHundredAndTwentySecond` | `122` |
| <a id="enumeration-member-onehundredandtwentyseventh"></a> `oneHundredAndTwentySeventh` | `127` |
| <a id="enumeration-member-onehundredandtwentysixth"></a> `oneHundredAndTwentySixth` | `126` |
| <a id="enumeration-member-onehundredandtwentythird"></a> `oneHundredAndTwentyThird` | `123` |
| <a id="enumeration-member-onehundredeightieth"></a> `oneHundredEightieth` | `180` |
| <a id="enumeration-member-onehundredfiftieth"></a> `oneHundredFiftieth` | `150` |
| <a id="enumeration-member-onehundredfortieth"></a> `oneHundredFortieth` | `140` |
| <a id="enumeration-member-onehundredninetieth"></a> `oneHundredNinetieth` | `190` |
| <a id="enumeration-member-onehundredseventieth"></a> `oneHundredSeventieth` | `170` |
| <a id="enumeration-member-onehundredsixtieth"></a> `oneHundredSixtieth` | `160` |
| <a id="enumeration-member-onehundredth"></a> `oneHundredth` | `100` |
| <a id="enumeration-member-onehundredthirtieth"></a> `oneHundredThirtieth` | `130` |
| <a id="enumeration-member-onehundredtwentieth"></a> `oneHundredTwentieth` | `120` |
| <a id="enumeration-member-outside"></a> `outside` | `268` |
| <a id="enumeration-member-right"></a> `right` | `270` |
| <a id="enumeration-member-second"></a> `second` | `2` |
| <a id="enumeration-member-seventeenth"></a> `seventeenth` | `17` |
| <a id="enumeration-member-seventh"></a> `seventh` | `7` |
| <a id="enumeration-member-seventieth"></a> `seventieth` | `70` |
| <a id="enumeration-member-seventyeighth"></a> `seventyEighth` | `78` |
| <a id="enumeration-member-seventyfifth"></a> `seventyFifth` | `75` |
| <a id="enumeration-member-seventyfirst"></a> `seventyFirst` | `71` |
| <a id="enumeration-member-seventyfourth"></a> `seventyFourth` | `74` |
| <a id="enumeration-member-seventynineth"></a> `seventyNineth` | `79` |
| <a id="enumeration-member-seventysecond"></a> `seventySecond` | `72` |
| <a id="enumeration-member-seventyseventh"></a> `seventySeventh` | `77` |
| <a id="enumeration-member-seventysixth"></a> `seventySixth` | `76` |
| <a id="enumeration-member-seventythird"></a> `seventyThird` | `73` |
| <a id="enumeration-member-sixteenth"></a> `sixteenth` | `16` |
| <a id="enumeration-member-sixth"></a> `sixth` | `6` |
| <a id="enumeration-member-sixtieth"></a> `sixtieth` | `60` |
| <a id="enumeration-member-sixtyeighth"></a> `sixtyEighth` | `68` |
| <a id="enumeration-member-sixtyfifth"></a> `sixtyFifth` | `65` |
| <a id="enumeration-member-sixtyfirst"></a> `sixtyFirst` | `61` |
| <a id="enumeration-member-sixtyfourth"></a> `sixtyFourth` | `64` |
| <a id="enumeration-member-sixtynineth"></a> `sixtyNineth` | `69` |
| <a id="enumeration-member-sixtysecond"></a> `sixtySecond` | `62` |
| <a id="enumeration-member-sixtyseventh"></a> `sixtySeventh` | `67` |
| <a id="enumeration-member-sixtysixth"></a> `sixtySixth` | `66` |
| <a id="enumeration-member-sixtythird"></a> `sixtyThird` | `63` |
| <a id="enumeration-member-supplementary"></a> `supplementary` | `265` |
| <a id="enumeration-member-tenth"></a> `tenth` | `10` |
| <a id="enumeration-member-third"></a> `third` | `3` |
| <a id="enumeration-member-thirteenth"></a> `thirteenth` | `13` |
| <a id="enumeration-member-thirtieth"></a> `thirtieth` | `30` |
| <a id="enumeration-member-thirtyeighth"></a> `thirtyEighth` | `38` |
| <a id="enumeration-member-thirtyfifth"></a> `thirtyFifth` | `35` |
| <a id="enumeration-member-thirtyfirst"></a> `thirtyFirst` | `31` |
| <a id="enumeration-member-thirtyfourth"></a> `thirtyFourth` | `34` |
| <a id="enumeration-member-thirtynineth"></a> `thirtyNineth` | `39` |
| <a id="enumeration-member-thirtysecond"></a> `thirtySecond` | `32` |
| <a id="enumeration-member-thirtyseventh"></a> `thirtySeventh` | `37` |
| <a id="enumeration-member-thirtysixth"></a> `thirtySixth` | `36` |
| <a id="enumeration-member-thirtythird"></a> `thirtyThird` | `33` |
| <a id="enumeration-member-top"></a> `top` | `258` |
| <a id="enumeration-member-twelveth"></a> `twelveth` | `12` |
| <a id="enumeration-member-twentieth"></a> `twentieth` | `20` |
| <a id="enumeration-member-twentyeighth"></a> `twentyEighth` | `28` |
| <a id="enumeration-member-twentyfifth"></a> `twentyFifth` | `25` |
| <a id="enumeration-member-twentyfirst"></a> `twentyFirst` | `21` |
| <a id="enumeration-member-twentyfourth"></a> `twentyFourth` | `24` |
| <a id="enumeration-member-twentynineth"></a> `twentyNineth` | `29` |
| <a id="enumeration-member-twentysecond"></a> `twentySecond` | `22` |
| <a id="enumeration-member-twentyseventh"></a> `twentySeventh` | `27` |
| <a id="enumeration-member-twentysixth"></a> `twentySixth` | `26` |
| <a id="enumeration-member-twentythird"></a> `twentyThird` | `23` |
| <a id="enumeration-member-twohundredandeighteenth"></a> `twoHundredAndEighteenth` | `218` |
| <a id="enumeration-member-twohundredandeighth"></a> `twoHundredAndEighth` | `208` |
| <a id="enumeration-member-twohundredandeleventh"></a> `twoHundredAndEleventh` | `211` |
| <a id="enumeration-member-twohundredandfifteenth"></a> `twoHundredAndFifteenth` | `215` |
| <a id="enumeration-member-twohundredandfifth"></a> `twoHundredAndFifth` | `205` |
| <a id="enumeration-member-twohundredandfiftyfifth"></a> `twoHundredAndFiftyFifth` | `255` |
| <a id="enumeration-member-twohundredandfiftyfirst"></a> `twoHundredAndFiftyFirst` | `251` |
| <a id="enumeration-member-twohundredandfiftyfourth"></a> `twoHundredAndFiftyFourth` | `254` |
| <a id="enumeration-member-twohundredandfiftysecond"></a> `twoHundredAndFiftySecond` | `252` |
| <a id="enumeration-member-twohundredandfiftythird"></a> `twoHundredAndFiftyThird` | `253` |
| <a id="enumeration-member-twohundredandfirst"></a> `twoHundredAndFirst` | `201` |
| <a id="enumeration-member-twohundredandfourteenth"></a> `twoHundredAndFourteenth` | `214` |
| <a id="enumeration-member-twohundredandfourth"></a> `twoHundredAndFourth` | `204` |
| <a id="enumeration-member-twohundredandfourtyeighth"></a> `twoHundredAndFourtyEighth` | `248` |
| <a id="enumeration-member-twohundredandfourtyfifth"></a> `twoHundredAndFourtyFifth` | `245` |
| <a id="enumeration-member-twohundredandfourtyfirst"></a> `twoHundredAndFourtyFirst` | `241` |
| <a id="enumeration-member-twohundredandfourtyfourth"></a> `twoHundredAndFourtyFourth` | `244` |
| <a id="enumeration-member-twohundredandfourtynineth"></a> `twoHundredAndFourtyNineth` | `249` |
| <a id="enumeration-member-twohundredandfourtysecond"></a> `twoHundredAndFourtySecond` | `242` |
| <a id="enumeration-member-twohundredandfourtyseventh"></a> `twoHundredAndFourtySeventh` | `247` |
| <a id="enumeration-member-twohundredandfourtysixth"></a> `twoHundredAndFourtySixth` | `246` |
| <a id="enumeration-member-twohundredandfourtythird"></a> `twoHundredAndFourtyThird` | `243` |
| <a id="enumeration-member-twohundredandnineteenth"></a> `twoHundredAndNineteenth` | `219` |
| <a id="enumeration-member-twohundredandnineth"></a> `twoHundredAndNineth` | `209` |
| <a id="enumeration-member-twohundredandsecond"></a> `twoHundredAndSecond` | `202` |
| <a id="enumeration-member-twohundredandseventeenth"></a> `twoHundredAndSeventeenth` | `217` |
| <a id="enumeration-member-twohundredandseventh"></a> `twoHundredAndSeventh` | `207` |
| <a id="enumeration-member-twohundredandsixteenth"></a> `twoHundredAndSixteenth` | `216` |
| <a id="enumeration-member-twohundredandsixth"></a> `twoHundredAndSixth` | `206` |
| <a id="enumeration-member-twohundredandtenth"></a> `twoHundredAndTenth` | `210` |
| <a id="enumeration-member-twohundredandthird"></a> `twoHundredAndThird` | `203` |
| <a id="enumeration-member-twohundredandthirteenth"></a> `twoHundredAndThirteenth` | `213` |
| <a id="enumeration-member-twohundredandthirtyeighth"></a> `twoHundredAndThirtyEighth` | `238` |
| <a id="enumeration-member-twohundredandthirtyfifth"></a> `twoHundredAndThirtyFifth` | `235` |
| <a id="enumeration-member-twohundredandthirtyfirst"></a> `twoHundredAndThirtyFirst` | `231` |
| <a id="enumeration-member-twohundredandthirtyfourth"></a> `twoHundredAndThirtyFourth` | `234` |
| <a id="enumeration-member-twohundredandthirtynineth"></a> `twoHundredAndThirtyNineth` | `239` |
| <a id="enumeration-member-twohundredandthirtysecond"></a> `twoHundredAndThirtySecond` | `232` |
| <a id="enumeration-member-twohundredandthirtyseventh"></a> `twoHundredAndThirtySeventh` | `237` |
| <a id="enumeration-member-twohundredandthirtysixth"></a> `twoHundredAndThirtySixth` | `236` |
| <a id="enumeration-member-twohundredandthirtythird"></a> `twoHundredAndThirtyThird` | `233` |
| <a id="enumeration-member-twohundredandtwelveth"></a> `twoHundredAndTwelveth` | `212` |
| <a id="enumeration-member-twohundredandtwentyeighth"></a> `twoHundredAndTwentyEighth` | `228` |
| <a id="enumeration-member-twohundredandtwentyfifth"></a> `twoHundredAndTwentyFifth` | `225` |
| <a id="enumeration-member-twohundredandtwentyfirst"></a> `twoHundredAndTwentyFirst` | `221` |
| <a id="enumeration-member-twohundredandtwentyfourth"></a> `twoHundredAndTwentyFourth` | `224` |
| <a id="enumeration-member-twohundredandtwentynineth"></a> `twoHundredAndTwentyNineth` | `229` |
| <a id="enumeration-member-twohundredandtwentysecond"></a> `twoHundredAndTwentySecond` | `222` |
| <a id="enumeration-member-twohundredandtwentyseventh"></a> `twoHundredAndTwentySeventh` | `227` |
| <a id="enumeration-member-twohundredandtwentysixth"></a> `twoHundredAndTwentySixth` | `226` |
| <a id="enumeration-member-twohundredandtwentythird"></a> `twoHundredAndTwentyThird` | `223` |
| <a id="enumeration-member-twohundredfiftieth"></a> `twoHundredFiftieth` | `250` |
| <a id="enumeration-member-twohundredfortieth"></a> `twoHundredFortieth` | `240` |
| <a id="enumeration-member-twohundredth"></a> `twoHundredth` | `200` |
| <a id="enumeration-member-twohundredthirtieth"></a> `twoHundredThirtieth` | `230` |
| <a id="enumeration-member-twohundredtwentieth"></a> `twoHundredTwentieth` | `220` |
| <a id="enumeration-member-unknown-1"></a> `unknown` | `0` |
| <a id="enumeration-member-upper"></a> `upper` | `260` |

***

### LogCategory

The log category indicates the component that created the log entry.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-access"></a> `access` | `"Access"` | Log created by the Access layer. |
| <a id="enumeration-member-bearer"></a> `bearer` | `"Bearer"` | Log created by the Bearer component. |
| <a id="enumeration-member-foundationmodel"></a> `foundationModel` | `"FoundationModel"` | Log created by the Foundation layer models. |
| <a id="enumeration-member-lowertransport"></a> `lowerTransport` | `"LowerTransport"` | Log created by the Lower Transport layer. |
| <a id="enumeration-member-model"></a> `model` | `"Model"` | Log created by the Access layer model. |
| <a id="enumeration-member-network"></a> `network` | `"Network"` | Log created by the Network layer. |
| <a id="enumeration-member-provisioning"></a> `provisioning` | `"Provisioning"` | Log created by the Provisioning component. |
| <a id="enumeration-member-proxy"></a> `proxy` | `"Proxy"` | Log created by the Proxy component. |
| <a id="enumeration-member-uppertransport"></a> `upperTransport` | `"UpperTransport"` | Log created by the Upper Transport layer. |

***

### MeshMessageSecurity

The mesh message security enum determines authentication level
which shall be used when encrypting a segmented mesh message.

This filed is used to determine the TransMIC.

The Message Integrity Check for Transport (TransMIC) is a 32-bit or 64-bit
field that authenticates that the Access payload has not been changed.

For a segmented message, where SEG is set to 1, the size of the TransMIC
is determined by the value of the SZMIC field in the Lower Transport PDU.
For unsegmented messages, the size of the TransMIC is 32 bits for data messages.

Control messages do not have a TransMIC.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-high"></a> `high` | `1` | Message will be sent with 64-bit Transport MIC. Unsegmented messages cannot be sent with this option. |
| <a id="enumeration-member-low"></a> `low` | `0` | Message will be sent with 32-bit Transport MIC. |

***

### ProxyFilterSetupType

An enumeration for different initial configurations of the Proxy Filter.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-acceptlist"></a> `acceptList` | `1` | The Proxy Filter on each connected Proxy Node will be set to `ProxyFilterType.acceptList` with given set of addresses. |
| <a id="enumeration-member-automatic"></a> `automatic` | `0` | In automatic Proxy Filter setup the filter will be set to `ProxyFilterType.acceptList` with Unicast Addresses of all local Elements, all Group Addresses with at least one local Model subscribed and the All Nodes (0xFFFF) address. This is the default configuration. |
| <a id="enumeration-member-rejectlist"></a> `rejectList` | `2` | The Proxy Filter on each connected Proxy Node will be set to `ProxyFilerType.rejectList` with given set of addresses. |

***

### ProxyFilterType

A type of the Proxy Filter.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-acceptlist-1"></a> `acceptList` | `0` | An accept list filter has an associated accept list containing destination addresses that are of interest for the Proxy Client. The accept list filter blocks all messages except those targeting addresses added to the list. |
| <a id="enumeration-member-rejectlist-1"></a> `rejectList` | `1` | A reject list filter has an associated reject list containing destination addresses that are NOT of the Proxy Client interest. The reject list filter forwards all messages except those targeting addresses added to the list. |

***

### SigModelId

#### Enumeration Members

| Enumeration Member | Value |
| ------ | ------ |
| <a id="enumeration-member-blobtransferclientmodelid"></a> `blobTransferClientModelId` | `5121` |
| <a id="enumeration-member-blobtransferservermodelid"></a> `blobTransferServerModelId` | `5120` |
| <a id="enumeration-member-bridgeconfigurationclientmodelid"></a> `bridgeConfigurationClientModelId` | `9` |
| <a id="enumeration-member-bridgeconfigurationservermodelid"></a> `bridgeConfigurationServerModelId` | `8` |
| <a id="enumeration-member-configurationclientmodelid"></a> `configurationClientModelId` | `1` |
| <a id="enumeration-member-configurationservermodelid"></a> `configurationServerModelId` | `0` |
| <a id="enumeration-member-directedforwardingconfigurationclientmodelid"></a> `directedForwardingConfigurationClientModelId` | `7` |
| <a id="enumeration-member-directedforwardingconfigurationservermodelid"></a> `directedForwardingConfigurationServerModelId` | `6` |
| <a id="enumeration-member-firmwaredistributionclientmodelid"></a> `firmwareDistributionClientModelId` | `5125` |
| <a id="enumeration-member-firmwaredistributionservermodelid"></a> `firmwareDistributionServerModelId` | `5124` |
| <a id="enumeration-member-firmwareupdateclientmodelid"></a> `firmwareUpdateClientModelId` | `5123` |
| <a id="enumeration-member-firmwareupdateservermodelid"></a> `firmwareUpdateServerModelId` | `5122` |
| <a id="enumeration-member-genericadminpropertyservermodelid"></a> `genericAdminPropertyServerModelId` | `4113` |
| <a id="enumeration-member-genericbatteryclientmodelid"></a> `genericBatteryClientModelId` | `4109` |
| <a id="enumeration-member-genericbatteryservermodelid"></a> `genericBatteryServerModelId` | `4108` |
| <a id="enumeration-member-genericclientpropertyservermodelid"></a> `genericClientPropertyServerModelId` | `4116` |
| <a id="enumeration-member-genericdefaulttransitiontimeclientmodelid"></a> `genericDefaultTransitionTimeClientModelId` | `4101` |
| <a id="enumeration-member-genericdefaulttransitiontimeservermodelid"></a> `genericDefaultTransitionTimeServerModelId` | `4100` |
| <a id="enumeration-member-genericlevelclientmodelid"></a> `genericLevelClientModelId` | `4099` |
| <a id="enumeration-member-genericlevelservermodelid"></a> `genericLevelServerModelId` | `4098` |
| <a id="enumeration-member-genericlocationclientmodelid"></a> `genericLocationClientModelId` | `4112` |
| <a id="enumeration-member-genericlocationservermodelid"></a> `genericLocationServerModelId` | `4110` |
| <a id="enumeration-member-genericlocationsetupservermodelid"></a> `genericLocationSetupServerModelId` | `4111` |
| <a id="enumeration-member-genericmanufacturerpropertyservermodelid"></a> `genericManufacturerPropertyServerModelId` | `4114` |
| <a id="enumeration-member-genericonoffclientmodelid"></a> `genericOnOffClientModelId` | `4097` |
| <a id="enumeration-member-genericonoffservermodelid"></a> `genericOnOffServerModelId` | `4096` |
| <a id="enumeration-member-genericpowerlevelclientmodelid"></a> `genericPowerLevelClientModelId` | `4107` |
| <a id="enumeration-member-genericpowerlevelservermodelid"></a> `genericPowerLevelServerModelId` | `4105` |
| <a id="enumeration-member-genericpowerlevelsetupservermodelid"></a> `genericPowerLevelSetupServerModelId` | `4106` |
| <a id="enumeration-member-genericpoweronoffclientmodelid"></a> `genericPowerOnOffClientModelId` | `4104` |
| <a id="enumeration-member-genericpoweronoffservermodelid"></a> `genericPowerOnOffServerModelId` | `4102` |
| <a id="enumeration-member-genericpoweronoffsetupservermodelid"></a> `genericPowerOnOffSetupServerModelId` | `4103` |
| <a id="enumeration-member-genericpropertyclientmodelid"></a> `genericPropertyClientModelId` | `4117` |
| <a id="enumeration-member-genericuserpropertyservermodelid"></a> `genericUserPropertyServerModelId` | `4115` |
| <a id="enumeration-member-healthclientmodelid"></a> `healthClientModelId` | `3` |
| <a id="enumeration-member-healthservermodelid"></a> `healthServerModelId` | `2` |
| <a id="enumeration-member-largecompositiondataclientmodelid"></a> `largeCompositionDataClientModelId` | `19` |
| <a id="enumeration-member-largecompositiondataservermodelid"></a> `largeCompositionDataServerModelId` | `18` |
| <a id="enumeration-member-lightctlclientmodelid"></a> `lightCTLClientModelId` | `4869` |
| <a id="enumeration-member-lightctlservermodelid"></a> `lightCTLServerModelId` | `4867` |
| <a id="enumeration-member-lightctlsetupservermodelid"></a> `lightCTLSetupServerModelId` | `4868` |
| <a id="enumeration-member-lightctltemperatureservermodelid"></a> `lightCTLTemperatureServerModelId` | `4870` |
| <a id="enumeration-member-lighthslclientmodelid"></a> `lightHSLClientModelId` | `4873` |
| <a id="enumeration-member-lighthslhueservermodelid"></a> `lightHSLHueServerModelId` | `4874` |
| <a id="enumeration-member-lighthslsaturationservermodelid"></a> `lightHSLSaturationServerModelId` | `4875` |
| <a id="enumeration-member-lighthslservermodelid"></a> `lightHSLServerModelId` | `4871` |
| <a id="enumeration-member-lighthslsetupservermodelid"></a> `lightHSLSetupServerModelId` | `4872` |
| <a id="enumeration-member-lightlcclientmodelid"></a> `lightLCClientModelId` | `4881` |
| <a id="enumeration-member-lightlcservermodelid"></a> `lightLCServerModelId` | `4879` |
| <a id="enumeration-member-lightlcsetupservermodelid"></a> `lightLCSetupServerModelId` | `4880` |
| <a id="enumeration-member-lightlightnessclientmodelid"></a> `lightLightnessClientModelId` | `4866` |
| <a id="enumeration-member-lightlightnessservermodelid"></a> `lightLightnessServerModelId` | `4864` |
| <a id="enumeration-member-lightlightnesssetupservermodelid"></a> `lightLightnessSetupServerModelId` | `4865` |
| <a id="enumeration-member-lightxylclientmodelid"></a> `lightXyLClientModelId` | `4878` |
| <a id="enumeration-member-lightxylservermodelid"></a> `lightXyLServerModelId` | `4876` |
| <a id="enumeration-member-lightxylsetupservermodelid"></a> `lightXyLSetupServerModelId` | `4877` |
| <a id="enumeration-member-ondemandprivateproxyclientmodelid"></a> `onDemandPrivateProxyClientModelId` | `13` |
| <a id="enumeration-member-ondemandprivateproxyservermodelid"></a> `onDemandPrivateProxyServerModelId` | `12` |
| <a id="enumeration-member-opcodesaggregatorclientmodelid"></a> `opcodesAggregatorClientModelId` | `17` |
| <a id="enumeration-member-opcodesaggregatorservermodelid"></a> `opcodesAggregatorServerModelId` | `16` |
| <a id="enumeration-member-privatebeaconclientmodelid"></a> `privateBeaconClientModelId` | `11` |
| <a id="enumeration-member-privatebeaconservermodelid"></a> `privateBeaconServerModelId` | `10` |
| <a id="enumeration-member-remoteprovisioningclientmodelid"></a> `remoteProvisioningClientModelId` | `5` |
| <a id="enumeration-member-remoteprovisioningservermodelid"></a> `remoteProvisioningServerModelId` | `4` |
| <a id="enumeration-member-sarconfigurationclientmodelid"></a> `sarConfigurationClientModelId` | `15` |
| <a id="enumeration-member-sarconfigurationservermodelid"></a> `sarConfigurationServerModelId` | `14` |
| <a id="enumeration-member-sceneclientmodelid"></a> `sceneClientModelId` | `4613` |
| <a id="enumeration-member-sceneservermodelid"></a> `sceneServerModelId` | `4611` |
| <a id="enumeration-member-scenesetupservermodelid"></a> `sceneSetupServerModelId` | `4612` |
| <a id="enumeration-member-schedulerclientmodelid"></a> `schedulerClientModelId` | `4616` |
| <a id="enumeration-member-schedulerservermodelid"></a> `schedulerServerModelId` | `4614` |
| <a id="enumeration-member-schedulersetupservermodelid"></a> `schedulerSetupServerModelId` | `4615` |
| <a id="enumeration-member-sensorclientmodelid"></a> `sensorClientModelId` | `4354` |
| <a id="enumeration-member-sensorservermodelid"></a> `sensorServerModelId` | `4352` |
| <a id="enumeration-member-sensorsetupservermodelid"></a> `sensorSetupServerModelId` | `4353` |
| <a id="enumeration-member-solicitationpdurplconfigurationclientmodelid"></a> `solicitationPduRplConfigurationClientModelId` | `21` |
| <a id="enumeration-member-solicitationpdurplconfigurationservermodelid"></a> `solicitationPduRplConfigurationServerModelId` | `20` |
| <a id="enumeration-member-timeclientmodelid"></a> `timeClientModelId` | `4610` |
| <a id="enumeration-member-timeservermodelid"></a> `timeServerModelId` | `4608` |
| <a id="enumeration-member-timesetupservermodelid"></a> `timeSetupServerModelId` | `4609` |

***

### StepResolution

The Step Resolution field enumerates the resolution of the Number of Steps field
in `TransitionTime` or `Period`.

#### Enumeration Members

| Enumeration Member | Value | Description |
| ------ | ------ | ------ |
| <a id="enumeration-member-hundredsofmilliseconds"></a> `hundredsOfMilliseconds` | `0` | The Step Resolution is 100 milliseconds. |
| <a id="enumeration-member-seconds"></a> `seconds` | `1` | The Step Resolution is 1 second. |
| <a id="enumeration-member-tensofminutes"></a> `tensOfMinutes` | `3` | The Step Resolution is 10 minutes. |
| <a id="enumeration-member-tensofseconds"></a> `tensOfSeconds` | `2` | The Step Resolution is 10 seconds. |

## Classes

### AccessError

A set of errors originating from the access layer.

#### Extends

- `Error`

#### Properties

##### busy

```ts
static busy: AccessError;
```

Error thrown when trying to send a message to an address
for which another message is already being sent.

##### cancelled

```ts
static cancelled: AccessError;
```

Thrown when sending the message was cancelled.

##### cannotDelete

```ts
static cannotDelete: AccessError;
```

Error thrown when the Provisioner is trying to delete
the last Network Key from the Node, or a key that is used
to secure the message.

##### cannotRelay

```ts
static cannotRelay: AccessError;
```

Thrown when a message is sent that is encrypted with a Network Key
that is not known to the connected GATT Proxy, or no GATT Proxy is
connected.

##### invalidDestination

```ts
static invalidDestination: AccessError;
```

Thrown when the destination Address is not known and the
library cannot determine the Network Key to use.

##### invalidElement

```ts
static invalidElement: AccessError;
```

Thrown when trying to send a message using an Element
that does not belong to the local Provisioner's Node.

##### invalidKey

```ts
static invalidKey: AccessError;
```

Thrown when the target Node cannot decrypt messages
sent with the given key.

##### invalidSource

```ts
static invalidSource: AccessError;
```

Error thrown when the local Provisioner does not have
a Unicast Address specified and is not able to send
requested message.

##### invalidTtl

```ts
static invalidTtl: AccessError;
```

Thrown when the given TTL is not valid. Valid TTL must
be 0 or in range 2...127.

##### modelNotBoundToAppKey

```ts
static modelNotBoundToAppKey: AccessError;
```

Thrown when trying to send a message from a Model that
does not have any Application Key bound to it.

##### noDeviceKey

```ts
static noDeviceKey: AccessError;
```

Thrown when trying to send a config message to a Node of
which the Device Key is not known.

##### timeout

```ts
static timeout: AccessError;
```

Thrown, when the acknowledgment has not been received until
the time run out.

***

### `abstract` AcknowledgedConfigMessage

A base protocol for acknowledged Configuration messages.

Acknowledged messages will be responded with a status message.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)\<`this`\> & [`StaticAcknowledgedMeshMessage`](#abstract-staticacknowledgedmeshmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new AcknowledgedConfigMessage(): AcknowledgedConfigMessage;
```

###### Returns

[`AcknowledgedConfigMessage`](#abstract-acknowledgedconfigmessage)

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).opCode
```

##### responseType

```ts
abstract responseType: Pick<typeof StaticMeshResponse, "fromData" | "opCode">;
```

The Type of the response message.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).responseType
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).parameters
```

##### responseOpCode

###### Get Signature

```ts
get responseOpCode(): number;
```

The Op Code of the response message.

###### Returns

`number`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).responseOpCode
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).security
```

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).encode
```

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).decode
```

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  StaticAcknowledgedMeshMessage,
).fromData
```

***

### `abstract` AcknowledgedMeshMessage

The base class for acknowledged messages.

An acknowledged message is transmitted and acknowledged by each
receiving element by responding to that message. The response is
typically a status message. If a response is not received within
an arbitrary time period, the message will be retransmitted
automatically until the timeout occurs.

Acknowledged messages are expected to be replied with a status message
with a message of type set as `AcknowledgedMeshMessage.responseOpCode`.

Access Layer timer will wait for
`NetworkParameters.acknowledgmentMessageTimeout` seconds
before throwing a timeout.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Constructors

##### Constructor

```ts
new AcknowledgedMeshMessage(): AcknowledgedMeshMessage;
```

###### Returns

[`AcknowledgedMeshMessage`](#abstract-acknowledgedmeshmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage).[`parameters`](#parameters-9)

##### responseOpCode

###### Get Signature

```ts
get abstract responseOpCode(): number;
```

The Op Code of the response message.

###### Returns

`number`

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### `abstract` AcknowledgedProxyConfigurationMessage

A base class for acknowledged proxy configuration messages.

An acknowledged message is transmitted and acknowledged by each
receiving element by responding to that message. The response is
typically a status message. If a response is not received within
an arbitrary time period, the message will be retransmitted
automatically until the timeout occurs.

#### Extends

- [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage)

#### Constructors

##### Constructor

```ts
new AcknowledgedProxyConfigurationMessage(): AcknowledgedProxyConfigurationMessage;
```

###### Returns

[`AcknowledgedProxyConfigurationMessage`](#abstract-acknowledgedproxyconfigurationmessage)

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`constructor`](#constructor-42)

#### Accessors

##### opCode

###### Get Signature

```ts
get abstract opCode(): number;
```

The message Op Code.

###### Returns

`number`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`opCode`](#opcode-27)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`parameters`](#parameters-15)

##### responseOpCode

###### Get Signature

```ts
get abstract responseOpCode(): number;
```

The Op Code of the response message.

###### Returns

`number`

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`fromData`](#fromdata-16)

***

### Address

Bluetooth Mesh address type. Type alias for `UInt16`.

In Bluetooth mesh addresses are divided into several categories:
- Unassigned Address - address 0x0000.
- Unicast Addresses - a unique address of an `Element`.
- Group Address - a group address allows sending messages to multiple receivers.
- Virtual Group Address - each virtual address is a hash of a Virtual Label (UUID).
- Fixed Group Addresses - set of predefined group addresses.

#### Extends

- `Number`

#### Constructors

##### Constructor

```ts
new Address(value: number): Address;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

[`Address`](#address)

###### Overrides

```ts
Number.constructor
```

#### Properties

##### allFriends

```ts
static allFriends: Address;
```

A message sent to the all-friends address will be processed by the
Primary Element of all nodes that have the friend functionality enabled.

That means, that Models on the Primary Element of all the Nodes are
automatically subscribed to all-friends address if the Node has
Friend functionality enabled. Models on the Primary and other Elements
of a Node may subscribe to this address to receive messages no matter
what the feature state is.

##### allNodes

```ts
static allNodes: Address;
```

A message sent to the all-nodes address will be processed by the
Primary Element of all nodes.

That means, that all Models on the Primary Element of all the Nodes
are automatically subscribed to all-nodes address. It is not possible
for Models on other Elements to receive messages sent to All Nodes address,
as they cannot subscribe to this address.

##### allProxies

```ts
static allProxies: Address;
```

A message sent to the all-proxies address will be processed by the
Primary Element of all nodes that have the friend functionality enabled.

That means, that Models on the Primary Element of all the Nodes are
automatically subscribed to all-proxies address if the Node has
Proxy functionality enabled. Models on the Primary and other Elements
of a Node may subscribe to this address to receive messages no matter
what the feature state is.

##### allRelays

```ts
static allRelays: Address;
```

A message sent to the all-relays address will be processed by the
Primary Element of all nodes that have the relay functionality enabled.

That means, that Models on the Primary Element of all the Nodes are
automatically subscribed to all-relays address if the Node has
Relay functionality enabled. Models on the Primary and other Elements
of a Node may subscribe to this address to receive messages no matter
what the feature state is.

##### maxGroupAddress

```ts
static maxGroupAddress: Address;
```

##### maxUnicastAddress

```ts
static maxUnicastAddress: Address;
```

##### maxVirtualAddress

```ts
static maxVirtualAddress: Address;
```

##### minGroupAddress

```ts
static minGroupAddress: Address;
```

##### minUnicastAddress

```ts
static minUnicastAddress: Address;
```

##### minVirtualAddress

```ts
static minVirtualAddress: Address;
```

##### unassignedAddress

```ts
static unassignedAddress: Address;
```

#### Accessors

##### bytes

###### Get Signature

```ts
get bytes(): Uint8Array;
```

###### Returns

`Uint8Array`

##### bytesBE

###### Get Signature

```ts
get bytesBE(): Uint8Array;
```

###### Returns

`Uint8Array`

##### dec

###### Get Signature

```ts
get dec(): number;
```

###### Returns

`number`

##### hex

###### Get Signature

```ts
get hex(): string;
```

###### Returns

`string`

##### isGroup

###### Get Signature

```ts
get isGroup(): boolean;
```

Returns `true` if the address is a Group Address.
Group addresses match 0b11xxxxxxxxxxxxxx.

###### Returns

`boolean`

##### isSpecialGroup

###### Get Signature

```ts
get isSpecialGroup(): boolean;
```

Returns `true` if the address is a special Group Address.

Special groups are:
* All Proxies: 0xFFFC
* All Friends: 0xFFFD
* All Relays: 0xFFFE
* All Nodes: 0xFFFF

###### Returns

`boolean`

##### isUnassigned

###### Get Signature

```ts
get isUnassigned(): boolean;
```

Returns `true` if the address is an Unassigned Address.
Unassigned addresses is equal to 0b0000000000000000.

###### Returns

`boolean`

##### isUnicast

###### Get Signature

```ts
get isUnicast(): boolean;
```

Returns `true` if the address is an Unicast Address.
Unicast addresses match 0b00xxxxxxxxxxxxxx (except 0b0000000000000000).

###### Returns

`boolean`

##### isValidAddress

###### Get Signature

```ts
get isValidAddress(): boolean;
```

Returns `true` if the address is from a valid range.

###### Returns

`boolean`

##### isVirtual

###### Get Signature

```ts
get isVirtual(): boolean;
```

Returns `true` if the address is a Virtual Address.
Virtual addresses match 0b10xxxxxxxxxxxxxx.

###### Returns

`boolean`

#### Methods

##### equal()

```ts
equal(other: Address): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`Address`](#address) |

###### Returns

`boolean`

##### fromHex()

```ts
static fromHex(hex: string): Address | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

###### Returns

[`Address`](#address) \| `undefined`

***

### `abstract` AttentionTimerHandler

An Attention Timer Handler is used to notify the app about the Attention Timer state.

The Attention Timer is used to attract the user's attention to the device.

It may only be started and stopped by sending a `HealthAttentionSet` or
`HealthAttentionSetUnacknowledged` message to the main Element
of the local Node.

#### Constructors

##### Constructor

```ts
new AttentionTimerHandler(): AttentionTimerHandler;
```

###### Returns

[`AttentionTimerHandler`](#abstract-attentiontimerhandler)

#### Methods

##### attentionTimerDidStart()

```ts
abstract attentionTimerDidStart(duration: number): void;
```

A callback called when the Attention Timer state has been started.

The app should start attracting the user's attention.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `duration` | `number` | The time after which the Attention Timer will time out, in range 1-255 seconds. |

###### Returns

`void`

##### attentionTimerDidStop()

```ts
abstract attentionTimerDidStop(): void;
```

A callback called when the Attention Timer state has been stopped.

This callback is called when the Attention Timer times out, or is stopped
remotely by a remote user.

###### Returns

`void`

***

### BackgroundTimer

#### Constructors

##### Constructor

```ts
new BackgroundTimer(
   interval: number, 
   repeats: boolean, 
   callback: TimerCallback, 
   queue?: DispatchQueue
): BackgroundTimer;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `interval` | `number` | `undefined` |
| `repeats` | `boolean` | `undefined` |
| `callback` | `TimerCallback` | `undefined` |
| `queue` | [`DispatchQueue`](#dispatchqueue) | `DispatchQueue.background` |

###### Returns

[`BackgroundTimer`](#backgroundtimer)

#### Properties

##### interval

```ts
readonly interval: number;
```

##### repeats

```ts
readonly repeats: boolean;
```

#### Accessors

##### remainingTime

###### Get Signature

```ts
get remainingTime(): number;
```

###### Returns

`number`

#### Methods

##### invalidate()

```ts
invalidate(): void;
```

###### Returns

`void`

##### scheduledTimer()

```ts
static scheduledTimer(
   interval: number, 
   repeats: boolean, 
   callback: TimerCallback, 
   queue?: DispatchQueue
): BackgroundTimer;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `interval` | `number` | `undefined` |
| `repeats` | `boolean` | `undefined` |
| `callback` | `TimerCallback` | `undefined` |
| `queue` | [`DispatchQueue`](#dispatchqueue) | `DispatchQueue.background` |

###### Returns

[`BackgroundTimer`](#backgroundtimer)

***

### `abstract` BaseMeshMessage

The base interface of every mesh message. Mesh messages can be sent to and
received from a mesh network.

#### Extended by

- [`MeshMessage`](#abstract-meshmessage)
- [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage)

#### Constructors

##### Constructor

```ts
new BaseMeshMessage(): BaseMeshMessage;
```

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage)

#### Accessors

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

***

### BigUInt

#### Constructors

##### Constructor

```ts
new BigUInt(decimalString: string): BigUInt;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `decimalString` | `string` |

###### Returns

[`BigUInt`](#biguint)

#### Properties

##### maxBytes

```ts
readonly static maxBytes: 14 = 14;
```

##### maxDecimalDigits

```ts
readonly static maxDecimalDigits: 32 = 32;
```

#### Methods

##### toBytes()

```ts
toBytes(sizeInBytes: number): Uint8Array<ArrayBufferLike> | undefined;
```

Big-endian bytes

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `sizeInBytes` | `number` |

###### Returns

`Uint8Array`\<`ArrayBufferLike`\> \| `undefined`

##### toDecimalString()

```ts
toDecimalString(): string;
```

###### Returns

`string`

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### random()

```ts
static random(length: number): BigUInt | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `length` | `number` |

###### Returns

[`BigUInt`](#biguint) \| `undefined`

***

### BindableTinyEmitter

#### Extended by

- [`CBPeripheral`](#abstract-cbperipheral)
- [`CBCentralManager`](#abstract-cbcentralmanager)

#### Type Parameters

| Type Parameter |
| ------ |
| `E` *extends* `{ [K in keyof E]: (args: any[]) => void }` |

#### Constructors

##### Constructor

```ts
new BindableTinyEmitter<E extends { [K in string | number | symbol]: (args: any[]) => void }>(): BindableTinyEmitter<E>;
```

###### Returns

[`BindableTinyEmitter`](#bindabletinyemitter)\<`E`\>

#### Methods

##### bindAllEvents()

```ts
bindAllEvents(handler: Partial<E>): () => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

() => `void`

##### emit()

```ts
emit<K extends string | number | symbol>(event: K, ...args: Parameters<E[K]>): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* `string` \| `number` \| `symbol` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| ...`args` | `Parameters`\<`E`\[`K`\]\> |

###### Returns

`void`

##### off()

```ts
off<K extends string | number | symbol>(event: K, fn?: E[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* `string` \| `number` \| `symbol` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn?` | `E`\[`K`\] |

###### Returns

`void`

##### on()

```ts
on<K extends string | number | symbol>(event: K, fn: E[K]): () => void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* `string` \| `number` \| `symbol` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | `E`\[`K`\] |

###### Returns

() => `void`

##### once()

```ts
once<K extends string | number | symbol>(event: K, fn: E[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* `string` \| `number` \| `symbol` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | `E`\[`K`\] |

###### Returns

`void`

##### unbindAllEvents()

```ts
unbindAllEvents(handler: Partial<E>): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

`void`

***

### BleError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new BleError(code: number, message: string): BleError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `code` | `number` |
| `message` | `string` |

###### Returns

[`BleError`](#bleerror)

###### Overrides

```ts
Error.constructor
```

#### Properties

##### code

```ts
code: number;
```

***

### CancellationError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new CancellationError(message?: string): CancellationError;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `message` | `string` | `"Operation was cancelled"` |

###### Returns

[`CancellationError`](#cancellationerror)

###### Overrides

```ts
Error.constructor
```

***

### `abstract` CBCentralManager

Abstract class representing a Bluetooth Central Manager.
Subclasses must implement scanning, connection, and state management logic.

#### Extends

- [`BindableTinyEmitter`](#bindabletinyemitter)\<[`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)\>

#### Constructors

##### Constructor

```ts
new CBCentralManager(): CBCentralManager;
```

###### Returns

[`CBCentralManager`](#abstract-cbcentralmanager)

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`constructor`](#constructor-8)

#### Properties

##### state

```ts
abstract state: CBCentralManagerState;
```

#### Methods

##### bindAllEvents()

```ts
bindAllEvents(handler: Partial<E>): () => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

() => `void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`bindAllEvents`](#bindallevents)

##### cancelPeripheralConnection()

```ts
abstract cancelPeripheralConnection(peripheral: CBPeripheral): Promise<void>;
```

Cancels an active or pending connection to a peripheral.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The peripheral to disconnect from. |

###### Returns

`Promise`\<`void`\>

##### connect()

```ts
abstract connect(peripheral: CBPeripheral): Promise<void>;
```

Attempts to connect to the specified peripheral.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The peripheral to connect to. |

###### Returns

`Promise`\<`void`\>

##### emit()

```ts
emit<K extends keyof CBCentralManagerHandler>(event: K, ...args: Parameters<CBCentralManagerHandler[K]>): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| ...`args` | `Parameters`\<[`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)\[`K`\]\> |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`emit`](#emit)

##### getState()

```ts
abstract getState(): CBCentralManagerState;
```

Returns the current Bluetooth state of the central manager.

###### Returns

[`CBCentralManagerState`](#cbcentralmanagerstate)

##### off()

```ts
off<K extends keyof CBCentralManagerHandler>(event: K, fn?: CBCentralManagerHandler[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn?` | [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)\[`K`\] |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`off`](#off)

##### on()

```ts
on<K extends keyof CBCentralManagerHandler>(event: K, fn: CBCentralManagerHandler[K]): () => void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)\[`K`\] |

###### Returns

() => `void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`on`](#on)

##### once()

```ts
once<K extends keyof CBCentralManagerHandler>(event: K, fn: CBCentralManagerHandler[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | [`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)\[`K`\] |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`once`](#once)

##### retrieveConnectedPeripherals()

```ts
abstract retrieveConnectedPeripherals(serviceUUIDs?: string[]): CBPeripheral[];
```

Retrieves peripherals that are already connected and match the specified services.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `serviceUUIDs?` | `string`[] | Optional service UUIDs to filter connected peripherals. |

###### Returns

[`CBPeripheral`](#abstract-cbperipheral)[]

##### retrievePeripherals()

```ts
abstract retrievePeripherals(identifiers: UUID[]): CBPeripheral[];
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `identifiers` | [`UUID`](#uuid-6)[] |

###### Returns

[`CBPeripheral`](#abstract-cbperipheral)[]

##### scanForPeripherals()

```ts
abstract scanForPeripherals(serviceUUIDs?: string[]): Promise<void>;
```

Starts scanning for peripherals.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `serviceUUIDs?` | `string`[] | Optional list of service UUIDs to filter results. |

###### Returns

`Promise`\<`void`\>

##### stopScan()

```ts
abstract stopScan(): Promise<void>;
```

Stops any ongoing scan for peripherals.

###### Returns

`Promise`\<`void`\>

##### unbindAllEvents()

```ts
unbindAllEvents(handler: Partial<E>): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`unbindAllEvents`](#unbindallevents)

***

### `abstract` CBCentralManagerHandler

Abstract base class representing the handler of a central Bluetooth manager.
Use this class to respond to central manager events such as state updates and peripheral discovery.

#### Constructors

##### Constructor

```ts
new CBCentralManagerHandler(): CBCentralManagerHandler;
```

###### Returns

[`CBCentralManagerHandler`](#abstract-cbcentralmanagerhandler)

#### Methods

##### centralManagerDidDisconnectPeripheral()

```ts
abstract centralManagerDidDisconnectPeripheral(
   central: CBCentralManager, 
   peripheral: CBPeripheral, 
   error?: Error
): void;
```

Called when a peripheral is disconnected.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `central` | [`CBCentralManager`](#abstract-cbcentralmanager) | The central manager instance. |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The disconnected peripheral. |
| `error?` | `Error` | Optional error if the disconnection was unexpected. |

###### Returns

`void`

##### centralManagerDidDiscoverPeripheral()

```ts
abstract centralManagerDidDiscoverPeripheral(
   central: CBCentralManager, 
   peripheral: CBPeripheral, 
   rssi?: number, 
   advertisementData?: Record<string, unknown>
): void;
```

Called when a peripheral is discovered.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `central` | [`CBCentralManager`](#abstract-cbcentralmanager) | The central manager instance. |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The discovered peripheral. |
| `rssi?` | `number` | Optional received signal strength indicator (RSSI) of the peripheral. |
| `advertisementData?` | `Record`\<`string`, `unknown`\> | Optional advertisement data from the peripheral. |

###### Returns

`void`

##### centralManagerDidUpdateState()

```ts
abstract centralManagerDidUpdateState(central: CBCentralManager, state: CBCentralManagerState): void;
```

Called when the central manager's state is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `central` | [`CBCentralManager`](#abstract-cbcentralmanager) | The central manager instance. |
| `state` | [`CBCentralManagerState`](#cbcentralmanagerstate) | A string representing the new state (e.g., 'poweredOn', 'poweredOff', etc.). |

###### Returns

`void`

***

### `abstract` CBCharacteristic

#### Constructors

##### Constructor

```ts
new CBCharacteristic(): CBCharacteristic;
```

###### Returns

[`CBCharacteristic`](#abstract-cbcharacteristic)

#### Properties

##### isNotifying

```ts
abstract isNotifying: boolean;
```

If true, the characteristic is currently notifying (i.e. streaming updates)

##### properties

```ts
abstract properties: CBCharacteristicProperties[];
```

Characteristic properties (e.g., read, write, notify)

##### serviceUUID

```ts
abstract serviceUUID: CBUUID;
```

UUID of the service this characteristic belongs to

##### uuid

```ts
abstract uuid: CBUUID;
```

THe UUID of the characteristic.

##### value?

```ts
abstract optional value?: Data;
```

Last known value, if read or notified

#### Accessors

##### isMeshProvisioningDataInCharacteristic

###### Get Signature

```ts
get isMeshProvisioningDataInCharacteristic(): boolean;
```

Whether the characteristic UUID matches the Data In characteristic UUID.

###### Returns

`boolean`

##### isMeshProvisioningDataOutCharacteristic

###### Get Signature

```ts
get isMeshProvisioningDataOutCharacteristic(): boolean;
```

Whether the characteristic UUID matches the Data Out characteristic UUID.

###### Returns

`boolean`

##### isMeshProxyDataInCharacteristic

###### Get Signature

```ts
get isMeshProxyDataInCharacteristic(): boolean;
```

Whether the characteristic UUID matches the Data In characteristic UUID.

###### Returns

`boolean`

##### isMeshProxyDataOutCharacteristic

###### Get Signature

```ts
get isMeshProxyDataOutCharacteristic(): boolean;
```

Whether the characteristic UUID matches the Data Out characteristic UUID.

###### Returns

`boolean`

#### Methods

##### equal()

```ts
equal(other: unknown): boolean;
```

Is the characteristic equal to another?

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `unknown` |

###### Returns

`boolean`

***

### `abstract` CBPeripheral

#### Extends

- [`BindableTinyEmitter`](#bindabletinyemitter)\<[`CBPeripheralHandler`](#abstract-cbperipheralhandler)\>

#### Constructors

##### Constructor

```ts
new CBPeripheral(): CBPeripheral;
```

###### Returns

[`CBPeripheral`](#abstract-cbperipheral)

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`constructor`](#constructor-8)

#### Properties

##### advertisementData?

```ts
abstract optional advertisementData?: Record<string, unknown>;
```

Optional manufacturer data or device metadata

##### identifier

```ts
abstract identifier: UUID;
```

A unique identifier of the peripheral

##### name?

```ts
abstract optional name?: string;
```

Display name (optional, may be null or undefined if not advertising)

##### rssi?

```ts
abstract optional rssi?: number;
```

Signal strength in dBm

##### services?

```ts
abstract optional services?: CBService[];
```

A list of services provided by the peripheral

##### state

```ts
abstract state: CBPeripheralState;
```

Current connection state

#### Methods

##### bindAllEvents()

```ts
bindAllEvents(handler: Partial<E>): () => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

() => `void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`bindAllEvents`](#bindallevents)

##### discoverCharacteristics()

```ts
abstract discoverCharacteristics(characteristicUUIDs: CBUUID[], service: CBService): Promise<CBCharacteristic[]>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `characteristicUUIDs` | [`CBUUID`](#cbuuid)[] |
| `service` | [`CBService`](#abstract-cbservice) |

###### Returns

`Promise`\<[`CBCharacteristic`](#abstract-cbcharacteristic)[]\>

##### discoverServices()

```ts
abstract discoverServices(serviceUUIDs: CBUUID[]): Promise<CBService[]>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `serviceUUIDs` | [`CBUUID`](#cbuuid)[] |

###### Returns

`Promise`\<[`CBService`](#abstract-cbservice)[]\>

##### emit()

```ts
emit<K extends keyof CBPeripheralHandler>(event: K, ...args: Parameters<CBPeripheralHandler[K]>): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBPeripheralHandler`](#abstract-cbperipheralhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| ...`args` | `Parameters`\<[`CBPeripheralHandler`](#abstract-cbperipheralhandler)\[`K`\]\> |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`emit`](#emit)

##### equal()

```ts
equal(other: unknown): boolean;
```

Is peripheral equal to other peripheral?

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `unknown` |

###### Returns

`boolean`

##### maximumWriteValueLength()

```ts
abstract maximumWriteValueLength(type: CBCharacteristicWriteType): Long;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `type` | [`CBCharacteristicWriteType`](#cbcharacteristicwritetype) |

###### Returns

`Long`

##### off()

```ts
off<K extends keyof CBPeripheralHandler>(event: K, fn?: CBPeripheralHandler[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBPeripheralHandler`](#abstract-cbperipheralhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn?` | [`CBPeripheralHandler`](#abstract-cbperipheralhandler)\[`K`\] |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`off`](#off)

##### on()

```ts
on<K extends keyof CBPeripheralHandler>(event: K, fn: CBPeripheralHandler[K]): () => void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBPeripheralHandler`](#abstract-cbperipheralhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | [`CBPeripheralHandler`](#abstract-cbperipheralhandler)\[`K`\] |

###### Returns

() => `void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`on`](#on)

##### once()

```ts
once<K extends keyof CBPeripheralHandler>(event: K, fn: CBPeripheralHandler[K]): void;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `K` *extends* keyof [`CBPeripheralHandler`](#abstract-cbperipheralhandler) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `event` | `K` |
| `fn` | [`CBPeripheralHandler`](#abstract-cbperipheralhandler)\[`K`\] |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`once`](#once)

##### readRSSI()

```ts
abstract readRSSI(): Promise<number>;
```

###### Returns

`Promise`\<`number`\>

##### setNotifyValue()

```ts
abstract setNotifyValue(enabled: boolean, characteristic: CBCharacteristic): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `enabled` | `boolean` |
| `characteristic` | [`CBCharacteristic`](#abstract-cbcharacteristic) |

###### Returns

`Promise`\<`void`\>

##### unbindAllEvents()

```ts
unbindAllEvents(handler: Partial<E>): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handler` | `Partial`\<`E`\> |

###### Returns

`void`

###### Inherited from

[`BindableTinyEmitter`](#bindabletinyemitter).[`unbindAllEvents`](#unbindallevents)

##### writeValue()

```ts
abstract writeValue(
   data: Data, 
   characteristic: CBCharacteristic, 
   type: CBCharacteristicWriteType
): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | [`Data`](#data) |
| `characteristic` | [`CBCharacteristic`](#abstract-cbcharacteristic) |
| `type` | [`CBCharacteristicWriteType`](#cbcharacteristicwritetype) |

###### Returns

`Promise`\<`void`\>

***

### `abstract` CBPeripheralHandler

Abstract base class representing the handler of a Bluetooth peripheral.
Use this class to respond to events such as service discovery and characteristic updates.

#### Constructors

##### Constructor

```ts
new CBPeripheralHandler(): CBPeripheralHandler;
```

###### Returns

[`CBPeripheralHandler`](#abstract-cbperipheralhandler)

#### Methods

##### didDisconnect()

```ts
abstract didDisconnect(peripheral: CBPeripheral): void;
```

Called when the peripheral disconnects.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The peripheral. |

###### Returns

`void`

##### didUpdateState()

```ts
abstract didUpdateState(peripheral: CBPeripheral): void;
```

Called when the peripheral connection state changes.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The peripheral. |

###### Returns

`void`

##### didUpdateValueForCharacteristic()

```ts
abstract didUpdateValueForCharacteristic(peripheral: CBPeripheral, characteristic: CBCharacteristic): void;
```

Called when a characteristic's value is updated.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `peripheral` | [`CBPeripheral`](#abstract-cbperipheral) | The peripheral containing the characteristic. |
| `characteristic` | [`CBCharacteristic`](#abstract-cbcharacteristic) | The characteristic whose value was updated. |

###### Returns

`void`

***

### `abstract` CBService

#### Constructors

##### Constructor

```ts
new CBService(): CBService;
```

###### Returns

[`CBService`](#abstract-cbservice)

#### Properties

##### characteristics

```ts
abstract characteristics: CBCharacteristic[];
```

##### peripheral

```ts
abstract peripheral: CBPeripheral;
```

##### uuid

```ts
abstract uuid: CBUUID;
```

The UUID of the service.

#### Accessors

##### isMeshProvisioningService

###### Get Signature

```ts
get isMeshProvisioningService(): boolean;
```

Whether the service UUID matches Mesh Provisioning Service UUID.

###### Returns

`boolean`

##### isMeshProxyService

###### Get Signature

```ts
get isMeshProxyService(): boolean;
```

Whether the service UUID matches Mesh Proxy Service UUID.

###### Returns

`boolean`

***

### CBUUID

Represents a Core Bluetooth UUID.
Can be constructed from a string, number, or Uint8Array.
Provides utility methods for comparison and string representation.

#### Constructors

##### Constructor

```ts
new CBUUID(value: string | Uint8Array<ArrayBufferLike>): CBUUID;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `string` \| `Uint8Array`\<`ArrayBufferLike`\> |

###### Returns

[`CBUUID`](#cbuuid)

#### Accessors

##### fullUuidString

###### Get Signature

```ts
get fullUuidString(): string;
```

###### Returns

`string`

##### shortUuid

###### Get Signature

```ts
get shortUuid(): string;
```

###### Returns

`string`

##### uuid

###### Get Signature

```ts
get uuid(): UUID;
```

Converts the CBUUID to foundation UUID.

###### Returns

[`UUID`](#uuid-6)

##### uuidString

###### Get Signature

```ts
get uuidString(): string;
```

###### Returns

`string`

#### Methods

##### equals()

```ts
equals(other: CBUUID): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`CBUUID`](#cbuuid) |

###### Returns

`boolean`

##### toBytes()

```ts
toBytes(): Uint8Array;
```

###### Returns

`Uint8Array`

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

***

### ClosedRange

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Constructors

##### Constructor

```ts
new ClosedRange<T>(
   lower: T, 
   upper: T, 
   comparator?: (a: T, b: T) => number
): ClosedRange<T>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `lower` | `T` |
| `upper` | `T` |
| `comparator?` | (`a`: `T`, `b`: `T`) => `number` |

###### Returns

[`ClosedRange`](#closedrange)\<`T`\>

#### Properties

##### lowerBound

```ts
readonly lowerBound: T;
```

##### upperBound

```ts
readonly upperBound: T;
```

#### Accessors

##### length

###### Get Signature

```ts
get length(): Long | undefined;
```

Length of range: (upper - lower + 1) for number types

###### Returns

`Long` \| `undefined`

#### Methods

##### contains()

```ts
contains(value: T): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `T` |

###### Returns

`boolean`

##### forEach()

```ts
forEach(callback: (value: T) => void): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `callback` | (`value`: `T`) => `void` |

###### Returns

`void`

##### overlaps()

```ts
overlaps(other: ClosedRange<T>): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`ClosedRange`](#closedrange)\<`T`\> |

###### Returns

`boolean`

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

***

### `abstract` ConfigAnyModelMessage

A base protocol for config messages related to Models, where the Model can be
a vendor model.

#### Extends

- [`ConfigModelMessage`](#abstract-configmodelmessage)

#### Constructors

##### Constructor

```ts
new ConfigAnyModelMessage(): ConfigAnyModelMessage;
```

###### Returns

[`ConfigAnyModelMessage`](#abstract-configanymodelmessage)

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`constructor`](#constructor-23)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`opCode`](#opcode-13)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`opCode`](#opcode-14)

#### Accessors

##### companyIdentifier

###### Get Signature

```ts
get abstract companyIdentifier(): number | undefined;
```

The Company identified, as defined in Assigned Numbers, or `undefined`
if the Model is defined in Bluetooth Mesh Model Specification.

###### See

https://www.bluetooth.com/specifications/assigned-numbers/company-identifiers/

###### Returns

`number` \| `undefined`

##### elementAddress

###### Get Signature

```ts
get abstract elementAddress(): Address;
```

The Unicast Address of the Model's parent Element.

###### Returns

[`Address`](#address)

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`elementAddress`](#elementaddress-2)

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`isAcknowledged`](#isacknowledged-6)

##### isBluetoothSIGAssigned

###### Get Signature

```ts
get isBluetoothSIGAssigned(): boolean;
```

Returns `true` for Models with identifiers assigned by Bluetooth SIG,
`false` otherwise.

###### Returns

`boolean`

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`isSegmented`](#issegmented-6)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`isVendorMessage`](#isvendormessage-6)

##### modelId

###### Get Signature

```ts
get modelId(): number;
```

The 32-bit Model identifier.

###### Returns

`number`

###### Overrides

[`ConfigModelMessage`](#abstract-configmodelmessage).[`modelId`](#modelid-1)

##### modelIdentifier

###### Get Signature

```ts
get abstract modelIdentifier(): number;
```

The 16-bit Model identifier.

###### Returns

`number`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`modelIdentifier`](#modelidentifier-1)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`parameters`](#parameters-8)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`security`](#security-6)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`encode`](#encode-5)

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`decode`](#decode-5)

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ConfigModelMessage`](#abstract-configmodelmessage).[`fromData`](#fromdata-8)

***

### `abstract` ConfigAppKeyMessage

A base protocol for config messages related to Application Keys.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)

#### Constructors

##### Constructor

```ts
new ConfigAppKeyMessage(): ConfigAppKeyMessage;
```

###### Returns

[`ConfigAppKeyMessage`](#abstract-configappkeymessage)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`constructor`](#constructor-22)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-11)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-12)

#### Accessors

##### applicationKeyIndex

###### Get Signature

```ts
get abstract applicationKeyIndex(): KeyIndex;
```

Application Key Index.

###### Returns

[`KeyIndex`](#keyindex-1)

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isAcknowledged`](#isacknowledged-5)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isSegmented`](#issegmented-5)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isVendorMessage`](#isvendormessage-5)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`parameters`](#parameters-7)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`security`](#security-5)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`encode`](#encode-4)

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`decode`](#decode-4)

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`fromData`](#fromdata-7)

***

### `abstract` ConfigElementMessage

A base protocol for config messages related to Elements.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)

#### Extended by

- [`ConfigModelMessage`](#abstract-configmodelmessage)

#### Constructors

##### Constructor

```ts
new ConfigElementMessage(): ConfigElementMessage;
```

###### Returns

[`ConfigElementMessage`](#abstract-configelementmessage)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`constructor`](#constructor-22)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-11)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-12)

#### Accessors

##### elementAddress

###### Get Signature

```ts
get abstract elementAddress(): Address;
```

The Unicast Address of the Model's parent Element.

###### Returns

[`Address`](#address)

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isAcknowledged`](#isacknowledged-5)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isSegmented`](#issegmented-5)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isVendorMessage`](#isvendormessage-5)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`parameters`](#parameters-7)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`security`](#security-5)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`encode`](#encode-4)

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`decode`](#decode-4)

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`fromData`](#fromdata-7)

***

### `abstract` ConfigMessage

A base protocol for all Configuration messages.

Configuration messages are used to configure Nodes. They are sent between
Configuration Client model on the Configuration Manager and Configuration Server
model on the device, which is being configured. All Config messages are encrypted
using target Node's Device Key.

#### Extends

- [`StaticMeshMessage`](#abstract-staticmeshmessage)

#### Extended by

- [`ConfigElementMessage`](#abstract-configelementmessage)
- [`ConfigAppKeyMessage`](#abstract-configappkeymessage)
- [`ConfigNetKeyMessage`](#abstract-confignetkeymessage)

#### Constructors

##### Constructor

```ts
new ConfigMessage(): ConfigMessage;
```

###### Returns

[`ConfigMessage`](#abstract-configmessage)

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`constructor`](#constructor-48)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`opCode`](#opcode-32)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`opCode`](#opcode-33)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`isAcknowledged`](#isacknowledged-14)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`isSegmented`](#issegmented-14)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`isVendorMessage`](#isvendormessage-14)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`parameters`](#parameters-18)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`security`](#security-14)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`StaticMeshMessage`](#abstract-staticmeshmessage).[`fromData`](#fromdata-19)

***

### `abstract` ConfigModelMessage

A base protocol for config messages related to Models.

#### Extends

- [`ConfigElementMessage`](#abstract-configelementmessage)

#### Extended by

- [`ConfigAnyModelMessage`](#abstract-configanymodelmessage)

#### Constructors

##### Constructor

```ts
new ConfigModelMessage(): ConfigModelMessage;
```

###### Returns

[`ConfigModelMessage`](#abstract-configmodelmessage)

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`constructor`](#constructor-21)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`opCode`](#opcode-9)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`opCode`](#opcode-10)

#### Accessors

##### elementAddress

###### Get Signature

```ts
get abstract elementAddress(): Address;
```

The Unicast Address of the Model's parent Element.

###### Returns

[`Address`](#address)

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`elementAddress`](#elementaddress-1)

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`isAcknowledged`](#isacknowledged-4)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`isSegmented`](#issegmented-4)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`isVendorMessage`](#isvendormessage-4)

##### modelId

###### Get Signature

```ts
get abstract modelId(): number;
```

The 32-bit Model identifier.

###### Returns

`number`

##### modelIdentifier

###### Get Signature

```ts
get abstract modelIdentifier(): number;
```

The 16-bit Model identifier.

###### Returns

`number`

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`parameters`](#parameters-6)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`security`](#security-4)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`encode`](#encode-3)

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`decode`](#decode-3)

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ConfigElementMessage`](#abstract-configelementmessage).[`fromData`](#fromdata-6)

***

### `abstract` ConfigNetAndAppKeyMessage

A base protocol for config messages related to Network Key and Application Key.

#### Extends

- [`ConfigNetKeyMessage`](#abstract-confignetkeymessage)\<`this`\> & [`ConfigAppKeyMessage`](#abstract-configappkeymessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigNetAndAppKeyMessage(): ConfigNetAndAppKeyMessage;
```

###### Returns

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage)

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).opCode
```

#### Accessors

##### applicationKeyIndex

###### Get Signature

```ts
get abstract applicationKeyIndex(): KeyIndex;
```

Application Key Index.

###### Returns

[`KeyIndex`](#keyindex-1)

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).applicationKeyIndex
```

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).isVendorMessage
```

##### networkKeyIndex

###### Get Signature

```ts
get abstract networkKeyIndex(): KeyIndex;
```

The Network Key Index.

###### Returns

[`KeyIndex`](#keyindex-1)

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).networkKeyIndex
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).security
```

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).encode
```

##### encodeNetAndAppKeyIndex()

```ts
encodeNetAndAppKeyIndex(): Data;
```

Encodes Network Key Index and Application Key Index in 3 bytes
using Little Endian.

###### Returns

[`Data`](#data)

Key Indexes encoded in 3 bytes.

##### encodeNetKeyIndex()

```ts
encodeNetKeyIndex(): Data;
```

Encodes Network Key Index in 2 bytes using Little Endian.

###### Returns

[`Data`](#data)

Key Index encoded in 2 bytes.

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).encodeNetKeyIndex
```

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).decode
```

##### decodeNetAndAppKeyIndex()

```ts
static decodeNetAndAppKeyIndex(data: Data, offset: number): {
  applicationKeyIndex: KeyIndex;
  networkKeyIndex: KeyIndex;
};
```

Decodes the Network Key Index and Application Key Index from
3 bytes at given offset.

There are no any checks whether the data at the given offset
are valid, or even if the offset is not outside of the data range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | [`Data`](#data) | The data from where the indexes should be read. |
| `offset` | `number` | The offset from where to read the indexes. |

###### Returns

```ts
{
  applicationKeyIndex: KeyIndex;
  networkKeyIndex: KeyIndex;
}
```

Decoded Key Indexes.

###### applicationKeyIndex

```ts
applicationKeyIndex: KeyIndex;
```

###### networkKeyIndex

```ts
networkKeyIndex: KeyIndex;
```

##### decodeNetKeyIndex()

```ts
static decodeNetKeyIndex(data: Data, offset: number): KeyIndex;
```

the Network Key Index from 2 bytes at given offset.

There are no any checks whether the data at the given offset
are valid, or even if the offset is not outside of the data range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | [`Data`](#data) | The data from where the indexes should be read. |
| `offset` | `number` | The offset from where to read the indexes. |

###### Returns

[`KeyIndex`](#keyindex-1)

Decoded Key Index.

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).decodeNetKeyIndex
```

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigNetKeyMessage,
  ConfigAppKeyMessage,
).fromData
```

***

### `abstract` ConfigNetKeyMessage

A base protocol for config messages related to Network Keys.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)

#### Constructors

##### Constructor

```ts
new ConfigNetKeyMessage(): ConfigNetKeyMessage;
```

###### Returns

[`ConfigNetKeyMessage`](#abstract-confignetkeymessage)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`constructor`](#constructor-22)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-11)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`opCode`](#opcode-12)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isAcknowledged`](#isacknowledged-5)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isSegmented`](#issegmented-5)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`isVendorMessage`](#isvendormessage-5)

##### networkKeyIndex

###### Get Signature

```ts
get abstract networkKeyIndex(): KeyIndex;
```

The Network Key Index.

###### Returns

[`KeyIndex`](#keyindex-1)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`parameters`](#parameters-7)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`security`](#security-5)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`encode`](#encode-4)

##### encodeNetKeyIndex()

```ts
encodeNetKeyIndex(): Data;
```

Encodes Network Key Index in 2 bytes using Little Endian.

###### Returns

[`Data`](#data)

Key Index encoded in 2 bytes.

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`decode`](#decode-4)

##### decodeNetKeyIndex()

```ts
static decodeNetKeyIndex(data: Data, offset: number): KeyIndex;
```

the Network Key Index from 2 bytes at given offset.

There are no any checks whether the data at the given offset
are valid, or even if the offset is not outside of the data range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `data` | [`Data`](#data) | The data from where the indexes should be read. |
| `offset` | `number` | The offset from where to read the indexes. |

###### Returns

[`KeyIndex`](#keyindex-1)

Decoded Key Index.

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ConfigMessage`](#abstract-configmessage).[`fromData`](#fromdata-7)

***

### `abstract` ConfigResponse

The base class for response messages.

#### Extends

- [`StaticMeshResponse`](#abstract-staticmeshresponse)\<`this`\> & [`UnacknowledgedConfigMessage`](#abstract-unacknowledgedconfigmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigResponse(): ConfigResponse;
```

###### Returns

[`ConfigResponse`](#abstract-configresponse)

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).security
```

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).encode
```

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).decode
```

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshResponse,
  UnacknowledgedConfigMessage,
).fromData
```

***

### `abstract` ConfigStatusMessage

A base protocol for config status messages.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)\<`this`\> & [`StatusMessage`](#abstract-statusmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new ConfigStatusMessage(): ConfigStatusMessage;
```

###### Returns

[`ConfigStatusMessage`](#abstract-configstatusmessage)

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).isSegmented
```

##### isSuccess

###### Get Signature

```ts
get isSuccess(): boolean;
```

Whether the operation was successful or not.

###### Returns

`boolean`

###### Overrides

```ts
Mixin(ConfigMessage, StatusMessage).isSuccess
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).isVendorMessage
```

##### message

###### Get Signature

```ts
get message(): string;
```

String representation of the status.

###### Returns

`string`

###### Overrides

```ts
Mixin(ConfigMessage, StatusMessage).message
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).security
```

##### status

###### Get Signature

```ts
get abstract status(): ConfigMessageStatus;
```

Operation status.

###### Returns

[`ConfigMessageStatus`](#configmessagestatus)

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).encode
```

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).decode
```

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(ConfigMessage, StatusMessage).fromData
```

***

### DecodingError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new DecodingError(message: string): DecodingError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

###### Returns

[`DecodingError`](#decodingerror)

###### Overrides

```ts
Error.constructor
```

***

### DispatchQueue

#### Constructors

##### Constructor

```ts
new DispatchQueue(label: string): DispatchQueue;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `label` | `string` |

###### Returns

[`DispatchQueue`](#dispatchqueue)

#### Properties

##### label

```ts
readonly label: string;
```

#### Accessors

##### background

###### Get Signature

```ts
get static background(): DispatchQueue;
```

###### Returns

[`DispatchQueue`](#dispatchqueue)

##### io

###### Get Signature

```ts
get static io(): DispatchQueue;
```

###### Returns

[`DispatchQueue`](#dispatchqueue)

##### main

###### Get Signature

```ts
get static main(): DispatchQueue;
```

Static properties for common queues

###### Returns

[`DispatchQueue`](#dispatchqueue)

#### Methods

##### async()

```ts
async(execute: () => void | Promise<void>): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `execute` | () => `void` \| `Promise`\<`void`\> |

###### Returns

`Promise`\<`void`\>

##### executeTimerCallback()

```ts
executeTimerCallback(callback: () => void): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `callback` | () => `void` |

###### Returns

`Promise`\<`void`\>

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### named()

```ts
static named(label: string): DispatchQueue;
```

Get or create a named queue

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `label` | `string` |

###### Returns

[`DispatchQueue`](#dispatchqueue)

***

### Failure

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `E` *extends* `Error` |

#### Constructors

##### Constructor

```ts
new Failure<T, E extends Error>(error: E): Failure<T, E>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | `E` |

###### Returns

[`Failure`](#failure)\<`T`, `E`\>

#### Properties

##### error

```ts
readonly error: E;
```

##### isFailure

```ts
readonly isFailure: true = true;
```

##### isSuccess

```ts
readonly isSuccess: false = false;
```

#### Methods

##### flatMap()

```ts
flatMap<U>(_fn: (val: T) => Result<U, E>): Failure<U, E>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `U` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_fn` | (`val`: `T`) => [`Result`](#result-1)\<`U`, `E`\> |

###### Returns

[`Failure`](#failure)\<`U`, `E`\>

##### getOrThrow()

```ts
getOrThrow(): T;
```

###### Returns

`T`

##### map()

```ts
map<U>(_fn: (val: T) => U): Result<U, E>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `U` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_fn` | (`val`: `T`) => `U` |

###### Returns

[`Result`](#result-1)\<`U`, `E`\>

##### mapError()

```ts
mapError<F extends Error>(fn: (err: E) => F): Failure<T, F>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `F` *extends* `Error` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | (`err`: `E`) => `F` |

###### Returns

[`Failure`](#failure)\<`T`, `F`\>

***

### `abstract` HeartbeatSubscription

#### Constructors

##### Constructor

```ts
new HeartbeatSubscription(): HeartbeatSubscription;
```

###### Returns

[`HeartbeatSubscription`](#abstract-heartbeatsubscription)

#### Properties

##### destination

```ts
abstract destination: Address;
```

The destination address for the Heartbeat messages.

It can be either a Group or Unicast Address.

##### source

```ts
abstract source: Address;
```

The source address for the Heartbeat messages.

It must be a Unicast Address.

##### state

```ts
abstract state: State | undefined;
```

The state contains variables used for handling Heartbeat messages received
by the local Node.

#### Accessors

##### isEnabled

###### Get Signature

```ts
get abstract isEnabled(): boolean;
```

Returns whether the subscription is enabled, or not.

Subscription gets disabled when the specified period times out.

###### Returns

`boolean`

#### Methods

##### matches()

```ts
abstract matches<T>(heartbeat: T): boolean;
```

Returns whether the received Heartbeat message matches subscription parameters.

###### Type Parameters

| Type Parameter |
| ------ |
| `T` |

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `heartbeat` | `T` | The received Heartbeat message. |

###### Returns

`boolean`

True, if Heartbeat message matches the subscription; false otherwise.

##### updateIfMatches()

```ts
abstract updateIfMatches<T>(heartbeat: T): void;
```

Updates the counter based on received Heartbeat message.

###### Type Parameters

| Type Parameter |
| ------ |
| `T` |

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `heartbeat` | `T` | The received Heartbeat message. |

###### Returns

`void`

##### periodLog2Period()

```ts
static periodLog2Period(periodLog: number): number;
```

Converts Subscription Period Log to Subscription Period.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `periodLog` | `number` | The logarithmic value in range 0x00...0x11. |

###### Returns

`number`

The value.

##### remainingPeriod2Period()

```ts
static remainingPeriod2Period(remainingPeriod: number): number;
```

Converts Subscription Period to Subscription Period Log.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `remainingPeriod` | `number` | The remaining period, as `TimeInterval`. |

###### Returns

`number`

The logarithmic value.

***

### IvIndex

#### Constructors

##### Constructor

```ts
new IvIndex(index?: number, updateActive?: boolean): IvIndex;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `index` | `number` | `0` |
| `updateActive` | `boolean` | `false` |

###### Returns

[`IvIndex`](#ivindex)

#### Properties

##### index

```ts
index: number = 0;
```

##### updateActive

```ts
updateActive: boolean = false;
```

##### indexKey

```ts
static indexKey: string = "IVIndex";
```

##### ivRecoveryKey

```ts
static ivRecoveryKey: string = "IVRecovery";
```

##### timestampKey

```ts
static timestampKey: string = "IVTimestamp";
```

#### Accessors

##### asMap

###### Get Signature

```ts
get asMap(): Record<string, unknown>;
```

Returns the IV Index as dictionary.

###### Returns

`Record`\<`string`, `unknown`\>

##### previous

###### Get Signature

```ts
get previous(): IvIndex | undefined;
```

The previous IV Index, or `undefined` in case of an initial one.

###### Returns

[`IvIndex`](#ivindex) \| `undefined`

##### transmitIndex

###### Get Signature

```ts
get transmitIndex(): number;
```

The IV Index used for transmitting messages.

###### Returns

`number`

#### Methods

##### equals()

```ts
equals(other: unknown): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `unknown` |

###### Returns

`boolean`

##### indexFor()

```ts
indexFor(ivi: number): number;
```

The IV Index that is to be used for decrypting messages.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `ivi` | `number` | The IVI bit of the received Network PDU. |

###### Returns

`number`

The IV Index to be used to decrypt the message.

##### lt()

```ts
lt(other: unknown): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | `unknown` |

###### Returns

`boolean`

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromMap()

```ts
static fromMap(map?: Record<string, unknown>): IvIndex | undefined;
```

Creates the IV Index from the given dictionary. It must be valid, otherwise `nil` is returned.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `map?` | `Record`\<`string`, `unknown`\> | The dictionary with IV Index. |

###### Returns

[`IvIndex`](#ivindex) \| `undefined`

The IV Index object or `undefined`.

***

### `abstract` Key

Base protocol for Network and Application Keys.

#### Constructors

##### Constructor

```ts
new Key(): Key;
```

###### Returns

[`Key`](#abstract-key)

#### Accessors

##### index

###### Get Signature

```ts
get index(): KeyIndex;
```

Index of this key, in range from 0 through to 4095.

###### Returns

[`KeyIndex`](#keyindex-1)

##### key

###### Get Signature

```ts
get key(): Data;
```

###### Returns

[`Data`](#data)

##### name

###### Get Signature

```ts
get name(): string;
```

UTF-8 string, which should be a human readable name of this key.

###### Returns

`string`

###### Set Signature

```ts
set name(value: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `string` |

###### Returns

`void`

***

### KeyIndex

The key index is a 12-bit unsigned integer identifying a Network
or an Application Key.

The key indexes within Network Keys and Application Keys must
be distinct.

This type is an alias for `UInt16`. To check the range, use
`isValidKeyIndex`.

#### Extends

- `Number`

#### Constructors

##### Constructor

```ts
new KeyIndex(value: number): KeyIndex;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

[`KeyIndex`](#keyindex-1)

###### Overrides

```ts
Number.constructor
```

#### Accessors

##### bytes

###### Get Signature

```ts
get bytes(): Uint8Array;
```

###### Returns

`Uint8Array`

##### bytesBE

###### Get Signature

```ts
get bytesBE(): Uint8Array;
```

###### Returns

`Uint8Array`

##### isValidKeyIndex

###### Get Signature

```ts
get isValidKeyIndex(): boolean;
```

###### Returns

`boolean`

#### Methods

##### equal()

```ts
equal(other: KeyIndex): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`KeyIndex`](#keyindex-1) |

###### Returns

`boolean`

***

### `abstract` KeySet

#### Constructors

##### Constructor

```ts
new KeySet(): KeySet;
```

###### Returns

[`KeySet`](#abstract-keyset)

#### Accessors

##### accessKey

###### Get Signature

```ts
get abstract accessKey(): Data;
```

The Access Layer key used to encrypt the message.

###### Returns

[`Data`](#data)

##### aid

###### Get Signature

```ts
get abstract aid(): number | undefined;
```

Application Key identifier, or `undefined` for Device Key.

###### Returns

`number` \| `undefined`

##### networkKey

###### Get Signature

```ts
get abstract networkKey(): Key;
```

The Network Key used to encrypt the message.

###### Returns

[`Key`](#abstract-key)

***

### `abstract` LoggerHandler

#### Constructors

##### Constructor

```ts
new LoggerHandler(): LoggerHandler;
```

###### Returns

[`LoggerHandler`](#abstract-loggerhandler)

#### Methods

##### d()

```ts
d(category: LogCategory, message: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `category` | [`LogCategory`](#logcategory) |
| `message` | `string` |

###### Returns

`void`

##### e()

```ts
e(category: LogCategory, message: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `category` | [`LogCategory`](#logcategory) |
| `message` | `string` |

###### Returns

`void`

##### i()

```ts
i(category: LogCategory, message: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `category` | [`LogCategory`](#logcategory) |
| `message` | `string` |

###### Returns

`void`

##### log()

```ts
abstract log(
   message: string, 
   category: LogCategory, 
   level: LogLevel
): void;
```

This method is called whenever a new log entry is to be saved.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `string` | The message. |
| `category` | [`LogCategory`](#logcategory) | The message category. |
| `level` | [`LogLevel`](#loglevel) | The log level. |

###### Returns

`void`

##### v()

```ts
v(category: LogCategory, message: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `category` | [`LogCategory`](#logcategory) |
| `message` | `string` |

###### Returns

`void`

##### w()

```ts
w(category: LogCategory, message: string): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `category` | [`LogCategory`](#logcategory) |
| `message` | `string` |

###### Returns

`void`

***

### LogLevel

Log level, which allows filtering logs by importance.

#### Properties

##### application

```ts
static application: LogLevel;
```

Messages about application level events, in this case DFU messages in human-readable form.

##### debug

```ts
static debug: LogLevel;
```

Lowest priority. Usually names of called methods or callbacks received.

##### error

```ts
static error: LogLevel;
```

Highest priority messages with errors.

##### info

```ts
static info: LogLevel;
```

Messages about completed tasks.

##### verbose

```ts
static verbose: LogLevel;
```

Low priority messages what the service is doing.

##### warning

```ts
static warning: LogLevel;
```

Important messages.

#### Methods

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

***

### `abstract` MeshMessage

The base class of every mesh message. Mesh messages can be sent to and
received from the mesh network. For messages with the Op Code known
during compilation a `StaticMeshMessage` protocol should be preferred.

Parameters `MeshMessage.security` and `MeshMessage.isSegmented`
are checked and should be set only for outgoing messages.

#### Extends

- [`BaseMeshMessage`](#abstract-basemeshmessage)

#### Extended by

- [`AcknowledgedMeshMessage`](#abstract-acknowledgedmeshmessage)
- [`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage)
- [`StaticMeshMessage`](#abstract-staticmeshmessage)
- [`TransactionMessage`](#abstract-transactionmessage)
- [`StatusMessage`](#abstract-statusmessage)
- [`TransitionStatusMessage`](#abstract-transitionstatusmessage)
- [`TransitionMessage`](#abstract-transitionmessage)

#### Constructors

##### Constructor

```ts
new MeshMessage(): MeshMessage;
```

###### Returns

[`MeshMessage`](#abstract-meshmessage)

###### Inherited from

[`BaseMeshMessage`](#abstract-basemeshmessage).[`constructor`](#constructor-6)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

##### opCode

```ts
static opCode: number;
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage).[`parameters`](#parameters-9)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`BaseMeshMessage`](#abstract-basemeshmessage).[`fromData`](#fromdata-3)

***

### MeshNetworkError

A set of generic mesh errors.

#### Extends

- `Error`

#### Properties

##### addressNotAvailable

```ts
static addressNotAvailable: MeshNetworkError;
```

Thrown when the address cannot be assigned as it is being used by
another node.

##### addressNotInAllocatedRange

```ts
static addressNotInAllocatedRange: MeshNetworkError;
```

Thrown when a node cannot be added due to its address not being
inside Provisioner's unicast address range.

##### cannotRemove

```ts
static cannotRemove: MeshNetworkError;
```

Thrown when the object cannot be removed.

##### groupAlreadyExists

```ts
static groupAlreadyExists: MeshNetworkError;
```

Thrown when a new Group is being added with the same address as one
that is already in the network.

##### groupInUse

```ts
static groupInUse: MeshNetworkError;
```

Thrown when trying to remove a Group that is either a parent of another
Group, or set as publication or subscription address for a Model.

##### invalidAddress

```ts
static invalidAddress: MeshNetworkError;
```

Thrown when the address is of a wrong type.

##### invalidKey

```ts
static invalidKey: MeshNetworkError;
```

Thrown when the provided key is not 128-bit long.

##### invalidRange

```ts
static invalidRange: MeshNetworkError;
```

Thrown when the range to be allocated is of invalid type.

##### ivIndexTooSmall

```ts
static ivIndexTooSmall: MeshNetworkError;
```

Thrown when setting too small IV Index. The new IV Index must be greater than
or equal to the previous one.

##### keyIndexAlreadyExists

```ts
static keyIndexAlreadyExists: MeshNetworkError;
```

Thrown when a key with the same index already exists in the network.

##### keyIndexOutOfRange

```ts
static keyIndexOutOfRange: MeshNetworkError;
```

Thrown when the given Key Index is not valid.

##### keyInUse

```ts
static keyInUse: MeshNetworkError;
```

Thrown when trying to remove a key that is being used by another Node.

##### keyNotKnown

```ts
static keyNotKnown: MeshNetworkError;
```

Thrown when trying to remove a key that is not known by the network.

##### noAddressAvailable

```ts
static noAddressAvailable: MeshNetworkError;
```

Thrown when a node cannot be added due to lack of available
addresses in Provisioner's range.

##### noApplicationKey

```ts
static noApplicationKey: MeshNetworkError;
```

Thrown when Application Key is required to continue with the operation.

##### nodeAlreadyExist

```ts
static nodeAlreadyExist: MeshNetworkError;
```

Thrown when a new Provisioner has the same UUID as one node that
is already in the mesh network.

##### noNetwork

```ts
static noNetwork: MeshNetworkError;
```

Thrown when trying to send a mesh message before setting up the mesh network.

##### noNetworkKey

```ts
static noNetworkKey: MeshNetworkError;
```

Thrown when Network Key is required to continue with the operation.

##### overlappingProvisionerRanges

```ts
static overlappingProvisionerRanges: MeshNetworkError;
```

Thrown when any allocated range of the new Provisioner overlaps
with an existing one.

##### provisionerNotInNetwork

```ts
static provisionerNotInNetwork: MeshNetworkError;
```

Thrown when the requested Provisioner is not in the Mesh Network.

##### provisionerUsedInAnotherNetwork

```ts
static provisionerUsedInAnotherNetwork: MeshNetworkError;
```

Thrown when trying to add a Provisioner that is already a part
of another mesh network.

##### sceneAlreadyExists

```ts
static sceneAlreadyExists: MeshNetworkError;
```

Thrown when a new Scene is being added with the same number as one
that is already in the network.

##### sceneInUse

```ts
static sceneInUse: MeshNetworkError;
```

Thrown when trying to remove a Scene stored by at least one Scene Register.

***

### MeshProvisioningService

A structure defining Mesh Provisioning service, which shall be present on
unprovisioned devices.

It allows sending provisioning messages. When provisioning is complete,
the service shall be replaced with Mesh Proxy service.

#### Extends

- [`MeshService`](#abstract-meshservice)

#### Properties

##### dataInUuid

```ts
static dataInUuid: CBUUID;
```

Data In characteristic UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`dataInUuid`](#datainuuid-2)

##### dataOutUuid

```ts
static dataOutUuid: CBUUID;
```

Data Out characteristic UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`dataOutUuid`](#dataoutuuid-2)

##### uuid

```ts
static uuid: CBUUID;
```

Service UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`uuid`](#uuid-5)

#### Methods

##### matches()

```ts
static matches(service: CBService): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `service` | [`CBService`](#abstract-cbservice) |

###### Returns

`boolean`

***

### MeshProxyService

A structure defining Mesh Proxy service, which shall be present on
provisioned Nodes.

The Mesh Proxy service is used to send mesh messages over GATT.

#### Extends

- [`MeshService`](#abstract-meshservice)

#### Properties

##### dataInUuid

```ts
static dataInUuid: CBUUID;
```

Data In characteristic UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`dataInUuid`](#datainuuid-2)

##### dataOutUuid

```ts
static dataOutUuid: CBUUID;
```

Data Out characteristic UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`dataOutUuid`](#dataoutuuid-2)

##### uuid

```ts
static uuid: CBUUID;
```

Service UUID.

###### Overrides

[`MeshService`](#abstract-meshservice).[`uuid`](#uuid-5)

#### Methods

##### matches()

```ts
static matches(service: CBService): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `service` | [`CBService`](#abstract-cbservice) |

###### Returns

`boolean`

***

### `abstract` MeshResponse

The base class for response messages.

#### Extends

- [`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage)

#### Constructors

##### Constructor

```ts
new MeshResponse(): MeshResponse;
```

###### Returns

[`MeshResponse`](#abstract-meshresponse)

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`constructor`](#constructor-62)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`opCode`](#opcode-50)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`opCode`](#opcode-51)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`isAcknowledged`](#isacknowledged-22)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`isSegmented`](#issegmented-22)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`isVendorMessage`](#isvendormessage-22)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage).[`parameters`](#parameters-9)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`security`](#security-22)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage).[`fromData`](#fromdata-28)

***

### `abstract` MeshService

A base class for mesh service objects.

#### Extended by

- [`MeshProvisioningService`](#meshprovisioningservice)
- [`MeshProxyService`](#meshproxyservice)

#### Constructors

##### Constructor

```ts
new MeshService(): MeshService;
```

###### Returns

[`MeshService`](#abstract-meshservice)

#### Properties

##### dataInUuid

```ts
readonly static dataInUuid: CBUUID;
```

Data In characteristic UUID.

##### dataOutUuid

```ts
readonly static dataOutUuid: CBUUID;
```

Data Out characteristic UUID.

##### uuid

```ts
readonly static uuid: CBUUID;
```

Service UUID.

***

### OobInformation

Information that points to Out-Of-Band (OOB) information
needed for provisioning.

#### Constructors

##### Constructor

```ts
new OobInformation(rawValue: number): OobInformation;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `rawValue` | `number` |

###### Returns

[`OobInformation`](#oobinformation)

#### Properties

##### rawValue

```ts
rawValue: number;
```

##### barCode

```ts
static barCode: OobInformation;
```

##### electronicURI

```ts
static electronicURI: OobInformation;
```

##### insideBox

```ts
static insideBox: OobInformation;
```

##### insideManual

```ts
static insideManual: OobInformation;
```

##### nfc

```ts
static nfc: OobInformation;
```

##### number

```ts
static number: OobInformation;
```

##### onBox

```ts
static onBox: OobInformation;
```

##### onDevice

```ts
static onDevice: OobInformation;
```

##### onPieceOfPaper

```ts
static onPieceOfPaper: OobInformation;
```

##### other

```ts
static other: OobInformation;
```

##### qrCode

```ts
static qrCode: OobInformation;
```

##### string

```ts
static string: OobInformation;
```

##### supportForCertificateBasedProvisioning

```ts
static supportForCertificateBasedProvisioning: OobInformation;
```

##### supportForProvisioningRecords

```ts
static supportForProvisioningRecords: OobInformation;
```

#### Methods

##### fromAdvertisementData()

```ts
static fromAdvertisementData(advertisementData: Record<string, unknown>): OobInformation | undefined;
```

Creates the object from the advertisement data.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | Received advertisement data. |

###### Returns

[`OobInformation`](#oobinformation) \| `undefined`

***

### OptionSet

#### Type Parameters

| Type Parameter |
| ------ |
| `F` *extends* `number` |

#### Constructors

##### Constructor

```ts
new OptionSet<F extends number>(value?: number): OptionSet<F>;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `value` | `number` | `0` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

#### Accessors

##### rawValue

###### Get Signature

```ts
get rawValue(): number;
```

###### Returns

`number`

#### Methods

##### contains()

```ts
contains(flag: F): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `flag` | `F` |

###### Returns

`boolean`

##### equals()

```ts
equals(other: OptionSet<F>): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`OptionSet`](#optionset)\<`F`\> |

###### Returns

`boolean`

##### insert()

```ts
insert(flag: F): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `flag` | `F` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### intersect()

```ts
intersect(other: OptionSet<F>): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`OptionSet`](#optionset)\<`F`\> |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### isDisjoint()

```ts
isDisjoint(other: OptionSet<F>): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`OptionSet`](#optionset)\<`F`\> |

###### Returns

`boolean`

##### isDisjointWithArray()

```ts
isDisjointWithArray(others: OptionSet<F>[]): boolean;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `others` | [`OptionSet`](#optionset)\<`F`\>[] |

###### Returns

`boolean`

##### remove()

```ts
remove(flag: F): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `flag` | `F` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### subtract()

```ts
subtract(other: OptionSet<F>): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`OptionSet`](#optionset)\<`F`\> |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### toggle()

```ts
toggle(flag: F): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `flag` | `F` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### union()

```ts
union(other: OptionSet<F>): OptionSet<F>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `other` | [`OptionSet`](#optionset)\<`F`\> |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### empty()

```ts
static empty<F extends number>(): OptionSet<F>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `F` *extends* `number` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

##### fromData()

```ts
static fromData<F extends number>(data: Data, offset: number): OptionSet<F>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `F` *extends* `number` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | [`Data`](#data) |
| `offset` | `number` |

###### Returns

[`OptionSet`](#optionset)\<`F`\>

***

### `abstract` ProxyConfigurationMessage

A base class for Proxy configuration messages.

#### Extends

- [`BaseMeshMessage`](#abstract-basemeshmessage)

#### Extended by

- [`StaticProxyConfigurationMessage`](#abstract-staticproxyconfigurationmessage)
- [`AcknowledgedProxyConfigurationMessage`](#abstract-acknowledgedproxyconfigurationmessage)

#### Constructors

##### Constructor

```ts
new ProxyConfigurationMessage(): ProxyConfigurationMessage;
```

###### Returns

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage)

###### Inherited from

[`BaseMeshMessage`](#abstract-basemeshmessage).[`constructor`](#constructor-6)

#### Accessors

##### opCode

###### Get Signature

```ts
get abstract opCode(): number;
```

The message Op Code.

###### Returns

`number`

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`BaseMeshMessage`](#abstract-basemeshmessage).[`parameters`](#parameters-3)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`BaseMeshMessage`](#abstract-basemeshmessage).[`fromData`](#fromdata-3)

***

### `abstract` ProxyFilterHandler

The delegate that will be notified about changes of the Proxy Filter.

#### Constructors

##### Constructor

```ts
new ProxyFilterHandler(): ProxyFilterHandler;
```

###### Returns

[`ProxyFilterHandler`](#abstract-proxyfilterhandler)

#### Methods

##### proxyFilterLimitReached()

```ts
abstract proxyFilterLimitReached(type: ProxyFilterType, maxSize: number): void;
```

This method is called when the max size of Proxy Filter list has been reached
and no more addresses can be added.

The delegate can switch to ``ProxyFilerType/rejectList``
filter type using ``ProxyFilter/setType(_:)``. This will allow receiving
messages sent to more addresses than supported by the ``ProxyFilerType/acceptList``.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `type` | [`ProxyFilterType`](#proxyfiltertype) | The current Proxy Filter type. |
| `maxSize` | `number` | The maximum Proxy Filter list size. |

###### Returns

`void`

##### proxyFilterUpdateAcknowledged()

```ts
abstract proxyFilterUpdateAcknowledged(type: ProxyFilterType, listSize: number): void;
```

Method called when the Proxy Filter has been acknowledged by proxy
and the reported list size is equal to the requested one.

In case the reported list size is lower than expected
``proxyFilterLimitReached(type:maxSize:)-1e1hd`` is called
instead.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `type` | [`ProxyFilterType`](#proxyfiltertype) | The current Proxy Filter type. |
| `listSize` | `number` | The addresses list's size in the filter |

###### Returns

`void`

##### proxyFilterUpdated()

```ts
abstract proxyFilterUpdated(type: ProxyFilterType, addresses: Map<string, Address>): void;
```

Method called when the Proxy Filter has been sent to proxy.

This method is followed by `proxyFilterUpdateAcknowledged()``
or `proxyFilterLimitReached()`, depending on the
acknowledged list size.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `type` | [`ProxyFilterType`](#proxyfiltertype) | The current Proxy Filter type. |
| `addresses` | `Map`\<`string`, [`Address`](#address)\> | The addresses in the filter. |

###### Returns

`void`

***

### RangeObject

A base class for an address or scene range.

Ranges are assigned to `Provisioner` objects. Each Provisioner
may provision new Nodes, create Groups and Scenes using only values
from assigned ranges. The assigned ranges may not overlap with the ranges
of other Provisioners, otherwise different instances could reuse the same
values leading to collisions.

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* `Number` | [`UInt16`](#uint16) |

#### Constructors

##### Constructor

```ts
new RangeObject<T extends Number = number>(range: ClosedRange<T>): RangeObject<T>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `range` | [`ClosedRange`](#closedrange)\<`T`\> |

###### Returns

[`RangeObject`](#rangeobject)\<`T`\>

#### Accessors

##### count

###### Get Signature

```ts
get count(): Long;
```

Number of elements in the range.

###### Returns

`Long`

##### lowerBound

###### Get Signature

```ts
get lowerBound(): T;
```

Lower bound of the range.

###### Returns

`T`

##### range

###### Get Signature

```ts
get range(): ClosedRange<T>;
```

###### Returns

[`ClosedRange`](#closedrange)\<`T`\>

##### upperBound

###### Get Signature

```ts
get upperBound(): T;
```

Upper bound of the range.

###### Returns

`T`

#### Methods

##### contains()

```ts
contains(value: T): boolean;
```

Returns whether the given value is in the range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `T` | The value to be checked. |

###### Returns

`boolean`

`True` if the value is inside the range, `false` otherwise.

##### containsRange()

```ts
containsRange(range: RangeObject<T>): boolean;
```

Returns whether the given range is within the range.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `range` | [`RangeObject`](#rangeobject)\<`T`\> | The range to be checked. |

###### Returns

`boolean`

`True` if the range is within the range, `false` otherwise.

##### overlaps()

```ts
overlaps(other: RangeObject<T>): boolean;
```

Returns a Boolean value indicating whether this range and the given
range contain a common element.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `other` | [`RangeObject`](#rangeobject)\<`T`\> | A range to check for elements in common. |

###### Returns

`boolean`

`True` if this range and other have at least one element in common; otherwise, `false`.

***

### SceneNumber

Scene number enum type.

#### See

UInt16

#### Extends

- `Number`

#### Constructors

##### Constructor

```ts
new SceneNumber(value: number): SceneNumber;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

###### Returns

[`SceneNumber`](#scenenumber)

###### Overrides

```ts
Number.constructor
```

#### Properties

##### invalidScene

```ts
static invalidScene: SceneNumber;
```

##### maxScene

```ts
static maxScene: SceneNumber;
```

##### minScene

```ts
static minScene: SceneNumber;
```

#### Accessors

##### hex

###### Get Signature

```ts
get hex(): string;
```

###### Returns

`string`

##### isValidSceneNumber

###### Get Signature

```ts
get isValidSceneNumber(): boolean;
```

Returns `true` if the scene number is valid.

Valid scenes have numbers from `minScene` to `maxScene`.

###### Returns

`boolean`

`true` if the scene number is valid, `false` otherwise.

#### Methods

##### fromHex()

```ts
static fromHex(hex: string): SceneNumber | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

###### Returns

[`SceneNumber`](#scenenumber) \| `undefined`

***

### `abstract` StaticAcknowledgedMeshMessage

A base class for acknowledged messages which opcode and the type of the
response message are known during compilation time.

The message must have the ``StaticAcknowledgedMeshMessage/responseType``
specified.

#### Extends

- [`StaticMeshMessage`](#abstract-staticmeshmessage)\<`this`\> & [`AcknowledgedMeshMessage`](#abstract-acknowledgedmeshmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new StaticAcknowledgedMeshMessage(): StaticAcknowledgedMeshMessage;
```

###### Returns

[`StaticAcknowledgedMeshMessage`](#abstract-staticacknowledgedmeshmessage)

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).opCode
```

##### responseType

```ts
abstract responseType: Pick<typeof StaticMeshResponse, "fromData" | "opCode">;
```

The Type of the response message.

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).parameters
```

##### responseOpCode

###### Get Signature

```ts
get responseOpCode(): number;
```

The Op Code of the response message.

###### Returns

`number`

###### Overrides

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).responseOpCode
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).security
```

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  AcknowledgedMeshMessage,
).fromData
```

***

### `abstract` StaticAcknowledgedProxyConfigurationMessage

A base class static acknowledged proxy configuration messages.

#### Extends

- [`AcknowledgedProxyConfigurationMessage`](#abstract-acknowledgedproxyconfigurationmessage)\<`this`\> & [`StaticProxyConfigurationMessage`](#abstract-staticproxyconfigurationmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new StaticAcknowledgedProxyConfigurationMessage(): StaticAcknowledgedProxyConfigurationMessage;
```

###### Returns

[`StaticAcknowledgedProxyConfigurationMessage`](#abstract-staticacknowledgedproxyconfigurationmessage)

###### Inherited from

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).constructor
```

#### Properties

##### opCode

```ts
static opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).opCode
```

##### responseType

```ts
static responseType: typeof StaticProxyConfigurationMessage;
```

The Type of the response message.

#### Accessors

##### opCode

###### Get Signature

```ts
get abstract opCode(): number;
```

The message Op Code.

###### Returns

`number`

###### Inherited from

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).opCode
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).parameters
```

##### responseOpCode

###### Get Signature

```ts
get responseOpCode(): number;
```

The Op Code of the response message.

###### Returns

`number`

###### Overrides

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).responseOpCode
```

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  AcknowledgedProxyConfigurationMessage,
  StaticProxyConfigurationMessage,
).fromData
```

***

### `abstract` StaticMeshMessage

A type of a mesh message which opcode is known during compilation time.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Extended by

- [`ConfigMessage`](#abstract-configmessage)

#### Constructors

##### Constructor

```ts
new StaticMeshMessage(): StaticMeshMessage;
```

###### Returns

[`StaticMeshMessage`](#abstract-staticmeshmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage).[`parameters`](#parameters-9)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### `abstract` StaticMeshResponse

The base class for response messages with an opcode known at the
compilation time.

#### Extends

- [`MeshResponse`](#abstract-meshresponse)\<`this`\> & [`StaticUnacknowledgedMeshMessage`](#abstract-staticunacknowledgedmeshmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new StaticMeshResponse(): StaticMeshResponse;
```

###### Returns

[`StaticMeshResponse`](#abstract-staticmeshresponse)

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).security
```

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  MeshResponse,
  StaticUnacknowledgedMeshMessage,
).fromData
```

***

### `abstract` StaticProxyConfigurationMessage

A type of Proxy Configuration message which opcode is known
during compilation time.

#### Extends

- [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage)

#### Constructors

##### Constructor

```ts
new StaticProxyConfigurationMessage(): StaticProxyConfigurationMessage;
```

###### Returns

[`StaticProxyConfigurationMessage`](#abstract-staticproxyconfigurationmessage)

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`constructor`](#constructor-42)

#### Properties

##### opCode

```ts
static opCode: number;
```

The message Op Code.

#### Accessors

##### opCode

###### Get Signature

```ts
get abstract opCode(): number;
```

The message Op Code.

###### Returns

`number`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`opCode`](#opcode-27)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`parameters`](#parameters-15)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage).[`fromData`](#fromdata-16)

***

### `abstract` StaticUnacknowledgedMeshMessage

The base class for unacknowledged messages with an opcode known at the
compilation time.

#### Extends

- [`StaticMeshMessage`](#abstract-staticmeshmessage)\<`this`\> & [`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new StaticUnacknowledgedMeshMessage(): StaticUnacknowledgedMeshMessage;
```

###### Returns

[`StaticUnacknowledgedMeshMessage`](#abstract-staticunacknowledgedmeshmessage)

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).security
```

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  StaticMeshMessage,
  UnacknowledgedMeshMessage,
).fromData
```

***

### `abstract` StatusMessage

A mesh message containing the operation status.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Constructors

##### Constructor

```ts
new StatusMessage(): StatusMessage;
```

###### Returns

[`StatusMessage`](#abstract-statusmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isSuccess

###### Get Signature

```ts
get abstract isSuccess(): boolean;
```

Returns whether the operation was successful or not.

###### Returns

`boolean`

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### message

###### Get Signature

```ts
get abstract message(): string;
```

The status as String.

###### Returns

`string`

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`parameters`](#parameters-13)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### `abstract` Storage

Abstract base class for storage engines.

Subclasses must implement the core storage methods: `get`, `set`, `remove`, and `clear`.
This class defines the contract for key-value storage with asynchronous access.

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `Value` | `unknown` |

#### Constructors

##### Constructor

```ts
new Storage<Value = unknown>(): Storage<Value>;
```

###### Returns

[`Storage`](#abstract-storage)\<`Value`\>

#### Methods

##### clear()

```ts
abstract clear(): void | Promise<void>;
```

Clears all stored key-value pairs.

###### Returns

`void` \| `Promise`\<`void`\>

##### get()

```ts
abstract get(key: string): Value | Promise<Value | undefined> | undefined;
```

Retrieves the value for the given key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | `string` | The unique identifier for the value. |

###### Returns

`Value` \| `Promise`\<`Value` \| `undefined`\> \| `undefined`

The stored value or `undefined` if not found.

##### load()

```ts
abstract load(): Data | Promise<Data | undefined> | undefined;
```

Loads data from the storage.

###### Returns

[`Data`](#data) \| `Promise`\<[`Data`](#data) \| `undefined`\> \| `undefined`

Data or `undefined` if not found.

##### remove()

```ts
abstract remove(key: string): void | Promise<void>;
```

Removes the value associated with the given key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | `string` | The key to remove. |

###### Returns

`void` \| `Promise`\<`void`\>

##### save()

```ts
abstract save(data: Data): boolean | Promise<boolean>;
```

Save given data.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | [`Data`](#data) |

###### Returns

`boolean` \| `Promise`\<`boolean`\>

`True` in case of success, `false` otherwise.

##### set()

```ts
abstract set(key: string, value: Value): void | Promise<void>;
```

Stores a value for the given key.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `key` | `string` | The unique identifier. |
| `value` | `Value` | The value to store. |

###### Returns

`void` \| `Promise`\<`void`\>

***

### Success

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `E` *extends* `Error` |

#### Constructors

##### Constructor

```ts
new Success<T, E extends Error>(value: T): Success<T, E>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `T` |

###### Returns

[`Success`](#success)\<`T`, `E`\>

#### Properties

##### isFailure

```ts
readonly isFailure: false = false;
```

##### isSuccess

```ts
readonly isSuccess: true = true;
```

##### value

```ts
readonly value: T;
```

#### Methods

##### flatMap()

```ts
flatMap<U>(fn: (val: T) => Result<U, E>): Result<U, E>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `U` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | (`val`: `T`) => [`Result`](#result-1)\<`U`, `E`\> |

###### Returns

[`Result`](#result-1)\<`U`, `E`\>

##### getOrThrow()

```ts
getOrThrow(): T;
```

###### Returns

`T`

##### map()

```ts
map<U>(fn: (val: T) => U): Result<U, E>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `U` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | (`val`: `T`) => `U` |

###### Returns

[`Result`](#result-1)\<`U`, `E`\>

##### mapError()

```ts
mapError<F extends Error>(_fn: (err: E) => F): Result<T, F>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `F` *extends* `Error` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_fn` | (`err`: `E`) => `F` |

###### Returns

[`Result`](#result-1)\<`T`, `F`\>

***

### Task

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | - |
| `E` *extends* `Error` | `Error` |

#### Constructors

##### Constructor

```ts
new Task<T, E extends Error = Error>(executor: (continuation: Continuation<T, E>) => void | Promise<void>): Task<T, E>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `executor` | (`continuation`: `Continuation`\<`T`, `E`\>) => `void` \| `Promise`\<`void`\> |

###### Returns

[`Task`](#task)\<`T`, `E`\>

#### Properties

##### signal

```ts
readonly signal: AbortSignal;
```

#### Accessors

##### result

###### Get Signature

```ts
get result(): Promise<T>;
```

###### Returns

`Promise`\<`T`\>

##### status

###### Get Signature

```ts
get status(): TaskStatus;
```

###### Returns

`TaskStatus`

#### Methods

##### cancel()

```ts
cancel(): void;
```

###### Returns

`void`

##### value()

```ts
value(): Promise<T>;
```

###### Returns

`Promise`\<`T`\>

##### detached()

```ts
static detached<T, E extends Error = Error>(executor: (continuation: Continuation<T, E>) => void | Promise<void>): Task<T, E>;
```

###### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | - |
| `E` *extends* `Error` | `Error` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `executor` | (`continuation`: `Continuation`\<`T`, `E`\>) => `void` \| `Promise`\<`void`\> |

###### Returns

[`Task`](#task)\<`T`, `E`\>

##### sleep()

```ts
static sleep(ms: number): Task<void, never>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ms` | `number` |

###### Returns

[`Task`](#task)\<`void`, `never`\>

***

### `abstract` TransactionMessage

A message with Transaction Identifier.

The Transaction Identifier will automatically be set and incremented
each time a message is sent. The counter is reused for all types that
extend this protocol.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Constructors

##### Constructor

```ts
new TransactionMessage(): TransactionMessage;
```

###### Returns

[`TransactionMessage`](#abstract-transactionmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### tid

```ts
abstract tid: number;
```

Transaction identifier. If not set, this field will automatically
be set when the message is being sent or received.

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### continueTransaction

###### Get Signature

```ts
get continueTransaction(): boolean;
```

Whether the message should start a new transaction.

The messages within a transaction carry the cumulative values of
a field. In case one or more messages within a transaction are not
received by the Server (e.g., as a result of radio collisions),
the next received message will make up for the lost messages,
carrying cumulative values of the field.

A new transaction is started when this field is set to `true`,
or when the last message of the transaction was sent 6 or
more seconds earlier.

This defaults to `false`, which means that each new message will
receive a new transaction identifier (if not set explicitly).

###### Returns

`boolean`

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`parameters`](#parameters-13)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### `abstract` TransitionMessage

A base protocol for a message that can initiate a non-immediate
state transition.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Constructors

##### Constructor

```ts
new TransitionMessage(): TransitionMessage;
```

###### Returns

[`TransitionMessage`](#abstract-transitionmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### delay

###### Get Signature

```ts
get abstract delay(): number | undefined;
```

execution delay in 5 millisecond steps.

The purpose of this field is to synchronize transitions initiated
by sending the same message multiple times with a short delay.
For example, a Node would want to send a Generic On Off Set
Unacknowledged message to a Group Address. In order to increase
changes of successful delivery, such message can be repeated.
The first message could be sent with longer ``TransitionMessage/delay``
and each following with a shorter one, so when different Nodes
receive different messages, the action they take seems more
synchronized.

This file has to be set together with ``TransitionMessage/transitionTime``.

###### Returns

`number` \| `undefined`

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`parameters`](#parameters-13)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

##### transitionTime

###### Get Signature

```ts
get abstract transitionTime(): TransitionTime | undefined;
```

The Transition Time field identifies the time that an Element will
take to transition to the target state from the present state.

###### Returns

[`TransitionTime`](#transitiontime-1) \| `undefined`

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### `abstract` TransitionStatusMessage

A base protocol for messages sent as responses to
``TransitionMessage``s.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Constructors

##### Constructor

```ts
new TransitionStatusMessage(): TransitionStatusMessage;
```

###### Returns

[`TransitionStatusMessage`](#abstract-transitionstatusmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`parameters`](#parameters-13)

##### remainingTime

###### Get Signature

```ts
get abstract remainingTime(): TransitionTime | undefined;
```

The Remaining Time field identifies the time that an element will
take to transition to the target state from the present state.

###### Returns

[`TransitionTime`](#transitiontime-1) \| `undefined`

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### TransitionTime

This structure represents a time needed to transition from one state to another,
for example dimming a light.

Internally, it uses steps and step resolution. Thanks to that only some time
intervals are possible. Use ``TransitionTime/interval`` to get exact time

#### Constructors

##### Constructor

```ts
new TransitionTime(steps: number, stepResolution: StepResolution): TransitionTime;
```

Creates the Transition Time object.

Only values of 0x00 through 0x3E shall be used to specify the value
of the Transition Number of Steps field.

NOTE: Use ``TransitionTime/init()`` to create a Transition Time
        representing an unknown time.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `steps` | `number` | Transition Number of Steps, valid values are in range 0...62. Value 63 means that the value is unknown and the state cannot be set to this value. |
| `stepResolution` | [`StepResolution`](#stepresolution) | The step resolution. |

###### Returns

[`TransitionTime`](#transitiontime-1)

#### Properties

##### stepResolution

```ts
stepResolution: StepResolution;
```

The step resolution.

##### steps

```ts
steps: number;
```

Transition Number of Steps, 6-bit value.

Value 0 indicates an immediate transition.

Value 0x3F means that the value is unknown. The state cannot be
set to this value, but an element may report an unknown value if
a transition is higher than 0x3E or not determined.

##### immediate

```ts
static immediate: TransitionTime;
```

Transition is immediate.

#### Accessors

##### interval

###### Get Signature

```ts
get interval(): number | undefined;
```

`undefined` value represents an unknown time.

###### Returns

`number` \| `undefined`

##### isImmediate

###### Get Signature

```ts
get isImmediate(): boolean;
```

Whether the transition is immediate.

###### Returns

`boolean`

##### isKnown

###### Get Signature

```ts
get isKnown(): boolean;
```

Returns whether the transition time is known.

###### Returns

`boolean`

##### milliseconds

###### Get Signature

```ts
get milliseconds(): number | undefined;
```

The transition time in milliseconds.

`undefined` value represents an unknown time.

###### Returns

`number` \| `undefined`

##### rawValue

###### Get Signature

```ts
get rawValue(): number;
```

The raw representation of the transition in a mesh message.

###### Returns

`number`

#### Methods

##### or()

```ts
or(defaultTransitionTime?: TransitionTime): TransitionTime;
```

Returns this Transition Time value, if it's known, or
the default value. If default value is `nil`, instantaneous
transition is returned.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `defaultTransitionTime?` | [`TransitionTime`](#transitiontime-1) | The optional default value of the transition time. |

###### Returns

[`TransitionTime`](#transitiontime-1)

##### toString()

```ts
toString(): string;
```

###### Returns

`string`

##### fromInterval()

```ts
static fromInterval(interval: number): TransitionTime;
```

the Transition Time object for the `TimeInterval`.

NOTE: Mind, that the transition time will be converted to steps
        and step resolution using rounding. Check implementation
        for details.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `interval` | `number` | The transition time in seconds. |

###### Returns

[`TransitionTime`](#transitiontime-1)

##### fromRawValue()

```ts
static fromRawValue(rawValue: number): TransitionTime;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `rawValue` | `number` |

###### Returns

[`TransitionTime`](#transitiontime-1)

##### unknown()

```ts
static unknown(): TransitionTime;
```

Creates the Transition Time object for an unknown time.

###### Returns

[`TransitionTime`](#transitiontime-1)

***

### `abstract` Transport

Abstract base class for transport implementations.
Provides a static method to retrieve the advertisement data service data key.

#### Constructors

##### Constructor

```ts
new Transport(): Transport;
```

###### Returns

[`Transport`](#abstract-transport)

#### Accessors

##### AdvertisementDataServiceDataKey

###### Get Signature

```ts
get static AdvertisementDataServiceDataKey(): string;
```

Returns the advertisement data service data key for the transport.
Subclasses should override this method to provide the appropriate key.

###### Throws

Error if not implemented by the subclass.

###### Returns

`string`

***

### `abstract` UnacknowledgedConfigMessage

A base protocol for unacknowledged Configuration messages.

Unacknowledged configuration messages are sent as replies to acknowledged messages.

#### Extends

- [`ConfigMessage`](#abstract-configmessage)\<`this`\> & [`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage)\<`this`\>

#### Constructors

##### Constructor

```ts
new UnacknowledgedConfigMessage(): UnacknowledgedConfigMessage;
```

###### Returns

[`UnacknowledgedConfigMessage`](#abstract-unacknowledgedconfigmessage)

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).constructor
```

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).opCode
```

##### opCode

```ts
static opCode: number;
```

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).opCode
```

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).isAcknowledged
```

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).isSegmented
```

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).isVendorMessage
```

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).parameters
```

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).security
```

#### Methods

##### encode()

```ts
encode(indexes: KeyIndex[], limit?: number): Data;
```

Encodes given list of Key Indexes into a Data.
As each Key Index is 12 bits long, a pair of them can fit 3 bytes.
This method ensures that they are packed in compliance to the
Bluetooth Mesh specification.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `indexes` | [`KeyIndex`](#keyindex-1)[] | `undefined` | An array of 12-bit Key Indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to encode. |

###### Returns

[`Data`](#data)

Key Indexes encoded to a Data.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).encode
```

##### decode()

```ts
static decode(
   data: Data, 
   offset: number, 
   limit?: number
): KeyIndex[];
```

Decodes number of Key Indexes from the given Data from the given offset.
This will decode as many Indexes as possible, until the end of data is
reached.

###### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | [`Data`](#data) | `undefined` | The data from where the indexes should be read. |
| `offset` | `number` | `undefined` | The offset from where to read the indexes. |
| `limit` | `number` | `10000` | Maximum number of Key Indexes to decode. |

###### Returns

[`KeyIndex`](#keyindex-1)[]

Decoded Key Indexes.

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).decode
```

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

```ts
Mixin(
  ConfigMessage,
  UnacknowledgedMeshMessage,
).fromData
```

***

### `abstract` UnacknowledgedMeshMessage

The base class for unacknowledged messages.

#### Extends

- [`MeshMessage`](#abstract-meshmessage)

#### Extended by

- [`MeshResponse`](#abstract-meshresponse)

#### Constructors

##### Constructor

```ts
new UnacknowledgedMeshMessage(): UnacknowledgedMeshMessage;
```

###### Returns

[`UnacknowledgedMeshMessage`](#abstract-unacknowledgedmeshmessage)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`constructor`](#constructor-37)

#### Properties

##### opCode

```ts
abstract opCode: number;
```

The message Op Code.

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-23)

##### opCode

```ts
static opCode: number;
```

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`opCode`](#opcode-24)

#### Accessors

##### isAcknowledged

###### Get Signature

```ts
get isAcknowledged(): boolean;
```

Whether the message is an acknowledged message, or not.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isAcknowledged`](#isacknowledged-11)

##### isSegmented

###### Get Signature

```ts
get isSegmented(): boolean;
```

Returns whether the message should be sent or was sent as
Segmented Access Message. By default, this parameter returns
`false`.

To force segmentation for shorter messages return `true` despite
payload length. If payload size is longer than 11 bytes this
field is not checked as the message must be segmented.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isSegmented`](#issegmented-11)

##### isVendorMessage

###### Get Signature

```ts
get isVendorMessage(): boolean;
```

Whether the message is a Vendor Message, or not.

Vendor messages use 3-byte Op Codes, where the 2 most significant
bits of the first octet are set to 1. The remaining bits of the
first octet are the operation code, while the last 2 bytes are the
Company Identifier (Big Endian), as registered by Bluetooth SIG.

###### Returns

`boolean`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`isVendorMessage`](#isvendormessage-11)

##### parameters

###### Get Signature

```ts
get abstract parameters(): Data | undefined;
```

Access Layer payload, including the Op Code.

###### Returns

[`Data`](#data) \| `undefined`

###### Inherited from

[`ConfigNetAndAppKeyMessage`](#abstract-confignetandappkeymessage).[`parameters`](#parameters-9)

##### security

###### Get Signature

```ts
get security(): MeshMessageSecurity;
```

Returns whether the message should be sent or has been sent using
32-bit or 64-bit TransMIC value. By default, `MeshMessageSecurity/low`
is returned.

Only Segmented Access Messages can use 64-bit MIC. If the payload
is shorter than 11 bytes, make sure you return `true` from
`MeshMessage.isSegmented-891sy`, otherwise this field will be ignored.

###### Returns

[`MeshMessageSecurity`](#meshmessagesecurity)

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`security`](#security-11)

#### Methods

##### fromData()

```ts
static fromData(_parameters: Data): BaseMeshMessage | undefined;
```

This initializer should construct the message based on the received
parameters.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_parameters` | [`Data`](#data) | Received Access Layer parameters. |

###### Returns

[`BaseMeshMessage`](#abstract-basemeshmessage) \| `undefined`

###### Inherited from

[`MeshMessage`](#abstract-meshmessage).[`fromData`](#fromdata-13)

***

### UserDefaults

#### Methods

##### clear()

```ts
clear(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### get()

```ts
get<T = StorageValue>(key: string, parser?: (v: unknown) => T): Promise<T | undefined>;
```

###### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `StorageValue` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |
| `parser?` | (`v`: `unknown`) => `T` |

###### Returns

`Promise`\<`T` \| `undefined`\>

##### lastSeqAuthValue()

```ts
lastSeqAuthValue(source: Address): Promise<Long | undefined>;
```

Returns the last SeqAuth value stored for the given source address, or nil,
if no message has ever been received from that address.

The SeqAuth value ensures uniqueness of each message. Each message from
the same source address must be sent with unique value of SeqAuth.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `source` | [`Address`](#address) | The source Unicast Address. |

###### Returns

`Promise`\<`Long` \| `undefined`\>

The 32+24 bit SeqAuth value, or undefined.

##### nextSequenceNumber()

```ts
nextSequenceNumber(source: Address): Promise<number>;
```

Returns the next SEQ number to be used to send a message from
the given Unicast Address.

Each time this method is called returned value is incremented by 1.

Size of SEQ is 24 bits.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `source` | [`Address`](#address) | The Unicast Address of local Element. |

###### Returns

`Promise`\<`number`\>

The next SEQ number to be used.

##### previousSeqAuthValue()

```ts
previousSeqAuthValue(source: Address): Promise<Long | undefined>;
```

Returns the previous SeqAuth value for the given source address, or nil,
if no more than 1 message has ever been received from that address.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `source` | [`Address`](#address) | The source Unicast Address. |

###### Returns

`Promise`\<`Long` \| `undefined`\>

The 32+24 bit SeqAuth value, or nil.

##### register()

```ts
register(defaults: Record<string, StorageValue>): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `defaults` | `Record`\<`string`, `StorageValue`\> |

###### Returns

`Promise`\<`void`\>

##### remove()

```ts
remove(key: string): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |

###### Returns

`Promise`\<`void`\>

##### removeSeqAuthValuesOfAddress()

```ts
removeSeqAuthValuesOfAddress(source: Address): Promise<void>;
```

Removes last known SeqAuth value associated with the given address
of a remote Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `source` | [`Address`](#address) | The forgotten Address. |

###### Returns

`Promise`\<`void`\>

##### removeSeqAuthValuesOfNode()

```ts
removeSeqAuthValuesOfNode(node: {
  elements: {
     unicastAddress: Address;
  }[];
}): Promise<void>;
```

Removes all known SeqAuth values associated with any of the Elements
of the given remote Node.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | \{ `elements`: \{ `unicastAddress`: [`Address`](#address); \}[]; \} | The remote Node. |
| `node.elements` | \{ `unicastAddress`: [`Address`](#address); \}[] | - |

###### Returns

`Promise`\<`void`\>

##### removeSequenceNumber()

```ts
removeSequenceNumber(source: Address): Promise<void>;
```

Removes the SEQ number associated with the given address.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `source` | [`Address`](#address) | The address to be forgotten. |

###### Returns

`Promise`\<`void`\>

##### resetSequenceNumbers()

```ts
resetSequenceNumbers(node: {
  elements: {
     unicastAddress: Address;
  }[];
}): Promise<void>;
```

Resets the SEQ associated with all Elements of the given Node to 0.

This method should be called when the IV Index is incremented and SEQ
number should be reset.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `node` | \{ `elements`: \{ `unicastAddress`: [`Address`](#address); \}[]; \} | The local Node. |
| `node.elements` | \{ `unicastAddress`: [`Address`](#address); \}[] | - |

###### Returns

`Promise`\<`void`\>

##### set()

```ts
set<T = StorageValue>(key: string, value: T): Promise<void>;
```

###### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `StorageValue` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `string` |
| `value` | `T` |

###### Returns

`Promise`\<`void`\>

##### storeLastSeqAuthValue()

```ts
storeLastSeqAuthValue(value: Long, source: Address): Promise<void>;
```

Stores the last received SeqAuth value in User Defaults.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `Long` | The SeqAuth value of received message. |
| `source` | [`Address`](#address) | The source Unicast Address. |

###### Returns

`Promise`\<`void`\>

##### storePreviousSeqAuthValue()

```ts
storePreviousSeqAuthValue(value: Long, source: Address): Promise<void>;
```

Stores the previously received SeqAuth value in User Defaults.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `Long` | The previously received SeqAuth value. |
| `source` | [`Address`](#address) | The source Unicast Address. |

###### Returns

`Promise`\<`void`\>

##### instance()

```ts
static instance(suiteName: string, storage: Storage): UserDefaults;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `suiteName` | `string` |
| `storage` | [`Storage`](#abstract-storage) |

###### Returns

[`UserDefaults`](#userdefaults)

***

### UUID

Represents a universally unique identifier (UUID) with Swift-like API

#### Constructors

##### Constructor

```ts
new UUID($uuidString?: string): UUID;
```

Create a UUID instance

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `$uuidString` | `string` | Optional UUID string (default: random v4 UUID) |

###### Returns

[`UUID`](#uuid-6)

#### Accessors

##### bytes

###### Get Signature

```ts
get bytes(): Data;
```

The UUID as Data.

###### Returns

[`Data`](#data)

##### hex

###### Get Signature

```ts
get hex(): string;
```

Returns the uuidString without dashes.

###### Returns

`string`

##### uuidString

###### Get Signature

```ts
get uuidString(): string;
```

Get the UUID string representation

###### Returns

`string`

#### Methods

##### equal()

```ts
equal(other: UUID): boolean;
```

Checks if this UUID is equal to another UUID.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `other` | [`UUID`](#uuid-6) | The other UUID instance to compare. |

###### Returns

`boolean`

True if both UUIDs have the same string representation.

##### toString()

```ts
toString(): string;
```

String representation (same as uuidString)

###### Returns

`string`

##### fromBluetoothUuidString()

```ts
static fromBluetoothUuidString(uuidString: string): UUID | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuidString` | `string` |

###### Returns

[`UUID`](#uuid-6) \| `undefined`

##### fromHex()

```ts
static fromHex(hex: string): UUID | undefined;
```

Creates the UUID from a 32-character hexadecimal string.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

###### Returns

[`UUID`](#uuid-6) \| `undefined`

##### fromUuidString()

```ts
static fromUuidString(uuidString: string): UUID | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuidString` | `string` |

###### Returns

[`UUID`](#uuid-6) \| `undefined`

##### isBluetoothUUID()

```ts
static isBluetoothUUID(str: string): boolean;
```

Checks if the given string is a valid Bluetooth UUID.
Bluetooth UUIDs follow the format: xxxxxxxx-xxxx-1000-8000-00805F9B34FB

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `str` | `string` | The string to check. |

###### Returns

`boolean`

True if the string is a valid Bluetooth UUID, false otherwise.

##### isValidUUIDString()

```ts
static isValidUUIDString(uuidString: string): boolean;
```

Validate a UUID string

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `uuidString` | `string` | String to validate |

###### Returns

`boolean`

True if valid RFC 4122 UUID

##### random()

```ts
static random(): UUID;
```

Generate a random UUID instance

###### Returns

[`UUID`](#uuid-6)

## Interfaces

### ProxyFilterEventHandler

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Methods

##### handle()

```ts
handle(message: ProxyConfigurationMessage, proxy: T): Promise<void>;
```

Handler for the received Proxy Configuration Messages.

This method notifies the delegate about changes in the Proxy Filter.

If a mismatch is detected between the local list of services and
the list size received, the method will try to clear the remote
filter and send all the addresses again.

If a Node with a Unicast Address of the received `FilterStatus` message
does not exist in the local database, the method will return an `UnknownNode`.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage) | The message received. |
| `proxy` | `T` | The connected Proxy `Node`, or `UnknownNode` if the Node does not exist in the local mesh network configuration. |

###### Returns

`Promise`\<`void`\>

##### managerDidDeliverMessage()

```ts
managerDidDeliverMessage(message: ProxyConfigurationMessage): void;
```

Callback called when a Proxy Configuration Message has been sent.

This method refreshes the local type and list of addresses.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage) | The message sent. |

###### Returns

`void`

##### managerFailedToDeliverMessage()

```ts
managerFailedToDeliverMessage(message: ProxyConfigurationMessage, error: Error): void;
```

Callback called when the manager failed to send the Proxy
Configuration Message.

This method clears the local filter and sets it back to `ProxyFilerType/acceptList`.
All the messages waiting to be sent are cancelled.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | [`ProxyConfigurationMessage`](#abstract-proxyconfigurationmessage) | The message that has not been sent. |
| `error` | `Error` | The error received. |

###### Returns

`void`

##### newNetworkCreated()

```ts
newNetworkCreated(): void;
```

Clears the current Proxy Filter state.

###### Returns

`void`

##### newProxyDidConnect()

```ts
newProxyDidConnect(): Promise<void>;
```

Callback called when a possible change of Proxy Node have been discovered.

This method is called in two cases: when the first Secure Network
beacon was received (which indicates the first successful connection
to a Proxy since app was started) or when the received Secure Network
beacon contained information about the same Network Key as one
received before. This happens during a reconnection to the same
or a different Proxy on the same subnetwork, but may also happen
in other Circumstances, for example when the IV Update or Key Refresh
Procedure is in progress, or a Network Key was removed and added again.

This method reloads the Proxy Filter for the local Provisioner,
adding all the addresses the Provisioner is subscribed to, including
its Unicast Addresses and All Nodes address.

###### Returns

`Promise`\<`void`\>

## Type Aliases

### AssertionTypeDescription

```ts
type AssertionTypeDescription = typeof _assertionTypeDescriptions[number];
```

***

### Class

```ts
type Class<T, Arguments extends unknown[] = any[]> = Constructor<T, Arguments> & {
  prototype: T;
};
```

Matches a [`class`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes).

#### Type Declaration

##### prototype

```ts
prototype: T;
```

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | - |
| `Arguments` *extends* `unknown`[] | `any`[] |

***

### Data

```ts
type Data = Uint8Array;
```

***

### Double

```ts
type Double = number;
```

Represents a 64-bit IEEE‑754 double-precision floating-point
(the same as JavaScript's built-in `number` type).

***

### Hex

```ts
type Hex = string;
```

Represents a hexadecimal string (e.g., color codes, hashes).

***

### Int16

```ts
type Int16 = number;
```

Represents a signed 16-bit integer.

***

### Int32

```ts
type Int32 = number;
```

Represents a signed 32-bit integer.

***

### Int64

```ts
type Int64 = Long;
```

Represents a signed 64-bit integer using Long.

***

### Int8

```ts
type Int8 = number;
```

Represents a signed 8-bit integer.

***

### NSNull

```ts
type NSNull = typeof NSNull;
```

Unique symbol used to represent a special "null" value within the namespace.
Useful for distinguishing between JavaScript's native `null` and a custom sentinel value.

***

### ProxyFilterSetup

```ts
type ProxyFilterSetup = 
  | {
  type: automatic;
}
  | {
  addresses: Map<string, Address>;
  type: acceptList;
}
  | {
  addresses: Map<string, Address>;
  type: rejectList;
};
```

***

### Result

```ts
type Result<T, E extends Error> = Success<T, E> | Failure<T, E>;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `E` *extends* `Error` |

***

### TimeInterval

```ts
type TimeInterval = Double;
```

Double

***

### UInt16

```ts
type UInt16 = number;
```

Represents an unsigned 16-bit integer.

***

### UInt32

```ts
type UInt32 = number;
```

Represents an unsigned 32-bit integer.

***

### UInt64

```ts
type UInt64 = Long;
```

Represents an unsigned 64-bit integer using bigint.

***

### UInt8

```ts
type UInt8 = number;
```

Represents an unsigned 8-bit integer.

## Variables

### LocalProvisionerUuidKey

```ts
const LocalProvisionerUuidKey: "provisioner" = "provisioner";
```

The key used in `UserDefaults` to store the UUID of the local Provisioner for
each loaded mesh network.

The intent is to restore the same instance (move to index 0) whenever the same mesh
network configuration is imported.

Local Provisioner UUID is saved whenever a new Provisioner is added or moved
to index 0 in the `MeshNetwork.provisioners` array in mesh network object.

Use `MeshNetwork.restoreLocalProvisioner()` to restore the Provisioner instance.

***

### NSNull

```ts
const NSNull: typeof NSNull;
```

Unique symbol used to represent a special "null" value within the namespace.
Useful for distinguishing between JavaScript's native `null` and a custom sentinel value.

***

### Result

```ts
Result: {
  failure: Failure<T, E>;
  success: Success<T, E>;
  try: Result<T, Error>;
  tryAsync: Promise<Result<T, Error>>;
};
```

#### Type Declaration

##### failure()

```ts
failure<T = never, E extends Error = Error>(error: E): Failure<T, E>;
```

###### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `never` |
| `E` *extends* `Error` | `Error` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | `E` |

###### Returns

[`Failure`](#failure)\<`T`, `E`\>

##### success()

```ts
success<T, E extends Error = Error>(value: T): Success<T, E>;
```

###### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | - |
| `E` *extends* `Error` | `Error` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `T` |

###### Returns

[`Success`](#success)\<`T`, `E`\>

##### try()

```ts
try<T>(fn: () => T): Result<T, Error>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `T` |

###### Returns

[`Result`](#result-1)\<`T`, `Error`\>

##### tryAsync()

```ts
tryAsync<T>(fn: () => Promise<T>): Promise<Result<T, Error>>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `fn` | () => `Promise`\<`T`\> |

###### Returns

`Promise`\<[`Result`](#result-1)\<`T`, `Error`\>\>

## Functions

### assertArray()

```ts
function assertArray<T = unknown>(
   value: unknown, 
   assertion?: (element: unknown, message?: string) => asserts element is T, 
   message?: string
): asserts value is T[];
```

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `assertion?` | (`element`: `unknown`, `message?`: `string`) => `asserts element is T` |
| `message?` | `string` |

#### Returns

`asserts value is T[]`

***

### assertBoolean()

```ts
function assertBoolean(value: unknown, message?: string): asserts value is boolean;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `message?` | `string` |

#### Returns

`asserts value is boolean`

***

### assertDirectInstanceOf()

```ts
function assertDirectInstanceOf<T>(
   instance: unknown, 
   class_: Class<T>, 
   message?: string
): asserts instance is T;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `instance` | `unknown` |
| `class_` | [`Class`](#class)\<`T`\> |
| `message?` | `string` |

#### Returns

`asserts instance is T`

***

### assertEnumCase()

```ts
function assertEnumCase<T = unknown>(
   value: unknown, 
   targetEnum: T, 
   message?: string
): asserts value is T[keyof T];
```

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `targetEnum` | `T` |
| `message?` | `string` |

#### Returns

`asserts value is T[keyof T]`

***

### assertNumber()

```ts
function assertNumber(value: unknown, message?: string): asserts value is number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `message?` | `string` |

#### Returns

`asserts value is number`

***

### assertObject()

```ts
function assertObject(value: unknown, message?: string): asserts value is object;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `message?` | `string` |

#### Returns

`asserts value is object`

***

### assertString()

```ts
function assertString(value: unknown, message?: string): asserts value is string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `message?` | `string` |

#### Returns

`asserts value is string`

***

### bitArrayToUint8Array()

```ts
function bitArrayToUint8Array(signedInts: BitArray): Uint8Array;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `signedInts` | `BitArray` |

#### Returns

`Uint8Array`

***

### calculateHMAC\_SHA256()

```ts
function calculateHMAC_SHA256(key: Uint8Array, data: Uint8Array): Uint8Array;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Uint8Array` |
| `data` | `Uint8Array` |

#### Returns

`Uint8Array`

***

### calculateSharedSecret()

```ts
function calculateSharedSecret(privateKeyHex: string, publicKeyHex: string): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `privateKeyHex` | `string` |
| `publicKeyHex` | `string` |

#### Returns

`string`

***

### chunkedMap()

```ts
function chunkedMap<T>(map: Map<string, T>, maxSize: number): Map<string, T>[];
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `map` | `Map`\<`string`, `T`\> |
| `maxSize` | `number` |

#### Returns

`Map`\<`string`, `T`\>[]

***

### dataViewToUint8Array()

```ts
function dataViewToUint8Array(view: DataView): Uint8Array;
```

Convert a DataView to a Uint8Array.
This is useful for extracting raw binary data from a DataView.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `view` | `DataView` | The DataView to convert |

#### Returns

`Uint8Array`

A Uint8Array containing the data from the DataView

***

### decryptAesCcm()

```ts
function decryptAesCcm(
   key: Uint8Array, 
   iv: Uint8Array, 
   cipherTextWithTag: Uint8Array, 
   aad: Uint8Array<ArrayBufferLike> | undefined, 
   tagLength?: number
): Uint8Array;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `key` | `Uint8Array` | `undefined` |
| `iv` | `Uint8Array` | `undefined` |
| `cipherTextWithTag` | `Uint8Array` | `undefined` |
| `aad` | `Uint8Array`\<`ArrayBufferLike`\> \| `undefined` | `undefined` |
| `tagLength` | `number` | `4` |

#### Returns

`Uint8Array`

***

### encryptAesCcm()

```ts
function encryptAesCcm(
   key: Uint8Array, 
   iv: Uint8Array, 
   data: Uint8Array, 
   aad: Uint8Array<ArrayBufferLike> | undefined, 
   tagLength?: number
): Uint8Array;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `key` | `Uint8Array` | `undefined` |
| `iv` | `Uint8Array` | `undefined` |
| `data` | `Uint8Array` | `undefined` |
| `aad` | `Uint8Array`\<`ArrayBufferLike`\> \| `undefined` | `undefined` |
| `tagLength` | `number` | `4` |

#### Returns

`Uint8Array`

***

### encryptAesEcb()

```ts
function encryptAesEcb(plaintext: Uint8Array, keyBytes: Uint8Array): Uint8Array<ArrayBufferLike>;
```

Encrypt plaintext (Uint8Array) using AES-ECB mode with a 128/192/256-bit key.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `plaintext` | `Uint8Array` | Uint8Array (must be multiple of 16 bytes) |
| `keyBytes` | `Uint8Array` | Uint8Array (16, 24, or 32 bytes) |

#### Returns

`Uint8Array`\<`ArrayBufferLike`\>

Uint8Array cipherText (same length as plaintext)

***

### generateElGamalKeyPair()

```ts
function generateElGamalKeyPair(): {
  privateKey: string;
  publicKey: string;
};
```

#### Returns

```ts
{
  privateKey: string;
  publicKey: string;
}
```

##### privateKey

```ts
privateKey: string;
```

##### publicKey

```ts
publicKey: string;
```

***

### getEnumKeys()

```ts
function getEnumKeys<T extends object>(_enum: T): string[];
```

Returns the keys of a TypeScript enum as an array of strings.

#### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* `object` |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_enum` | `T` | The enum object. |

#### Returns

`string`[]

***

### getEnumValues()

```ts
function getEnumValues<T extends object, V = T[keyof T]>(_enum: T): V[];
```

Returns the values of a TypeScript enum as an array.

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` *extends* `object` | - |
| `V` | `T`\[keyof `T`\] |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `_enum` | `T` | The enum object. |

#### Returns

`V`[]

***

### getSecureRandomBytes()

```ts
function getSecureRandomBytes(length: Long): Uint8Array<ArrayBuffer> | Error;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `length` | `Long` |

#### Returns

`Uint8Array`\<`ArrayBuffer`\> \| `Error`

***

### hexToUuid()

```ts
function hexToUuid(hex: string): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

#### Returns

`string`

***

### importKeyFromPrivate()

```ts
function importKeyFromPrivate(privateKeyHex: string): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `privateKeyHex` | `string` |

#### Returns

`string`

***

### isArray()

```ts
function isArray<T = unknown>(value: unknown, assertion?: (value: T) => value is T): value is T[];
```

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |
| `assertion?` | (`value`: `T`) => `value is T` |

#### Returns

`value is T[]`

***

### isDirectInstanceOf()

```ts
function isDirectInstanceOf<T>(instance: unknown, class_: Class<T>): instance is T;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `instance` | `unknown` |
| `class_` | [`Class`](#class)\<`T`\> |

#### Returns

`instance is T`

***

### isEnumCase()

```ts
function isEnumCase<T = unknown>(value: unknown, targetEnum: T): value is T[keyof T];
```

Returns whether the given value is a valid case of the provided enum.

#### Type Parameters

| Type Parameter | Default type |
| ------ | ------ |
| `T` | `unknown` |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `unknown` | The value to check. |
| `targetEnum` | `T` | - |

#### Returns

`value is T[keyof T]`

***

### isFunction()

```ts
function isFunction(value: unknown): value is (args: unknown[]) => unknown;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |

#### Returns

`value is (args: unknown[]) => unknown`

***

### isNull()

```ts
function isNull(value: unknown): value is null;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |

#### Returns

`value is null`

***

### isNumber()

```ts
function isNumber(value: unknown): value is number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |

#### Returns

`value is number`

***

### isObject()

```ts
function isObject(value: unknown): value is object;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `unknown` |

#### Returns

`value is object`

***

### isValidUuid()

```ts
function isValidUuid(hex: string): boolean;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `hex` | `string` |

#### Returns

`boolean`

***

### longMin()

```ts
function longMin(a: Long, b: Long): Long;
```

Returns the minimum of two Long values.
Compares a and b, and returns the smaller value.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `a` | `Long` | The first Long value. |
| `b` | `Long` | The second Long value. |

#### Returns

`Long`

The smaller of a and b.

***

### meshTimestamp()

```ts
function meshTimestamp(timestamp: number): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `timestamp` | `number` |

#### Returns

`string`

***

### packInt16BE()

```ts
function packInt16BE(n: number): Uint8Array;
```

Signed 16-bit big-endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packInt16LE()

```ts
function packInt16LE(n: number): Uint8Array;
```

Signed 16-bit little endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packInt32BE()

```ts
function packInt32BE(n: number): Uint8Array;
```

Signed 32-bit big-endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packInt32LE()

```ts
function packInt32LE(n: number): Uint8Array;
```

Signed 32-bit little endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packUInt16BE()

```ts
function packUInt16BE(n: number): Uint8Array;
```

Unsigned 16-bit big-endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packUInt16LE()

```ts
function packUInt16LE(n: number): Uint8Array;
```

Unsigned 16-bit little endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packUInt32BE()

```ts
function packUInt32BE(n: number): Uint8Array;
```

Unsigned 32-bit big-endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### packUInt32LE()

```ts
function packUInt32LE(n: number): Uint8Array;
```

Unsigned 32-bit little endian integer

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `n` | `number` |

#### Returns

`Uint8Array`

***

### readUInt16BE()

```ts
function readUInt16BE(data: Uint8Array, offset?: number): number;
```

Reads an unsigned 16-bit big-endian integer from a Uint8Array at the specified offset.

#### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | `Uint8Array` | `undefined` | The Uint8Array to read from. |
| `offset` | `number` | `0` | The offset in the array to start reading (default is 0). |

#### Returns

`number`

The unsigned 16-bit big-endian integer value.

***

### readUInt16LE()

```ts
function readUInt16LE(data: Uint8Array, offset?: number): number;
```

Reads an unsigned 16-bit little-endian integer from a Uint8Array at the specified offset.

#### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | `Uint8Array` | `undefined` | The Uint8Array to read from. |
| `offset` | `number` | `0` | The offset in the array to start reading (default is 0). |

#### Returns

`number`

The unsigned 16-bit integer value.

***

### readUInt32BE()

```ts
function readUInt32BE(bytes: Uint8Array, offset: number): number;
```

Reads a 32-bit unsigned integer from the given byte array at the specified offset in big-endian order.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array` | The source Uint8Array containing the bytes. |
| `offset` | `number` | The offset within the array to start reading from. |

#### Returns

`number`

The 32-bit unsigned integer value.

***

### readUInt32LE()

```ts
function readUInt32LE(data: Uint8Array, offset?: number): number;
```

Reads an unsigned 32-bit little-endian integer from a Uint8Array at the specified offset.

#### Parameters

| Parameter | Type | Default value | Description |
| ------ | ------ | ------ | ------ |
| `data` | `Uint8Array` | `undefined` | The Uint8Array to read from. |
| `offset` | `number` | `0` | The offset in the array to start reading (default is 0). |

#### Returns

`number`

The unsigned 32-bit integer value.

***

### timeIntervalSinceNow()

```ts
function timeIntervalSinceNow(futureOrPastDateMillis: number): number;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `futureOrPastDateMillis` | `number` |

#### Returns

`number`

***

### toPaddedHex16()

```ts
function toPaddedHex16(value: number): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

`string`

***

### toPaddedHex32()

```ts
function toPaddedHex32(value: number): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

`string`

***

### toPaddedHex64()

```ts
function toPaddedHex64(value: Long): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `Long` |

#### Returns

`string`

***

### toPaddedHex64Signed()

```ts
function toPaddedHex64Signed(value: Long): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `Long` |

#### Returns

`string`

***

### toPaddedHex8()

```ts
function toPaddedHex8(value: number): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `number` |

#### Returns

`string`

***

### tryOptionalAsync()

```ts
function tryOptionalAsync<T>(fn: () => Promise<T>): Promise<T | null>;
```

Executes an asynchronous function and returns its result.
If the function throws an error, returns null instead.
Useful for optional async operations where failure is not critical.

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `fn` | () => `Promise`\<`T`\> | A function that returns a Promise. |

#### Returns

`Promise`\<`T` \| `null`\>

The resolved value of the Promise, or null if an error occurs.

***

### typeOf()

```ts
function typeOf<T>(value: T): string;
```

Returns a string representing the type of the given value.
Handles null and undefined explicitly, otherwise returns the constructor name or typeof value.

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `value` | `T` | The value to check the type of. |

#### Returns

`string`

The type of the value as a string.

***

### unpackInt16BE()

```ts
function unpackInt16BE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackInt16LE()

```ts
function unpackInt16LE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackInt32BE()

```ts
function unpackInt32BE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackInt32LE()

```ts
function unpackInt32LE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackUInt16BE()

```ts
function unpackUInt16BE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackUInt16LE()

```ts
function unpackUInt16LE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackUInt32BE()

```ts
function unpackUInt32BE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### unpackUInt32LE()

```ts
function unpackUInt32LE(
   bytes: Uint8Array<ArrayBufferLike> | number[], 
   offset?: number, 
   safe?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `bytes` | `Uint8Array`\<`ArrayBufferLike`\> \| `number`[] | `undefined` |
| `offset` | `number` | `0` |
| `safe` | `boolean` | `false` |

#### Returns

`number`

***

### uuidToHex()

```ts
function uuidToHex(uuid: string): string;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuid` | `string` |

#### Returns

`string`

***

### uuidToUint8Array()

```ts
function uuidToUint8Array(uuid: string): Uint8Array;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuid` | `string` |

#### Returns

`Uint8Array`

***

### writeInt16BE()

```ts
function writeInt16BE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeInt16LE()

```ts
function writeInt16LE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeInt32BE()

```ts
function writeInt32BE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeInt32LE()

```ts
function writeInt32LE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeUInt16BE()

```ts
function writeUInt16BE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeUInt16LE()

```ts
function writeUInt16LE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeUInt32BE()

```ts
function writeUInt32BE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### writeUInt32LE()

```ts
function writeUInt32LE(
   n: number, 
   out: Uint8Array, 
   offset?: number, 
   clamp?: boolean
): number;
```

#### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `n` | `number` | `undefined` |
| `out` | `Uint8Array` | `undefined` |
| `offset` | `number` | `0` |
| `clamp` | `boolean` | `false` |

#### Returns

`number`

***

### xorUint8Arrays()

```ts
function xorUint8Arrays(a: Uint8Array, b: Uint8Array): Uint8Array;
```

Bitwise XOR of two Uint8Arrays of equal length.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `a` | `Uint8Array` | First input |
| `b` | `Uint8Array` | Second input |

#### Returns

`Uint8Array`

New Uint8Array with a[i] ^ b[i]

#### Throws

If arrays are not the same length
