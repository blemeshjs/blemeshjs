---
title: SDK Package
description: High-level mesh manager, model extensions, and shared SDK types used across runtimes.
icon: BookOpenText
---

{/* Generated by apps/docs/scripts/generate-api-docs.mjs. Do not edit by hand. */}
{/* Change the TSDoc comment in `packages/sdk/src/index.ts` and run `yarn workspace docs api:generate`. */}

Generated from `packages/sdk/src/index.ts`.

<Callout title="Re-exports">
  `@blemeshjs/sdk` also re-exports everything from `@blemeshjs/utils`. This page documents only what packages/sdk/src/** declares itself.
</Callout>

## Classes

### CompositeLogger

#### Extends

- `LoggerHandler`

#### Constructors

##### Constructor

```ts
new CompositeLogger(handlers: LoggerHandler[]): CompositeLogger;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `handlers` | `LoggerHandler`[] |

###### Returns

[`CompositeLogger`](#compositelogger)

###### Overrides

```ts
LoggerHandler.constructor
```

#### Methods

##### log()

```ts
log(
   message: string, 
   category: LogCategory, 
   level: LogLevel
): void;
```

This method is called whenever a new log entry is to be saved.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `string` | The message. |
| `category` | `LogCategory` | The message category. |
| `level` | `LogLevel` | The log level. |

###### Returns

`void`

###### Overrides

```ts
LoggerHandler.log
```

***

### ConsoleLogger

#### Extends

- `LoggerHandler`

#### Constructors

##### Constructor

```ts
new ConsoleLogger(minLevel?: LogLevel): ConsoleLogger;
```

###### Parameters

| Parameter | Type | Default value |
| ------ | ------ | ------ |
| `minLevel` | `LogLevel` | `LogLevel.verbose` |

###### Returns

[`ConsoleLogger`](#consolelogger)

###### Overrides

```ts
LoggerHandler.constructor
```

#### Methods

##### log()

```ts
log(
   message: string, 
   category: LogCategory, 
   level: LogLevel
): void;
```

This method is called whenever a new log entry is to be saved.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `message` | `string` | The message. |
| `category` | `LogCategory` | The message category. |
| `level` | `LogLevel` | The log level. |

###### Returns

`void`

###### Overrides

```ts
LoggerHandler.log
```

***

### CoreMeshNetworkManager

#### Extends

- `MeshNetworkManager`

#### Accessors

##### localProvisionerNode

###### Get Signature

```ts
get localProvisionerNode(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>
  \| `undefined`

##### nodes

###### Get Signature

```ts
get nodes(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">[]
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>[]
  \| `undefined`

##### instance

###### Get Signature

```ts
get static instance(): CoreMeshNetworkManager;
```

###### Returns

[`CoreMeshNetworkManager`](#coremeshnetworkmanager)

#### Methods

##### initialize()

```ts
static initialize(storage: Storage): CoreMeshNetworkManager;
```

Initializes the shared instance with the given storage.
Must be called once before accessing `instance`.

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `storage` | `Storage` |

###### Returns

[`CoreMeshNetworkManager`](#coremeshnetworkmanager)

***

### InternalElement

#### Accessors

##### models

###### Get Signature

```ts
get models(): InternalModel & Pick<Model, never> & Pick<Model, 
  | "name"
  | "companyIdentifier"
  | "companyName"
  | "isBluetoothSIGAssigned"
  | "modelIdentifier"
  | "isGenericOnOffServer"
  | "modelId"
  | "relatedModels"
  | "boundApplicationKeys"
  | "applicationKeys">[];
```

###### Returns

[`InternalModel`](#internalmodel) & `Pick`\<`Model`, `never`\> & `Pick`\<`Model`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"companyName"`
  \| `"isBluetoothSIGAssigned"`
  \| `"modelIdentifier"`
  \| `"isGenericOnOffServer"`
  \| `"modelId"`
  \| `"relatedModels"`
  \| `"boundApplicationKeys"`
  \| `"applicationKeys"`\>[]

#### Methods

##### toProxy()

```ts
static toProxy(element: Element, coreMeshNetworkManager: CoreMeshNetworkManager): InternalElement & Pick<Element, never> & Pick<Element, "name" | "index" | "location" | "unicastAddress">;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `element` | `Element` |
| `coreMeshNetworkManager` | [`CoreMeshNetworkManager`](#coremeshnetworkmanager) |

###### Returns

[`InternalElement`](#internalelement) & `Pick`\<`Element`, `never`\> & `Pick`\<`Element`, `"name"` \| `"index"` \| `"location"` \| `"unicastAddress"`\>

***

### InternalModel

#### Methods

##### bindApplicationKey()

```ts
bindApplicationKey(applicationKey: ApplicationKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | `ApplicationKey` |

###### Returns

`Promise`\<`void`\>

##### unbindApplicationKey()

```ts
unbindApplicationKey(applicationKey: ApplicationKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | `ApplicationKey` |

###### Returns

`Promise`\<`void`\>

##### use()

```ts
use<T>(ext: ModelExtension<T>): T;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `ext` | [`ModelExtension`](#modelextension)\<`T`\> |

###### Returns

`T`

##### toProxy()

```ts
static toProxy(model: Model, coreMeshNetworkManager: CoreMeshNetworkManager): InternalModel & Pick<Model, never> & Pick<Model, 
  | "name"
  | "companyIdentifier"
  | "companyName"
  | "isBluetoothSIGAssigned"
  | "modelIdentifier"
  | "isGenericOnOffServer"
  | "modelId"
  | "relatedModels"
  | "boundApplicationKeys"
| "applicationKeys">;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `model` | `Model` |
| `coreMeshNetworkManager` | [`CoreMeshNetworkManager`](#coremeshnetworkmanager) |

###### Returns

[`InternalModel`](#internalmodel) & `Pick`\<`Model`, `never`\> & `Pick`\<`Model`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"companyName"`
  \| `"isBluetoothSIGAssigned"`
  \| `"modelIdentifier"`
  \| `"isGenericOnOffServer"`
  \| `"modelId"`
  \| `"relatedModels"`
  \| `"boundApplicationKeys"`
  \| `"applicationKeys"`\>

***

### InternalNode

#### Extends

- `BindableTinyEmitter`\<[`NodeEvents`](#nodeevents)\>

#### Accessors

##### applicationKeysWithKnownToNetworkKeys

###### Get Signature

```ts
get applicationKeysWithKnownToNetworkKeys(): ApplicationKey[];
```

Keys that have Network Keys known to the Node.

###### Returns

`ApplicationKey`[]

##### applicationKeysWithUnknownNetworkKeys

###### Get Signature

```ts
get applicationKeysWithUnknownNetworkKeys(): ApplicationKey[];
```

Keys which bound Network Keys are not known to the Node.

###### Returns

`ApplicationKey`[]

##### availableApplicationKeys

###### Get Signature

```ts
get availableApplicationKeys(): ApplicationKey[];
```

###### Returns

`ApplicationKey`[]

##### availableNetworkKeys

###### Get Signature

```ts
get availableNetworkKeys(): NetworkKey[];
```

###### Returns

`NetworkKey`[]

##### elements

###### Get Signature

```ts
get elements(): InternalElement & Pick<Element, never> & Pick<Element, "name" | "index" | "location" | "unicastAddress">[];
```

###### Returns

[`InternalElement`](#internalelement) & `Pick`\<`Element`, `never`\> & `Pick`\<`Element`, `"name"` \| `"index"` \| `"location"` \| `"unicastAddress"`\>[]

#### Methods

##### addApplicationKey()

```ts
addApplicationKey(applicationKey: ApplicationKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | `ApplicationKey` |

###### Returns

`Promise`\<`void`\>

##### addNetworkKey()

```ts
addNetworkKey(networkKey: NetworkKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKey` | `NetworkKey` |

###### Returns

`Promise`\<`void`\>

##### discover()

```ts
discover(): Promise<void>;
```

Makes sure the Composition Data Page 0 is obtained.

This requires the Proxy Filter setup to be complete. If not ready, this method does nothing.

###### Returns

`Promise`\<`void`\>

##### getCompositionData()

```ts
getCompositionData(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### getTtl()

```ts
getTtl(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### readAppKeys()

```ts
readAppKeys(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### readApplicationKeysBoundToNetworkKey()

```ts
readApplicationKeysBoundToNetworkKey(networkKey: NetworkKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKey` | `NetworkKey` |

###### Returns

`Promise`\<`void`\>

##### removeApplicationKey()

```ts
removeApplicationKey(applicationKey: ApplicationKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `applicationKey` | `ApplicationKey` |

###### Returns

`Promise`\<`void`\>

##### removeNetworkKey()

```ts
removeNetworkKey(networkKey: NetworkKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `networkKey` | `NetworkKey` |

###### Returns

`Promise`\<`void`\>

##### reset()

```ts
reset(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### toProxy()

```ts
static toProxy(node: Node, coreMeshNetworkManager: CoreMeshNetworkManager): InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
| "isCompositionDataReceived">;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | `Node` |
| `coreMeshNetworkManager` | [`CoreMeshNetworkManager`](#coremeshnetworkmanager) |

###### Returns

[`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>

***

### MeshNetworkManager

#### Properties

##### provision

```ts
provision: ProvisioningManager;
```

#### Accessors

##### allNodes

###### Get Signature

```ts
get allNodes(): InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">[];
```

###### Returns

[`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>[]

##### applicationKeys

###### Get Signature

```ts
get applicationKeys(): ApplicationKey[];
```

###### Returns

`ApplicationKey`[]

##### configuredNodes

###### Get Signature

```ts
get configuredNodes(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">[]
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>[]
  \| `undefined`

##### connection

###### Get Signature

```ts
get connection(): NetworkConnection;
```

###### Returns

[`NetworkConnection`](#networkconnection)

##### groups

###### Get Signature

```ts
get groups(): Group[];
```

###### Returns

`Group`[]

##### isNetworkCreated

###### Get Signature

```ts
get isNetworkCreated(): boolean;
```

###### Returns

`boolean`

##### logger

###### Get Signature

```ts
get logger(): CompositeLogger;
```

###### Returns

[`CompositeLogger`](#compositelogger)

##### meshNetwork

###### Get Signature

```ts
get meshNetwork(): MeshNetwork | undefined;
```

###### Returns

`MeshNetwork` \| `undefined`

##### networkKeyExists

###### Get Signature

```ts
get networkKeyExists(): boolean;
```

###### Returns

`boolean`

##### networkKeys

###### Get Signature

```ts
get networkKeys(): NetworkKey[];
```

###### Returns

`NetworkKey`[]

##### nextAvailableApplicationKeyIndex

###### Get Signature

```ts
get nextAvailableApplicationKeyIndex(): KeyIndex;
```

###### Returns

`KeyIndex`

##### nextAvailableGroupAddress

###### Get Signature

```ts
get nextAvailableGroupAddress(): Address | undefined;
```

###### Returns

`Address` \| `undefined`

##### nextAvailableNetworkKeyIndex

###### Get Signature

```ts
get nextAvailableNetworkKeyIndex(): KeyIndex;
```

###### Returns

`KeyIndex`

##### notConfiguredNodes

###### Get Signature

```ts
get notConfiguredNodes(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">[]
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>[]
  \| `undefined`

##### provisionerNode

###### Get Signature

```ts
get provisionerNode(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>
  \| `undefined`

##### provisioners

###### Get Signature

```ts
get provisioners(): Provisioner[] | undefined;
```

###### Returns

`Provisioner`[] \| `undefined`

##### provisionersNodes

###### Get Signature

```ts
get provisionersNodes(): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">[]
  | undefined;
```

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>[]
  \| `undefined`

##### instance

###### Get Signature

```ts
get static instance(): MeshNetworkManager;
```

###### Returns

[`MeshNetworkManager`](#meshnetworkmanager)

#### Methods

##### addApplicationKey()

```ts
addApplicationKey(key: NewKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | [`NewKey`](#newkey) |

###### Returns

`Promise`\<`void`\>

##### addApplicationKeys()

```ts
addApplicationKeys(numberOfKeys: number): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `numberOfKeys` | `number` |

###### Returns

`Promise`\<`void`\>

##### addGroup()

```ts
addGroup(_args: any): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_args` | `any` |

###### Returns

`Promise`\<`void`\>

##### addNetworkKey()

```ts
addNetworkKey(key: NewKey): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | [`NewKey`](#newkey) |

###### Returns

`Promise`\<`void`\>

##### createNewMeshNetwork()

```ts
createNewMeshNetwork(): Promise<MeshNetwork>;
```

###### Returns

`Promise`\<`MeshNetwork`\>

##### export()

```ts
export(): Promise<Data>;
```

###### Returns

`Promise`\<`Data`\>

##### getElement()

```ts
getElement(nodeUuid: string, elementIndex: number): 
  | InternalElement & Pick<Element, never> & Pick<Element, "name" | "index" | "location" | "unicastAddress">
  | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `nodeUuid` | `string` |
| `elementIndex` | `number` |

###### Returns

  \| [`InternalElement`](#internalelement) & `Pick`\<`Element`, `never`\> & `Pick`\<`Element`, `"name"` \| `"index"` \| `"location"` \| `"unicastAddress"`\>
  \| `undefined`

##### getGroup()

```ts
getGroup(_groupAddress: string): any;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_groupAddress` | `string` |

###### Returns

`any`

##### getModel()

```ts
getModel(
   nodeUuid: string, 
   elementIndex: number, 
   modelId: number
): 
  | InternalModel & Pick<Model, never> & Pick<Model, 
  | "name"
  | "companyIdentifier"
  | "companyName"
  | "isBluetoothSIGAssigned"
  | "modelIdentifier"
  | "isGenericOnOffServer"
  | "modelId"
  | "relatedModels"
  | "boundApplicationKeys"
  | "applicationKeys">
  | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `nodeUuid` | `string` |
| `elementIndex` | `number` |
| `modelId` | `number` |

###### Returns

  \| [`InternalModel`](#internalmodel) & `Pick`\<`Model`, `never`\> & `Pick`\<`Model`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"companyName"`
  \| `"isBluetoothSIGAssigned"`
  \| `"modelIdentifier"`
  \| `"isGenericOnOffServer"`
  \| `"modelId"`
  \| `"relatedModels"`
  \| `"boundApplicationKeys"`
  \| `"applicationKeys"`\>
  \| `undefined`

##### getNode()

```ts
getNode(uuid: string): 
  | InternalNode & Pick<Node, never> & Pick<Node, 
  | "name"
  | "companyIdentifier"
  | "applicationKeys"
  | "uuid"
  | "isConfigComplete"
  | "elementsCount"
  | "primaryUnicastAddress"
  | "deviceKey"
  | "defaultTtl"
  | "isLocalProvisioner"
  | "isProvisioner"
  | "networkKeys"
  | "isCompositionDataReceived">
  | undefined;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `uuid` | `string` |

###### Returns

  \| [`InternalNode`](#internalnode) & `Pick`\<`Node`, `never`\> & `Pick`\<`Node`, 
  \| `"name"`
  \| `"companyIdentifier"`
  \| `"applicationKeys"`
  \| `"uuid"`
  \| `"isConfigComplete"`
  \| `"elementsCount"`
  \| `"primaryUnicastAddress"`
  \| `"deviceKey"`
  \| `"defaultTtl"`
  \| `"isLocalProvisioner"`
  \| `"isProvisioner"`
  \| `"networkKeys"`
  \| `"isCompositionDataReceived"`\>
  \| `undefined`

##### init()

```ts
init(centralManager: CBCentralManager, storage: Storage): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `centralManager` | `CBCentralManager` |
| `storage` | `Storage` |

###### Returns

`void`

##### meshNetworkDidChange()

```ts
meshNetworkDidChange(): void;
```

Sets up the local Elements and reinitializes the `NetworkConnection`
so that it starts scanning for devices advertising the new Network ID.

Safe to call from a synchronous context. Closing the previous connection
and opening the new one happen in the background, and their failures are
reported through the logger rather than surfacing as unhandled
rejections. `connection` is reassigned synchronously, so callers can use
it as soon as this returns.

###### Returns

`void`

##### removeApplicationKey()

```ts
removeApplicationKey(key: Key): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Key` |

###### Returns

`Promise`\<`void`\>

##### removeGroup()

```ts
removeGroup(_group: any): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `_group` | `any` |

###### Returns

`Promise`\<`void`\>

##### removeNetworkKey()

```ts
removeNetworkKey(key: Key): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `key` | `Key` |

###### Returns

`Promise`\<`void`\>

##### reset()

```ts
reset(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### setup()

```ts
setup(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### updateMeshNetwork()

```ts
updateMeshNetwork(__namedParameters: {
  meshName?: string;
}): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `__namedParameters` | \{ `meshName?`: `string`; \} |
| `__namedParameters.meshName?` | `string` |

###### Returns

`Promise`\<`void`\>

##### getInstance()

```ts
static getInstance<T extends typeof MeshNetworkManager>(this: T): ClassInstance<T>;
```

###### Type Parameters

| Type Parameter |
| ------ |
| `T` *extends* *typeof* [`MeshNetworkManager`](#meshnetworkmanager) |

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `this` | `T` |

###### Returns

[`ClassInstance`](#classinstance)\<`T`\>

***

### MeshNetworkManagerError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new MeshNetworkManagerError(message: string): MeshNetworkManagerError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

###### Returns

[`MeshNetworkManagerError`](#meshnetworkmanagererror)

###### Overrides

```ts
Error.constructor
```

#### Properties

##### SaveError

```ts
readonly static SaveError: MeshNetworkManagerError;
```

***

### NetworkConnection

#### Extends

- `BindableTinyEmitter`\<[`NetworkConnectionEvents`](#networkconnectionevents), `this`\> & `Bearer`\<`this`\>

#### Constructors

##### Constructor

```ts
new NetworkConnection($coreMeshNetworkManager: CoreMeshNetworkManager): NetworkConnection;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `$coreMeshNetworkManager` | [`CoreMeshNetworkManager`](#coremeshnetworkmanager) |

###### Returns

[`NetworkConnection`](#networkconnection)

###### Overrides

```ts
Mixin(BindableTinyEmitter<NetworkConnectionEvents>, Bearer).constructor
```

#### Properties

##### isOpen

```ts
isOpen: boolean = false;
```

A flag set to `true` when any of the underlying bearers is open.

###### Overrides

```ts
Mixin(BindableTinyEmitter<NetworkConnectionEvents>, Bearer).isOpen
```

##### meshNetwork

```ts
meshNetwork: MeshNetwork;
```

The Mesh Network for this connection.

##### proxies

```ts
proxies: Map<string, GattBearer>;
```

The list of connected GATT Proxies.

##### maxConnections

```ts
static maxConnections: number = 1;
```

Maximum number of connections that ``NetworkConnection`` can handle.

NOTE: In BLEMeshJS app this value is set to 1 due to UI limitations.
When applying in 3rd party app, higher values should work.

#### Accessors

##### bleReady

###### Get Signature

```ts
get bleReady(): boolean;
```

###### Returns

`boolean`

##### bleState

###### Get Signature

```ts
get bleState(): CBCentralManagerState;
```

###### Returns

`CBCentralManagerState`

###### Set Signature

```ts
set bleState(newState: CBCentralManagerState): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newState` | `CBCentralManagerState` |

###### Returns

`void`

##### centralManager

###### Get Signature

```ts
get centralManager(): CBCentralManager;
```

###### Returns

`CBCentralManager`

###### Set Signature

```ts
set centralManager(newCentralManager: CBCentralManager): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newCentralManager` | `CBCentralManager` |

###### Returns

`void`

##### isConnectionAutomatic

###### Get Signature

```ts
get isConnectionAutomatic(): boolean;
```

Whether the connection to mesh network should be managed automatically,
or manually.

###### Returns

`boolean`

###### Set Signature

```ts
set isConnectionAutomatic(newValue: boolean): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newValue` | `boolean` |

###### Returns

`void`

##### isStarted

###### Get Signature

```ts
get isStarted(): boolean;
```

A flag indicating whether the network connection is open.
When open, it will scan for mesh nodes in range and connect to
them if found.

###### Returns

`boolean`

##### logger

###### Get Signature

```ts
get logger(): LoggerHandler | undefined;
```

###### Returns

`LoggerHandler` \| `undefined`

###### Set Signature

```ts
set logger(value: LoggerHandler | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `value` | `LoggerHandler` \| `undefined` |

###### Returns

`void`

##### name

###### Get Signature

```ts
get name(): string | undefined;
```

Returns the name of the first connected Proxy.

###### Returns

`string` \| `undefined`

##### status

###### Get Signature

```ts
get status(): ConnectionStatus;
```

###### Returns

[`ConnectionStatus`](#connectionstatus)

###### Set Signature

```ts
set status(newStatus: ConnectionStatus): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `newStatus` | [`ConnectionStatus`](#connectionstatus) |

###### Returns

`void`

#### Methods

##### close()

```ts
close(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

###### Overrides

```ts
Mixin(BindableTinyEmitter<NetworkConnectionEvents>, Bearer).close
```

##### connect()

```ts
connect(proxy: DiscoveredProxyPeripheral): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `proxy` | [`DiscoveredProxyPeripheral`](#discoveredproxyperipheral) |

###### Returns

`Promise`\<`void`\>

##### open()

```ts
open(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

###### Overrides

```ts
Mixin(BindableTinyEmitter<NetworkConnectionEvents>, Bearer).open
```

##### scan()

```ts
scan(options?: ScanOptions): Promise<DiscoveredProxyPeripheral>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `options?` | [`ScanOptions`](#scanoptions) |

###### Returns

`Promise`\<[`DiscoveredProxyPeripheral`](#discoveredproxyperipheral)\>

##### send()

```ts
send(data: Data, type: PduType): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `data` | `Data` |
| `type` | `PduType` |

###### Returns

`Promise`\<`void`\>

###### Overrides

```ts
Mixin(BindableTinyEmitter<NetworkConnectionEvents>, Bearer).send
```

##### stopScan()

```ts
stopScan(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### use()

```ts
use(bearer: GattBearer): Promise<void>;
```

Switches connection to the given GATT Bearer.

If the limit of `NetworkConnection.maxConnections` connections is reached,
the older one will be closed.

###### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `bearer` | `GattBearer` | The GATT Bearer proxy to use. |

###### Returns

`Promise`\<`void`\>

##### to()

```ts
static to(
   meshNetwork: MeshNetwork, 
   centralManager: CBCentralManager, 
   coreMeshNetworkManager: CoreMeshNetworkManager
): NetworkConnection;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `meshNetwork` | `MeshNetwork` |
| `centralManager` | `CBCentralManager` |
| `coreMeshNetworkManager` | [`CoreMeshNetworkManager`](#coremeshnetworkmanager) |

###### Returns

[`NetworkConnection`](#networkconnection)

***

### NodeError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new NodeError(message: string): NodeError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

###### Returns

[`NodeError`](#nodeerror)

###### Overrides

```ts
Error.constructor
```

#### Properties

##### NodeReset

```ts
readonly static NodeReset: NodeError;
```

***

### ProvisioningManager

#### Extends

- `BindableTinyEmitter`\<`RNProvisionEvents`\>

#### Constructors

##### Constructor

```ts
new ProvisioningManager(): ProvisioningManager;
```

###### Returns

[`ProvisioningManager`](#provisioningmanager)

###### Overrides

```ts
BindableTinyEmitter<RNProvisionEvents>.constructor
```

#### Properties

##### centralManager

```ts
centralManager: CBCentralManager;
```

The Bluetooth Central Manager used for provisioning scans.
Must be set before calling scan() or connect().

#### Accessors

##### canProvisionDevice

###### Get Signature

```ts
get canProvisionDevice(): boolean;
```

###### Returns

`boolean`

##### capabilitiesReceived

###### Get Signature

```ts
get capabilitiesReceived(): boolean;
```

###### Returns

`boolean`

##### device

###### Get Signature

```ts
get device(): UnprovisionedDevice | undefined;
```

###### Returns

`UnprovisionedDevice` \| `undefined`

##### isAddressValid

###### Get Signature

```ts
get isAddressValid(): boolean;
```

###### Returns

`boolean`

##### isDeviceSupported

###### Get Signature

```ts
get isDeviceSupported(): boolean;
```

###### Returns

`boolean`

##### networkKey

###### Get Signature

```ts
get networkKey(): NetworkKey | undefined;
```

###### Returns

`NetworkKey` \| `undefined`

##### provisionerAvailable

###### Get Signature

```ts
get provisionerAvailable(): boolean;
```

###### Returns

`boolean`

##### state

###### Get Signature

```ts
get state(): ProvisioningState | undefined;
```

###### Returns

`ProvisioningState` \| `undefined`

##### unicastAddress

###### Get Signature

```ts
get unicastAddress(): Address | undefined;
```

###### Returns

`Address` \| `undefined`

###### Set Signature

```ts
set unicastAddress(address: Address | undefined): void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `address` | `Address` \| `undefined` |

###### Returns

`void`

#### Methods

##### connect()

```ts
connect(peripheral: DiscoveredUnprovisionedPeripheral): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `peripheral` | [`DiscoveredUnprovisionedPeripheral`](#discoveredunprovisionedperipheral) |

###### Returns

`Promise`\<`void`\>

##### disconnect()

```ts
disconnect(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

##### identify()

```ts
identify(attentionTimer: number): Promise<void>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `attentionTimer` | `number` |

###### Returns

`Promise`\<`void`\>

##### quick()

```ts
quick(peripheral: DiscoveredUnprovisionedPeripheral): Promise<DiscoveredProxyPeripheral>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `peripheral` | [`DiscoveredUnprovisionedPeripheral`](#discoveredunprovisionedperipheral) |

###### Returns

`Promise`\<[`DiscoveredProxyPeripheral`](#discoveredproxyperipheral)\>

##### scan()

```ts
scan(options?: ScanOptions): Promise<DiscoveredUnprovisionedPeripheral>;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `options?` | [`ScanOptions`](#scanoptions) |

###### Returns

`Promise`\<[`DiscoveredUnprovisionedPeripheral`](#discoveredunprovisionedperipheral)\>

##### start()

```ts
start(): Promise<unknown>;
```

###### Returns

`Promise`\<`unknown`\>

##### stopScan()

```ts
stopScan(): Promise<void>;
```

###### Returns

`Promise`\<`void`\>

***

### ScanError

#### Extends

- `Error`

#### Constructors

##### Constructor

```ts
new ScanError(message: string): ScanError;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `message` | `string` |

###### Returns

[`ScanError`](#scanerror)

###### Overrides

```ts
Error.constructor
```

#### Properties

##### BleUnavailable

```ts
readonly static BleUnavailable: ScanError;
```

##### BleUnready

```ts
readonly static BleUnready: ScanError;
```

##### ScanTimeout

```ts
readonly static ScanTimeout: ScanError;
```

##### UserCancelled

```ts
readonly static UserCancelled: ScanError;
```

## Type Aliases

### ClassInstance

```ts
type ClassInstance<T> = T extends {
  prototype: infer R;
} ? R : never;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

***

### ConnectionStatus

```ts
type ConnectionStatus = 
  | "scanning"
  | "waiting-for-advertisements"
  | "connecting"
  | "discovering-services"
  | "initializing"
  | "connected"
  | "disconnected";
```

***

### DiscoveredProxyPeripheral

```ts
type DiscoveredProxyPeripheral = {
  device: GattBearer;
  rssi: Int32;
};
```

#### Properties

##### device

```ts
device: GattBearer;
```

##### rssi

```ts
rssi: Int32;
```

***

### DiscoveredUnprovisionedPeripheral

```ts
type DiscoveredUnprovisionedPeripheral = {
  bearer: ProvisioningBearer[];
  device: UnprovisionedDevice;
  rssi: Int32[];
};
```

#### Properties

##### bearer

```ts
bearer: ProvisioningBearer[];
```

##### device

```ts
device: UnprovisionedDevice;
```

##### rssi

```ts
rssi: Int32[];
```

***

### Element

```ts
type Element = ReturnType<typeof toProxy>;
```

***

### Model

```ts
type Model = ReturnType<typeof toProxy>;
```

***

### ModelExtension

```ts
type ModelExtension<T> = (model: Model, coreMeshNetworkManager: CoreMeshNetworkManager) => T & {
  key: string;
};
```

#### Type Declaration

##### key

```ts
key: string;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

***

### NetworkConnectionEvents

```ts
type NetworkConnectionEvents = {
  ble:error: (error: Error) => void;
  ble:state-change: (state: CBCentralManagerState) => void;
  connection:status: (status: ConnectionStatus) => void;
  scan:new-proxy: (discoveredPeripheral: DiscoveredProxyPeripheral) => void;
};
```

#### Properties

##### ble:error

```ts
ble:error: (error: Error) => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `error` | `Error` |

###### Returns

`void`

##### ble:state-change

```ts
ble:state-change: (state: CBCentralManagerState) => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `state` | `CBCentralManagerState` |

###### Returns

`void`

##### connection:status

```ts
connection:status: (status: ConnectionStatus) => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `status` | [`ConnectionStatus`](#connectionstatus) |

###### Returns

`void`

##### scan:new-proxy

```ts
scan:new-proxy: (discoveredPeripheral: DiscoveredProxyPeripheral) => void;
```

###### Parameters

| Parameter | Type |
| ------ | ------ |
| `discoveredPeripheral` | [`DiscoveredProxyPeripheral`](#discoveredproxyperipheral) |

###### Returns

`void`

***

### NewKey

```ts
type NewKey = Pick<Key, "key" | "name" | "index">;
```

***

### Node

```ts
type Node = ReturnType<typeof toProxy>;
```

***

### NodeEvents

```ts
type NodeEvents = {
  node:reset: () => void;
};
```

#### Properties

##### node:reset

```ts
node:reset: () => void;
```

###### Returns

`void`

***

### ProvisionScanOptions

```ts
type ProvisionScanOptions = ScanOptions;
```

***

### ProxyScanOptions

```ts
type ProxyScanOptions = ScanOptions;
```

***

### ScanOptions

```ts
type ScanOptions = {
  timeout?: number;
};
```

#### Properties

##### timeout?

```ts
optional timeout?: number;
```

## Variables

### GenericOnOff

```ts
const GenericOnOff: (model: Model, coreMeshNetworkManager: CoreMeshNetworkManager) => {
  remainingTime: TransitionTime | undefined;
  state: boolean | undefined;
  targetState: boolean | undefined;
  get: Promise<void>;
  set: Promise<void>;
  toDelay: {
     delay: number;
     text: string;
  };
  toTransitionTime:   | {
     stepResolution: StepResolution;
     steps: number;
     text: string;
   }
     | undefined;
} & {
  key: string;
};
```

#### Type Declaration

##### key

```ts
key: string = "genericOnOff";
```

***

### logger

```ts
const logger: CompositeLogger;
```

## Functions

### createProxy()

```ts
function createProxy<TInternal extends object, TPublic extends object, TAllowedMethods extends readonly keyof TInternal[], TAllowedProps extends readonly keyof TInternal[]>(
   node: TInternal, 
   api: TPublic, 
   allowedMethods: TAllowedMethods, 
   allowedProps: TAllowedProps
): TPublic & Pick<TInternal, TAllowedMethods[number]> & Pick<TInternal, TAllowedProps[number]>;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `TInternal` *extends* `object` |
| `TPublic` *extends* `object` |
| `TAllowedMethods` *extends* readonly keyof `TInternal`[] |
| `TAllowedProps` *extends* readonly keyof `TInternal`[] |

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `node` | `TInternal` |
| `api` | `TPublic` |
| `allowedMethods` | `TAllowedMethods` |
| `allowedProps` | `TAllowedProps` |

#### Returns

`TPublic` & `Pick`\<`TInternal`, `TAllowedMethods`\[`number`\]\> & `Pick`\<`TInternal`, `TAllowedProps`\[`number`\]\>

***

### keysOf()

```ts
function keysOf<T>(): <K>(keys: K) => K;
```

#### Type Parameters

| Type Parameter |
| ------ |
| `T` |

#### Returns

\<`K`\>(`keys`: `K`) => `K`

***

### networkIdentity()

```ts
function networkIdentity(advertisementData: Record<string, unknown>): PublicNetworkIdentity | PrivateNetworkIdentity | null;
```

#### Parameters

| Parameter | Type |
| ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> |

#### Returns

`PublicNetworkIdentity` \| `PrivateNetworkIdentity` \| `null`

***

### nodeIdentity()

```ts
function nodeIdentity(advertisementData: Record<string, unknown>): PublicNodeIdentity | PrivateNodeIdentity | null;
```

Attempts to create a node identity from advertisement data.
Tries to parse the data as a PublicNodeIdentity first, then as a PrivateNodeIdentity if unsuccessful.

#### Parameters

| Parameter | Type | Description |
| ------ | ------ | ------ |
| `advertisementData` | `Record`\<`string`, `unknown`\> | The advertisement data to parse. |

#### Returns

`PublicNodeIdentity` \| `PrivateNodeIdentity` \| `null`

A PublicNodeIdentity or PrivateNodeIdentity instance, or undefined if parsing fails.
